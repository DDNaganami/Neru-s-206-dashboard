# 美术素材许可证 —— 单独声明（保留所有权利）

**本仓库的美术素材（图片、图标、位图、生成的二进制素材等）不随 MIT 也不随 CC BY 4.0 授权。**

## 授权口径（保守）

Copyright (c) 2026 DDNaganami. **保留所有权利（All rights reserved）。**

在不额外取得书面许可的前提下，仅授权以下一种用途：

> **运行本仓库构建产物所必需的使用** —— 即为编译、烧写、运行本项目固件与预览工具而复制、
> 转换、打包、烧录这些素材（含把素材刷进设备的 `theme` / `image` 分区），以及为此目的在
> 本仓库内保留副本。

**除此之外的一切用途均需另行授权**，包括但不限于：重新发布、公开分发、再许可、商用、
把素材单独抽取出来用于其它项目、训练数据集、印刷品、周边商品。

## 范围（逐个核对仓库后的实际清单）

### 1. 当前仓库内的美术素材：**无**

本声明是**预防性**的。截至本文档引入时，在 git 跟踪的文件清单里**没有任何美术素材**——
按扩展名逐个核对，`*.png` / `*.webp` / `*.gif` / `*.jpg` / `*.jpeg` / `*.bmp` / `*.svg` /
`*.ico` / `image-*.bin` / `*.ttf` / `*.otf` / `*.wav` **一个都没有**，整个工作树里也搜不到这些文件。

也就是说：**今天克隆本仓库拿不到任何需要本许可证约束的素材**；本仓库内的图形**全部是代码
画出来的**（LVGL 绘制的弧、刻度、数字与表情几何），那些代码属于 MIT。

### 2. 本声明覆盖哪些文件（按路径与扩展名界定）

**A. 仓库内任何位置**，凡属于下列形式之一者：

- 图片 / 媒体：`*.png`、`*.webp`、`*.gif`、`*.jpg`、`*.jpeg`、`*.bmp`、`*.svg`、`*.ico`、`*.tif`、`*.tiff`
- 字体文件：`*.ttf`、`*.otf`、`*.woff`、`*.woff2`
- 位图素材二进制：`image-*.bin`，以及任何承载图片像素、且**不是**由源码编译产生的 `.bin`
- 音频素材：`*.wav`、`*.mp3`、`*.ogg`

**B. `tools/theme-editor/` 目录下的图片与生成物** —— 该目录是**美术素材与本项目自带配色的
归属地**，其中属于本声明的有：

| 路径 | 说明 |
|---|---|
| `tools/theme-editor/theme-default.json` | 本仓库随附的**默认运行时主题**：配色 + 几何 + 面谱分档（详见下节「主题文件」） |
| `tools/theme-editor/spec-240.json` | 240 档占位配方（同上，配色 + 几何） |
| `tools/theme-editor/` 下将来新增的 `*.png` / `*.webp` / `image-*.bin` 等图形或生成物 | 按 A 的扩展名判定 |
| 编辑器**导出**的 `theme.json`、`image.bin` 等文件 | 见下节 |

★ **注意**：`tools/theme-editor/` 下的 `.html` / `.js` 是**工具源码**，属于 **MIT**（见 `LICENSE`），
**不是**美术素材。

### 3. 生成物与不入库的文件

| 文件 | 是否入库 | 归属 |
|---|---|---|
| 编辑器导出的 `theme.json` | **不入库**（`theme.json` 被 `.gitignore` 刻意排除，属车主的私人资产） | 导出者的作品；若由本仓库随附的默认主题派生，则仍受本声明约束 |
| 编辑器导出的 `image.bin` / `image-*.bin` | 通常不入库（由用户在本地生成、直接刷进 `image` 分区） | 同上：其中的图片像素受本声明约束 |
| `preview/frames/` 下本机渲染出的 `*.bmp` 帧 | **不入库**（在 `.gitignore` 内） | 本机预览产物，随构建产生、不随仓库分发 |
| 固件 / 工具构建出的 `*.bin` | **不入库** | 编译产物，其源码部分适用 MIT（其中若打包了素材，素材部分仍适用本声明） |

### 4. 关于「主题文件」（`theme.json`）的归属说明

运行时的 `theme.json` 里存的是**颜色值、几何参数与面谱分档**——单看它是数值配置，没有像素。
本声明把它一并纳入「美术素材」是为口径统一（它是**外观设计**的载体，而不是代码）。若某个
`theme.json` 完全由第三方独立创作、未参考本仓库随附的默认主题，则它属于该第三方。

### 5. 文档里出现的 `bg.png` / `L_idle.png` 等名字

`tools/theme-editor/README.md` 的素材规格表里列了 `bg.png`、`L_idle.png`、`L_cruise.png`、
`L_sport.png`、`L_high.png`、`L_redline.png`、`R_idle.png` 等名字。这些是**用户自备源图的
逻辑占位名**（用来指代某个用途槽位），**不是本仓库内存在的文件**。用户自己拖进编辑器的
源图归用户自己；只有当它被转成素材、随本仓库分发时，才受本声明约束。

## 如需其它用途

如需上述范围之外的任何用途（再发布、商用、借用素材到别的项目等），**可联系作者另行授权**：

- 仓库地址：<https://github.com/DDNaganami/Neru-s-206-dashboard>
- 联系方式：请通过 GitHub 仓库开 issue 或直接联系仓库所有者 `DDNaganami`

> ★ 备注（供仓库所有者参考）：本文件是**默认的保守口径**。所有者以后若愿意放宽，可以自行把
> 本文件改成 **CC BY-NC-SA 4.0**（署名 — 非商业性使用 — 相同方式共享）或其它许可证，
> 只需替换本文件的授权段落；在此之前，一律按「保留所有权利 + 仅授权运行本仓库构建产物所必需的使用」理解。
