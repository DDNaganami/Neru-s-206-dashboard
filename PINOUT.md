# 引脚（预案）

ESP32 型号：ESP32-S3 N16R8（硬约束：真屏双 480×480 全缓冲必须
S3 + PSRAM，见 ARCHITECTURE.md；N16R8 八线 PSRAM 走 flash 总线，
GPIO33-37 仍可用）
屏型号：（待定）

以下为按「双 SPI 屏 + K 线 OBD + VAN 监听」预分配的引脚，未实测；
接好线后把实际值更新回这张表，并同步 main.cpp 里的串口初始化。

功能            GPIO   说明
------------    ----   ------------------------------------------------
屏 SPI SCK      36     两屏共用一条 SPI 总线
屏 SPI MOSI     35
屏 SPI MISO     37     多数屏只写不读，可省
左屏 CS         4
左屏 DC         5
左屏 RST        6
左屏背光       7      PWM
右屏 CS         8
右屏 DC         9
右屏 RST        10
右屏背光       11     PWM
K 线 OBD RX     18     UART1 ← ELM327 TX（38400 8N1，Serial1）
K 线 OBD TX     17     UART1 → ELM327 RX
VAN RX          16     UART2 ← SN65HVD230 RO
VAN TX          15     UART2 → SN65HVD230 DI（纯监听可不接）
VAN RS          GND    SN65HVD230 第 8 脚：接 GND = 高速模式（**不是 DE/RE**，接 VCC 会进待机收不到）

约束：
- 避开 strapping 脚（GPIO0/3/45/46）、flash 脚（GPIO26-32）、USB 脚（19/20）。
- 上表按 N16R8 排的 SPI；若买 N16R2（四线 PSRAM 占 GPIO33-37），
  SPI 需整体挪到 5-11 区段之外的空闲脚。
- SN65HVD230 已拆 120Ω 终端电阻（见 ARCHITECTURE.md），RO/DI 为 3.3V 电平，
  与 ESP32 直连。
- 屏模块（如 T-RGB 2.8）到货后以模块丝印为准，本表只是占位。
- **OBD 座没有 VAN**（只有 K 线 + 12V + GND），车速要从组合仪表/收音机取 ——
  见下面「接线：三处取信号」。

---

## 接线：三处取信号，别指望 OBD（2026-09-15 修正）

⚠️ **重要事实：206 的 OBD 诊断座上没有 VAN。**
一位 2002 年 206 1.4HDi MUX 车主实测过诊断座针脚：只有 K 线（7 脚发动机/变速箱、
11 脚其他模块、12 脚 ABS）、16 脚 +12V、两个 GND —— **没有 VAN 引脚**。
所以车速只能从下面的 VAN 落点取，供电和转速/水温才从 OBD 取。

| 要什么 | 从哪取 | 针脚 |
|---|---|---|
| **12V 供电** | OBD 座 | 16（+12V）、4/5（GND）——⚠️ 16 脚是**常电** |
| **转速 / 水温** | OBD 座 K 线 | 7 脚 → ELM327 |
| **车速（VAN）** | **组合仪表连接器**（首选） | **5 / 10**（线号 9004 / 9005） |
| 车速（备选） | 收音机黑色 7 针音频插头 | 2 / 3（线号 9004 / 9005） |
| 车速（备选） | 多功能显示屏 MFD 18 针 | 4 / 17（线号 9004A / 9005A） |

**为什么首选组合仪表**：本项目本来就要拆仪表加屏，而仪表连接器是**插接件**，
直接拔下来并接即可，**不需要破线、不需要买转接头**。

> 车型已确认是 **MUX**（用户 2026-09-15 确认）。MUX 自 2001-10 起使用，
> 上面这些针脚与线号来自同期 206 MUX 电路图。

### 怎么用万用表认出那两根线

MUX 车实测特征（点火关着、BSI 醒着时，对车 GND）：

- 9004（VAN DATA）≈ **4V**
- 9005（VAN DATA BAR）≈ **1V**
- 差分 ≈ 3V

两根都是"不接地、不对 12V、也不是 0V"的线，这个特征比记线号更可靠 ——
尤其安卓车机装过之后，原车线可能被包起来看不见标记。

### 第一段：SN65HVD230 ↔ ESP32（杜邦线，母对母）

| 模块脚 | 接到 | 说明 |
|---|---|---|
| VCC | ESP32 **3.3V** | **不是 5V**。收发器 3.3V 供电，与 ESP32 电平一致 |
| GND | ESP32 GND | |
| **RO** | **GPIO16** | 收：解出的数据 → ESP32（对应 UART2 RX） |
| **DI** | 不接 | 纯监听不需要发（主表已注明） |
| **RS**（第 8 脚） | **接 GND** | 高速模式，免固件控制。★ SN65HVD230 只有 `RS`；若模块丝印写的是 `DE/RE`（TJA1050 那类），则 DE/RE 都接 GND |

⚠️ **先拆 120Ω 再接线**。模块上标 R1/120 的贴片电阻必须拆掉：不拆会在总线上
并联出约 60Ω，阻抗失配 → 反射 → 误码率飙升。拆完用万用表确认。

---

## 开箱当天：先量，再接（万用表能省的三个坑）

2026-09-18 板子/收发器/一分二线到货。**按这个顺序做**，别跳步 ——
前两步都是"花五分钟，避免一次冒烟或一个白跑的下午"。

### ① S3 板子：先量有没有短路，再看自检输出

| 步骤 | 怎么量 | 合格判据 |
|---|---|---|
| 上电前 | 蜂鸣档量 **3V3 ↔ GND** | 不响（电容会让读数慢慢爬，等两秒再看） |
| 上电后 | 直流档量 **3V3 ↔ GND** | **3.3V ±0.1**；低于 3.0 说明供电/线材有问题 |
| 上电后 | **UART 口**（CH340，见下）115200 看开机自检 | `chip`/`flash`/`psram` 三行 —— 见下 |

> ★ **两个 USB 口别插错**（2026-09-18 实测，坑了一整天，详见 `ACCEPTANCE.md`）：
> 标着 **UART** 的那个口是板载 **CH340** —— 它给的是**真 UART0**（ROM、
> bootloader、panic 的日志全在这里）和**真 EN/IO0 复位线**（所以刷完 app 真的会
> 启动）。标着 **USB** 的那个是 S3 的原生 USB（USB-Serial-JTAG）：它那条"复位"
> 是软复位请求，esptool 每次复位都会把芯片送进**下载模式**。
> **刷写、监视、排障一律用 UART 口。** GPIO43/44 就是这条 UART0，别再挪作他用。

开机自检是这一轮新加的（`src/main.cpp` 的 `setup()`），换板/换芯片后第一眼看它：

```
206 dash ok
chip  : ESP32-S3 rev0, 2 核 @ 240 MHz
flash : 16 MB
psram : 8189 KB 可用 / 8189 KB 总
heap  : 259 KB
image : 分区 8192 KB @ 0x254000(编译期口径 8192 KB)
```

- **`psram` 必须是 8000+ KB**：报 0 时**先查编译开关** `-DBOARD_HAS_PSRAM`
  （缺了它，核心会主动把 `CONFIG_SPIRAM` 整个 `#undef` 掉 —— 这是 2026-09-18
  真正踩到的原因，见 `platformio.ini` 的 `[env:esp32s3]`），再查板子是不是 R8、
  `board_build.arduino.memory_type` 对不对。这一行直接决定"双 480×480 整屏缓冲"
  能不能做。
- **`flash` 必须是 16 MB**：报 4/8 MB 说明买到的不是 N16；
  16MB 的分区表刷到小 flash 上会直接起不来（刷之前先看这一行）。
- **`image` 那行最后若出现"← 不一致,检查分区表!"**，说明烧进去的分区表与
  编译期口径对不上（4MB 那份 vs 16MB 那份），重新 `pio run -e esp32s3 -t upload` 即可。

刷写与环境（本机是 ASCII 副本那一套，见 `ARCHITECTURE.md`）：

```powershell
# 新环境:真屏/实车要用的那块板子 —— 线插 UART 口
python -m platformio device list                    # 看串口号(CH340 通常是 COM4)
python -m platformio run -e esp32s3 -t upload --upload-port COM4
# 想从第一个字节看开机日志(监视器做不到这件事,原因见 ACCEPTANCE.md):
python tools/serial-capture/capture.py COM4
```

### ② SN65HVD230 模块：拆 120Ω + 认脚位

| 步骤 | 怎么量 | 合格判据 |
|---|---|---|
| 拆电阻前 | 电阻档量 **CANH ↔ CANL** | 约 120Ω（模块自带终端电阻） |
| **拆电阻后** | 同上 | **高阻/开路**（不再是 60~120Ω）→ 这才算拆干净 |
| 上电前 | 蜂鸣档量 **VCC ↔ GND** | 不响 |
| 认丝印 | 看模块上是不是 **SN65HVD230** | 是 3.3V 器件 ✅；**TJA1050 / MCP2551 是 5V 的，不能用** |

⚠️ 不同模块的丝印顺序不一样（有的 `VCC GND RO DI RS`，有的 `TXD RXD ...`）。
**按丝印对，不要按排针位置猜** —— 接错 VCC/GND 是最容易烧模块的一种错。

### ③ OBD 一分二线：逐针量通断（这条最容易被坑）

**很多一分二线只接了 4/5/7/16 这四根**，其它针是空的。现在量一次，
将来想用别的针（比如从 OBD 取别的信号）才不会白折腾：

| 量什么 | 合格 |
|---|---|
| 公头 **每一针** ↔ 两个母头的同一针 | **全通**（16 针都通最好；至少 4/5/7/16 必须通） |
| 公头 **16(+12V) ↔ 4/5(GND)** | **不能短路**（电阻档应是高阻;线里若有电容读数会爬，正常） |
| 母头与母头之间同一针 | 通（一分二是并联，不是切换） |

### ④ 上车次序（重要：别一步到位）

```
桌面自检(充电宝供电)  →  车内只接 VAN 两根信号线(仍用充电宝)  →  抓帧成功  →  最后才从 OBD 取电
```

**第一次上车不要接 12V。** 用充电宝给板子供电、只把 CANH/CANL 并到 VAN 两根线上，
这样即使接错极性也只是"收不到数据"，不会伤车也不会伤板子。

### ⑤ VAN 极性：万用表就能先定下来

PINOUT 上面说"极性未确认、要用逻辑分析仪"，其实**万用表能先给出答案**：

1. 点火开关打到 **ON（不用启动发动机）**，让 VAN 上有帧在跑；
2. 直流档量 **组合仪表连接器 5 脚与 10 脚各自对地（车身搭铁）的电压**；
3. VAN 是差分总线，两线静止电平不同 —— **记下"哪根高、哪根低"**（公开资料的说法是
   一根约 4V、另一根约 1V，但这个数**以实测为准**，别照抄）；
4. 按 **CANH → 高那根、CANL → 低那根** 接，然后看 RO 上有没有数据；
5. **没有就把两根对调** —— 差分输入接反不会烧任何东西，只是收不到。

> 直流档读的是**平均值**（总线一直在跑帧），所以判据是"两根明显不一样"，
> 不是精确的 4V/1V。真要确认波特率/波形，才需要逻辑分析仪（`PURCHASE.md` T1 第 7 项）。



### 第二段：模块 ↔ 车的 VAN 线

```
模块 CANH ─┐
模块 CANL ─┘
           └─→ 组合仪表连接器 5 / 10 脚(首选)
               或 收音机原车线 9004 / 9005
```

在组合仪表处是插接件，拔出插头并接即可；在收音机处则是原车被弃用的两根线
（安卓车机不吃 VAN，那两根线通常还在），**并接而不是剪断**。

⚠️ **极性问题：尚未确认，必须实测。**
VAN 的差分极性与标准 CAN **相反**（公开资料的说法是 CANH 接 VAN BAR、CANL 接 VAN），
但**哪一脚对应 DATA、哪一脚对应 DATA BAR，本项目尚未实测确认**。
由于 9004/9005 的线号标记在安卓车机装过之后不一定还看得见，
建议**按电压认线**（4V 那根 / 1V 那根），并且接反了也能救：

1. 先按"CANH→9004(4V)、CANL→9005(1V)"接
2. 逻辑分析仪挂 RO 脚，看有没有 125kbps 的方波
3. 没有就**把两根对调**再试

这也是逻辑分析仪（`PURCHASE.md` T1 第 7 项）不能省的原因之一。

### 供电怎么接（从 OBD 取电）

想只用一个 OBD 口同时插 ELM327 和自己的板子，买一根 **OBD 一分二转接线**
（`PURCHASE.md` T0）。

⚠️ **最容易买错的一步：OBD 座 16 脚是方形 16 针，和点烟器圆口不是一个东西。**
普通的"车充"（插点烟器那种）**插不进 OBD 座**，所以它对不上。

板子侧要的是 **OBD 取电降压线（一体）**：OBD 公头直接插诊断座，
线里已经做了 12V→5V，另一头**输出 USB-C 公头**接板子的 Type-C 口 ——
**免焊接**。搜 `OBD 取电降压线 Type-C`。

⚠️ **输出口规格最容易买错**：这类线是给行车记录仪用的，**默认款多半是
Micro-USB 公头**，USB-C 是可选规格 —— 下单时必须在规格里**明确点 Type-C**。
若买到 USB-A 母口版本也行，用一根 **A-to-C** 线接板子即可。
（**A 母口 → C 公头一定能用**；反过来 **C-to-C 需要线里有 eMarker**，
随便一根可能不供电，别用。）

```
车里 OBD 座
   └─ OBD 一分二
        ├─ 口1 → ELM327（读 K 线）
        └─ 口2 → OBD 取电降压线（带开关，输出 Type-C）→ 板子 Type-C 口
```

若买的是 **OBD 公头带线 + DC-DC 模块**（第二种做法）：公头插 OBD，
12V/GND 拧进模块的接线端子，多数模块**也不用焊**。
最差的是"点烟器车充 + 剪线"——那个必须焊，别选。

### ⚠️ 供电一律走 Type-C 口，不要用 5V 排针

国内通用 S3 板（芯片原装 + 向下焊 + Type-C 那种）有个常见毛病：
**5V 排针在出厂状态下不是 5V** —— 有的板子那个针是 LDO 的输出端、
有的根本没接通原 USB 的 VBUS。**直接灌电会灌进稳压器输出端。**

**到货第一件事：插上 USB 后量一下那个标着 5V 的排针**，记下它到底是多少。
之后接线就一直按这个结论走 —— 供电走 Type-C，5V 排针只当"待确认"的针脚。

**电流**：ESP32-S3 + PSRAM 待机 40~80mA、开 WiFi 150~250mA，
**启动瞬间有 500mA 级尖峰**，所以选 5V **2A**；1A 的会掉压重启
（串口打印 `Brownout detector was triggered`）。
**收发器（SN65HVD230 / L9637D）不用单独供电** —— 吃 3.3V、几毫安，
直接从板子 3.3V 引脚取。

**先在家里台架上量电流**：充电宝 + **A-to-C 线**给板子供电（完全安全，
笔记本不接入），看板子能否正常跑、顺手量运行电流。
这一步别用 C-to-C（需要 eMarker，随便一根可能不供电）。

⚠️ **OBD 16 脚是常电**：长时间不开发动机时不要让板子一直跑，会耗电瓶。
**所以降压线一定要带开关**，调试完关掉（或拔掉）。
更规范的做法是走保险丝取电器取 ACC 电（`PURCHASE.md` T3 第 21 项），装车阶段再说。

### 安全：别让笔记本和车共地

若用**笔记本 USB** 给 ESP32 供电，同时模块又接到**车的 12V 系统**
（哪怕只从 OBD 座取电），就把车的电气地和笔记本的地连起来了，
最坏会带走笔记本或板子。二选一：

- 用充电宝 / OBD 取电降压线给 ESP32 供电，**笔记本完全不接入车的电气系统**（推荐，零成本）
- 加 USB 隔离器（`PURCHASE.md` T3 第 20 项）

调试阶段优先在家里台架跑（模块 + 板子 + USB 供电），这一段不用上车。

### 接线顺序

1. 拆 120Ω → 万用表确认
2. 接第一段（4 根）→ 上电，量 VCC 是否有 3.3V
3. 万用表认 VAN 两根线（4V / 1V 特征）
4. 逻辑分析仪挂 RO → 上车试极性（两根对调各试一次）
5. 出方波后再看解码：`VanPhyWire` 整链已有 46 例宿主机测试覆盖，
   抓不到**帧**时先怀疑极性和电平，不要先改解码逻辑

---

## 相关文档

- `PURCHASE.md`：采购清单（OBD 一分二在 T0，逻辑分析仪在 T1）
- `ARCHITECTURE.md`：VAN 数据源与线路层的设计
- `ACCEPTANCE.md`：实车必验清单（极性、IDEN、FCS 约定等）

## 抓帧盒：用裸 S3 板 + SN65HVD230 去车上抓 VAN（2026-09-18）

**它解决什么**：`ACCEPTANCE.md` 的实车必验清单第 1 条（FCS 约定、IDEN 到底是不是
0x824、ACK 位行为）。那几条**只能靠真实原始数据定位** —— 之前拿公开抓包做全枚举
（32768 个多项式 × 6 种字段拆解 × 位序/字节序）复现不出 FCS，所以必须自己抓。
**它不需要屏**，用那块只有排针的 S3 裸板正好（带屏的整板留着调屏）。

### A. 接线表（桌面先量，别直接上车）

| 从 | 到 | 说明 |
|---|---|---|
| SN65HVD230 `VCC` | S3 **3V3** | 收发器吃 3.3V，**不是 5V** |
| SN65HVD230 `GND` | S3 `GND` | 必须共地 |
| SN65HVD230 `R` / `RO`（接收输出） | S3 **GPIO16** | 固件里 `VAN_RX_PIN=16`（`lib/dashcore/van_phy_gpio.cpp`） |
| SN65HVD230 `RS` / `S`（第 8 脚） | **GND** | ★ 高速模式。**别接 VCC** —— 那是待机，一帧都收不到 |
| SN65HVD230 `D` / `TXD`（发送输入） | 悬空 | 我们**只收不发**（听总线，不做节点） |
| SN65HVD230 `CANH` / `H` | 仪表连接器 **5 或 10 脚**（线号 9004/9005，极性待实测） | 免破线用插接件端子 |
| SN65HVD230 `CANL` / `L` | 另一个（5/10 里剩下的那根） | 先随便接，方向不对按下面第 4 步对调 |
| 模块上的 **120Ω 终端电阻** | **拆掉** | 车总线两端已有终端，多一个会拉低幅度。见「开箱当天」那一节 |

> ### ★ 如果你的模块只有 `3V3 / GND / RX / TX` 四个脚（很常见）
> 那就是一块**纯收发器小板**，丝印是按"接你单片机"的方向标的，不是按收发器内部脚名：
>
> | 上面表里写的 | 模块丝印 | 接法 |
> |---|---|---|
> | `VCC` | **`3V3`** | 开发板任意 **3V3** |
> | `GND` | **`GND`** | 开发板任意 **GND** |
> | `RO`（接收输出） | **`RX`** | ★ **GPIO16**（唯一必须接的信号线） |
> | `D`（发送输入） | **`TX`** | **悬空**（只收不发） |
> | `RS` | **没有** | 不用管（TJA1050 那类本就没这个脚；SN65HVD230 模块常在 PCB 上已接地） |
>
> **`RX` / `TX` 哪个才是"接收输出"**：4 脚模块一般按单片机视角标（`RX` = 收发器输出 ✓），
> 但也有厂家反过来。**判定法**：接 `RX` → 上车后若 `edges=0`（且供电、接线都确认过），
> **把 GPIO16 换接到 `TX` 再试** —— 这比查资料快，也不会弄坏东西（`TX` 悬空时收发器不发）。
>
> ### ★ 共地（最容易漏的一条）
> S3 与车是两个独立电源，差分接收器缺一个共同参考：
> · ✓ 把模块 `GND` 也接到**车的 GND**（OBD 座 4/5 脚，或仪表侧搭铁）—— 一根线，
>   给差分信号一个共同电平参考；
> · ✓ 记录日志**必须**用笔记本插 USB（裸板没有 SD 卡，"充电宝 + 抓帧"根本记不下来
>   —— 这句以前写错了，2026-09-18 改正）：笔记本地 = 板子地 = 车地，
>   只要笔记本**拔掉充电器、只用电池**，整机就浮在车电位上，没有地环流。
> · ✗ 唯一真会打坏 USB 口的组合：笔记本**一边插着市电充电器**一边接车
>   （市电大地 ↔ 车 12V 地，见 `PURCHASE.md` 第六节）。
>   所以顺序固定为：**先拔充电器 → 再接线**；收工**先断 VAN 两根线 → 再拔 USB**。
>   （用车上 12V 转 USB 的充电器给笔记本供电是安全的 —— 那是同一个地。）
> ★ **`RS` 不是 `DE/RE`**：SN65HVD230 只有 RS（斜率/模式脚）。
> 若你手上的模块丝印是 `DE`/`RE`（那是 TJA1050 那类），则 `DE→GND`（关发送）、
> `RE→GND`（开接收）。**先看丝印再接线**，这两种接法完全不同。

### B. 桌面自检（5 分钟，上车前必做）

1. 断电：蜂鸣档量 **3V3 ↔ GND** 不响。
2. 上电刷固件（线插 **UART 口**）：
   `python -m platformio run -e esp32s3 -t upload --upload-port COM4`
3. 抓开机日志：`python tools/serial-capture/capture.py COM4`
   必须看到这一行 —— 看到 `van phy: stub` 就说明这次编译没开 GPIO 接收：
   ```
   van phy: gpio 就绪 RX=GPIO16(RO),空闲 300us 关帧
   ```
4. 断电量收发器供电：`VCC` 对 `GND` 应 ≈3.3V（上电时）。
5. **桌面"信号通路到底通没通"的判定法（2026-09-18 实测有效）**：
   桌上没有总线，所以 `frames` 必然是 0，但 `edges` 能告诉你**收发器是不是活着** ——
   用手指碰几下 / 拔插几次 `CANL` 那个接线端子，再看 `van: edges=`：
   - 实测：碰之前累计 `edges=15`，碰了 5~6 下之后再读 `edges=334`；
     而**静置 15 秒一个边沿都不涨**（漂移 ≈ 0）→ "跳一大截"只可能来自那几下 →
     **通路确认**：收发器有电，RO 确实在驱动 GPIO16。
   - 若怎么碰都只在 ±1 上下爬 → RO 没接上 / 芯片没供电 / 接错脚：
     把 GPIO16 换到模块的 `TX` 再试（见上面那张 4 脚模块表）。
   - ★ 这个测试**只证明"线通、芯片活"**，不证明极性、速率、协议对 ——
     那些只能上车看 `edges` 和 `frames` 谁涨。
   - ★ 每次上电 `edges` 从 0 重新数，所以车上第一次读到的那行就是干净基线。

### C. 上车步骤（顺序别改）

1. **笔记本先拔掉充电器（只用电池）**，USB 线插板子的 **UART 口**（CH340 / COM4）。
   抓帧必须靠笔记本记录 —— 裸板没有 SD 卡，光用充电宝供电就没人记日志
   （2026-09-18 改正了这句）。板子由 USB 供电即可，不用接车上的 12V。
2. **认线**：★ 2026-09-18 实测更正 —— **别用"2~3V"当判据**，那是我原来写窄了。
   真车实测（206 MUX 1.4HDi，线号 9004/9005，BSI 醒着、只在开门状态下量的）：
   ```
   9004 对车地 ≈ 4V      9005 对车地 ≈ 1V      两根之间 ≈ 3V
   ```
   （来源：EEVblog「VAN bus interfacing … Peugeot 206」主题贴，见文末链接。）
   所以判据是：**两根对地电压明显不相等、且都不是 0V / 12V / 两者几乎一样**。
   读到 0V / 12V / 两根相同 → 认错线了，**立刻停手**（可能是电源或 K 线）。
   ★ **更省事的取信号点**：不用拆仪表 —— 同一对 VAN 网络也引到
   **收音机后面那个黑色 7 脚插头**（`2` = 9004、`3` = 9005、`8` = 车地搭铁，
   插头上针脚号是模压出来的，好认）。同一对线还出现在 **MFD 显示屏插头 4/17 脚**。
   注意查线号：**9004 / 9005**（MFD 那一段是 9004A / 9005A）。
   ★ 认线前先**把 BSI 叫醒**（开门、开收音机、钥匙拧到 ON）——
   睡着时总线是静默的，两根线的电压也跟醒着时不一样，量了会误判。
3. 接 VAN_H/VAN_L，看串口每秒那行的四个数：

   | 现象 | 含义 | 怎么办 |
   |---|---|---|
   | `edges=0` | 没信号 | 检查供电 / RS 有没有接 VCC / 线接错 |
   | `edges` 涨、`frames=0` | 收到电平但解不出帧 | **先把 VAN_H/VAN_L 对调**（极性反了最常见） |
   | `frames` 涨、`fcs_ok=0` | 解出帧但 CRC 不符 | ★ **这就是我们要的原始数据**（FCS 约定问题），继续抓 |
   | `dropped` 涨 | 串口/中断太密，主循环排空不及 | ★ 先把串口提到 921600 重刷再抓（见 E 节）；短期方案 RMT 在 `van_phy_gpio.cpp` 文末 |

4. **记录**（10 分钟起步，越长越好；车速要覆盖怠速→加速→巡航→减速）。

   **脚本有两种，看笔记本上有没有 Python：**

   | 情况 | 用哪个 |
   |---|---|
   | 这台开发机（有 `.pio-pylibs`、仓库就在本地） | `capture-van.ps1` |
   | **另一台笔记本**（不确定有没有 Python / pyserial） | **`capture-van-nopy.ps1`**（零依赖） |

   ★ 2026-09-18 实测教训：抓帧往往是在**另一台笔记本**上做的，而那台机器上没有
   这个仓库、也不一定装了 Python + pyserial —— 照抄开发机上的绝对路径会直接报
   `-File 形式参数的实际参数"…"不存在`（那次就是这么栽的，跟中文路径无关）。
   **把脚本拷过去就能用**，从开发机取文件的路径（纯 ASCII，好拷）：
   `C:\Users\Public\206dash\Neru-s-206-dashboard\tools\serial-capture\`

   ```powershell
   # 端口号先查一下(笔记本上多半不是 COM4):
   [System.IO.Ports.SerialPort]::GetPortNames()

   # A. 有 Python + pyserial(没有就 python -m pip install pyserial):
   powershell -ExecutionPolicy Bypass -File <拷过去的目录>\capture-van.ps1 -Port COM5 -Seconds 600

   # B. 什么都不想装(推荐):
   powershell -ExecutionPolicy Bypass -File <拷过去的目录>\capture-van-nopy.ps1 -Port COM5 -Seconds 600
   #    抓够了就收工:-UntilFrames 200      短一点:-Seconds 180
   ```

   ★ `capture-van-nopy.ps1` 只用 Windows 自带的 PowerShell，**刻意不用串口终端**：
   PuTTY / Tera Term / 串口助手 打开串口时会拉 DTR/RTS，而那两根线在板上接的正是
   EN / IO0 —— 等于把芯片按住复位，屏幕上**一个字都不会有**，看着像固件坏了。
   .NET 的 `SerialPort` 默认 `DtrEnable = RtsEnable = false` ✓ 与 `capture.py` 一致。
   它把日志写到**脚本自己所在的目录**下的 `van_capture_1.txt`。
   自测：`-SelfTest`（9 项）；手边没板子时用
   `-ReplayFile sample-log.txt` 能把"分块→续行→统计→落盘"整条流水线跑一遍
   （已验证写出文件与输入**逐字节一致**）。

   两个脚本的日志格式完全一致、都是 UTF-8 无 BOM，拿回开发机都能分析/回放。
   屏幕上会有一条实时滚动的进度行（字节/好帧/坏帧/各 IDEN 计数），
   结束时直接给一张"这趟抓到了什么"的小结 —— **车上当场就知道有没有白跑**。
5. **只抓，不动别的**：不要同时接 OBD / 不要开第二块设备 —— 一次只引入一个变量。
6. 结束：**先断开 VAN 两根线，再断充电宝**。

### D. 回来怎么分析（把原始数据变成结论）

1. **先看完整性**（`capture-van.ps1` 收尾时已经把这张表打出来了，这里是复查）：
   ```powershell
   $f = 'C:\Users\Public\206dash\van_capture_1.txt'
   (Select-String -Path $f -Pattern '^VAN ').Count                  # 好帧(可回放)
   (Select-String -Path $f -Pattern '^# VAN 校验失败').Count         # 坏帧(收到但 CRC 没过)
   Select-String -Path $f -Pattern '^van: ' | Select-Object -Last 3  # edges/frames/fcs_ok 累计
   ```
   ★ 别用 `fcs_ok=1` 去数帧 —— 那行里的 `fcs_ok` 是**累计个数**、不是标志位，
   而且整行只在计数有变化时才打印（2026-09-18 改正：原来那条命令根本数不出东西）。
2. **找车速帧**：车速是最会变的量。把日志按时间切片，找"**某个 IDEN 的
   `data[2..3]` 随车速单调变化**"的那一条 —— 如果是 `0x824` 就与假设一致 ✓
   如果 IDEN 是别的值，现场改一行就能用：
   `VanSource::configureSpeedFrame(iden, offset, scale)`（API 已经在，见 `van_source.h`）。
3. **FCS 约定**：拿"成对的真实数据"（同一 IDEN 的连续帧）重跑枚举 ——
   上次是用公开抓包、只能猜字段边界；这次**字节边界是真的**，成功率完全不同。
   枚举代码可直接复用 `test/test_dashcore/test_van_wire.cpp` 里那套。
4. **ACK 位**：`Stats::ackDominant()` 与 `frames_dropped` 已经在统计，看它是否稳定。
5. **桌面上回放验证**：把抓到的加速片段用 `tools/serial-capture/replay.py` 贴回设备，
   看 `SRC speed=van` 与表情反应 —— 整条链在桌面就能验，不用再上车。
6. **结论写进 `ACCEPTANCE.md`**：实车必验清单那 5 条逐条打勾或改写，
   并把"抓到的原始文件"路径记下来（下次不用重抓）。

### E. 已知的坑（都踩过或差点踩）

- **`RS` 接到 VCC** → 收发器进待机，`edges` 恒为 0，看起来像"没接上"。
- **120Ω 没拆** → 总线多一个终端，边沿变缓/幅度变小，解码时好时坏。
- **VAN_H/L 接反** → `edges` 涨、`frames` 不涨（先对调，别急着改代码）。
- **笔记本插着市电充电器 + 接车 12V** → 地环流（`PURCHASE.md` 第六节），可能打坏 USB 口。
  （只用电池、或用车上 12V 转 USB 供电都安全。）
- **手敲 `python tools/serial-capture/capture.py`** → `ModuleNotFoundError: No module named
  'serial'`：pyserial 只装在 `.pio-pylibs` 里，必须先 `$env:PYTHONPATH=...`（脚本已代劳）。
- **用 `>` 重定向存日志** → PS 5.1 写成 UTF-16LE，回来 grep/rg 全是乱码；
  用 `--out`（或 `capture-van.ps1`），写出来的是 UTF-8 无 BOM。
- **`dropped` 一直在涨** → 串口 115200 只有 ≈11.5KB/s，而 VAN 帧一行约 48 字符，
  总线超过 ~230 帧/秒时 `Serial0.write` 会阻塞、把主循环一起拖住 → 丢边沿 →
  坏帧变多，**看起来像"FCS 约定不对"，其实是串口自己吞了**（这是最坏的一种误判）。
  逃生门：把 `src/main.cpp:209` 的 `dash_log_begin(115200)` 改成 `921600` 重刷，
  再用 `capture-van.ps1 -Baud 921600` 抓。**先看 `dropped` 再怀疑协议。**
- **拿 MCP2515 那类"CAN 模块"当控制器用** → 不需要：VAN 的 4B5B/E-Manchester/CRC
  全在我们固件里做，`MCP2515` 不参与；只用模块上**收发器那半边**（RO→GPIO16）。
- **同时接 OBD 抓帧** → 两个变量一起动，出了问题说不清是谁的。
- ★ **"车上一接就有几千边沿/秒、但一帧都解不出"**（2026-09-18 实车实测）：
  拔掉 GPIO16 → 立刻 0 边沿；把 CANH/CANL 从车上完全拆下来（悬空）→ **也是 0 边沿**。
  说明模块本身安静、没问题，是**那两根线在往接收器里送东西**。
  那两根线**是双绞线、对地 2.82V / 4.05V、不相等且电压在浮动** —— 长得就像一对醒着的
  差分总线 ✓。但 60 秒里边沿率**几乎不变**：3316~4166/s、平均 3487/s
  （期间还开门 2 次、按危险灯 3 次，**没有对应的凸起**）✗。
  → 两种可能还没分开：①这是真广播（周期性广播本来就稳，事件只多几帧，看不出来）；
  ②这是个与数据无关的稳定 ~1.7kHz 方波（3400 边沿/秒 = 294µs 均匀间隔）。
  **分开它们只需要一眼直方图**（`tools/van-decode/edge_stats.py`）：
  · 有 **4µs / 8µs 一撮** → 真是 125kbit/s 的 VAN 数据 ✓（那就是我们解码器的错）
  · 全是 **≈294µs** → 就是那个 1.7kHz 方波，跟总线数据无关
  · 出现 **16µs** → 62.5kbit/s
  ★ 已定案（2026-09-18/19，这一节原本的疑问都答完了）：真的是 VAN，
  而且**槽时间是 8.25µs（≈121kbit/s），不是 125kbit/s 的 8.00µs**；
  固件已改成实测值（`van_wire.h` 的 `kTsNs = 8250u`）。上面那几条"看着像 8µs"
  的经验已过时 —— 下次量到一撮 8.25µs 才对。详见 `ACCEPTANCE.md` 里两条实车条目。
  真总线的判据：`edges` ≥ 10k/s 且起伏明显，同时 `frames > 0`。
  来源：<https://www.eevblog.com/forum/projects/van-bus-interfacing-(to-read-car-speed-and-engine-rpm-peugeot-206-1-4-hdi-2002)/>

### F. USB 逻辑分析仪该怎么用（买了，但**不是**上车必需品）

> **这一节写在实车量槽时间之前（2026-09-17/18）。结论已经出来了**：
> 槽时间 = **8.25µs（≈121kbit/s）**，不是下面预期的 8µs/125kbit/s；
> 固件时基已按实测值改成 `kTsNs = 8250u`。下面的采样率/用法仍然有效，
> 但凡是写"槽 = 8µs"的地方都按 8.25µs 读。

上车只带板子 + 笔记本就够。分析仪是"**第三种意见**"：当固件给出的数字讲不通时，
它能给出与我们的代码完全无关的**时间真相**。三个值钱的用法：

1. **位级取证（现在最值钱）**：整条解码链架在"一个槽 ≈8.25µs（≈121 kbit/s）、
   4B5B + E-Manchester"这个**实测**结论上（`van_wire.h` 的 `kTsNs`；规范标称的
   125 kbit/s ⟹ 8µs 只是参考）。分析仪夹在
   **模块 `RX` 输出**上，量出来的槽宽、SOF 长度、FCS 位数就是**实测**而不是假设。
   算采样率：24MHz 的便宜款 = 每槽 ≈42 个采样点，量槽宽绰绰有余。
2. **丢边沿交叉验证**：ISR 里两个边沿挨得太近会**静默**丢一个，
   而 `dropped` 只统计"队列满"，看不到这种丢法。分析仪和固件同时抓同一根线，
   两边帧数对不上 → 说明边沿真的丢了（升级方向：RMT，见 `van_phy_gpio.cpp` 文末）。
3. **3.3V 链路核对**：模块 `RX` 输出 与 GPIO16 上看到的应当是同一串边沿，
   有差就是接线/电平问题，不用怀疑协议。

以后屏幕阶段：用它量 **SPI 的真实时钟与整屏 flush 时间**，把"SPI 到底够不够
480×480"从估算变成实测。注意 24MHz 的便宜款**看不了 40~80MHz 的 SCK**
（采样定理），要量就先把 SPI 时钟降到 ~10MHz 再量。

它**做不到**的（别抱错期待）：
- sigrok/PulseView 的 131 个解码器里有 CAN / LIN / FlexRay / SAE J1850，
  **没有 VAN** —— 它给你时间真相和原始位流，不会替你解出 `IDEN=0x824`。
  （真要在 PulseView 里解 VAN，得自己写 PD，libsigrokdecode 的解码器就是 Python。）
- 不能替代万用表：认 5/10 脚那两根线靠的是**对地 2~3V 的共模电压**，
  分析仪只有"高/低"，量不出 2.3V 和 2.7V 的区别。
- 不能给板子供电，也不能替你记录日志（那是笔记本 + 脚本的事）。
- 多数廉价克隆款**只能收不能发**，不能当信号源往总线上灌帧。

⚠️ **接法红线**：只夹 **3.3V 侧**（模块 `RX` / GPIO16 / 板子 GND）。
**绝不夹 CANH/CANL** —— 那是车的 12V 域差分线，可能打坏输入，
而且等于把车地拉到笔记本上（同一个地环流问题，见 A 节共地那一段）。
用分析仪时笔记本同样要**拔掉市电充电器**。

#### 具体到手上这块：24MHz / 8 通道（FX2LP + fx2lafw 克隆款）

采样点存在**主机内存**里，不在板子里 —— 所以"能抓多久"由采样率决定
（8 通道、每采样点 1 字节）：

| 采样率 | 每槽采样点数（槽 ≈ 8.25µs，实测） | 1 秒占内存 | 适合干什么 |
|---|---|---|---|
| 24 MHz | 198 | 24 MB | 精细量槽宽/边沿抖动，**只能抓几秒** |
| **2 MHz** | 16 | 2 MB | 抓 1 分钟做帧数交叉验证 —— **主用档** |
| 1 MHz | 8 | 1 MB | 抓 10 分钟（占用 ~600MB）做长时间对比 |

- 软件：PulseView（sigrok）选 `fx2lafw` 驱动；Windows 上一般先用 Zadig 装 WinUSB
  驱动；**固件是上位机连接时上传的**，不用自己刷。它的解码器里没有 VAN，
  也没有 Manchester（"Bi-Phase encoding family" 还停在候选清单），
  所以实际只用得上 `Counter`（数边沿）和 `Timing`（量边沿间隔）。
- **第一个该量的数：最短脉冲宽度**。**实测 ≈8.25µs（≈121 kbit/s）** ——
  与 `van_wire.h` 现在的 `kTsNs = 8250u` 一致 ✓（规范的 125 kbit/s ⟹ 8µs 只是标称）；
  ≈16µs = 62.5 kbit/s —— 那整套槽时间假设都要重算，比 CRC 问题更根本。
- **最漂亮的一次对比**：同一根线（模块 `RX`），分析仪用 `Counter` 数边沿，
  对固件那行报的 `edges=`。基线对齐办法：先用
  `python tools\serial-capture\capture.py COM4 8 reset` 复位板子
  （`edges` 归零 + 打出开机横幅），**同一时刻**开始分析仪抓取，跑 10~30 秒后对数。
  差得多 = ISR 真在丢边沿（升级 RMT）；对得上 = `edges`/`dropped` 这两个数可以信。
- **量 SPI 的正确姿势**：24MHz 采样对 10MHz 的 SCK 只有 2.4 点/周期（解不出来），
  40~80MHz 直接看不见 ✗。它只能验证**协议/时序/字节序**（先把 SPI 降到 1~4 MHz），
  "SPI 够不够快"要问固件自己 —— `dash_display_spi.cpp` 每秒已经打一行
  `spi: 左 flush=…(+…/s …KB/s ~…fps)`，那才是带宽问题的答案来源。

#### 抓一段真波形 → 离线判定"槽时间"（2026-09-18 加）

实测证实那对线是**醒着的差分总线**（双绞线、对地 2.82V/4.05V 不相等、电压浮动），
但固件 `frames=0` —— 分歧点只剩一个：**真实的槽时间是多少**。
别再靠假设猜，抓真波形算：

1. 分析仪夹 **模块 `RX`**（红）+ **模块 `GND`**（黑）。只夹 3.3V 侧，别夹 CANH/CANL。
2. PulseView：`fx2lafw`、采样率 **4 MHz**、采样数 **20000（= 5 ms）**、上升沿触发。
3. `Save As → CSV`（5ms 很小）或 `VCD`，然后：

   ```powershell
   python tools\van-decode\edge_stats.py 抓的.csv
   ```

它会直接给出**边沿间隔直方图**（协议无关）和判定：
**≈4.1µs = 半槽 → ≈121 kbit/s（实测；规范标称 125 kbit/s 的"4µs 半槽"）**／
**16µs → 62.5 kbit/s**／
都不接近 → 不是 VAN；另外还会按"间隔 >20µs 算空闲"分出**突发段**，
帧长和帧间隔一眼可见。工具自带 `--selftest`（合成 8µs/16µs 两种，已通过）。

## 18PIN FPC 那两个座到底是什么（2026-09-21 查官方原理图）

单页原理图里两个 18PIN FPC = **L7**（LCD1-Board，带 MCU 的主半边＝wiki 第⑰项）与 **L9**（LCD2-Board，可掰下的半边＝第⑱项），逐脚（只有第 10 脚不同）：
`0=GND 1=3V3 2=LCD_BL2 3=GND 4=LCD_CLK 5=LCD_DIN 6=LCD_DOUT 7=LCD_DC 8=LCD_RST2 9=GPIO1 10=L7:GPIO0(经R8 0R)/L9:SD_CS 11=LCD_CS2 12=TP2_RST 13=TP2_SCL 14=TP2_SDA 15=TP2_INT 16=SD_CMD 17=SD_SCK 18=SD_D0`
两座都**不是独立通道**，是板载 **LCD2** 那一路（CLK/DIN/DOUT/DC/RST2/BL2/CS2＋触摸 TP2_*＋TF 线）的**并联引出**；LCD1 的信号（CS=GPIO47、RST=48、BL=46、TP1_*）一个都没引出来。
不掰板加第三块屏只能走 **L7**，其 CS 必须接**第 9 脚 GPIO1**（第 11 脚 LCD_CS2 是板载 LCD2 的 CS，接上去两块同时被选中＝抢总线、会打架）；DC/RST/背光与 LCD2 共用，不能独立复位。
GPIO1 平时经 **R14（0R/NC，图上注 `GPIO1（BAT_ADC）`）** 接电池 ADC＝wiki 第⑫项「GPIO1 功能选择电阻」：装上＝量电池，拆/挪＝才空出来给 FPC 第 9 脚。★ 资源清单写的「R12」不是位号：原理图 R12 = MP1605GTF-Z 反馈下电阻 **44.2K±1%**（与 R6 200K 分压，图上自注 `VOUT=0.6*(1+200/44.2)=3.314V`），改它改的是 3V3 电源 —— 别动 → 待确认。
L9 第 9 脚另有 **R13（0R/NC）** 可把 GPIO1 并到 `LCD_CS2'`（板载 LCD2 面板的 CS 网），默认装配状态**文档未说明**；座上空脚只有 GPIO1（两座第 9 脚，图上粗线）与 GPIO0（仅 L7 第 10 脚，经 R8 0R）→ 只能加 **1 块**，加 2 块文档未说明。
来源：原理图单页 L7/L9/R8/R12/R13/R14 区；wiki ⑫⑰⑱＋Interfaces 两表；例程 `…/LVGL_Arduino_Touch-Half-B/ReadMe.txt`（Half-B 由 ESP32-S3-LCD-Driver-Board 的 FPC 排线口驱动）。
