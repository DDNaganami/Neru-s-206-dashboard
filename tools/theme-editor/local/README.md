# local/ —— 你自己的产物，**不提交**

放什么都行：页面导出的 `theme.json` / `image.bin` / `assets.bin` 副本、临时对照文件……

- 本目录内容已被 `.gitignore` 忽略（只保留本文件）⇒ `git status` 不会被弄脏。
- 用途是"**别把导出物乱丢在源码目录里**"：要刷板的那份，直接用页面导出的文件即可。
- 为什么美术资产不入库：`LICENSE-ARTWORK` 是"保留所有权利"，误提交会让许可证声明与仓库内容不符。
- ★ 注意：`../theme.json`（配色页/图片页与 JS 回归按这个路径找它）**不要挪走** ——
  它同样已被忽略，但路径是功能性的。