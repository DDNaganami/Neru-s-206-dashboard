# 法系车 VAN 舒适总线 —— 实测协议参考

> **这是一份协议参考，不是工作日志。**
> 「谁在哪天做了什么」留在 `ACCEPTANCE.md`；本文只回答四件事：
> **这条总线上有什么 · 字段怎么解 · 证据是什么 · 哪些还没解。**
>
> 本文所有结论都带证据（提交哈希 / 数据文件名 / 具体测法）。
> 没有证据的一律标 `推断（待确认）`，并写明怎么确认。
> 需要细节时**引原文，不抄原文**（见文末「引用索引」）。

---

## 1. 适用范围与数据来源

### 1.1 适用对象

PSA / AEE2001 平台的 **VAN 舒适总线**（VAN = Vehicle Area Network）。
本项目的实测对象是一台 **标致 206 MUX 1.4HDi（左舵）**，VIN `VF32DNFUR2W005450`
（`VF3` = 法国·标致）——VIN 是从总线本身读出来的，见 §4.7。

**换车、换网络段必须重跑全部结论。** 本文的帧目录表只在**这一辆车的这一段总线**上验证过。

### 1.2 两类来源（★ 别混）

| 类别 | 内容 | 出处 |
|---|---|---|
| **A. 本项目实测** | 全部帧目录、字段偏移、位定义、速率、CRC 参数 | 2026-09-18 起的实车抓包，见下表 |
| **B. 外部资料（仅作背景）** | VAN 帧的槽位结构、4B5B / E-Manchester、CRC-15 多项式、SOF 图案 | Graham Auld, *VAN bus line protocol*；`morcibacsi/VanAnalyzer`、`morcibacsi/psa_van_bus_packet_descriptions` |

**外部资料只用来"提出假设"，不用来"当结论"。** 本项目已有多处实测**推翻或修正**外部资料的例子：

- 外部资料说 125 kbit/s（8 µs/槽）⇒ **实测 8.25 µs/槽（≈121 kbit/s）**，差 3.1%（§2.2）；
- 早期按公开文档把车速读成 `data[2..3]` 16 位 ×100 km/h ⇒ **实测是单字节 `data[2]`**，
  且标度既不是 100 也不是 1.0，而是 **2.56**（§4.2）；
- SOF「折 10 槽 == 0x0F」这个说法**是错的**，SOF 是**裸槽**不走 4B5B（`lib/dashcore/van_wire.h` 文件头）。

### 1.3 数据文件清单

| 文件 | 内容 | 用途 |
|---|---|---|
| `tools/van-decode/sample-diffmanchester.csv` | 1.2 s 真边沿切片，66 帧 | 解码链的最小复现夹具 |
| `tools/van-decode/sample-speed-824.csv` | 0.6 s 真边沿切片，33 帧 / 其中 `0x824` 12 帧 | 车速字段的黄金值（`test_van_real_capture.cpp`） |
| `tools/serial-capture/drive-2026-09-22-van.csv` | 6561 行 / 368.2 s；**只有 `0x824`**，旧列宽 | 车速标定（§4.2） |
| `tools/serial-capture/drive-2026-09-22-obd.csv` | 599 条 `010D` + 599 条 `010C`（**成功 599/599**） | 车速/转速的**地面真值** |

> ★ `drive-2026-09-22-*.csv` 两份是**旧列宽**（`spd_count,data0,data1,rpm_count,seq`），
> `data[3]` 及以后**没有落盘**。所以「VAN 上有没有进气温度副本」这类问题
> **用这两份答复不了**，必须用 `drive-log.ps1` 的新列宽重跑。
> 采集脚本已扩容到整帧落盘（见 `ACCEPTANCE.md` 的 2026-09-22 采集工具扩容条）。

---

## 2. 物理层与位层

### 2.1 差分与硬件

VAN 是**差分**总线，**极性与标准 CAN 相反**。收发器用 SN65HVD230（只用收发那半边，
`RO` → ESP32 GPIO；`MCP2515` 那类 CAN 控制器**不参与**，因为 4B5B/CRC 全在我们固件里做）。

> ★ **接线、认线、上车次序、供电与安全，一律见 `PINOUT.md`**（§「接线：三处取信号」、
> §⑤「VAN 极性」、§「抓帧盒」A~F）。本文**不重复**那份内容。
> 只记一条最容易白跑一趟的判据：**`edges` 涨、`frames=0` ⇒ 第一处置就是对调那两根线**
> （`PINOUT.md` §⑤；实测依据 `d512d7c`、`PINOUT.md` 227 行的 11 秒 492 帧）。

### 2.2 时基：★ 实测 8.25 µs/槽（≈121 kbit/s），不是标称的 125 kbit/s

| 项 | 规范标称 | **本项目实测** | 证据 |
|---|---|---|---|
| 槽时间 TS | 8.00 µs（125 kbit/s） | **8.250 µs** | 2026-09-18/19 逻辑分析仪抓包（30 s + 5 min），跳变间隔全落在它的 1/2/3/4/5 倍上；`ACCEPTANCE.md` 917 行那条 |
| 等效速率 | 125 kbit/s | **≈121 kbit/s** | 同上 |

★ 这是一个**必须按实测走**的数：按 8.00 µs 解码每 32 位就漂掉一个整槽，
这正是固件 `frames=0` 的直接原因（3.1% 偏差）。
在代码里，`lib/dashcore/van_wire.h` 的 `kTsNs = 8250u` 是**唯一时基**；
`kBitRateHz = 125000u` 只是规范标称值，**不要从它反推时基**。

### 2.3 帧结构（槽位表）

TS = Time Slice = 线上一个位的时间。`n` = 数据字节数（**本文全部 `data[i]` 的下标口径见 §2.8**）。

| 字段 | 槽数 | 说明 |
|---|---|---|
| Start of frame (SOF) | **10 TS** | 固定图案 `0000111101`。★ **裸槽，不走 4B5B** |
| Identifier (IDEN) | **15 TS** | 3 个 4B5B 组 = 线上承载 **12 位**（`0x824` 就是它） |
| Command (CMD) | **5 TS** | 1 个 4B5B 组 = **4 位**（EXT / RAK / R-W / RTR） |
| Data | **10n TS** | `n` 字节（`data[0] … data[n-1]`），每字节 2 个 4B5B 组；协议允许 `n ≤ 224` |
| FCS | **20 TS** | 4 个 4B5B 组 = 16 位 = **15 位 CRC + 1 个固定 0 位** |
| End of data (EOD) | **0 TS** | 不额外占槽：FCS 末字节的 bit0（恒 0）与它后面那个编码位（恒 dominant）构成一次 E-Manchester 违约 ⇒ 这一位就是 EOD |
| Acknowledge (ACK) | **2 TS** | 1 recessive + 1 dominant，**仅被应答的帧有**（判据见下） |
| End of frame (EOF) | **8 TS** | 8 个连续 recessive |
| Inter-frame | **8 TS** | 帧间至少 8 TS recessive（不算帧内） |

**帧长公式**：`槽数 = 10 + 15 + 5 + 10n + 20 (+2 若被应答) = 50 + 10n (+2)`
⇒ **反解 `n` 就是独立测 `n` 的手段**：`n = (实测槽数 − 50 − (被应答 ? 2 : 0)) / 10`，
不需要逐字节对照，也不需要 FCS（§3 那张表的 `n` 就是这么定的，见 §3.0）。

**实测逐族吻合**：`0x824/0x8` = **120 槽**（`n=7`，无 ACK）、`0x464/0xC` = **102 槽**（`n=5`，有 ACK）、
`0x4EC/0xF` = **50 槽**（`n=0`，无 ACK）……16 个族全部逐帧吻合。

### 2.4 编码：E-Manchester（= 4B/5B）

每 4 个数据位编成 5 个 TS，**第 5 位是编码位**，解码时丢弃。
★ 实测的编码位规则比「丢弃第 5 位」更强：**第 5 位 = 第 4 位的反相**。

由此得到两个很有用的性质：

- 正常的 5 槽组**永远含一次跳变**，而**全帧恰好最后一组违反它** ⇒ 这一位就是 **EOD**
  ——「帧尾在哪」的唯一可靠判据；
- 帧内**最多 4 个连续 recessive**，所以「8 个连续 recessive」只能出现在 ACK/EOF
  ⇒ 帧尾判据无歧义。

### 2.5 FCS：CRC-15 参数与覆盖范围（已定案）

| 参数 | 值 |
|---|---|
| 多项式 | `x^15+x^11+x^10+x^9+x^8+x^7+x^4+x^3+x^2+1`（完整掩码 `0x8F9D`，15 位寄存器实取 **`0x0F9D`**） |
| 初值 | **`0x7FFF`** |
| 输出 | **取反**（`^ 0x7FFF`） |
| 位序 | **MSB-first，不反射** |
| **覆盖范围** | **IDEN(12 位) + CMD(4 位) + 全部数据字节**，即 `[idenByte1, idenByte2, data[0] … data[n-1]]` 这 `2+n` 个字节。**不含 SOF，也不含 FCS 自己** |
| 线上字段 | 16 位**大端**（高字节先发），内容 = `[15 位 CRC][1 个固定 0 位]`，即 `field = crc << 1` |

★ **校验前必须先确认最低位是 0**，否则等于把 EOD 当成 CRC 的一位。

★ **`crc15()` ≠ VAN 的 FCS**：`crc15()` 用的是 CAN-15 多项式 `0x4599`，
在本总线上**实测 17106 帧 0 命中**，只留给历史测试当对照，**不许拿它校验帧**。

**实测命中**：`drive5min.csv` **17106/17106 帧**、`sample-diffmanchester.csv` **66/66 帧**，
两条独立抓包逐帧命中。复现工具 `tools/van-decode/fit_fcs.py`。

### 2.6 空闲与关帧

- **关帧门限**：`70 µs`（2026-09-19 先量后定：`300 µs` 会把背靠背帧并成一帧）。
- **帧内最长 recessive 连段** = FCS 末字节全 1 时的 4 槽 ≈ 33 µs（@8.25 µs/槽），
  远小于 70 µs；帧间空闲实车常见 ms 级。

### 2.7 IDEN 与 CMD 的位定义

**IDEN = 12 位有效**。规范里 IDEN 是 15 位（`0x000`/`0xFFF` 保留），但线上只用 15 个 TS
= 3 个 4B5B 组承载 12 位 ⇒ **bit12..14 不在线上**，实际能收到、能还原的就是 12 位。
15 位与 12 位的换算关系**尚未用真实位流确认**（`推断（待确认）`）。

**IDEN 的字节布局**（编解码唯一契约，只在 `van_wire.h` 定义）：

```
byte1 = IDEN 的 bit11..4      ← 组 1(bit11..8) + 组 2(bit7..4)
byte2 的高 4 位 = IDEN 的 bit3..0 ← 组 3
byte2 的低 4 位 = CMD
```

> ★ 旧实现写的是「byte1 = IDEN 低 8 位、byte2 高 4 位 = IDEN 高位」——**错的**，
> 与真实抓包解出的字节流（`0x82 0x48` ⇒ IDEN `0x824`）对不上。

**CMD 4 位**：

| 位 | 掩码 | 含义 |
|---|---|---|
| bit3 | `0x8` | **EXT**，保留，应为 1 |
| bit2 | `0x4` | **RAK** —— ★ 实测语义：「本帧**要**应答」 |
| bit1 | `0x2` | **RW**：1 = 读，0 = 写 |
| bit0 | `0x1` | **RTR**：1 = 无数据（请求帧） |

★ **RAK 的实测语义与旧注释相反**（2026-09-19，17106 帧）：
FCS 之后到底有没有 ACK 两槽与这一位一一对应 ——

- `bit2=1` 且 `RTR=0`（CMD = `0xC` / `0xE`）⇒ 帧尾多 2 槽，第 2 槽 dominant ⇒ **有应答**；
- `bit2=0`（`0x8`）或 `RTR=1`（`0xF`）⇒ 帧尾**没有那 2 槽**（全 recessive，并进空闲），
  帧体因此**少 2 槽**（这也是帧长随报文族变化的原因之一）。

⇒ 判据函数：`cmdExpectsAck(cmd) = (cmd & 0x4) && !(cmd & 0x1)`。
在 §3 的帧目录里，**ACK 列与这条规则 16/16 全部吻合**，是这套位语义的独立旁证。

### 2.8 ★ `data[i]` 的下标约定（**全文唯一口径，与固件同一套**）

**约定**（一句话）：**`data[0]` = IDEN / CMD 之后的第 1 个数据字节；`n` = 数据字节数
⇒ 合法下标恰好是 `0 .. n-1`；`data[i]` 与固件的 `VanPacket::data[i]` 是同一套下标、同一个字节。**
`data[n]` 及更大的下标**不存在** —— 任何 `i ≥ n` 的写法都是错的。

这条约定不是新发明的，它就是固件实际在用的那套，三处可查（本轮核过，不是转抄注释）：

| 出处 | 它怎么定死这条约定 |
|---|---|
| `lib/dashcore/van_source.h` → `struct VanPacket` | `uint8_t data[28]; uint8_t len;` —— `len` 就是 `n`，数据独立成数组；CMD **不在** `data[0]` 里（它是单独的 `cmd` 成员） |
| `lib/dashcore/van_wire.cpp` → `frameToPacket()` | `uint8_t n = f.len; out->len = n; for (i = 0; i < n; ++i) out->data[i] = f.data[i];` —— 从 `0` 起、**左闭右开**地搬 `n` 个字节 |
| `lib/dashcore/van_source.cpp` → `VanSource::onPacket()` | 车速读 `data[2]` 之前先判 **`speed_offset_ < pkt.len`**、转速读 `data[0..1]` 之前先判 **`kRpmOffset + 1 < pkt.len`** —— 这就是「下标必须落在 `0..n-1` 内」的代码化写法 |
| `src/main.cpp` → `VanLogSink::onPacket()` | 打印机器可读的帧行 `VAN %03X <data[0]> … <data[len-1]>`（`for (i = 0; i < pkt.len; ++i)`），CMD/ACK 打在行尾注释 `# cmd=.. ack=..` 里、**不算载荷** ⇒ 那行 hex 的第 i 个就是 `data[i]` |

★ **一个直接的自证（本车的 `0xE24`）**：它的 `n = 17`，而载荷恰好是 **17 个可打印 ASCII 字节
= 17 位 VIN**（`VF32DNFUR2W005450`，§4.7：`56 46 33 32 44 4E 46 55 52 32 57 30 30 35 34 35 30`）。
**VIN 是 17 位**，载荷也是 17 字节 ⇒ 下标只能是 `data[0..16]`（最后一个字符落在 `data[16]` = `0x30`）。
下标、字节数、字符串长度三者对齐，**`0 .. n-1` 这条不用再解释**。

**为什么必须写死这条**：同一族报文的「帧里有多少字节」与「某字段落在第几个字节」是两个独立的事实，
写岔一个就会出现「本行标 `n=4`、却在别处引用 `data[5]`」这种**自相矛盾**（`data[5]` 要求 `n ≥ 6`）。
本文 §3 的表以前就有 4 行是这么岔的（`4FC` / `4DC` / `664` / `524`），
2026-09-23 用实测槽数逐行对齐过，改法与证据见 **§3.0**。

**遇到「表里的 `n` 和下标打架」时的判定顺序**（别反过来）：

1. **先信实测的槽数** —— `n = (实测槽数 − 50 − (被应答 ? 2 : 0)) / 10`（§2.3）；
2. **再看被引用的最大下标**：字号段在**这一趟操作的窗口内**动过（有专门实验）⇒ 那个字节必然真实存在
   ⇒ **`n` 必须 ≥ 该下标 + 1**；
3. 两者都指向同一个 `n` 就改 `n`；**只有在拿不到槽数、也没有操作窗口证据时**，才把该行标 `待确认`，
   并写清待确认的是 `n` 还是下标，以及用 `drive-log.ps1` 的 `len` 列（它落 `iden/len/cmd/ack/d0..d6`）去测。

---

## 3. 帧目录表（实测）

**来源**：`5f43d1d`（全量 IDEN 嗅探）+ `f839a7d`（总线全貌实测表）。
**采集条件**：2026-09-22，车通电并点火、**怠速、无行驶**、逐帧打印开启，采样 **110 秒**。

**置信度三级**：

- **实测确认** —— 有专门实验（逐档开关、两次印证、回归、全帧 CRC 命中）
- **单次观察** —— 只在一趟抓包里看到，没做重复实验
- **推断（待确认）** —— 从规律反推，未直接验证

| IDEN | CMD | 帧数 | 速率 /s | 占比 | ACK | 长度 n | 已知字段 | 证据 | 置信度 |
|---|---|---|---|---|---|---|---|---|---|
| `824` | 8 | 1977 | 18.0 | 35.2% | 无 | 7 | **转速、车速、里程累计、帧序号**（§4.1/§4.2） | `f839a7d` 表；`1174` 条（2026-09-20 双抓包定案） | **实测确认** |
| `464` | 12 | 1043 | 9.5 | 18.1% | 有 | 5 | 未解（**已排除它不是转速**：只有加速时才从 0 跳到 4096，不是平滑怠速平台） | `f839a7d`；`ACCEPTANCE.md` 1209 行 | 未解 |
| `4FC` | 12 | 557 | 5.1 | 9.1% | 有 | **11** ★ | `data[5]` = **灯光位域**（§4.3/§4.4）；`data[1]` = 门状态（§4.6）；`data[0]` bit5 = 双闪继电器回读 | `ee15e9e` `1a5d69b` `2826610` `9ad3d10` `fa583f4`；`n` 见 §3.0 | **部分实测确认**（远光未解） |
| `4DC` | 12 | 515 | 4.7 | 9.4% | 有 | **7** ★ | 未解（`data[4]` 有 208 种取值，像连续量；★ 已确认它是**单调递增计数器**而非门位，见 §6） | `f839a7d`；`fa583f4`；`n` 见 §3.0 | 未解 |
| `664` | 12 | 480 | 4.4 | 8.5% | 有 | **11** ★ | 未解 | `f839a7d`；`n` 见 §3.0 | 未解 |
| `524` | 8 | 206 | 1.9 | 3.5% | 无 | **14** ★ | `data[0]` = **门状态**（与 `4FC.data[1]` 逐帧同步）；`data[9]` 门开段 `01`/`18`、静息 `FF` | `9ad3d10` `fa583f4`；`n` 见 §3.0 | **部分实测确认** |
| `8A4` | 8 | 209 | 1.9 | 3.7% | 无 | 7 | `data[0]` **逐帧交替** `8F`/`AF`；`data[6]` ★ **是慢变量，不是门位**（§6 撤回②） | `f839a7d`；`fa583f4` | 部分（`data[6]` 已否定） |
| `9C4` | 12 | 206 | 1.9 | 3.7% | 有 | 2 | 未解（★ 与仓库别处记的 `984/0x8` = 5 字节**对不上**，见 §6） | `f839a7d` | 未解 |
| `5E4` | 12 | 194 | 1.8 | 3.4% | 有 | 2 | 未解（2 种取值；与 VIN **无关**，见 §4.7） | `f839a7d` | 未解 |
| `E24` | 8 | 101 | 0.9 | 1.9% | 无 | 17 | **VIN 明文广播**（§4.7）：17 字节 = `data[0..16]`，自证下标约定（§2.8） | `f839a7d`；`n` 见 §3.0 | **实测确认** |
| `4EC` | 15 | 99 | 0.9 | 1.9% | 无 | 0 | 0 字节请求帧 | `f839a7d` | 单次观察 |
| `4D4` | 15 | 98 | 0.9 | 1.7% | 无 | 0 | 0 字节请求帧 | `f839a7d` | 单次观察 |
| `564` | 14 | 7 | — | — | 有 | 27 | 稀有 | `f839a7d` | 单次观察 |
| `8C4` | 12 | 7 | — | — | 有 | 2 | 稀有 | `f839a7d` | 单次观察 |
| `984` | 8 | 1 | — | — | 无 | 5 | 本趟只出现 1 次；★ 与 `9C4` 的关系未澄清（§6） | `f839a7d` | 单次观察 |

### 3.0 ★ `长度 n` 这一列是从哪来的（2026-09-23 逐行重核，改掉 4 行）

**为什么要重核**：上表 `长度 n` 这一列在 `fec6dda` 建本文时**没有任何出处**，而且是**逐行独立**的
一个数字。它与本文自己引用的下标对了 12 行、错了 4 行 —— 错的 4 行属于**自相矛盾**：
本行标 `n=4`，同一行却引用 `data[5]`（§2.8：`data[5]` 要求 `n ≥ 6`）。下表逐行核对后**只改了 `n`，
没有改任何下标**（改的是那个无出处的数字，字号段的位置另有专门实验支撑，见 §4.3~§4.7）。

**独立口径（两条，都不是本文自证）**：`n = (实测槽数 − 50 − (被应答 ? 2 : 0)) / 10`。

- **口径 A** —— `ACCEPTANCE.md` 1015 行那条 2026-09-19 帧布局定案（**slot 级、逐族**）：
  `0x824/0x8=120`、`0x464/0xC=102`、`0x4FC/0xC=162(11)`、`0x524/0x8=190(14)`、
  `0xE24/0x8=220(17)`、`0x564/0xE=322(27)`、`0x4EC/0xF=50(0)`、`0x984/0x8=100(5)`。
- **口径 B** —— 本轮用仓库现成工具重跑同一份抓包（`decode_frames.py` 的
  `load_frames`/`frame_to_slots`/`split_sof`/`n15_of`/`n5_of`，输入 `C:\Users\Public\206dash\drive5min.csv`，
  **17106 帧、SOF 命中 17106/17106**），16 个族的槽数都只有**一个取值**：

  | IDEN/CMD | 帧数 | 槽数 | 被应答 | `n` | IDEN/CMD | 帧数 | 槽数 | 被应答 | `n` |
  |---|---|---|---|---|---|---|---|---|---|
  | `824/8` | 5978 | 120 | 否 | 7 | `5E4/C` | 602 | 72 | 是 | 2 |
  | `464/C` | 3001 | 102 | 是 | 5 | `9C4/C` | 597 | 72 | 是 | 2 |
  | `4FC/C` | 1506 | 162 | 是 | **11** | `E24/8` | 299 | 220 | 否 | 17 |
  | `664/C` | 1479 | 162 | 是 | **11** | `4EC/F` | 298 | 50 | 否 | 0 |
  | `4DC/C` | 747 | 122 | 是 | **7** | `4D4/F` | 296 | 50 | 否 | 0 |
  | `4DC/8` | 747 | 120 | 否 | **7** | `564/E` | 168 | 322 | 是 | 27 |
  | `524/8` | 614 | 190 | 否 | **14** | `8C4/C` | 162 | 72 | 是 | 2 |
  | `8A4/8` | 607 | 120 | 否 | 7 | `984/8` | 5 | 100 | 否 | 5 |

  两条口径**逐族一致**（口径 A 的 `984` 与口径 B 的 `984` 都是 5；`4FC` 都是 11）；
  再叠加**仓库里两份切片**（`sample-diffmanchester.csv` 68 帧、`sample-speed-824.csv` 33 帧：
  `4FC` 一律 **162 槽**、`524` 一律 **190 槽**、`E24` 一律 **220 槽**）⇒ 同一族跨三份独立抓包**零分歧**。
  ⚠ 切片里 `4FC` 出现过 1 帧 **149 槽**（不是 10 的倍数）—— 那是**切片首尾被切掉的半帧**
  （快照工具不保证切在帧界上），不能拿来反推长度；判据用「同一族 5..5978 帧内槽数是否为**单值**」。

**★ 改法：改 `n`，不改下标**（4 行，均已在表内以 ★ 标出）：

| 行 | 原 `n` | 改为 | 依据（两条独立口径 + 该行自己的下标需要） |
|---|---|---|---|
| `4FC` | 4 | **11** | 162 槽 − 50 − 2 = 110 ⇒ `n=11`；`data[5]` 需 `n ≥ 6`（§4.3/§4.4 的灯光位域、§4.5） |
| `4DC` | 4 | **7** | 122/120 槽（有/无 ACK）⇒ `n=7`；`data[4]` 有 208 种取值 ⇒ 必然 `n ≥ 5`（§6.1 撤回①是同一条字节） |
| `664` | 4 | **11** | 162 槽 ⇒ `n=11`（该行未解、无下标引用，长度本身仍是实测） |
| `524` | 9 | **14** | 190 槽 − 50 = 140 ⇒ `n=14`；引用过的 `data[9]` 只是**不再越界**（`9 < 14`），下标本身未动 |

**没有改的 12 行**（原值即实测值，一并记下来免得下次再查一遍）：
`824=7`、`464=5`、`8A4=7`、`9C4=2`、`5E4=2`、`E24=17`、`4EC=0`、`4D4=0`、`564=27`、`8C4=2`、`984=5`
（11 个数字、12 行 —— `4DC` 有 `0xC`/`0x8` 两个 CMD 行，两行同为 `n=7`）。

**★ 表里 `n` 与「本车 2026-09-22 那一趟」是什么关系（别读过头）**：
2026-09-22 那趟**确实把每一族都解出来了**（表的帧数/占比就是那趟），但它开的是**逐帧打印**
（`≈54 帧/秒`），而**那份逐帧原文日志不在仓库**（只有汇总表；查过的结论见 `ACCEPTANCE.md` 2211 行）。
所以本轮**没有**用那趟的日志复核 `n`，而是**绕开它**：槽数是从 `drive5min.csv` 与两份切片**重新量出来的**
（上面口径 A/B），与逐帧打印是否留存无关。⇒ 表的「帧数/占比/ACK」来自 2026-09-22，
「`n`」来自 2026-09-19 的槽级实测 —— **两类证据各管各的，不混用**。

**没有 `待确认` 的 `n`** —— 16 个族全部有槽级实测（单值、跨三份抓包一致）。
唯一**仍未澄清**的是 `984` ↔ `9C4` 的 IDEN 关系（§6.2 第 7 条）：两条 `n`（5 与 2）各自都有实测支撑，
所以那里待确认的是**「`984` 和 `9C4` 是不是同一族」**，**不是**某一行的 `n`。

**以后新增一族怎么定 `n`**：`drive-log.ps1` 落 `iden,len,cmd,ack,d0..d6`，
`len` 列就是固件解出的 `n`（与 `VanPacket::len` 同一个值）；但它**只落 7 个字节**（`d0..d6`），
所以 `n > 7` 的族要拿槽数公式复核（`n = (槽数 − 50 − ack·2)/10`），或用
`capture-van-nopy.ps1` 抓**不截断的整帧原文**。

**总帧率** ≈ **54 帧/秒**（实测每 5 秒 262~272 帧），`fcs_ok == frames`（**100% CRC**）。

**结构规律**：高 2 族（`824`/`464`）占 **53%**，中 3 族（`4FC`/`4DC`/`664`）再占 **27%**，
其余是低频族 —— 与「动态量高频、静态量低频」的直觉一致。

★ **另一份独立观测**（`5f43d1d`，车通电**未打火**、30 秒）：
`共 1556 帧 / 12 种 IDEN` —— `824=547 464=282 4DC=146 4FC=141 664=133 9C4=58 8A4=57 524=54 5E4=53 4EC=29 E24=29 4D4=27`。
两份观测的**种类集合一致（12 种）**，但速率不同（点火 vs 未打火）⇒ 各族的速率**随整车状态变化**，不是常量。

### 3.1 ★ 速率口径提醒：CSV 行速率 ≠ 总线帧速率

`ACCEPTANCE.md` 里出现过的「`0x824` ≈ **17.8 帧/秒**（抓包 6561 帧 / 368.2 秒）」是
**CSV 行速率**，不是真正的总线帧速率。依据（2026-09-22 独立复核该 CSV）：

- CSV 共 **6561** 行 / 368.207 s ⇒ **17.82 行/秒**；
- 但列 `seq`（`data[6]`，帧序号）的**模 256 递增量累加只有 4146** ⇒ **≈11.3 帧/秒**；
- `6561 / 4146 = 1.58` ⇒ 串口侧把同一个帧打印了约 1.58 次（串口行的推进快于总线帧的产生）。

**这不影响采集质量**：`115200` 下 17.8 × 46 字节 ≈ 820 B/s，离 11.5 KB/s 的上限差得远，
`fcs_ok == frames` 全程 100%。
但**引用速率时要说清是哪个口径**（行/秒 还是 帧/秒），否则会差 1.6 倍。
（总线帧速率本身随状态变化：点火怠速 18.0/s、未打火 18.2/s —— 见上表与 `5f43d1d`。）

---

## 4. 已解字段明细

### 4.1 转速 —— `0x824` / CMD `0x8`，`data[0..1]`，16 位大端 × **0.125**

| 项 | 值 | 证据 |
|---|---|---|
| 字段 | `data[0..1]`，**16 位大端**，÷8 | `1174` 条（2026-09-20 双抓包定案） |
| 怠速实测 | raw 6230..7570，**均值 7237.6 / sd 215.0** ⇒ **904.7 rpm / sd 26.9** | 怠速抓包 453 帧（去掉前 20 帧瞬态） |
| 地面真值 | 约 **900 rpm**，偏差 **0.52%** | 与转速那路的 OBD 读数互证 |
| 排除项 | ÷4 得 1809 rpm（差 101%）、÷2 得 3619 rpm —— 都排除 | 同上 |
| 量程 | 行驶抓包 raw 5988..47100 ⇒ **748..5888 rpm**，全程在断油 6300 以下 | `ACCEPTANCE.md` 1205 行 |
| 点火瞬态 | 起车前 raw 恒 0（前 127 帧唯一取值 `{0}`），t=6.376 s 跳到 6152（= 769 rpm，起动机拖动），峰值 10692（= 1336.5 rpm） | 同上 |
| 代码常量 | `kRpmOffset = 0`、`kRpmScale = 0.125f` | `lib/dashcore/van_source.h` |

★ **独立复核（2026-09-22 的行驶抓包，与 OBD `010C` 对齐）**：两份 CSV 的 `t_s` 时间轴同源
（各自 599 / 598 个时间戳，**共享 598 个**），即便**不做任何时间偏移校正**，
配对后的绝对差**中位数只有 12.5 rpm**；只取「OBD 车速为 0（车停、转速稳定）」的样本，
**中位数差 5.75 rpm，且两边的怠速中位数完全相等（都是 898.75 rpm）**。
⇒ **`kRpmScale = 0.125` 再次被独立数据支持。**

### 4.2 车速 —— `0x824` / CMD `0x8`，`data[2]`，**单字节**，× **2.56**

| 项 | 值 |
|---|---|
| 字段 | `data[2]`，**单字节**（不是 `data[2..3]` 16 位） |
| 标度 | **1 计数 = 2.56 km/h**（`kSpeedScale = 2.56f`） |
| 停车 | 恒 **0**，**不做偏置** |
| 倒车 | 全程 ≥ 0，**没有符号位或负值**；倒车在总线上与前进同码（**「倒车到底是不是 0」未验**） |
| 字段宽度 | **8 位**（取值 0..39，40 个不同值，**无「恒偶」性质**） |

**标度 2.56 的实测来源**（`c596351`；以下是 2026-09-22 独立复核的完整数字）：

- **方法**：VAN 侧（自制板，`0x824.data[2]`）与 OBD 侧（蓝牙 ELM327，PID **`010D`** = 地面真值）
  **接同一台笔记本的两个串口** ⇒ 时间戳同源（同一个 Stopwatch），不需要事后对墙上时间。
- **样本**：368.2 s 行驶；OBD `010D` **599 条全部成功（599/599）**，
  其中 **286 条非零**；按时间戳配对后 **284 对非零**（计数>0 且 km/h>0）。
- **回归**（x = VAN 计数，y = OBD km/h，配对方式见下）：

  | 拟合 | 斜率 | 截距 | R² | σ | 95% 置信区间 |
  |---|---|---|---|---|---|
  | **自由截距** | **2.5549** | +0.3010 | **0.998385** | **1.18 km/h** | 斜率 **[2.5429, 2.5669]** |
  | 过原点 | 2.5679 | 0 | 0.998340 | — | 斜率 [2.4342, 2.7016] |

- **残差**：中位数 0.057 km/h、RMSE 1.175 km/h、**max |残差| 3.73 km/h**。
  VAN 计数本身的量化（±0.5 计数 = ±1.28 km/h）+ OBD 的 1 km/h 量化 ⇒ 理论上限 σ ≈ 0.79 km/h，
  实测 1.18 km/h ⇒ **残差已经和「1 个计数就是一个刻度」的量化极限同量级**，没有系统性弯曲。
- **分档均值单调**（独立于回归）：按 OBD km/h 分档取 VAN 计数均值 ——
  `0-9 km/h → 2.12`、`20-29 → 9.05`、`40-49 → 16.57`、`60-69 → 24.82`、
  `80-89 → 32.64`、`100 → 38.67`。⇒ 是干净的线性编码。
- **覆盖**：计数 **1..39**（最大 39）、真值 **0..100 km/h**（最大 100）。

**⇒ 结论：2.56 是被本数据支持的。**

- **推翻 1.0**：按 `y = 1.0x` 拟合，RMSE **27.6 km/h**、MAE **21.0 km/h**、最大偏差 **62 km/h**
  ⇒ 「1 计数 = 1 km/h」被彻底否掉（实测差 2.5 倍）。
- **2.56 与 2.571 在噪声内不可区分**：自由截距拟合给 **2.5549**、过原点给 **2.5679**，
  取整 **2.56** 落在两者之间。按 `2.56x` 的 RMSE 是 1.20 km/h，按 `2.571x` 是 1.20 km/h ——
  **差 0.0004 km/h，完全不可分辨**。所以「2.56」是一个**方便且统计上无可指责的取整**，
  而不是数据能分辨出的唯一值；真实标度在 [2.55, 2.57] 这个窄带里。
- **★ 一个值得记下的巧合（推断，未证实）**：2.56 = 256/100。
  若底层是「8 位字段、每 0.64 km/h 一个刻度」（0.64 × 400 = 256），
  则标度 2.56 会自然浮现 —— 这也能解释为什么**取整成 2.56 而不是 2.571 反而 RMSE 最小**。
  `推断（待确认）`；确认方式见 §7 第 5 条。

**⚠ 配对方式会明显影响 R²（引用时要说清）**：OBD 那一行是在**应答结算时**落盘的
（见 `drive-log.ps1`：收全到 `>` 提示符或超时才结算），而 `t_s` 是**结算那一刻**的秒数。
所以它对应的总线时刻比 `t_s` 早约 **0.2~0.4 s**。三种配对方式的差别：

| 配对方式 | n | 自由截距斜率 | R² |
|---|---|---|---|
| 取该 tick **最后一帧** | 284 | 2.5549 | **0.9984** |
| 该 tick 的**众数**计数 | 281 | 2.5526 | 0.9944 |
| 取该 tick **第一帧** | 280 | 2.5475 | 0.9891 |

⇒ **斜率稳（2.548~2.555），但 R² 从 0.989 到 0.998**。本文的主张（标度 = 2.56）**与配对方式无关**。

**★ 已知边界（别当已验）**：计数上界 39 ⇒ 按 2.56 换算是约 **100 km/h**。
本次最高只跑到 **100 km/h**，「再快会饱和」**未验**。
补充一条**反对饱和**的观察：在计数 = 39 的样本上，OBD 真值是 **98 与 100 km/h**（两条），
即 39 × 2.56 = 99.84 的**两侧**都有点，**没有**出现「计数钉在 39 而真值继续上涨」的平台
⇒ 至少到 100 km/h 为止**没有饱和迹象**。要真验饱和得跑到 130 km/h 以上。

### 4.3 转向灯 / 双闪 —— `0x4FC` 的 `data[5]` **是个位域**

| 值 | 含义 |
|---|---|
| `0x04` | 左转向灯 |
| `0x08` | 右转向灯 |
| `0x0C` | **双闪**（= `0x04 \| 0x08`，两位按位或） |
| `0x00` | 全灭 |

- **「双闪 = 左 \| 右」这一点独立证明它是位域，而不是三个枚举值。**
- **测法**：点火后依次 左 → 右 → 双闪，各拨一次。`data[5]` 出现三段方波：
  左转 9 次闪（`0x04`）、右转 12 次（`0x08`）、双闪 9 次（`0x0C`）；
  每段内部 `0x04`/`0x08`/`0x0C` 与 `0x00` **交替**，非零段只持续 1~3 帧。
- **闪烁全周期 = 0.80 / 0.804 / 0.79 秒 ⇒ 约 1.24~1.27 Hz**，三段一致（同一只闪烁继电器）。
  ★ 比常见的 1.5 Hz 标称**略慢**，这是本车的实测值。
- ⚠ **采样坑**：`0x4FC` 只有 **4.7 帧/秒**，所以闪烁波形是**欠采样**的 ——
  0.80 s 是**相邻两次「亮」的间隔**（全周期），不是半周期；若按半周期解读会误得 0.63 Hz
  （与车灯明显不符）。**这个坑记在这里，免得下次重算一遍。**
- 旁证：从「右转」拨到「双闪」的途中出现过一次短暂的 `0x04`（左）——
  正是拨杆经过左位的过渡，与操作路径吻合。
- 证据提交：`ee15e9e`（解出转向灯/双闪）。

### 4.4 灯位 —— `0x4FC` 的 `data[5]` 高位（`bit7` / `bit6`）

| 位 | 值 | 含义 |
|---|---|---|
| `bit7` | `0x80` | **仪表盘灯**（灯杆第 1 档） |
| `bit6` | `0x40` | **近光**（灯杆第 2 档） |

- **测法（为什么这一轮才算定死）**：**逐档停住、每档开关两次**，
  同一档位两次给出**同一字节值** ⇒ 归因干净。
  - `0x80`：34.7–40.6 s（5.9 s）与 46.3–54.1 s（7.8 s）
  - `0x40`：59.6–64.6 s（5.0 s）与 69.5–74.5 s（5.0 s）
- ★ **「持续 vs 闪烁」是区分两类灯的硬判据**：
  **闪烁**（`0x04`/`0x08`/`0x0C`）= 继电器驱动的**转向灯/双闪**；
  **持续**（`0x40` / `0x80`）= 开关直接控制的**稳态灯**（大灯那一类）。
- ★ 更早一轮只解出 `0x40`/`0x80` 两位的存在、**没定死哪位是哪个** ——
  当时灯杆是「一路推上去」连续操作的，两次持续段之间没有逐档独立确认。
  逐档停住之后才定死（先 `1a5d69b`、后 `2826610`）。
- 证据提交：`1a5d69b`（再解两位）、`2826610`（定死 bit7/bit6，两次开关印证）。

### 4.5 `0x4FC` 的 `data[0]` bit5 = 危险警告灯（双闪）继电器状态

- 全程 `data[0] = 0xC0`，**只在双闪时段变成 `0xE0`**（57 帧，92 s–99.8 s），
  与 `data[5] = 0x0C` **精确同步** ⇒ **双闪一次改两个字节**：`data[5]` 的两位 + `data[0]` 的 bit5。
- 后者更像「警告灯继电器已激活」的**状态回读**，而不是灯本身的指令位
  （因为它只在双闪时置位，左右转向时不置位）。`推断（待确认）`。
- ✅ **可直接用作已知时间标记**：抓包时按一下双闪，`data[0]` 与 `data[5]` 会同时动。
  （`drive-log.ps1` 的跑法纪律就是这么用的。）
- 证据提交：`ee15e9e`（结清一条旧告诫）。

### 4.6 门状态 —— 三个族同步跳动；**左右门不可分辨**

76–125 s 窗口内三族字节**同步**跳动：

| 族.字节 | 门关 | 门开 |
|---|---|---|
| `4FC.data[1]` | `0x00` | `0x01` |
| `524.data[0]` | `0x00` | `0x02` |
| `524.data[9]` | `0x01` | `0x18`（全程静息是 `0xFF`） |
| `8A4.data[6]` | `0x8E`（静息） | `0x8F`/`0x90` ← ★ **本条已被撤回**，见 §6 |

- `524.data[0]` 与 `4FC.data[1]` **逐帧同步** ⇒ 同一个事件的两种编码。
- ★ **关键旁证**：同族的 `4FC.data[0]` 与 `data[5]` 在整个门测试期间**完全没动**
  （b0 全程 `0xC0`、b5 全程 `0x00`）⇒ `data[1]` 是**独立于灯光的门状态位**，
  不是灯光位域的残留。
- ⚠⚠ **「左右门能不能分辨」= 未解**（§6 撤回①）。
- ⚠ **一个更根本的观察**：`4FC.data[1]` / `524.data[0]` 在**每段脉冲内部都在跳变**
  （段内 2 种取值 `00`↔`01`、`00`↔`02`）—— 所以把它们描述成「门开期间恒为 1」是**过度简化**。
  它们更像门状态的**瞬时/边沿信号**，不是稳态位。
- 证据提交：`9ad3d10`（门位定位）、`2826610`、`fa583f4`（撤回，见 §6）。

### 4.7 VIN —— `0xE24` / CMD `0x8`，**17 字节明文 ASCII 广播**

- `0xE24` 的 **17 个数据字节恰好就是 17 位 VIN**，逐字节可打印 ASCII：

  ```
  VAN E24 56 46 33 32 44 4E 46 55 52 32 57 30 30 35 34 35 30
  = VF32DNFUR2W005450        (VF3 = 法国·标致, 2 = 206)
  ```

- **静态**：全程 101 帧，`data` **只有 1 种取值** —— 是常量广播，不是状态量。
- ★★ **一处更正（别沿用最初的说法）**：最初把 `0xE24` 和 `0x5E4` **拼起来**当作 VIN，
  **那是错的**。复核后：`0xE24` 自己就是完整的 17 字节 VIN，**不需要拼任何东西**；
  而 `0x5E4` 只有 **2 字节**（`00 01` / `20 1E`，有 2 种取值），拼上去只会得到不可打印的
  控制字符 —— 它和 VIN **无关**，是独立的一族。
  **教训：字节数先数清楚（`17 == 17` 本身就是「不用拼」的证据）。**
- 证据提交：`f839a7d`。

---

## 5. 实测定案「不在这条总线上」的

这三条都是**排除法定案**：按固定顺序做完整一轮操作，逐帧记录，在**全部 12 个族的每一个字节**上
找**只出现在该窗口内的取值**。

| 项 | 结论 | 依据 |
|---|---|---|
| **刹车灯** | **不在这条总线上** | 刹车踩到底，**35 秒**（约 57–91 s）在 12 个族的每一个字节上**零变化** |
| **雨刮** | **不在这条总线上** | 同上（雨刮二/三档各放一次，同一 35 秒窗口） |
| **闪远光** | **不在这条总线上** | 灯杆第 3 档按压（瞬时/闪远光），按 **0.25 秒**粒度重扫 75–85 s 窗口的**全部族全部字节**，除门信号与两个背景量外**零变化** |

**已排除的解释**（三条都排除了，所以是结论不是猜测）：

- 不是手刹（手刹全程拉着，在差分里是常量，不影响归因）；
- 不是操作问题（owner 确认刹车踩到底、雨刮实际在动、顺序无误）；
- 不是抓包失效（**同一方法在同一轮里正确解出了双闪的 `0x0C`**）。

**合理解释**：它们走**本地硬线**（BSI 直接驱动继电器）或**另一段 VAN（车身段）**。
⇒ 任何「想从这条总线读刹车/雨刮」的设想都应先按**不可行**处理，除非换 VAN 落点重抓。

**⚠ 但「远光常开」仍未解**：本轮测到的是**闪远光**（直接按压灯杆）。
远光的**常开**模式（先进近光档再按压进入常开）**本轮没测到**，
⇒ **别把「闪远光不在总线上」推广成「远光不在总线上」**。

证据提交：`1a5d69b`（刹车/雨刮）、`9ad3d10`（闪远光）。

---

## 6. 未解 / 待确认清单

### 6.1 ★★ 两条撤回（**必须如实保留 —— 撤回记录是本文最有价值的部分之一**）

#### 撤回①：「门状态是单一状态位、双门车够用」—— **已撤回**

- **撤回的是什么**：早期记「`4FC.data[1]` 是单一状态位，双门车（206 是双门）够用」，
  即**不区分左右门**也够。
- **为什么撤**：owner 提出一个有力的反证 —— **车上主控屏能显示左右门**。
  若屏能分辨左右，那个信息就必然在车上某处存在；而当时只看了 `4FC.data[1]` 一个位
  （四次全是 `0x01`）就下了「单一状态位」的结论，**下得太早**。
- **第四轮实验（专门为分辨左右设计）**：
  - 协议：静置 15 s → **只开左门** 开/关/开/关（各 10 s）→ **空档 15 s**（两门都关）
    → **只开右门** 开/关/开/关（各 10 s）
  - 四段脉冲边界：左1 11.19–25.13 s / 左2 36.43–49.54 s /
    ── **29 秒干净空档**（这一轮不再有「间隔污染」，上一轮的伪命中就是它造成的）── /
    右1 78.68–91.59 s / 右2 103.37–115.49 s
  - 判据 (a)「段内取值恒定 且 四段呈 `[L,L,R,R]` 且 `L≠R`」⇒ **零命中**；
  - 判据 (b)「左门取值集合 ∩ 右门取值集合 = 空」⇒ 只命中 `4DC.data[4]`，
    但它的左 = `[8D..92]`、右 = `[93..96]` 是**相邻连续区间**
    ⇒ 那是**单调递增的计数器**（左门测得早、值自然小），**伪命中，已排除**。
- **结论（定性，不是「已解」）**：就我们抓的这段总线而言，**找不到能区分左右门的字节**。
  三种可能无法从数据分辨：
  1. 左右信息**不经这段总线**（BSI 只广播「有门开着」，左右走另一段 VAN / 独立线）；
  2. 它在某个未解族里，但**编码不是简单位域**（状态机值等），现有判据抓不到；
  3. 需要更长的单次开门或更严谨的边界划分才能稳定。
  **但有一条是确定的**：owner 的屏能显示左右 ⇒ 信息在车上存在，只是不在我们能读到的地方。
- **怎么继续验**：待 owner 确认 **那块能显示左右门的屏是原车还是后装、走 VAN 还是另接线** ——
  若它也挂在这段 VAN 上却仍能分辨，说明信息一定在这段总线上（我们漏了），值得再挖。
  实验设计上：**单次开门、只动一扇、拉长到 30 s 以上、四次以上重复**。

**落点**：`ACCEPTANCE.md` ③之五（1300–1347 行）。

#### 撤回②：「`8A4.data[6]` 带门侧信息（左 `8F` / 右 `90`）」—— **已撤回**

- **撤回的是什么**：早期据 76–125 s 的观察记「`8A4.data[6]` 带门侧信息」。
- **为什么撤**：第四轮它四段**全是 `0x92`** ⇒ 那是个**慢变量**（随温度/时间漂移），
  **不是门位**。
- **怎么继续验**：`8A4.data[6]` 需要一次**长时间静置**采集（≥10 分钟不动任何开关），
  看它的漂移时间常数与温度的相关性，才能定性。

**落点**：`ACCEPTANCE.md` 1331–1333 行。
**★ 两条撤回都记在那里，别再从旧提交里捡回来。**

### 6.2 未解 / 待确认（非撤回）

| # | 项 | 现状 | 怎么继续验 |
|---|---|---|---|
| 1 | **左右门可分辨性** | 未解（撤回①） | 见 §6.1 |
| 2 | **远光常开** | 未解（只测了闪远光） | 先进近光档、再按压进入常开，逐档停住 + 两次开关 |
| 3 | **倒车** | 未解 —— 抓包里**没有能独立判出倒车的字段**，「倒车时 `data[2]` 是不是 0 / 有没有符号」没验证 | 倒一段车，同时看 `data[2]` 与 `data[4..5]`（位移累计量） |
| 4 | **车速饱和行为** | 未验 —— 只跑到 100 km/h（计数 39） | 跑到 130 km/h 以上，看计数是否钉在 39 |
| 5 | **2.56 是否其实是「0.64 km/h × 4」** | `推断（待确认）`（§4.2） | 精度/量程二选一地验：① 找一段**极慢速**（1~3 km/h，人推车/怠速蠕行）与 OBD 逐点对照，看最小可分辨步长是不是 0.64；② 跑到 130 km/h 以上看饱和点是否在 100（= 39 计数）—— 若底层是 0.64 且量程到 163.8（= 255 × 0.64 之上），则标度会被重估 |
| 6 | **`data[3]` 物理含义** | 未知 —— 与 `data[2]` 一阶差分相关性 **−0.954**，像同一物理量的两个不同分辨率副本，但 **`data[3]` 不参与车速**（已加用例钉住） | 需要新列宽的抓包（`drive-log.ps1` 已落盘 `d3`） |
| 7 | **`0x984` / `0x9C4` 不一致** | 仓库别处记 `0x984/0x8` = **5 字节**，本趟实测 `0x9C4` 是 **2 字节**（`00 80`）。两者都过了 CRC ⇒ 要么是**笔误（`984` ↔ `9C4`）**，要么是**两个不同的族**。★ 2026-09-23 补充：两条 `n` **各自都有槽级实测**（`984/0x8` = **100 槽 ⇒ n=5**；`9C4/0xC` = **72 槽 − 50 − 2 ⇒ n=2**，见 §3.0），所以**待确认的不是 `n`，而是「这两条是不是同一族的笔误」** | 取证方向：抓一趟同时含 `984` 与 `9C4` 的日志（`drive-log.ps1` 现在落 `iden`，一个 `iden` 列就能判死）；未澄清前**别把「16 族」那张表的 IDEN 当准** |
| 8 | **VAN 上有没有进气温度副本** | **本轮仍然没验**（不是「没有」） | 旧 `van_drive.csv` 只落 `data[0..2]`，`data[3]`/`data[4..5]` 根本没记 ⇒ 必须用新列宽脚本重跑一次双路抓包 |
| 9 | **IDEN 15 位 ↔ 12 位的换算关系** | 未用真实位流确认 | 需要位级取证（`PINOUT.md` §F 的逻辑分析仪路线） |
| 10 | **`0x464` / `0x4DC` / `0x664` / `0x5E4` / `0x4D4` / `0x4EC` / `0x564` / `0x8C4` / `0x984`** | 未解 | 逐族做「单变量操作 + 两次印证」（§8 的方法） |
| 11 | **`8A4.data[0]` 逐帧交替 `8F`/`AF`** | 未解（像是某种状态翻转，周期未定） | 长时间静置 + 与某个状态量对齐 |
| 12 | **`524.data[9]` 门开段 `01`/`18`、静息 `FF`** | 部分已解，编码未明（为什么是 `18`？） | 同 §6.1 的左右门实验一起做 |

---

## 7. 复现方法

### 7.1 硬件与接线

> ★ **一律见 `PINOUT.md`**（不在此重复）：§「接线：三处取信号」、§⑤「VAN 极性」、
> §「抓帧盒：用裸 S3 板 + SN65HVD230 去车上抓 VAN」（A 接线表 / B 桌面自检 /
> C 上车步骤 / D 回来怎么分析 / E 已知的坑 / F 逻辑分析仪）。

只记三条最容易白跑一趟的：

1. **`edges` 涨、`frames=0` ⇒ 第一处置就是对调那两根线**（别先动固件、别先怀疑解码器）。
2. **极性靠编译宏 `-DVAN_RX_INVERT=1`（`[env:esp32s3-vaninv]`）只是对照确认手段，不是修法**
   —— 修法就是对调线、然后用默认的 `[env:esp32s3]`。
3. **`[env:esp32s3-vansniff]`（`VAN_SNIFF=1`）是排障用的**，它每帧多打好几行，
   会明显压低 VAN 输出 ⇒ **正式采集别用它**。

### 7.2 采集脚本（`tools/serial-capture/`）

| 脚本 | 用途 | 备注 |
|---|---|---|
| `capture-van.ps1` / `capture.py` | 抓串口日志到 UTF-8 无 BOM | `capture.py` 要 pyserial（须先设 `PYTHONPATH`） |
| `capture-van-nopy.ps1` | 零依赖抓帧 | 只读不复位 |
| `capture-boot-nopy.ps1` | 零依赖**复位**抓开机日志 | 为「看开机那行极性」而写 |
| **`drive-log.ps1`** | ★ **行驶采集：VAN + OBD 双串口同时记录，时间戳同源** | 为车速标定而写；`-SelfTest` 不碰串口 |
| `replay.py` | 把抓到的帧贴回设备复现 | 桌面就能验整链 |

**`drive-log.ps1` 的关键口径**（引用时别记错）：

- 时间轴 = **同一个 Stopwatch 的秒数**（`t_s`），两个串口接同一台笔记本 ⇒ **天然同源**，
  不需要事后对墙上时间。`wall_time` 只是 `HH:mm:ss.fff`，**没有日期**。
- ★ **OBD 那一行的 `t_s` 是「应答结算时刻」，不是「发问时刻」** ——
  它对应的总线时刻比 `t_s` 早约 **0.2~0.4 s**（`010D`/`010C` 实测）。
  用这份数据做对齐/回归时**必须考虑这个偏移**（见 §4.2 的配对方式表）。
- ★ **OBD 每条记录里含两个 ECU 应答帧**（`raw` 字段里 `41 xx` 出现 **2 次**，599/599 都是），
  脚本按**第一个** `41 xx` 取值 ⇒ 取到的是**较旧**的那一帧，又加了一层滞后。
- 跑法纪律（为了事后能对齐）：出发前/到达后**各按一次双闪**（`4FC` 的已知时间标记）、
  中途**停两次车**各 5 s 以上、**倒一段车**（顺手回答 §6.2 第 3 条）。

### 7.3 离线解帧工具（`tools/van-decode/`）

| 工具 | 用途 |
|---|---|
| `decode_frames.py` | 边沿 → SOF → 4B5B → FCS → 帧 |
| `edge_stats.py` | 边沿/槽时间统计（判 8.25 µs） |
| `fit_fcs.py` | ★ **FCS 约定枚举**（§2.5 的结论就是这么来的） |
| `gap_stats.py` | 帧间隔/关帧门限统计 |
| `probe_van_callback.cpp` | 宿主机跑真实 `VanPhyWire` 的探针 |

### 7.4 分析纪律（本轮复核也踩到，写下来）

- **判「采集够不够」只看 `frames/s` 与 `824/s` 两个数，别看边沿数** ——
  极性反了边沿照样涨。
- ★ **引用速率时先说清口径是「CSV 行/秒」还是「总线帧/秒」**（§3.1，差 1.6 倍）。
- ★ **OBD CSV 的 `raw` 字段内含换行**（`\r\n` 与裸 `\r` 混用），所以
  **「物理行数」≠「逻辑记录数」**：文件 1199 行物理行 = 表头 1 + **1198 条逻辑记录**
  （`010D` 599 + `010C` 599）。用普通的行计数或 `csv` 模块读它会读歪
  （裸 `\r` 会被某些解析器当记录分隔符）—— 按 `\r\n` 切记录、再按正则取字段才稳。
  写脚本解析时注意；**这两个文件本身没有错**（写它的 `WriteAllLines` 与字段内的 `\r\n` 叠加所致）。

---

## 8. 与实现的关系

### 8.1 协议结论落在哪些常量上

| 协议结论 | 代码落点 |
|---|---|
| 时基 8.25 µs/槽（≈121 kbit/s）；帧长公式 `n = (槽数 − 50 − ack×2) / 10` | `lib/dashcore/van_wire.h` → `kTsNs = 8250u`（**唯一时基**）；`kBitRateHz = 125000u` 仅标称；帧长那条的槽数是 `kSofSlots(10) + 15 + 5 + 10n + kFcsSlots(20) (+ kAckSlots(2))` |
| SOF 图案 `0000111101`（裸槽） | `van_wire.h` → `kSofPattern = 0x003Du`、`kSofSlots = 10u` |
| FCS 20 槽 / ACK 2 槽 | `van_wire.h` → `kFcsSlots = 20u`、`kAckSlots = 2u` |
| CRC-15（`0x0F9D` / init `0x7FFF` / 输出取反 / MSB-first） | `van_wire.h` → `crc15_van_iso()`；★ **不要用 `crc15()`**（那是 CAN-15，0 命中） |
| FCS 字段 = `crc << 1`，最低位恒 0 | `van_wire.h` → `fcsFieldFromCrc()` / `fcsCrcFromField()` / `fcsFieldWellFormed()` |
| IDEN 字节布局（`0x82 0x48` ⇒ `0x824`） | `van_wire.h` → `idenByte1()` / `idenByte2()` |
| `cmdExpectsAck()` 判据 | `van_wire.h` → `cmdExpectsAck()` |
| `data[i]` 的下标约定（`0..n-1`，与 `VanPacket::data` 同一套，§2.8） | `van_source.h` → `struct VanPacket`（`data[28]` + `len`）；`van_wire.cpp` → `frameToPacket()`（`for (i = 0; i < n; ++i)`，左闭右开）；`van_source.cpp` → `onPacket()` 的两条 `... < pkt.len` 判据 |
| **车速 = `data[2]` 单字节 × 2.56** | `lib/dashcore/van_source.h/.cpp` → `kSpeedIden = 0x824`、`kSpeedOffset = 2`、**`kSpeedScale = 2.56f`** |
| 转速 = `data[0..1]` 16 位大端 × 0.125 | `van_source.h` → `kRpmOffset = 0`、`kRpmScale = 0.125f` |
| 帧长公式与关帧 70 µs | `van_wire.h` / `van_phy_wire.h`（`kSlotsMax`、`dom_run` 判据） |
| 极性（差分与 CAN 相反） | `lib/dashcore/van_phy_gpio.cpp`（ISR 内可翻转）+ `platformio.ini` 的 `-DVAN_RX_INVERT=1` |

### 8.2 改协议结论时要同步改哪些测试

**改常量会立刻变红的用例**（这是刻意的钉子）：

| 测试文件 | 钉住什么 |
|---|---|
| `test/test_dashcore/test_van_source.cpp` | `kSpeedScale` / `kSpeedOffset` / `kRpmScale` / `kRpmOffset` 的值与语义；`data[3]` 不得影响车速 |
| `test/test_dashcore/test_van_real_capture.cpp` | 真边沿链的**黄金值**（整段 33 帧 / `0x824` 12 帧 / 末帧 `speed = 35.84`（= 14 × 2.56）/ `rpm = 2744.5`）+ 四个常量一起钉 |
| `test/test_dashcore/test_van_phywire.cpp` | 链路 → `VanSource` 的端到端（`0x64` ⇒ `256.0 km/h`） |
| `test/test_dashcore/test_van_wire.cpp` | 位层：SOF / IDEN 字节布局 / FCS 约定 |
| `test/test_dashcore/test_van_replay.cpp`、`test_data_service.cpp` | 回放行与数据源优先级 |

★ **历史上改 `kSpeedScale` 时就是这么做的**：`c596351` 一次改了 5 个文件
（`van_source.cpp/.h` + 3 个测试文件），把期望值按新标度手算更新
（`100 → 256.0`、`13 → 33.28`、黄金值 `14.0 → 35.84`），并把常量本身也钉进断言。

★ **跑 native 的纪律**（避免误判成败）：看 **摘要**（`[FAILED]` 行 / Unity 的
`N Tests M Failures`），**不要只看进程退出码** —— 本仓库曾有过一个既有崩溃让
退出码恒为 3 而摘要仍是 0 失败。详见 `ARCHITECTURE.md` 的测试一节。

---

## 引用索引

| 主题 | 原文位置 |
|---|---|
| 帧结构 / 位层 / CRC 参数 / 槽时间 | `lib/dashcore/van_wire.h` 文件头（唯一权威，本文 §2 是它的索引） |
| `data[i]` 下标约定（`0..n-1`） | `lib/dashcore/van_source.h` → `struct VanPacket`；`van_wire.cpp` → `frameToPacket()`；`van_source.cpp` → `onPacket()`；`src/main.cpp` → `VanLogSink::onPacket()`（本文 §2.8 是它们的索引） |
| 各族帧长 `n` 的槽级实测（16 族） | `ACCEPTANCE.md` 1015 行那条帧布局定案；复现输入 `C:\Users\Public\206dash\drive5min.csv`（17106 帧）+ 仓库两份切片（本文 §3.0） |
| 极性、认线、抓帧盒、上车次序、已知的坑 | `PINOUT.md` §①~§F（尤其 §⑤ 与 227~233 行） |
| 每一条实测的完整过程与数字 | `ACCEPTANCE.md`：917 行（槽时间）、1015 行（FCS 定案）、1174 行（转速/车速字段）、1236~1399 行（转向灯/灯位/门/VIN/总线全貌）、2026-09-22 各路提交 |
| 车速标定 2.56 的回归 | `c596351` 提交信息 + `lib/dashcore/van_source.cpp` 注释 |
| 采集脚本口径 | `tools/serial-capture/drive-log.ps1` 文件头 |
| 双板链路协议（**不是 VAN 协议**） | `ARCHITECTURE.md` §「双板链路协议 v1 范围」 |
