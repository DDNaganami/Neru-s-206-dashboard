# 测试设备采购清单（淘宝）

编制日期：2026-09-15（2026-09-15 修订：预算调整 + 接线必需件 + 与代码同步）
依据：ARCHITECTURE.md / PINOUT.md / ACCEPTANCE.md
车型约束：2001-11 206 CC MUX，K 线 + VAN（AEE2001），**不是 CAN**

> 修订说明（第三次，2026-09-15）：**纠正一个事实错误** —— 此前把「ISO 音响尾线插头」
> 从可选提到必买，理由是"VAN 取信号的唯一免破线途径"，**该理由不成立**：206 的
> **OBD 座上没有 VAN**，而 VAN 落点里**组合仪表连接器本身就是插接件**（免破线），
> 且本项目本来就要拆仪表。ISO 尾线已退回 T3 可选项，T0 改列 **OBD 一分二转接线**。
> 详见第九节「踩坑留档」与 `PINOUT.md`。
>
> 第二次修订：万用表此前写的是 UT61E+（¥300+），与实际需要不匹配，
> 已改为入门自动量程表；**数字示波器从"强烈建议"降为"确认信号不通时再考虑"**，
> 理由见 T1 表内。

---

## 一、当前卡在哪（清单的出发点）

软件侧已经推进到「逻辑层完备、只等硬件」：

- 纯逻辑层 `lib/dashcore/` 拆完：obd_source（K 线状态机）、van_source（0x824 解帧）、
  van_replay（串口离线回放 VAN 帧）、data_service（多源合并 / 3 秒回退）、expression
- **VAN 线路层已完成**：`van_wire.h/.cpp`（SOF / E-Manchester 4B5B / IDEN/CMD /
  CRC-15 / 帧尾）与 `van_phy_wire.h/.cpp`（边沿解码器，整链回归通过）
- 46 例宿主机单测全绿（`python -m platformio test -e native`）
- 宿主机预览可用（pcpreview 渲 BMP → preview/preview.html 看双屏扫表）
- UI 定案：无指针圆弧 + 表情，`ui_theme.h` 单文件换皮，480 基准乘 `theme_scale()`

**没硬件就再也推不动的三处**：

1. VAN 收发器已买（SN65HVD230）——到货后接上 `VanPhyWire` 即可实车抓帧
2. `main.cpp` 里 `VehicleDataService g_data(nullptr)` —— 没有 K 线适配器，OBD 源接不上
3. `dash_display.cpp` 实驱动分支是 `#error` —— 没有屏，锁不了分辨率

所以清单的顺序 = 解锁顺序，不是「买齐一套开发板」。

---

## 二、采购红线（买错就白花钱）

| 红线 | 原因 |
|---|---|
| **不买 CAN-only 适配器 / CAN 分析仪** | 206 是 K 线 + VAN，没有 CAN。CAN 收发器模块（SN65HVD230/TJA1050）**可以用**，那只是物理层芯片，不是 CAN 协议 |
| **别以为 OBD 座上有 VAN** | 206 MUX 的 OBD 座只有 K 线 + 12V + GND，**没有 VAN**。车速要从组合仪表/收音机取 —— 详见第九节与 `PINOUT.md` |
| **不买 V2C TINY 协议盒** | 给「换新主机」用的，除非将来换 RD45 |
| **不用纯 SPI 屏做 480×480 双屏** | 480×480×16bit ≈ 3.7 Mbit/帧；SPI 80MHz 理论 27fps，两屏共用一条总线各 13fps。这个分辨率必须走 **RGB 并口（S3 的 LCD_CAM）**，PINOUT.md 现在那套「两块 SPI 屏 + CS/DC/RST」的引脚预案要改 |
| **不按 esp32dev 买屏** | 320KB RAM 只够跑桩。真屏必须 **ESP32-S3 + PSRAM（N16R8）** |
| **最后两块真屏先别下单** | 硬约束是「两屏外径 < 95mm」且「总厚度 ≤ 表玻-表盘纸间隙（未量）」。间隙没量之前买成品模块 = 赌 |
| **别为"找线"买高精度万用表** | 本项目要量的只有：通断、12V/3.3V、<1kΩ 电阻。4½ 位 / 真有效值在这里完全用不上（见 T1 的 8 号） |

---

## 三、清单

价格是 2026-09 的常见档位，仅供预算参考，以实际页面为准。

### T0 —— 必须买，直接解锁实车链路（约 ¥65~130，4 号按需另加）

| # | 品名 | 淘宝搜索词 | 关键规格 / 怎么验 | 数量 | 用途 / 解锁什么 | 参考价 |
|---|---|---|---|---|---|---|
| 1 | CAN/VAN 收发器模块 | `SN65HVD230 模块 3.3V` | SN65HVD230（**不要 MCP2551，那是 5V 的**）；板载 120Ω 终端电阻**可拆**（丝印标 R1/120 那种）；带 4pin 排针 | ✅ **已买** | 接 `VanPhyWire` 抓 VAN 帧。到货先拆 120Ω | ¥5~12/个 |
| 2 | **OBD 一分二转接线** | `OBD2 一分二 转接线 延长` | 一公两母（或一公一母 + 分叉），16 针全通 | 1 | **供电用**：一个口插 ELM327、一个口供自己的板子，不用抢一个口。⚠️ 16 脚是常电 | ¥15~30 |
| 3 | 杜邦线 | `杜邦线 母母 20cm 40根` | 母对母（模块与板子都是排针）；彩色分色 | 1 板 | 收发器 ↔ ESP32 那一段（VCC/GND/RO/DE-RE 四根） | ¥10~15 |
| 4 | K 线 OBD 适配器（USB） | `ELM327 USB 有线 带开关` | **USB 口能传数据**（不是"USB 只供电、数据走蓝牙"）；带电源开关更省事。芯片与协议别在页面上找，按下面的「买法」验 | 1（先买 1 个） | 接 `Serial1` 跑 `ObdSource`，验 PID 010C/0105 | ¥30~70 |
| 5 | 蓝牙 OBD 诊断头（**先买这个**） | `ELM327 蓝牙 安卓 诊断` | Mini 版即可；**先用手机 App 验证车能不能读** | 1 | 成本最低的一次验证：App 里能读到转速/水温，才值得再买 USB 版 | ¥25~50 |
| 6 | **OBD 取电 USB 降压线**（一体，**输出 USB-C 公头**） | `OBD 取电降压线 Type-C` / `OBD2 转 USB 供电线` | **OBD 公头 → 线内含 12V→5V → USB-C 公头**；**5V 2A**（1A 在 ESP32 启动尖峰时会掉压重启）；**务必带开关**（OBD 16 脚是常电）。⚠️ 默认款常是 Micro-USB，规格里要选 Type-C | 1 | 从 OBD 16 脚取电给板子。**唯一免焊接的对插方案** | ¥15~35 |

> **关于 K 线适配器怎么挑（2026-09-15 补充）**
>
> 此前这里写的是"芯片认 CH340 / CP2102 / FT232"、"写明支持 ISO 9141-2 与
> KWP2000"——**这两条在淘宝页面上基本看不到**，等于没法用来挑货。实际上：
>
> - **USB 转串口芯片无所谓**：CH340/CP2102/FT232 对上层都一样，Windows 认得出
>   就行。这不是需要"认准"的指标
> - **协议支持也没法从页面判断**：便宜克隆板几乎都印"支持全部 OBD-II 协议"，
>   但不少 clone 的 K 线部分根本跑不起来（尤其 KWP2000 的 5-baud 慢初始化）。
>   页面文字不可信，**实测才算数**
> - 至于 v1.5 / v2.1 这类版本号，是**芯片固件版本**，市场标注混乱，也不能当依据
>
> **实际买法**：
> 1. 先下单最便宜的**蓝牙版**（¥25~50），到手用免费 App（Car Scanner / Torque /
>    OBD Auto Doctor，都有中文版，几个免费）连一下
> 2. **能读到转速或水温** → 说明 K 线这条路通，再去买 USB 版接 ESP32
> 3. **读不到** → 先怀疑适配器：退回，换另一家再试。别急着怀疑车或代码
> 4. 要买两家对比的话，先买最便宜的那家，别一次下两单
>
> 这条验证只需要 ¥25~50，比买错两个 USB 版（¥60~140）便宜得多。

> **关于 6 号取电件怎么挑（2026-09-16 补充）—— 差点买错的地方**
>
> 此前这里写的是「`12V转5V USB 车充` 或 `DC-DC 12V转5V 3A`」，
> **这个写法会把人引去买点烟器车充，而那个插不进 OBD 座。**
>
> 原因很直白：**OBD 座 16 脚是方形 16 针，和点烟器圆口完全不是一个东西。**
> 普通的"车充"（插点烟器那种）只对应点烟器口；OBD 座上没有点烟器口。
>
> 三种做法，焊接需求差别很大：
>
> | 方案 | 要焊接吗 | 说明 |
> |---|---|---|
> | **A. OBD→USB 降压线（一体）** | **完全不用** ✅ | OBD 公头插诊断座，另一头 USB 头，**线里已经做了 12V→5V** |
> | B. OBD 公头带线 + DC-DC 模块 | 不用（多数带接线端子） | 公头插 OBD，12V/GND 拧进模块端子 |
> | C. 点烟器车充 + 剪线 | **要焊** ❌ | 车充插不进 OBD，必须剪掉点烟器头、剥线、接到 OBD 公头上 |
>
> **买 A。** 搜 `OBD 取电降压线 Type-C` 或 `OBD2 转 USB 供电线`。
>
> 四个必须看的参数：
> - **输出口要 USB-C 公头**（直接插板子的 Type-C 口，一根线到位）
> - **5V 2A**：ESP32-S3 + PSRAM 待机 40~80mA、开 WiFi 150~250mA，
>   但**启动瞬间有 500mA 级尖峰**。1A 的会掉压 → 板子反复重启
>   （串口会打印 `Brownout detector was triggered`）
> - **带开关**：OBD 16 脚是**常电**，熄火也有 12V。不开发动机时别让板子一直跑
> - **线长**：车里走线，0.5~1m 够用；太长反而不好收
>
> ⚠️ **输出口这个规格最容易买错**：这类降压线是给**行车记录仪**用的，
> **默认款式多半是 Micro-USB 公头**，USB-C 是可选规格 ——
> 下单时必须在规格选择里**明确点 Type-C**，别用默认项。
>
> | 降压线的输出头 | 能直接用吗 | 怎么接板子 |
> |---|---|---|
> | **USB-C 公头** | ✅ 最好 | 直接插板子 Type-C 口 |
> | USB-A 母口 | ✅ 也行 | 用一根 **A-to-C** 数据线（手边大概率有） |
> | Micro-USB 公头 | ⚠️ 要转接 | 买 Micro→Type-C 转接头 |
>
> 兼容性说明：**A 母口 → C 公头一定能用**（线里自带 CC 上拉电阻）。
> 反过来 **C-to-C 需要线里有 eMarker 芯片**，随便一根可能不供电 ——
> 所以先用 A-to-C，别用 C-to-C。
>
> 顺带：**PID 收发器（SN65HVD230 / L9637D）不用单独供电** ——
> 它们吃 3.3V、电流只有几毫安，直接从板子 3.3V 引脚取。
>
>
> **如果便宜适配器都不行**：还有个后手 —— 自己读 K 线。K 线是 12V 单线，
> 单片机侧要用专用接口芯片（**MC33290 / L9637 / MC33190**）或光耦 + 三极管
> 做电平转换，从 OBD 7 脚直接进 ESP32 的 UART。这样能省掉 ELM327，
> 但要自己实现 KWP2000 帧格式（工作量不小，`obd_source` 现在是按
> ELM327 的 AT/PID 文本协议写的）。**先走上面 1~3 步，卡住了再考虑**。

> **已订正**：此前这一档写的是「ISO 音响尾线插头（必买）」，理由是"VAN 取信号的唯一
> 免破线途径"——**该理由不成立**。实测经验与 MUX 电路图显示：**206 的 OBD 座上
> 根本没有 VAN**（只有 K 线 + 12V + GND），而 VAN 的落点里**组合仪表连接器本身就是
> 插接件**（免破线），且本项目本来就要拆仪表。所以 ISO 尾线降为可选项（见 T3）。
> 详见 `PINOUT.md`「接线：三处取信号」。

**1 号已买；2、3、5、6 合计约 ¥65~130，4 号（USB 版 ELM327）等蓝牙版验通再买。**
到货后 T0 就能做：实车读 OBD、VAN 抓帧。
其中 4 号到货前，`van_replay` 那条串口贴帧的路已经能用来联调（不用等收发器）。

### T1 —— 调试件（按需，合计约 ¥80~130）

| # | 品名 | 淘宝搜索词 | 关键规格 | 数量 | 用途 | 参考价 |
|---|---|---|---|---|---|---|
| 7 | USB 逻辑分析仪 | `USB 逻辑分析仪 24M 8通道` | 8 通道 24MHz，配 PulseView / sigrok | 1 | **VAN 抓包主力**。125kbps E-Manchester 解码、确认 IDEN、量位宽。**数字信号，示波器替代不了它，它也替代不了示波器** | ¥20~40 |
| 8 | 万用表 | `数字万用表 自动量程 通断蜂鸣` | 有**通断蜂鸣 / 直流电压 / 欧姆**三档即可。入门款如优利德 UT136B+ | 1 | 找线（ISO 2/3 脚哪根是哪根）、查地、**确认 120Ω 真拆掉了**、量 12V/3.3V | ¥40~90 |
| 9 | 塞尺 | `塞尺 0.02-1.00mm 32片` | 0.02mm 起 | 1 | **量「表玻 → 表盘纸」间隙**（ACCEPTANCE 里唯一未量的硬约束），决定能塞多厚的屏 | ¥10~20 |
| 10 | 数显卡尺 | `数显卡尺 150mm 0.01` | 0.01mm 分辨率 | ✅ **已有** | 复测 Ø89 孔 / Ø100 台阶 / 屏模块厚度 | — |
| 11 | 12V 台架电源 | `12V 5A 开关电源` | 5A 起 | 1 | 长时间台架，不用耗车电瓶 | ¥40~200 |
| 12 | OBD-II 公头 端子 | `OBD2 公头 接线端子 16pin` | 免焊端子式 | 1~2 | 给 ELM327 临时供电 / 自己接线 | ¥10~20 |

**7 号不要省**（¥20~40）：它是 VAN 抓包的**唯一**手段，也是判断"极性接反没有"的工具。

#### 关于万用表（8 号）——为什么不需要 UT61E+

本项目真正要量的东西只有这些：

| 要量的 | 精度要求 |
|---|---|
| 通断 / 找线 | 只要响 |
| 12V 常电 / ACC | 整数伏（接错会把 3.3V 逻辑烧掉） |
| 3.3V | 整数伏 |
| 拆掉的 120Ω 是否拆干净 | ~1kΩ 以内 |
| 电阻/二极管档 | 常规 |

UT61E+ 是 ¥300+ 的 4½ 位真有效值表——**精度和真有效值在这里都用不上**（VAN 是数字差分总线，没有需要真有效值的东西）。任何带通断蜂鸣的自动量程表都够。

> 别买"测电笔"（只显示高压/低压那种）：查不了通断，找线用不了。

#### 关于数字示波器（原 7 号，已降级）

**结论：先不买。** 这个项目没有一处**必须**看波形：

- VAN 帧抓取靠逻辑分析仪（数字信号 + 边沿时间），**不需要示波器**
- K 线是极低速串行（ELM327 38400 波特），物理层出问题的概率低
- 示波器能多给的是**信号质量**（差分幅度、上升沿、振铃、终端匹配）——这是
  "信号不通时诊断为什么"的工具，不是"能不能做成"的前提

**什么时候才需要买**：逻辑分析仪上 RO 脚完全没有沿、或者波形明显畸变，
需要判断"是电平不够 / 极性反了 / 终端没拆干净"的时候。
到那一步再考虑：借一台，或买个 ¥100~200 的 USB 玩具示波器应急。

### T2 —— 屏与主控（先买 1 块验证，别一次买两块成品）

| # | 品名 | 淘宝搜索词 | 关键规格 | 数量 | 用途 | 参考价 |
|---|---|---|---|---|---|---|
| 13 | 圆屏验证板 | `ESP32-S3 2.1寸 圆屏 480x480 开发板` | **480×480、RGB 并口、ST7701S、8MB PSRAM**（微雪 ESP32-S3-Touch-LCD-2.1 同级） | 1 | 拿现成板子把「双 480×480 全缓冲 + 5Hz 渲染」跑通，**验证内存和帧率**，再决定最终屏 | ¥90~160 |
| 14 | ESP32-S3 开发板 | `ESP32-S3 开发板 N16R8` | **N16R8**（16MB flash + 8MB 八线 PSRAM）；不要 N16R2/N8R2 | 1 | 板载 S3 + 排线接屏；也做 VAN/OBD 逻辑联调 | ¥40~70 |
| 15 | 大圆屏（结构可行性） | `2.8寸 圆形 IPS 480x480 RGB 屏 模块` | 480×480、可视 Ø70.7（对应 2.8"）；**优先选「裸屏 + FPC 转接板」**，别买带整块 PCB 的板 | 1 | 量是否塞得进 Ø89 孔 + 间隙；确认屏厚 | ¥70~150 |
| 16 | 小圆屏（备选/对照） | `2.1寸 圆形 IPS 480x480 SPI RGB 模块` | 480×480，带 FPC 转接板 | 1 | 与 15 号对比留环宽度（2.1" 约留 9mm/边，观感更"居中"） | ¥60~110 |
| 17 | 排线 / 转接 | `FPC 排线 同向 反向 0.5mm` + `FPC 转接板 0.5转2.54` | 按到货屏的脚距和针数配 | 各 2~3 | 屏与驱动板之间走线，方便做结构 | ¥5~15/条 |

> 若预算只够一块屏：**买 13 号**（2.1" 带 S3 的整板）。它能同时验证「S3 能不能驱动 480×480 双屏」和「圆弧 UI 实际观感」，
> 是风险最高、最该先证伪的一环。屏的**最终选型**等间隙量完再定，且要买两块。

> **14 号为什么是当前唯一的硬阻塞**：所有 VAN 侧的工作（`VanPhyWire` 上板、
> 用真实位流对 FCS 约定与极性、`pio run -e esp32dev -t upload` 看串口输出）
> 都必须有板子。没有它，线路层只能停在"宿主机测试全绿"这个状态 ——
> 而宿主机测试是**自编自解**，证明不了线上行为。
>
> **买的时候别买错**：要 **N16R8**（16MB flash + 8MB 八线 PSRAM）。
> N16R2/N8R2 是四线 PSRAM，会占掉 GPIO33-37，而 `PINOUT.md` 的屏 SPI
> 预案用了 35/36/37 —— 换板型就得整段重排引脚。
>
> ⚠️ **到货第一件事：量一下那个标着 5V 的排针是不是真的 5V。**
> 国内通用 S3 板（芯片原装 + 向下焊 + Type-C 那种）有个常见毛病：
> **5V 排针在出厂状态下不是 5V** —— 有的板子那个针是 LDO 的输出端、
> 有的根本没接通原 USB 的 VBUS。**直接往 5V 排针灌电会灌进稳压器输出端。**
> 相关讨论：[Arduino 论坛](https://forum.arduino.cc/t/chinese-esp32-s3-5v-pin-warning/1192758/3)、
> [ESPHome 论坛](https://community.home-assistant.io/t/how-to-enable-output-on-5v-pin-on-knockoff-esp32-s3-devkitc-1-boards/734727/3)。
>
> **所以：供电一律走 Type-C 口**（板子自己的电源管理会处理），
> **不要用 5V 排针当电源输入**（除非量过确认它是输入且是 5V）。
> 实测这个针的电压只要一次，之后接线就一直记着这个结论。
>
> **到货后的供电测试顺序**（顺便量出板子真实吃多少电）：
> 1. **先用充电宝 + 一根 A-to-C 线**供电 —— 完全安全，笔记本不接入
> 2. 看板子能否正常跑，顺手量一下**运行电流**（充电宝带显示，或串个 USB 电流表）
> 3. 这个数就是选降压线的依据。文档写的 5V 2A 是保守值；实测若只有 ~150mA，
>    1A 也够 —— 但**启动尖峰万用表看不出来**，所以仍建议 2A
> 4. ⚠️ 这一步**别用 C-to-C 线**（充电宝 C 口直连板子 C 口）：需要线里有
>    eMarker 芯片，随便一根可能不供电。**A-to-C 最稳**

> 若预算只够一块屏：**买 13 号**（2.1" 带 S3 的整板）。它能同时验证「S3 能不能驱动 480×480 双屏」和「圆弧 UI 实际观感」，
> 是风险最高、最该先证伪的一环。屏的**最终选型**等间隙量完再定，且要买两块。

### T3 —— 装车用（可选，等台架跑通再买）

| # | 品名 | 淘宝搜索词 | 用途 | 参考价 |
|---|---|---|---|---|
| 18 | ISO 音响尾线插头 | `标致 206 收音机 尾线 ISO 插头` | **仅当**你决定从收音机取 VAN（备选落点）才需要。首选是从**组合仪表连接器 5/10 脚**取（插接件，免破线，反正要拆仪表） | ¥15~35 |
| 19 | OBD 延长线 | `OBD2 延长线 公对母 1米` | 车里把诊断口引出来，方便接线 | ¥15~30 |
| 20 | USB 隔离器 | `USB 隔离器 ADUM3160` | **笔记本给板子供电 + 板子接车 12V 时防地环流**（见下方安全提示） | ¥30~80 |
| 21 | 保险丝取电器 | `汽车 保险丝取电器 小号` | 从保险盒取 ACC 常电（比 OBD 16 脚常电更规范，不耗电瓶） | ¥10~20 |
| 22 | 汽车 DC-DC | `12V转5V DC-DC 3A 车规` | 实车供电，防浪涌 | ¥15~30 |
| 23 | K 型热电偶温度计 | `K型热电偶 温度计 双通道` | 标定水温弧：对照 ECU 0105 读数（量程 60~130℃） | ¥60~150 |
| 24 | PSA 诊断仪（克隆） | `lexia3 PP2000 DiagBox 标致雪铁龙` | 读 BSI / 组合仪表 / 发动机 ECU，确认这车哪些参数走 VAN、哪些走 K 线 | ¥300~650 |

---

## 四、明确不要买

| 不买 | 原因 |
|---|---|
| CAN 分析仪（USB-CAN 盒子） | 车上没 CAN，用不上 |
| V2C TINY 协议盒 | 换新主机才用（ARCHITECTURE.md 已排除） |
| 带 `LilyGO T-RGB 2.8"` 成品板 | 整板 Ø96 > 95mm（两表圆心距），且厚 18mm，装不进去 |
| 双 480×480 的纯 SPI 屏 | 带宽不够，见红线 |
| ESP32-C3 / S2 / Pico | 引脚和 PSRAM 都不够，别被便宜带偏 |
| 现成的 206 液晶仪表总成 | 会绕开本项目「原表仓加屏、原表保留」的方案 |
| 800×800 屏（除非先算内存） | 单屏全缓冲 1.28MB，双屏 2.56MB + LVGL 工作缓冲，PSRAM 要留余量；先把 480×480 跑通 |
| **UT61E+ 这类 ¥300+ 高精度万用表** | 见 T1 的 8 号，本项目用不上 |
| **¥1500+ 台式示波器** | 见 T1 的降级说明，先不买 |
| 测电笔（只显示高压/低压） | 没有通断档，找线用不了 |

---

## 五、下单顺序建议

**第一单（约 ¥95~165）**：3 杜邦线 + 2 OBD 一分二 + 6 取电件 + **5 蓝牙 OBD 头**
→ 1 号（收发器）已到货。这一单到货就能做：拆 120Ω、接第一段、
上车用手机 App 试 K 线能不能读、以及 VAN 抓帧

**第二单（约 ¥70~150）**：7 逻辑分析仪 + 9 塞尺 + 8 万用表
→ 7 是 VAN 抓包唯一手段，也是试极性的工具，别省；9 决定屏能多厚，量完才敢买屏

**第三单（按需）**：4 USB 版 ELM327（¥30~70）
→ **只在蓝牙版验证通过之后才买**。蓝牙版读不到就换店再试，不要直接上 USB 版

**第四单（约 ¥90~160）**：13 圆屏验证板
→ 验证 S3 双 480×480 是否真的跑得动。跑通再谈最终屏

**第五单（等间隙实测数据）**：15 或 16 一块 + 17 → 结构验证 → 最后才买两块真屏

**第六单（可选）**：11 台架电源 + 12 OBD 公头 + 18~24
→ 18（ISO 尾线）**只在决定从收音机取 VAN 时才买**；示波器不在此列

---

## 五之二、下单核对（2026-09-16，第一批到货/在途）

对照上面的清单逐项核对已下单的东西。**结论：车上的信号能抓了，但固件跑不了 —— 漏了 14 号。**

| 清单 # | 品名 | 状态 | 备注 |
|---|---|---|---|
| 1 | SN65HVD230 模块 | ✅ 已买 | 到货先拆 120Ω 终端电阻 |
| 2 | OBD 一分二转接线 | ✅ 已买 | |
| 3 | 杜邦线 母对母 40pin | ✅ 已买 | |
| 4 | ELM327 USB（CH340，带开关） | ✅ 已买 **2 个** | 清单只要 1 个；多一个当备件可以 |
| 7 | USB 逻辑分析仪 24M 8通道 | ✅ 已买 | **VAN 抓包唯一手段** |
| 8 | 万用表 | ⚠️ 型号不同（DT9205A） | 能用（自动量程 + 通断），但比 UT136B+ 大、精度一般。**别买"测电笔"** |
| — | L9637D ×2（清单外） | ➕ 额外买的，**这步走对了** | K 线收发器：可替代 ELM327 直连 ESP32 UART，也用来把 12V K 线**安全**接到逻辑分析仪 |
| **14** | **ESP32-S3 开发板 N16R8** | ❌ **没买** | **当前唯一硬阻塞**，见 T2 的说明 |
| 6 | OBD 取电 USB 降压线（**输出 Type-C**） | ❌ 没买 | 实车供电，**免焊接**（别买成点烟器车充，插不进 OBD；规格要选 Type-C），见第六节地环流 |
| 5 / 13 / 15 / 16 / 17 | 蓝牙 ELM327 / 各屏 | 未买 | 蓝牙版现在可以省了（已有 USB 版 2 个）；屏等间隙实测 |

### 本轮必须补的两件

```
ESP32-S3 开发板 N16R8（16MB flash + 8MB 八线 PSRAM）   ¥40~70
OBD 取电降压线（带开关，5V 2A，**输出 Type-C 公头**）  ¥15~35   ← 别买成点烟器车充 / Micro-USB
```

> 板子到货先做两件事：① **量一下标着 5V 的排针是不是真的 5V**
> （国内 S3 通用板常见它不是 5V，别拿它当电源输入）；
> ② 用**充电宝 + A-to-C 线**跑一下，量出真实运行电流，再决定降压线买 1A 还是 2A。

> 补 14 号时**别买成 N16R2/N8R2**：四线 PSRAM 占 GPIO33-37，
> 而 `PINOUT.md` 的屏 SPI 预案用了 35/36/37。

### 现在就能做（不需要 ESP32）

到货后按这个顺序做，**每步都有明确的通过/不通过判据**：

**① 车上插 ELM327，手机 App 读一次** ← 最便宜的证伪，今天就能做

- 装 App：Car Scanner / Torque / OBD Auto Doctor（都有中文版）
- 点火或 ACC，插 OBD 口，连上，看**转速**和**水温**两个值
- **读得到** → K 线这条路通，206 支持标准 OBD PID。后面 ESP32 直接用
  `ObdSource` 发 `010C`/`0105`，`obd_protocol` 的文本解析已经写好并有单测
- **读不到** → 先换一家适配器再试，**别急着怀疑车或代码**
  （UART 只读 TX 是常见原因，见本节末尾）
- ⚠️ 这一步不需要 ESP32，也不需要拆任何东西 —— 它决定后面所有 ESP32 上的调试值不值得做

**② 万用表量组合仪表插头的 VAN 两针**（`PINOUT.md` 的 5/10 脚）

- 点火/ACC 状态下，黑表笔接**车体地**，红表笔分别点这两根针
- 判据：**一根约 4V、一根约 1V，两根之间差分约 3V** → 就是 VAN（9004/9005）
- 量到别的值（例如 12V）说明认错了针，回去对 `PINOUT.md`
- ⚠️ **极性（哪根是 DATA / DATA BAR）这样量不出来**，那是②之后用逻辑分析仪看
  差分相位才能定的；接反不会烧，但收不到帧 —— 所以后面接 SN65HVD230 时
  两根对调一次再试是很正常的操作

**③ 逻辑分析仪抓 K 线（经 L9637D）**

- ⚠️ **别把分析仪直接夹到 OBD 针脚上**：OBD 的 K 线是 **12V 电平**，
  而这类 ¥20~40 的 8 通道克隆分析仪输入通常是 3.3V/5V 逻辑电平，
  原版 [SparkFun 那款](https://www.sparkfun.com/usb-logic-analyzer-24mhz-8-channel.html)
  标 5V 容忍，克隆版各不相同 —— **直接接有烧输入的风险**
- 安全接法（用已买的 L9637D）：`OBD 7 脚(K线 12V) → L9637D 的 K 脚`，
  `L9637D 的 RX 输出(3.3V/5V) → 分析仪输入`
- SN65HVD230 的 RO 输出是 3.3V，VAN 那边可以直接接
- 抓 K 线时用 PulseView/sigrok，采样率 24MHz 对 10.4 kbit/s 绰绰有余
- 判据：能看到 **ISO 9141-2 的 5-baud 慢初始化**（一串长短不一的起始位）
  或标准的 10.4 kbit/s 波形，位宽约 **96 µs**

> **UART 只读 TX 的坑**：不少便宜 ELM327 的 K 线只有发送方向能跑，
> 或者 clone 的 KWP2000 5-baud 初始化有问题。症状是 App 能连上、
> 但一直"正在连接 ECU"或读不到数据。**这正是先做①的价值**：
> 花 5 分钟排除掉这个可能，而不是等 ESP32 代码写完才发现。

---

## 六、安全提示：笔记本 + 车 12V 的地环流

如果用**笔记本 USB** 给 ESP32 供电，同时模块又接到**车的 12V 系统**（哪怕只从 OBD 座取电），
就把车的电气地和笔记本的地连在一起了。最坏情况会带走笔记本或板子。

> **装车后"要上传固件"这件事本身就是这个冲突**：板子用 OBD 降压线供电（接车 12V）
> 时，Type-C 口就空不出来了；而插上笔记本上传固件，就同时把两边地接上了。
> 三种处理办法：
>
> | 办法 | 说明 |
> |---|---|
> | **台架上刷好再装车** | 家里用笔记本刷完，装车只做验证。零成本，推荐 |
> | **OTA 无线升级** | 分区表里 **app0/app1 双槽本来就是为 OTA 准备的**，装车后用 WiFi 推固件，笔记本完全不接线。装车调试阶段可能比插线舒服得多 |
> | USB 隔离器 | T3 的 20 号（¥30~80） |
>
> 注意 OTA 需要板子在车上能连 WiFi（手机热点即可），且 `platformio.ini`
> 里要配 `upload_protocol = espota`。

二选一：

- **不要在车里用笔记本给板子供电**——用充电宝或 OBD 取电降压线给 ESP32 供电，笔记本完全不接入车的电气系统（推荐，零成本）
- 加 **USB 隔离器**（T3 的 20 号，¥30~80）

另外两条：

- **OBD 16 脚是常电**：不开发动机时别让板子一直跑，会耗电瓶。想规范取电就用
  保险丝取电器接 **ACC**（T3 的 21 号）
- 调试阶段优先在**家里台架**跑（模块 + 板子 + USB 供电），这一段不需要上车

---

## 七、到货后的验证动作（对照 ACCEPTANCE.md）

> **顺序很重要**：下面 1~3 步**不需要 ESP32**，到货就能做，而且它们决定
> 后面固件调试值不值得做。第 4 步之后才需要板子（14 号还没买，见第五之二节）。

**不需要 ESP32（现在就能做）**

1. **ELM327 到货**（已有 2 个）：**先用手机 App 读一次** —— 能读到转速/水温才说明
   K 线这条路通（读到再谈接 ESP32；读不到先换适配器，见 T0 的「关于 K 线适配器怎么挑」）。
   App：Car Scanner / Torque / OBD Auto Doctor 都行。
   通之后：`Serial1.begin(38400, 8N1, 17/18)`，把 `g_data(nullptr)` 换成 `&Serial1`，
   看串口 `SRC rpm=obd coolant=obd`
2. **万用表量 VAN 两针**（组合仪表连接器 5/10 脚）：对车 GND 量，
   9004 ≈ 4V、9005 ≈ 1V（差分约 3V）→ 确认这两根是 VAN。
   ⚠️ **极性这样量不出来**（哪根 DATA / 哪根 DATA BAR），要靠抓包看相位
3. **逻辑分析仪抓 K 线**（**必须经 L9637D**）：
   ⚠️ OBD 的 K 线是 **12V 电平**，而 ¥20~40 的 8 通道克隆分析仪输入通常是
   3.3V/5V 逻辑电平 —— **直接夹上去有烧输入的风险**。
   安全接法：`OBD 7 脚(12V) → L9637D 的 K 脚`，`L9637D RX → 分析仪输入`。
   判据：能看到 5-baud 慢初始化，或 10.4 kbit/s 波形（位宽约 96 µs）

**需要 ESP32 板子（14 号到货后）**

4. **SN65HVD230 上板**：
   - 拆掉板载 120Ω（两颗并排的 120/121 贴片），用万用表确认
   - 按 PINOUT.md 接第一段：VCC→3.3V、GND→GND、RO→GPIO16、DE/RE→GND
   - 逻辑分析仪挂 RO 脚 → 上车看有没有 125kbps 方波
   - **有波形但解不出帧** → 先把两根 VAN 线**对调**（VAN 极性与标准 CAN 相反，
     本项目尚未实测确认哪根对应 DATA/DATA BAR）
   - 出帧后：确认 IDEN（本项目假设 0x824，公开样例是 0x8C4）→ 不符就用
     `configureSpeedFrame` 现场改，不用重刷逻辑层
   - ★ **这一趟是唯一能定 FCS 约定与极性的机会**（见 ACCEPTANCE.md 的实车必验清单）。
     宿主机测试是自编自解，证明不了线上行为 —— 所以抓到真实位流后优先做这件事，
     **不要回头改 SOF 图案**（那是已冻结的契约）
5. **固件上板**：`pio run -e esp32dev -t upload` → 串口应打印 `206 dash ok`，
   之后每 5 秒一行 `SRC ... | v=..km/h ..rpm ..C`，车速应从 0 扫到 210
6. **圆屏到货**：改 `ui_theme.h` 的 `THEME_DISPLAY_RES` → 写实驱动 → **最后**才删 `DASH_DISPLAY_STUB`
   （只删宏会 `#error`，这是防上电黑屏的保护，别绕过）
7. **屏结构验证**：卡尺量外径是否 <95mm、总厚是否 ≤ 塞尺量出的间隙 → 通过才下两块的单

---

## 八、一句话总结

**第一批已到/在途：SN65HVD230、OBD 一分二、杜邦线、两个 USB ELM327(CH340)、
逻辑分析仪、万用表、L9637D —— 车上的信号能抓了，但漏了 ESP32-S3（14 号），
所以固件还跑不了。本轮必须补的只有两件：ESP32-S3 N16R8（¥40~70）与
12V→5V 取电（¥15~35）。等板子的同时，车上先做三件事：手机 App 读 ELM327、
万用表量组合仪表 5/10 脚确认 VAN、
逻辑分析仪经 L9637D 抓 K 线（别把 12V 直接夹到分析仪上）。
VAN 从组合仪表连接器取，不是从 OBD —— OBD 只有 K 线。
示波器和高精度万用表都不在必买清单里；ISO 音响尾线只在决定从收音机取 VAN 时才买；
屏相关的 13/15/16/17 全部可以缓 —— 它们验证的是"双 480×480 能不能跑"，
与当前 VAN 线路层无关。**

---

## 九、踩坑留档：OBD 座上没有 VAN

这条值得单独记，因为它推翻了一个很自然的假设（"OBD 座是车上总线的总入口"）：

- 206 MUX 的 **OBD 诊断座只有**：K 线（7 脚发动机/变速箱、11 脚其他模块、
  12 脚 ABS）、16 脚 +12V、4/5 脚 GND
- **没有 VAN 引脚** —— 所以车速**不能**从 OBD 取，只能从组合仪表 / 收音机 / MFD 取
- 这与 CAN 车完全不同：CAN 车的数据通常就在 OBD 的 6/14 脚上

来源：206 1.4HDi MUX 车主实测针脚（EEVblog「VAN bus interfacing」长帖），
与同期 206 MUX 电路图的线号（VAN 9004/9005）相互印证。
车型已确认是 MUX（用户 2026-09-15 确认）。

---

## 十、踩坑留档：清单里那些"看不见的规格"

采购清单最容易犯的错，是把**数据手册上的指标**当成**页面上能筛选的条件**。
本项目已经踩过两次，记下来：

| 写过的"规格" | 问题 |
|---|---|
| 万用表要 **UT61E+**（4½ 位真有效值） | 项目只需通断 + 12V/3.3V + <1kΩ，高精度纯浪费。→ 改入门自动量程 ¥40~90 |
| ELM327 要"芯片认 CH340/CP2102/FT232" | 淘宝页面根本不标芯片型号；而且对上层无差别。**不是筛选条件** |
| ELM327 要"写明支持 ISO 9141-2 与 KWP2000" | 页面几乎都印"支持全部协议"，克隆板照写不误，**文字不可信** |
| 数字示波器"强烈建议" | 本项目没有必须看波形的地方。→ 降为"确认信号不通再考虑" |

**结论 / 挑货原则**：

1. 能**在页面上直接看到**的才是筛选条件（接口形态、是否带开关、几 pin、尺寸、分辨率）
2. 看不到的（芯片、协议、晶振、真实带宽）**一律靠实测验证**，并且把验证成本压到最低
   —— 比如先买 ¥25 的蓝牙 OBD 头验车，而不是直接买两个 ¥70 的 USB 版
3. 遇到"认准某个型号"的建议时，先问一句：**这个指标本项目真的用得上吗？**
   （UT61E+ 就是典型的"用不上"）
