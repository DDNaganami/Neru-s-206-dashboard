# 206-dash

车型：2001-11 206 CC MUX，自动挡，白底积家
目标：原表仓加屏，原表可继续通电当备份

显示分工（已定）：
- 屏负责：车速、转速、水温（油量待定）
- 原表负责：挡位显示（屏不重复做）；警告灯是否屏上复刻，待定

数据源（已定）：
- 转速 / 水温 / 进气温度 / **车速**：有线 K 线 OBD（ELM327，ISO 9141/KWP2000，
  PID 010C/0105/010F/010D）
  注意：206 是 K 线 + VAN（AEE2001）架构，不是 CAN，别买 CAN-only 适配器
  ★ 2026-09-17 用**蓝牙** ELM327 + 手机 App（EOBD）实测确认：这台车的 ECU
  确实回 010C / 0105 / **010F**（进气温度）/ **010D**（车速）。
  车速那一条的**判据**值得记住（2026-09-18 用户指出，见 ACCEPTANCE）：
  测试是在**地库**里做的（没有 GPS 定位），松刹车后车挪不到一米，
  车速读数立刻跟着变 —— 这种响应只可能来自车轮转速传感器，
  不可能来自 GPS。蓝牙版本身不能给 ESP32 用
  （S3 只有 BLE，没有蓝牙经典/SPP），它是"上车前先体检一次"的工具；
  车上那条通道仍是 USB 有线版接 `Serial1` —— 见 PURCHASE.md。
- 供电：OBD 座 16 脚（+12V，**常电**）+ 4/5 脚（GND）
- 车速（第二条路，**可选**）：DIY 读 VAN 总线（SN65HVD230 模块 + 自研 van_wire
  线路层，ESP32 直连）。既然 OBD 也有车速，VAN 的价值就变成"**更快 + 更独立
  + 能拿到 OBD 永远不给的数据（油量/门灯/警告灯/仪表自身状态）**"，
  而不再是"唯一来源"—— 两条路的取舍见 data_service.h 的优先级说明。
  ⚠️ **OBD 座上没有 VAN**（只有 K 线 + 12V + GND）。VAN 落点（线号 9004/9005）：
    组合仪表连接器 5/10 脚（首选，插接件免破线，反正要拆仪表）
    收音机黑色 7 针插头 2/3 脚（备选）
    MFD 18 针 4/17 脚（备选）
  认线特征（MUX，对车 GND）：9004 ≈ 4V、9005 ≈ 1V、差分 ≈ 3V
  极性（哪根对应 DATA / DATA BAR）**尚未实测确认**，接反就两根对调
  SN65HVD230 需拆掉 120Ω 终端电阻
  帧 ID 参考 pinterpeti.hu / graham.auld.me.uk / EEVblog 206 帖
- 不采购 V2C TINY 协议盒（它是给换新主机用的；除非将来换 RD45 主机）

阶段：
第一阶段：假数据驱动 UI
第二阶段：有线 OBD（K 线）
第三阶段：VAN 车速

软件分层（已定）：
- VehicleState：全项目唯一的状态结构
- 纯逻辑层在 lib/dashcore/（无 Arduino/LVGL 依赖，宿主机可直接单测）；
  src/ 只留平台与 UI（main、dash_display、dash_ui、boot_anim、ui_theme）
- data_service：字段级多源合并与超时回退（3 秒无新数据回退下一源，行车中断线不黑屏）
  优先级：**车速 Van > Obd > Sim**；转速/水温 Obd > Van > Sim；油量/挡位 Sim
  转速/水温按字段独立计时：一个 PID 死了不影响另一个字段继续用 OBD
  ★ 车速的三档各有理由（2026-09-18 加 OBD 那一档时定的）：
  VAN 上的车速是仪表**广播**的 —— 纯听，零 K 线成本，所以优先，
  把那个时隙留给转速；OBD 的 010D 是**兜底**（VAN 没接/没解出帧时车速
  照样是真值，而不是掉回假数据）；两个都断 3 秒才回假数据。
  实现上"两段代码的先后顺序就是优先级"（OBD 段在前、VAN 段在后直接覆盖），
  由 `test_speed_priority_van_obd_sim` 钉住 —— 别把两段调过来。
- obd_source：K 线 ELM327 非阻塞状态机，**轮流请求 PID 010C/0105/010F
  （+ 条件性的 010D 车速）**；
  ★ 010D **不是无条件加进轮询表的**：AT 序列之后先问一次 0100（支持的 PID
  位图），**只有 ECU 说支持才占那个时隙** —— 一是每个时隙都会把其余字段
  拖慢 ~25%，二是"ECU 不支持"的代价更大（ELM327 要等到超时才回 NO DATA）。
  拿不到位图就按默认三路继续跑，不猜。`-DOBD_SPEED_PID=0` 可强制关掉
  （用来对比两种刷新率）。位图解析在 obd_protocol.cpp 的
  `parseSupportedPids` / `pidSupported`（上位机可测）。
  ★ 每个字段还统计**实测刷新率**（Hz，每秒结算），随 `SRC-Hz` 行打到串口 ——
  "K 线够不够用、要不要降频/加时隙"只能靠这几个数说话，算不出来
  （ELM327 自身开销 + ECU 响应快慢都在里面）。
  ★ **轮询节奏（2026-09-18 定）**：快路每轮都问、慢路每 N 轮问一次
  （`kSlowEveryNTurns = 4`）：
  快路 = 转速 0x0C（+ ECU 支持时 0x0D 车速），慢路 = 水温 0x05、进气温度 0x0F。
  一个周期的请求序列是 `0C 0D 05 | 0C 0D 0F | 0C 0D | 0C 0D`。
  用户确认过理由：**温度是慢变量，不需要那么快的更新**，而 K 线那条管子
  得优先喂给要"跟手"的转速和车速。
  ★ 同时修掉一个更大的浪费：原先"等响应"那一格**只会等满 250ms 超时才走**，
  收到响应也不提前离开 → 每格实际 250(等)+200(间隔)=450ms，只有 ~2.2 请求/秒。
  现在按 `waiting_pid_` 认领当次响应、当即回到间隔态（没响应才走超时），
  间隔也从 200ms 收到 80ms。
  预估（**待车上 `SRC-Hz` 实测替换**）：每题 ≈110~180ms；
  快路两路时 转速/车速 ≈2.2~3.6Hz、水温/进气 ≈0.55~0.9Hz；
  只有转速时 转速 ≈3.6~5.7Hz。比改动前（每字段 ≈0.7Hz）快 3~4 倍。
  （后两个响应是单字节 A-40，车速也是单字节 A=km/h）
  文本解析在 obd_protocol.h/.cpp（纯函数，**表驱动**，加 PID 只动那张表）；
  坏帧值域钳制（转速≤9000、温度 -40..215；车速单字节不用钳）
- van_source：VAN 帧解析。车速/转速帧 IDEN 0x824（BSI→仪表，7 字节）：
  data[0..1]=转速×8，data[2]=车速（**单字节**，**1 计数 = 2.56 km/h**，`kSpeedScale = 2.56`
  —— 2026-09-22 实测定标；取自蓝牙 ELM327 `010D` 回归，数据可分辨范围 **2.55~2.57**，
  **2.56 是取整**），data[3] 含义未知
  依据 morcibacsi/psa_van_bus_packet_descriptions（307 实测、206 适用，实车需验证）
  坏帧值域钳制（车速≤300、转速≤9000）
- van_phy.h：VAN 物理层接缝（接口 + VanPhyStub）。当前 main 用桩占位；
  实驱动（SN65HVD230 + VanBus 库 / RMT）到货后实现 VanPhy，把解出的帧
  喂给 VanSource::onPacket()，数据层不动
- van_wire.h/.cpp：VAN **线路层**（已实现,纯逻辑,宿主机可测）——
  SOF 识别 → 4B5B（E-Manchester,每 5 槽丢弃第 5 位）→ 字节流 →
  IDEN/CMD 拆分 → CRC-15 校验 → 帧尾判据。含编码器（测试/仿真用）
  和 BitDecoder（喂边沿时间戳→出字节,带字节回调与队列两条路径）。
  关键设计:字节走**回调**(ByteSink)而不是等 drain 队列 —— 一个边沿区间
  内可能同时含数据字节和帧尾,等区间处理完再取会丢字节。
  帧间空隙用**超时重同步**(默认 1ms)处理:总线空闲可达几十毫秒,
  逐槽展开会把相位冲垮。
  未定项（实车必验,见 ACCEPTANCE.md）：~~FCS 约定、字节位序、SOF 槽数、
  206 实车 IDEN~~ —— **2026-09-19 已用真实抓包定案**（FCS = crc15_van_iso
  0x0F9D/init 0x7FFF/取反/MSB-first，覆盖 IDEN+CMD+DATA；SOF 就是 10 槽
  0000111101；线上 IDEN 12 位、字节流 `[IDEN>>4, (IDEN&0xF)<<4|CMD]`；
  206 实车开车帧 IDEN=0x824）。**背靠背帧那个尾巴已修**（2026-09-19）：关帧门限
  按实测从 300µs 改成 **70µs** —— 帧内最长间隔 49.0µs < 70 < 帧间最短空闲
  95.5µs（量法 `tools/van-decode/gap_stats.py`）。详见 ACCEPTANCE.md 文末那条。
- van_phy_wire.h/.cpp：线路层接到 VanPhy 上的边缘解码器（**整链已回归**）。
  硬件侧只需在 RO 脚电平变化时调 `onEdge(t_us, level)`,不碰任何寄存器。
  **职责三分（改这里之前先看完这条）**：
  ① 帧起点：`BitDecoder` 匹配到 SOF 的 10 个裸槽后，经
     `ByteSink::onFrameStart()` 回调 → relay 调 `FrameParser::beginFrame()`。
     必须是回调而不是"边沿返回后再判断"：SOF 命中与后续数据字节可能落在
     **同一次 `pushEdge`** 里，返回时字节早吐出去了。
  ② 帧尾：**只有 `finish()` 能关帧**。`onEdge()` 一律不准调 `endFrame()` ——
     边沿路径抢收会在缓冲已被消费成空之后把帧收掉，字节再也救不回来
     （实测症状 `finish 前: frames=1 pending=0 bytes=0`）。
     `finish()` 以 `FrameParser::inFrame()` 为准，不看解码器相位。
     ★ 什么时候**可以**关：唯一判据是 `van_edge_queue.h` 的
     `vanIdleCloseReady(frame_pending, queue_empty, now, last_edge, idle)` ——
     **队列非空就不许关**（2026-09-18 审核指出）。只看"距最后一条已喂边沿多久"
     会漏掉"队列里还压着同一帧的后续边沿"这种情况，此时关帧等于当场截断，
     症状是 `edges` 在涨而 `frames/fcs_ok` 不涨。抽成纯函数就是为了能在
     宿主机上把这条边界钉死（两条用例，其中一条把截断场景整个复现出来）。
  ③ SOF 是**固定 10 TS 同步图案 `0000111101`**，不是 4B5B 数据字节：
     编码器写裸槽（`putSof()`），解码器按同一常数滑窗匹配。
     别用 `putByte(0x0F)` 生成它（会得到 `0000111111`，差第 9 槽），
     也别拿"折 10 槽 == 0x0F"当它正确的证据 —— 按规范折出来是 **0x0E**。
- van_phy.h 的 VanSink/VanSourceSink:物理层只依赖 VanSink 接口,
  数据源用 VanSourceSink 转一层(main.cpp 已接)。
- sim_source：无硬件时的默认假数据源

界面（已定，LVGL v9）：
- 无传统指针：外圈圆弧进度条承载车速/转速/水温，表盘中间放角色表情
- **屏与表的对应按法系车来（标致 206 实车）**：
  **左屏 = 转速表（带水温表）**，**右屏 = 速度表（带进气温度表）**。
  别按"左车速右转速"的日德习惯改回去。
  左屏：转速弧（外）+ 水温弧（内）+ 表情；
  右屏：车速弧（外）+ **进气温度弧**（内）+ 表情。
  **两块表左右对称**：两条副弧（水温 / 进气温度）几何完全相同
  （0→180、radius 168、width 10、`reverse=1`），只有屏号与颜色不同
  （水温绿 `0x7CFF6B`、进气琥珀 `0xFFB020`）—— 这条由
  `test-gauge-geometry.js` 的「进气温度弧 = 水温弧的镜像版」逐字段钉住。
  **外圈弧开口朝下、内圈副弧开口朝上**（产品决定，用户要求）：
  转速/车速弧 135→405（拱在上方），副弧 0→180（兜在下方）—— 一眼分得清。
  **涨幅方向也相反**：外圈弧从 start 端起涨；副弧 `reverse=1`
  （从 end 端起涨 = 镜像），因为"下方半圆"默认只会从右端开始亮，
  而水温/进气温度表该从左端起涨。
  建屏 / 开机扫表 / 正常渲染三处都走 `arc_set_progress()` —— 固定哪一端只在那一个地方决定。
  ★ **副表的定义集中在 `dash_ui.cpp` 的 `is_aux_kind()`**（水温或进气温度）：
  "大数字跟哪条弧"= 该屏第一条**非副表**的弧。加进气温度时如果只写
  `!= Coolant`，一条 `[Intake, Speed]` 顺序的屏会让速度表的大数字变成进气温度。
  另注：主题里的 `radius` 是弧的**外沿**半径（LVGL 把弧画在盒子内、`width` 往里长），
  预览若按"带宽居中在 radius"画就会整体外移半个带宽（实测外圈差 12 像素）。
- ui_theme.h：所有颜色/弧角/半径/量程/表情占位参数集中于此
- **主题是运行时可换的**（`lib/themetool/`）：`g_theme_ptr` 指向"闪存里的默认值"
  或"从 theme 分区读来的主题"，改配色只需重刷 16KB 的 theme 分区，固件不动。
  关键取舍：默认主题必须是 **const 静态**，不能是可写全局 —— 可写全局会让
  LVGL 无法把样式对象折叠进 rodata，一次多吃约 36KB DRAM，直接把
  esp32dev 撑爆（实测 `dram0_0_seg overflowed by 8872 bytes`）。
  因此所有主题宏都是**转发宏**（`#define THEME_BG_COLOR (g_theme.bg_color)`），
  换主题时不需要重新编译任何消费方代码。
  解析失败一律**回退默认值继续跑**，绝不让主题问题导致黑屏。
- **图片资源也是运行时可换的**（`lib/themetool/image_blob.*`）：图片放在
  `image` 分区，用 `esp_partition_mmap` 只读映射，再组装成 `lv_image_dsc_t`
  交给 LVGL。选 `LV_IMAGE_SRC_VARIABLE` 而不是 `LV_IMAGE_SRC_FILE` 的理由：
  前者**不复制像素**（后者会），所以 mmap 指过去不额外吃 DRAM ——
  这对只有 320KB RAM 的 esp32dev 是硬要求。
  mmap 的 handle 存成 static 且永不 unmap：一旦被回收，那块虚拟地址会被
  别的映射复用，LVGL 就会读到别人的数据。
  空分区（全 0xFF）和任何坏镜像都**拒收并降级**，同样是"资源出问题不能让固件崩"。
  角色编号：1 = 背景，**保留编号（一律不复用）** 2（开机帧）/ 5（左屏惊喜）/
  7（右屏红区）/ 9、10（开机图）/ 11、16（眨眼图）/ 14、15、19、20（冷车、过热图），
  在用的 8 个是 **左屏 3=常态 4=红区 12=巡航 13=运动**、
  **右屏 6=常态 8=超速 17=巡航 18=运动**（8 号当年是"惊喜"，只改名不改号）。
  复用保留编号会让老 image.bin 里那几张图突然变成别的表情，而且不报错 ——
  **新角色从 21 开始接**。
  表情图用 RGB565A8（带 alpha 平面，能叠在弧上），背景用 RGB565。
  存储预算：一张 240×240 表情 RGB565A8 = 169KB，一张 480×480 背景 RGB565 = 450KB。
  8 张表情 + 一张 240 背景 = 1.46MB 仍然超 1MB，**降到 192×192 就是
  8×108+72 = 936KB —— 能装下了**。这也是"把状态数压到每屏 4 个"的直接原因。
  换 S3（16MB flash）时把 image 分区放大到 4MB 以上即可，分区表在 partitions.csv。
- ui_theme.h 原先把颜色/几何写死在编译期；现在这些宏转发到运行时指针，
  `theme_clamp()` 统一钳制值域（越界值不会画到屏外或触发 LVGL 断言）
- 分辨率适配：所有尺寸按 480×480 基准书写，渲染统一乘 theme_scale()；
  换屏只改 THEME_DISPLAY_RES 一行（3.4" 圆屏 800×800 → 填 800），
  **这一项仍是编译期**（不是运行时），因为缓冲区大小要静态分配；
  桩驱动的显示宽高也读这个常量，两处永远一致
  · **字号也随分辨率**（2026-09-20 加，240 那块验证板点出来的）：几何乘了个
    小数看不出来，字体不行 —— 48 号字在 240 宽的屏上五个字符就占满整行
    （owner 实屏反馈"数字太大"）。所以点数查表拿：
    `ui_theme.h` 末尾的 `kReadoutFontPx[分辨率档][字号档]`
    （480 → 48/18；240 → 24/10），`readout_font()` 只认档位、不看点数。
    主题文件里存的永远是**档位**，于是同一份 theme.json 在两种屏上都成立。
    表和 static_assert 在**一处**：不要在 dash_ui 或主题里写 `if (res == 240)`
- 表情由 face_stages.h + expression.cpp 状态机驱动，**每屏一套、各看各的表**：
  · **左屏（转速表）只看转速**：怠速(<1500) / 巡航(1500) / 运动(3500) / 高转(4500) / 红区(5800)
    三档阈值按**实车地标**推（用户实测：点火怠速 900、稳定巡航 2000、表盘上限 **7000**）：
    1800 包住怠速 900 并给起步留余量；巡航 2000 稳稳落在巡航档；
    红区在上限前 1000 转开始报红（83% 刻度）—— 不是"踩到断油才亮"。
  · **右屏（速度表）只看车速**：静止(<30) / 市区(30) / 快速路(65) / 高速(95) / 超速(>130)
    车速四档的前三个是 城区/快速路/高速 取 30/90（还没有实车数据）；
    第 4 档超速是用户定的 130（进入 >130、退出 ≤127 有迟滞，
    免得定速巡航压在 130 上下时脸来回跳）。
    ★ 这一档原来叫"惊喜"（急加速 > 15 km/h/s 的 400ms 瞬态），已改掉：
    它不是按车速分的档，所以阶段模拟里点不出来；而且 15 km/h/s ≈ 4.2 m/s²
    对 206 CC 自动挡（0-100 约 11s）基本到不了，那张图导进去永远不亮。
    改成超速之后，车速组和转速组一样是 **每屏 5 档 5 个状态**（2026-09-18 各加一档：
     左加"高转"、右加"市区"，依据是 TU5JP4 + AL4 的实际活动范围），
     每个状态都能在模拟器里点出来 —— 这也是用户报"这个档选不出来"的修法。
  · **阈值怎么定的**（那笔账在 expression.cpp 顶部，摘要）：
    日常全落在 1600~3400 转（4 档 100km/h≈2620、130≈3400），所以**分界刻意
    避开这一段**；**断油 6300（实测）** → 红区从 5800 起（留 420 转提前量）。
    每条边界都有 **±80 转 / ±3km/h 迟滞**（`face_ladder.h` 的 ladderStep）：
    **进入门槛 = 阈值 + 迟滞，退出门槛 = 阈值 − 迟滞（等于门槛仍算在档内）**。
    标定值改一处要改三处：expression.cpp、face_stages.h 的阶段表、网页镜像。
  · **表盘刻度上限 `kRpmMax = 7000`**（vehicle_state.h）与红区阈值刻意分开：
    弧画满的位置看表盘，表情报红的位置看发动机。两者都随实车数据走。
  · **水温不参与表情**：它只驱动水温弧与水温数字。
    理由：一套水温表情要再加 4 张图（冷车/过热 × 两屏），而 image 分区只有 1MB；
    而且水温是慢变量，屏幕下方本来就有弧和数字，再让脸跟着变信息重复、
    还容易误读（看到"冷车脸"会以为车有问题，其实只是刚启动）。
  · `face_update()` 返回 `FaceSet{left, right}`；`Face` 枚举是**两屏共用的档位名**，
    哪一屏有哪些状态由 `kFaceLeftStates` / `kFaceRightStates` 明确列出，
    并有测试断言"列出的状态必须有图、没列出的必须没有"。
  · 稳态**只由数据决定，与时间无关**（`test_time_independent` 连跑 20 秒断言不变）。
    历史：曾经有过"眨眼 Blink"状态，因为只跟时间有关、说不出什么工况会触发，
    已被用户否掉并删除（编号 11/16 保留不复用）。
- 阶段表贯穿固件与网页：`lib/dashcore/face_stages.h` 是唯一事实来源，
  16 条用例（转速 5 档 + 车速 5 档 + 水温 3 档 + 进气温度 3 档），**每行同时给出左右两屏的期望**，
  外加"缺图降级链"与"槽位→角色编号"两张表。
  两组硬规则都有两端测试钉住：
  ① 每组只变自己那一维（点速度档不能动转速表）；
  ② 只有该屏自己的那一路能改它的表情（转速组的右屏恒为常态、速度组的左屏恒为常态、
     水温组两屏都不动）。
- 表情状态机现在是**纯数据驱动、与时间无关**：眨眼删了、惊喜（瞬态）改成
  超速（稳态）之后，`face_update()` 里只剩一个"超速迟滞位"，
  `now` 参数保留但不用（有单测钉住"表情与时刻无关"）。
- **表盘数字读数**（转速/速度大数字 + 单位 + 水温数字）：
  · 大数字放表盘正上方，单位紧跟其下，水温数字在**转速表底部**
  · 字号是**档位**、点数是**分辨率派生**的（见上面"字号也随分辨率"）：
    480 屏 48/18 号、240 屏 24/10 号 —— 主题里只存档位，
    所以"240 上读数太大"这种问题不需要动主题文件
  · 大数字跟哪一路**由弧决定**（该屏第一条非水温弧），不看屏幕序号 ——
    以后把水温弧挪屏、或加第三条弧，读数自动跟着走
  · 格式写死在固件里：速度取整到 1 km/h、转速取整到 10 rpm、水温取整到 1 ℃；
    可调的是颜色/字号/位置/开关（主题里的 `readout` 段）
  · 竖直可用区间只有 59..120（上面是**外弧带 35..59**——`radius` 205 是外沿、
    `width` 24 往内长，不是旧文档里的 23..47；下面是表情图顶边 120），
    所以默认位置是 digit_cy=72 / unit_cy=107 —— 这条由单测
    test_readout_defaults_fit_gap 与预览帧的墨迹外框共同钉住
    （实测 48 号数字墨迹顶在 y=55，蹭进弧带 4 像素，刻意接受：读数画在弧之上）
  · 开机扫表期间数字栏是**空的**（标签以空文本创建，读数只在实际渲染时写入）；
    刻意不做淡入：给对象设 opa<255 会让 LVGL 开离屏层，这个驱动上会错位
- 表情/背景图片：dash_ui 里按"背景图(最底) → 弧 → 表情图 → 读数"的创建顺序叠层；
  **缺图有降级链**（face_stages.h 的 kFaceFallback）：只导入常态一张也能跑，
  运动缺图退巡航、超速缺图退运动…… 一张都没有才回落到程序化占位表情。
  左右屏各查各的：两套差分图不能互相顶替，否则角色会串
- dash_ui.cpp 的 faceResolve() 是固件端唯一的"状态→图片"解析入口，
  网页端 face-stages.js 的 resolve() 是它的镜像，两边由
  tools/theme-editor/test-face-stages.js 解析 face_stages.h 逐字段对账
- dash_display.h/.cpp：显示驱动接口，当前为无屏桩（DASH_DISPLAY_STUB=1）；
  非桩分支是 #error——只删宏会编译失败，防止上电黑屏；屏到货后
  先锁分辨率、写实驱动、换 S3+PSRAM，最后才删宏
- 开机动画（boot_anim.h/.cpp，纯时间逻辑）：淡入 → 双屏错峰扫表 → 表情显形；
  时长常量在 ui_theme.h（BOOT_*），按主循环频率刷、不受 5Hz 渲染节流；
  正常渲染带弧值缓动（kArcSmoothPerSec=1.44/s 指数趋近，按实际时间算，
  渲染频率变化不改变手感），开机→真实数据无缝过渡。
  总时长 = `boot_face_start_ms + boot_face_blink_ms`（第二个字段名是历史遗留，
  现在是"显形后的一拍收尾"；以前是 +4 拍眨眼，眨眼删除后那 4 拍纯属白等，
  而开机期间 dash_ui_render 会早退、根本不显示真实数据）。

机械尺寸（实测，2026-09）：
- 大表盘孔内径 Ø89 mm；台阶外沿 Ø100 mm（台阶宽 5.5 mm）
- 两表圆心距 95 mm → 两个 Ø100 台阶重叠 5 mm，遮罩环需做成 8 字形合并件
- 表盘处面板背面到后盖 22 mm，结构紧凑（基本不可用）
- 表壳内部：上部大半圆弧空腔最高 135 mm、向两侧收拢至 38 mm；下部均匀 25 mm 深 → 驱动板/PCB 放这些区域
- 装屏方案（已定）：拆转速/水温/车速三根指针，表盘纸不动，屏嵌在表玻与表盘纸之间、盖住两大表盘；油表与挡位显示保留原表
- 选屏硬约束：可视直径 ≤ Ø89；两屏外径 < 95（圆心距）；总厚度 ≤ 表玻-表盘纸间隙（原文"待量"）
- 2.8" 圆屏（可视 Ø70.7）留环 9.2 mm/边；3.4" 圆屏（可视 Ø86.4）留 1.3 mm/边
- LilyGO T-RGB 2.8" 整板 Ø96 > 95 且厚 18 mm，排除
- 原"待量"（表玻到表盘纸间隙：表盘中心/上缘/下缘三点、表玻曲率）—— 2026-09-21 owner 判断
  **「仪表盘内部空间肯定足够」⇒ 不再测量**（对应上面「最终显示架构」一节待确认 #2，已关闭）
- 平台硬约束（已定）：真屏双 480×480（或 800×800）全缓冲必须
  ESP32-S3 + PSRAM（建议 N16R8）；esp32dev 只有 320KB RAM，
  只用于桩开发/逻辑联调（现在靠 480×10 窄条缓冲才活得下来）。
  采购清单按 S3 列，别按 DevKit 买完线才发现画不动。

## 最终显示架构：两块 S3、一板一屏、一条 UART 串联（2026-09-21，owner 已定）

**决定**：两块 ESP32-S3，每块只驱动**一块** 2.8" 圆屏 480×480，两板之间**一条 UART**。每板只走一路 RGB
⇒ 框架那份**精简 esp_lcd 就够**（不要 1→2 选通、不要升 IDF 5.x —— 那两样是"两块 480×480 挂一块板"的
必要条件，见 ACCEPTANCE 的 RGB 骨架那节）；两块小板也比"一块宽屏 + 40pin 转接 + 选通板"更贴表仓弧形空腔。
2.8" 圆外形 ≈Ø73 进 Ø89：每边留 ~8.0 mm（按有效区 Ø70.13 算是 9.4），Ø100 台阶在后面。
**两个候选**：
- 主选 2× 微雪 `ESP32-S3-LCD-2.8C`：480×480 圆、ST7701S、RGB，**料表就带 ST7701S 规格书**（init/例程
  有出处）；wiki 口径 S3R8 + 8MB PSRAM + 16MB flash、板载屏 + TCA9554 扩展 + TF + 电池充电，$32.99~39.99。
- 备选 2× 裸 `ESP32-S3-DevKitC-1 N16R8` + 2× Dwin `LI48480T028BA3098`：外形 73.03×76.48、有效区 Ø70.13、
  厚 2.3、40pin/0.5mm FPC、ST7701S、RGB24 + 仅 init 的 3 线 SPI，裸屏。
**否掉的（各一句）**：一块板带两块 480×480 → 要 1→2 选通 + IDF 5.x（精简 esp_lcd 缺 num_fbs / 事件回调 /
bounce buffer）；480×480 QSPI 圆屏 → 最大的圆只到 2.1"（Ø53），太小，几何与字号全得重做；480×480 方形
QSPI（TDO 3.95"）→ 对角线 ≈135 mm，圆孔把四角切掉；2.8" 圆 MIPI-DSI → 模组确实有，但 S3 没有 DSI
（带 DSI 的是 P4），买到即废；一块宽条屏（185~200 mm）→ 得打掉两孔之间的塑料桥，且这一档只有 RGB/MIPI。
**分工**：**右板 = 主（A）**、**左板 = 从（B）**（2026-09-21 owner 更正：此前写的是"左板主、右板从"）。
右板（主）挂 VAN 收发器只用 **1 根 GPIO**（只听，只用 RO/RX；`D`/`TX` 接 **3V3**、`RS` 接 **GND**
—— 这两根**不占 GPIO**，也从物理上排除了"发"的可能，口径见 `PINOUT.md`「抓帧盒」一节）+ 板载日志 **USB-C**，渲染**右盘**
（车速 + 进气温度）并把整车状态**广播**给左板；左板（从）**不接任何 VAN 硬件**，只收那条 **4 线线缆**
（5V/GND/TX/RX），渲染**左盘**（转速 + 水温）。
**为什么主在右边**：车是**左舵**，VAN 那一对线（线号 **9004/9005**）在中控、仪表罩**右侧**够得到
⇒ 主放右边 **VAN 走线最短**，主板的日志 USB-C 也落在更好够到的一侧。**屏上内容不变**：
左屏 = 转速 + 水温，右屏 = 车速 + 进气温度。
**链路**：115200 8N1、**单向**（**右 TX → 左 RX**）+ 一条 **4 线线缆：5V / GND / TX / RX**
—— **5V 是电源**、**TX/RX 是 3.3 V 逻辑**、**GND 兼作信号地**（电源回流与信号参考共用这一根）；
复用**已经实现的回放行格式**
`VAN 824 18 F8 27 10 00 00 00` ⇒ 左板（从）**零新协议**（`van_replay` 那条路本来就在解它）。
★ **已知可用，不是推测** —— 同类通路已在真板上验过：2026-09-18 桌面联调用 `tools/serial-capture/replay.py`
往设备 UART 贴同样的行，设备端解出来并驱动了界面（`SRC speed=van … v=100.0km/h`，见 ACCEPTANCE）。
速率：0x824 ≈79.7 Hz → 每行 29 B ≈ **2.3 KB/s**，115200 8N1 ≈11.5 KB/s ⇒ 占两成，够（以后要省可用紧凑行
`S 45 2100`，现在不做）。★ 约束：**从板不做日志**（那条 UART 要收广播），要看从板只能走 USB-C（以后 SD/WiFi）
—— 这也是当初把日志从显示板移走的原因之一。

**接线定案：主选板的可用脚（2026-09-21，owner 给了厂商图 —— 已定，不是待确认）**

| 脚 | 从哪个口引出 | 本方案用途 |
|---|---|---|
| **GPIO15** | I2C 口 `SDA` / 12PIN `SDA` | **主板（右）：VAN 收发器 RX（RO → 板）** |
| **GPIO43** | UART 口 `TXD` / 12PIN `TXD` | **主板（右）：板间链路 TX →（从板 RX）** |
| **GPIO44** | UART 口 `RXD` / 12PIN `RXD` | **从板（左）：板间链路 RX ←（主板 TX）** |
| GPIO7 | I2C 口 `SCL` / 12PIN `SCL` | **备用**（空着） |
| GPIO0 | 12PIN `GPIO0` | **不用** —— BOOT/strapping 脚 |
| GPIO19 / GPIO20 | 12PIN `D-` / `D+` | 原生 USB：**日志**（各板插各板的） |
| 5V | 12PIN `VBUS(5V)` | **给从板供电**（就是那 4 线缆里的 5V） |
| 3V3 / GND | 各口都有 | 共地；**3V3 不过板** |
| 屏那 20 根 | 板载走线 + TCA9554 | 一块 480×480 / ST7701S / RGB（不占排针） |

- **VAN RX 为什么挑 GPIO15、不挑 GPIO44**：43/44 是板载 USB-串口桥（CH343P）那一对，桥的 TX 是**推挽输出**，和收发器的 RO 并在一起会**对打**（一个推高、一个拉低）；43/44 当**输出**没事、当**输入**才有这个风险 ⇒ RO 走 15，43/44 只留给「主板只发、从板只收」的那条单向链路。
  ★ **2026-09-23 按官方原理图追记（不改上面这条决定的结论）**：43/44 上的 CH343P **不是硬并线**，而是经 **`FSUSB42UMX`（U10）二选一**（详见 §0 与 §8 的 L1）。所以"VAN RX 走 15"**仍然是稳的**（15 与 43/44 相安无事），但"43/44 当输入会对打"这个**理由**被修正了；而"43/44 只留给单向链路"这半句**已被 §0 扩成双向**（v1 要 B→A 上行）。
- **两板之间那根线（4 线）＝ 5V / GND / TX / RX**：**5V 是电源**（主板 12PIN `VBUS(5V)` → 从板），**TX/RX 是 3.3 V 逻辑**（主板 GPIO43 → 从板 GPIO44），**GND 既当电源回流也当信号地**（就一根）。★ **跨板只送 5V，绝不送 3V3** —— 每块板自己的 LDO 管自己那一路；送 3V3 等于让一块 LDO 扛两个负载，还会倒灌。
- **收发器上那几根"不占 GPIO"的线**（2026-09-22 口径，详见 `PINOUT.md`「抓帧盒」一节）：`VCC` → 板子 **3V3**、`GND` → 板子 `GND`、`D`/`TX`（芯片第 1 脚，驱动输入）→ **3V3**、`RS`（芯片第 8 脚）→ **GND**（**6 脚模块**上没引出这一脚，模块内部已接 GND ⇒ **无需外接**）⇒ 主板实际只占 **GPIO15** 一根。★ **`D` 绝不能悬空** —— `D` 是驱动输入，**低电平 = 显性**（收发器主动把总线拉低），悬空时被噪声拉低就等于我们**主动干扰被测的 VAN 总线**；接**高** = **隐性 = 不驱动**。目的不变：**只收不发**。
- **日志一律走各板自己的原生 USB-C**（19/20），UART0（43/44）整条让给链路 —— 见下面的**待办**。

**引脚预算**（每板一块屏；先扣掉 flash 26–32 / **OPI PSRAM 33–37** / USB 19-20 / strapping 0/3/45/46）：

| 项 | RGB565 | RGB888 |
|---|---|---|
| 数据 R0-R4 / G0-G5 / B0-B4 | 16 | 24 |
| PCLK / DE / HSYNC / VSYNC | 4 | 4 |
| 背光 PWM | 1 | 1 |
| **每板小计** | **21** | **29** |
| 右板（主）+VAN RX +链路 TX；左板（从）+链路 RX | 主 23 / 从 22 | 主 31 / 从 30 |

- 40pin FPC 只引 24 位；走 RGB565 就是**把不用的 LSB 接 GND + init 里选 16bit** —— 真能省 8 根，只是不太常规；裸屏还要 +4（init 的 3 线 SPI + 复位）。
- 备选板（裸 N16R8）可用约 30 根 ⇒ 26~27 放得下 ✓；代价是自画转接板（FPC 座 + 33Ω 串阻）与 Dwin 的 LED×4 背光（12V/20mA 要升压）。
- ✓ **"决定性的未知数"已解**（2026-09-21，owner 给厂商图，即上面那张表）：那 20 根屏线在板上走、不占排针，能引出的就是 I2C 口 7/15 + UART 口 43/44 + 12PIN 的 0/19/20 ⇒ **主板用 2 根（15 收 VAN + 43 发链路）、从板用 1 根（44 收链路）：够**，而且**不必抢 BOOT 键**（GPIO0 不碰）、**不占原生 USB**（19/20 留给日志）。wiki 旧口径说 I2C 口那两根「不能当普通 GPIO」—— 按厂商图 owner 已定 **15 拿来收 VAN**，接线照表走 —— 原第 1 条待确认**已关闭**。
- ✓ PINOUT.md 开头那句反向说法**已改正**（2026-09-21，commit `543c151`）：现为「八线(OPI) PSRAM 固定占 GPIO33-37 ⇒ 33-37 不可用；只有四线(Quad)才不占」，屏 SPI 原排 35/36/37 的那几行随之改「**脚位待定**」（等**微雪 ESP32-S3-LCD-2.8C 原理图**，即下面待确认 #1 —— **该条 2026-09-21 已关闭**），PURCHASE 同口径 —— 与本表一致，本表按**被占**算。

**待确认（owner 欠）** —— 2026-09-21 复盘：**1、2 已关闭；3 只在走备选时才复活；4 作废**，没有还欠 owner 的项
1. ✓ **已关闭**（2026-09-21，owner 给了厂商图）：微雪 2.8C 的**自由 GPIO 已全部知道** —— 就是上面那张表（I2C 口 7/15、UART 口 43/44、12PIN 的 0 + 19/20 + 43/44 + 7/15 + 5V/3V3/GND）；主板 2 根（15 + 43）、从板 1 根（44）**够**。wiki 旧口径说 I2C 口那两根「不能当普通 GPIO」—— 按厂商图 owner 已定 **15 收 VAN**，接线照表走。PSRAM/flash 沿用 wiki 的 8MB/16MB 口径。
2. ✓ **已关闭**（2026-09-21，owner 判断）：机械三项（**表玻→表盘纸净厚度 / 台阶面→表盘纸深度 / 表盘后方净空**）与**孔的真实最小内径（到 0.1 mm）** —— owner：**「仪表盘内部空间肯定足够」** ⇒ **不再测量、别再问**（上面「机械尺寸」一节里那两条"待量"同此作废）。
3. **降级为"只对备选有意义"**：Dwin 多出来的 **~3.45 mm**（76.48 − 73.03）在哪条边（FPC/IC 台阶）—— 选**主选（微雪整板）**用不到它；哪天真走备选（裸 Dwin 屏）再问。
4. **作废**：表仓中间那道塑料桥能不能打掉 —— **只对上面已否掉的"一块宽条屏"有意义**，两条已定的路都不需要它。

**待办（固件）—— ★ 本节的第 1 条已于 2026-09-23 做掉（下面保留原文并注明改法）**
- ✓ **显示那两个 env 关掉 UART0 日志 —— 已做（2026-09-23）**。原文（保留，好让下一次改动知道当初在防什么）：
  「`lib/dashcore/dash_log.h` 的 `dash_log_begin()` 现在**同时**开 USB-CDC（`Serial`）和 UART0（`Serial0`），
  `dash_logf()` 也是两个口各写一遍；上了两板架构后 **UART0 就是 43/44 那条板间链路**，日志文本会**混进链路数据流**
  （从板会拿它当 VAN 回放行去解）⇒ 显示构建必须把 UART0 那一路**关掉，日志只走原生 USB-CDC**。」
  **改法（一行判据，没有第二处开关）**：`dash_log.h` 新增 **`DASH_LOG_UART0`**，
  默认值按"**这份固件里有没有链路 PHY**"判定（`LINK_PHY_UART`）——
  **没有**（VAN 采集那条路）⇒ `1`，两个口都发，**行为一字未变**；
  **有**（显示/链路构建）⇒ `0`，`dash_log_begin()` 不开 `Serial0`、`dash_logf()` 不写 `Serial0`，日志只走原生 USB-CDC。
  ⇒ 显示构建**不再需要**在 `platformio.ini` 里写 `-DLINK_PHY_UART_ALLOW_LOG_ON_UART0=1` 那种豁免
  （那条宏已退役、恒 0；`env:esp32s3` 与 `env:esp32s3-linkloop` 里的两处都已删掉）。
  ★ 而 `lib/link/link_phy_uart.cpp` 里那道**编译期闸门保留着**：判据换成 `LINK_UART_PORT == 0 && DASH_LOG_UART0`，
  谁要显式 `-DDASH_LOG_UART0=1` 把日志又放回 UART0，编译期当场报错。
  ★ `USB_MODE` / `CDC_ON_BOOT` **一个字都没动**（那是 2026-09-18 踩过的坑，见 `platformio.ini` 的串口那段）。

构建注意：
- ESP32 工具链（MinGW g++）不支持非 ASCII 构建路径：中文路径下报
  g++ "Invalid argument" / "Arduino.h: No such file or directory"
- 已验证：项目 + Core 放纯 ASCII 路径（C:\Users\Public\206dash）编译通过
- 本机 PlatformIO Core 装在工作区 .pio-pylibs（pip --target），用
  python -m platformio 调用，PLATFORMIO_CORE_DIR 指向 ASCII 路径的 .pio-core
- **`-I include` 不能省**（esp32dev 的 build_flags）：LVGL 找 lv_conf.h 靠的是
  包含路径（`LV_CONF_INCLUDE_SIMPLE` → `#include "lv_conf.h"`）。少了这一条，
  `include/lv_conf.h` 根本不会被读，一切走 LVGL 内部默认值 ——
  而且**不报错**：14 号字体恰好是 LVGL 的默认值，看起来像"生效了"，
  直到要开 18/48 号字体时才炸出 `lv_font_montserrat_48 was not declared`。
  加上之后 RAM 反而从 37.5% 降到 32.7%（lv_conf.h 里 LV_MEM_SIZE=48KB
  比 LVGL 默认的 64KB 小），Flash 从 51.3% 升到 62.6%（两套字体点阵）。
- **分区表 CSV 必须纯 ASCII**（`partitions.csv` / `partitions-s3.csv`）：
  PlatformIO 的 `builder/main.py` 用 `open(partitions_csv)` **不指定编码**读它，
  中文 Windows 上就是 GBK 解码 —— 注释里一个"★"（UTF-8 = E2 98 85）足以让
  构建在最后一步 `checkprogsize` 抛 UnicodeDecodeError。症状极具误导性：
  固件**已经编译并链接成功**（"Successfully created … image"），
  看着却像编译失败。BOM 也不行（第一个字段会读成空）。
  由 `tools/theme-editor/test-image-blob-build.js` 的一条守卫盯着。
- **四个构建目标**（2026-09-18 起）：
  | env | 板子 | 分区表 | 用途 |
  |---|---|---|---|
  | `esp32dev` | 经典 ESP32（4MB，无 PSRAM） | `partitions.csv` | 廉价回归；**没有** PSRAM/双屏 |
  | `esp32s3` | **ESP32-S3 N16R8**（16MB + 8MB OPI PSRAM） | `partitions-s3.csv` | **实车与真屏**；`-DVAN_PHY_GPIO=1` 开硬件收帧 |
  | `native` | 宿主机 | — | 单元测试（`pio test -e native`） |
  | `pcpreview` | 宿主机 | — | 渲染落帧做像素核对 |
  两份分区表的 theme/image **偏移相同**（0x210000 / 0x254000），
  所以 esptool 命令两块板通用；只有 image 的大小不同（1MB vs 8MB）。
  设备专属代码（`ESP.*`、`esp_partition.h`、GPIO 中断）一律用 `ARDUINO`
  判定圈起来 —— pcpreview 用的是宿主机桩，不定义 `ARDUINO`，
  少了这层判定预览构建会直接编不过（踩过）。
- **S3 目标上两件"少一个字就白干"的事**（2026-09-18 实测，详见 `ACCEPTANCE.md`）：
  · `-DBOARD_HAS_PSRAM` 必须有：`cores/esp32/esp32-hal-psram.h` 在缺这个宏时
    会 `#undef CONFIG_SPIRAM` —— **核心主动把 PSRAM 编掉**，
    现象是自检报 `psram : 0 KB`，而 `memory_type` 配得再对也没用。
  · **刷写/调试走 UART 口（板载 CH340）而不是原生 USB 口**：前者是真 UART0 +
    真 EN/IO0 复位线（刷完 app 真的会跑，ROM/bootloader/panic 日志全在），
    后者的复位是软复位请求，每次都把芯片留在 ROM 下载模式。
    上传要指定口：`--upload-port COM4`；抓完整开机日志用
    `python tools/serial-capture/capture.py COM4`。
- **日志是双通道的（★ 2026-09-23 起：链路构建里只有一通道）**：`lib/dashcore/dash_log.h` 的
  `dash_logf()` 一次格式化、USB-CDC 与 UART0 各写一遍。所以插哪个 USB 口都看得见日志；经典 ESP32 上
  `Serial` 与 `Serial0` 是同一个 UART0，按 `CDC_ON_BOOT` 判断只写一次。
  ★ **但带链路 PHY 的构建（`-DLINK_PHY_UART`，即显示/镜像两块板那种）里 UART0 那一路是关的**
  （`DASH_LOG_UART0=0`，因为 UART0 的 43/44 让给了板间链路）⇒ 那边**只有原生 USB 口**能看日志；
  VAN 采集那条路不带链路 PHY，**双通道照旧**。见本节「待办」第 1 条与 `dash_log.h` 的文件头。

测试（宿主机，不烧板）：
- test/test_dashcore/ 为 native 单元测试（Unity，当前 **注册 132 例 / 130 通过 / 0 失败 /
  2 跳过**，其中 2 例需环境变量否则跳过；★ **退出码不是判据**，看摘要里有没有 `[FAILED]`），
  覆盖 OBD 文本解析、OBD 状态机（假串口）、VAN 解析与钳制、VAN 线路层
  （4B5B/CRC-15/帧字节契约/空闲不入队）、**VanPhyWire 整链**（边沿→包→车速/转速）、
  15 位 IDEN 与回放语法、主题 JSON 解析与钳制（含数字读数那一段）、
  图片镜像解析（坏镜像必须被拒 + 头部字节布局钉死 + 角色编号与表情槽位对账）、
  多源回退、表情状态机（**每屏一套独立**：左看转速、右看车速、水温不参与 +
  超速只属于速度表 + 稳态与时间无关；原"急加速瞬态"那一档 2026-09-18 已删）、
  **表情阶段表**（16 条用例 = 转速 5 + 车速 5 + 水温 3 + 进气 3，
  逐条断言 face_update 对**左右两屏**的输出；
  检查降级链一定能退到常态；检查每组只变自己那一维、每屏只被自己那一路驱动 ——
  这两条都是用户试用时抓出来的：点"速度·中"时转速表跟着动过）
- **双板链路 v1 的 CRC-15 检错覆盖**（`test_link_crc_coverage.cpp`，5 条用例）：帧长
  12..23 B **逐帧长全量枚举** —— 1/2/3 位错与长度 2..15 bit 的突发**未检出都是 0**，
  16 bit 突发的漏检**只落在被 CRC 覆盖的数据区内**（每帧长 `8(L−2)−15`、合计 **1308**，
  图案恒 `0xB9F1`），**最小汉明距离 4**（完整数字与口径见 §8 的 L4）
- 跑法（纯 ASCII 路径下，env 变量同上）：
    python -m platformio test -e native
  ★ **进程退出码不是成败判据**：PlatformIO 的 native runner
  （`test/runners/readers/native.py`）**只看非零退出码**，而退出码本身还有个坑 ——
  Unity 拿"**失败条数**"当退出码，**2 在 Windows 上正好撞上 `signal.Signals(2)` = SIGINT**，
  于是"2 条用例红了"会被报成 `Program received signal SIGINT (Interrupt)`、环境标成
  `ERRORED`（修前那个越界崩溃是 exit 3，3 不是 Windows 信号，才报 `Program errored with
  3 code`）。**⇒ `ERRORED` 不等于"崩了"**：有没有红一律看摘要里的 `[FAILED]` 行与
  **注册 / 通过 / 失败 / 跳过**四项。
  ★ **那个越界崩溃已经修了**：`test_face_stages.cpp` 的 `panic: index 4 out of bounds for
  type 'Face[4]'`（进程 exit 3）由 `e97b13a` 修掉 —— `Face seen[4]` 的容量改成由本头文件的
  `kFaceStageCount` 推导（**不再写死档数**），断言一条没动；崩溃消失后注册在它后面的 4 条
  用例第一次真的跑起来，当场红了 2 条同族的"写死 4"，再由 `a75b94a` 修掉。现状：
  `132 Tests 0 Failures 2 Ignored`、环境 `PASSED`、退出码 0。
  ⇒ "新用例**必须注册在 `face_stages` 之前**"这条纪律**已无必要**（来龙去脉见
  `README.md` 的「编译 / 测试 / 刷机」；`-v` 才转发 `printf`、中文输出会打乱用例统计
  这两条**仍然有效**）。
- **网页端与固件的一致性**（四套 JS 镜像，Node 里跑）：
    node tools/theme-editor/test-face-stages.js     # 解析 face_stages.h 逐字段对账
    node tools/theme-editor/test-theme-json.js      # 主题文件的 0x 颜色能不能读回来
    node tools/theme-editor/test-gauge-geometry.js   # 表盘朝向 + 弧带半径语义(编辑器侧)
    node tools/theme-editor/test-image-blob-build.js
    node tools/theme-editor/syntax-check-pages.js   # 三个页面的内联脚本语法
  test-face-stages.js 的存在理由：表情导入页的"阶段模拟"是用户刷图前**唯一**
  能看到的证据；网页那份表和固件那份不一致，预览就是在骗人。
  test-theme-json.js 的存在理由：主题文件是**固件与编辑器共读**的同一个文件，
  固件接受 `0xRRGGBB` 而 `JSON.parse` 不接受 —— 少了这一层就是
  "固件读得进去、编辑器导入报语法错"（实测踩过）。
  test-gauge-geometry.js 的存在理由：LVGL 的 `lv_arc` 是 0°=3 点钟、顺时针，
  而 canvas 的 `arc()` 约定**完全一样**，所以角度不需要任何偏移；
  两个编辑器曾经都写成 `(d - 90)`，把预览里的表整块逆时针转了 90° ——
  固件是对的，但用户先看到预览，于是问"表的方向是不是要向右转 90 度"。
  这类"约定对不上"不报错、只是看着怪，只能靠断言钉住。
  同一条测试还管另外两件事：默认主题的**两条弧开口方向相反**（转速朝下、水温朝上），
  以及 `radius` 是**外沿**半径、预览必须画在 `radius - width/2`（固件实测
  radius 205/width 24 → 弧带 181..205；预览原来画在 193..217，整体外移 12 像素）。
  固件那一侧的朝向由 check-preview-frame.js 的「表盘朝向」检查兜着（读真实落帧）。
- **跨语言格式核对**（图片镜像由 JS 生成、C 读取，编译期看不出不一致）：
    pwsh tools/theme-editor/test-image-roundtrip.ps1
  它用 JS 打一个镜像并生成可读的 manifest，再让固件解析器读同一个文件、
  逐字段逐字节对账（C 侧用例见 test_image_roundtrip.cpp，未设环境变量时自动跳过）。
  这个核对实测抓出过四个真 bug（清单见 `ACCEPTANCE.md`），都是"编译通过、肉眼看不出来"的类型。
- **像素级验收**（真屏到货前唯一的"证据"）：
    # ASCII 路径下，先造测试图，再带 IMAGE_BLOB 跑预览
    node tools/theme-editor/make-test-blob.js <ascii>\test-image.bin
    $env:IMAGE_BLOB='<ascii>\test-image.bin'; .\.pio\build\pcpreview\program.exe
    node tools/theme-editor/check-preview-frame.js preview\frames\l_0140.bmp redline yes left yes
    node tools/theme-editor/check-preview-frame.js preview\frames\r_0140.bmp idle yes right yes
  （帧号别随手改：假数据是波形，先扫一遍两屏表情序列再挑**平台段**的帧 ——
   140 帧两侧都稳定，100 帧左屏已经是 cruise 了。）
  check-preview-frame.js 除了背景/弧/表情图层与透明通道，还核对**读数**：
  数字/单位/水温三处墨迹的**外接框**（能区分"画的是数字"和"忘了清空的
  LVGL 默认文本 Text"）、右屏不该有水温数字、开机期间数字栏必须为空。
- 本机没有宿主机 gcc：装了便携 zig（pip ziglang，见 .tools\pyzig），
  并用 .tools\zigbin 里的 gcc/g++/cc/c++ 转发桩调用 zig cc/c++
  （源码 .tools\zigwrap.c）。跑测试前把 zigbin 加进 PATH。
  有真 gcc 的机器不需要转发桩，直接跑 pio test 即可。
- 测试用 Arduino 最小桩在 test/arduino_shim/，只进测试编译，不进固件
- 接线预案见 PINOUT.md（ESP32-S3 双 SPI 屏 + OBD UART1 + VAN UART2）；
  **OBD 座上没有 VAN**，车速要从组合仪表（首选）/ 收音机 / MFD 取，
  极性问题尚未实测确认 —— 见 PINOUT.md「接线：三处取信号」

## 显示约定：**不是实测数据就必须看得出来**（2026-09-24）

### 0. 产品要求（这一节的根，下面每一条都是它的落地）

> **当仪表显示的不是实测数据时，必须在屏上让驾驶员看得出来。** ✗

**为什么要有这一条**：VAN 断流/数据冻结时，数据层会**按既有规则回退到 Sim**
（`data_service.cpp` 的 `kStaleMs = 3000`）⇒ 指针照样动、数字照样跳，
但那是**假数据**。驾驶员看到一块"一切正常"的表，而这块表在撒谎 ——
比黑屏更危险：黑屏他知道表坏了，假数据他以为车就是这样。

★ 这一条与 §3/§8 已定的裁决**不冲突**（见 §4 的 L11 合规说明），
也不要读成"要做一个故障页"：它是**一枚小角标 + 一声轻提示**，明确但不刺眼。

### 2. 显示分层（两类信息，别混）

| 层 | 回答的问题 | 数据来源 | 形态 | 现在有什么 |
|---|---|---|---|---|
| **车况灯** | "车现在是什么状态" | 全部来自 VAN（4 类已解字段） | 表盘底部六格灯（`lamp_view.h`） | 左右转/双闪/近光/仪表盘灯/门 ✓ |
| **系统状态** | "**这块表本身可不可信**" | 四个触发条件（全部既有信息） | ① 表盘右缘小角标 ② 诊断页 | 本轮新增 ✓ |

### 1.5 表盘的**图层顺序**（2026-09-24 车主定稿，**显示约定之一**）

**约定（从下到上，五层）**：

| # | 这一层 | 谁建的（`src/dash_ui.cpp` 的 `dash_ui_init()`） |
|---|---|---|
| 1 | **背景图** | `g_bg_img`（第一个子对象 ⇒ 最底） |
| 2 | **表情**（程序化形状 + **表情图片**） | `build_face()` + `g_face_img` |
| 3 | **表盘弧**（主弧 + 两条副弧） | `build_arcs()` |
| 4 | 六格指示灯 | `build_lamps()` |
| 5 | **读数**（大数字 / 单位 / 水温 / 进气） | `build_readout()`（**最高层**） |

**为什么是这个顺序**（两条都出自车主原话）：

* **"表盘弧应该永远是最上层"** ⇒ 表情在弧**之下**。弧是仪表的"骨架"：
  它一旦被表情图咬掉一块，读数就失去了参照（弧是刻度本身）。
  ★ **代价（设计如此，不是 bug）**：表情图是**方块画布**，四角伸到半径 226，
  而主弧带在半径 181..205 ⇒ **四角会被弧裁掉**。这一条同步写在
  `tools/theme-editor/asset-spec.js`、`image-editor.html` 的页面说明与
  `tools/theme-editor/README.md` 的「图层顺序」一节里，
  **表情的尺寸上限（480 档 320 / 240 档 160）一个数都没放宽**。
* **"读数应该是最高层的"** ⇒ 读数（与它下面的六格灯）在弧**之上**。
  实测证据：大数字墨迹顶在 y=55，**本来就蹭进弧带内沿 4 像素**，
  而它读出来是读数色（白 `#ffffff`）而不是弧色 —— 见 `ACCEPTANCE.md`
  当天的「弧像素 / 读数像素」前后采样值。

**★ 落地纪律（改层序前先读这一条）**：

1. LVGL 里**创建顺序 = 绘制顺序**（后建的在上面）⇒ 层序**不是**某处的
   一个列表，而是 `dash_ui_init()` 里那几段的**先后**。
   ★ 一个实测踩到的坑：只把 `build_face()` 挪到 `build_arcs()` 前面
   **不够** —— **图片表情 `g_face_img` 是另一个循环里建的**，
   它仍然落在弧之上（落帧上表现为"弧被咬了四个缺口"，而改完一个像素都没变）。
   ⇒ **两个表情对象都要在 `build_arcs()` 之前**。
2. **预览是这一条的把关人**：`tools/theme-editor/image-editor.html` 的
   `drawScreen()` 必须按**同一顺序**合成（背景 → 表情 → 弧 → 读数）。
   两处不一致 ⇒ 预览不再能代表真机。
3. 判据是**像素**（不是"看代码里谁先建"）：
   `tools/theme-editor/check-layer-order.js`（单帧）与
   `tools/theme-editor/check-layer-ab.js`（改前/改后逐点对比）。

### 1.6 ★★ 「角色 ↔ 哪一块屏」—— 一条**以前只活在旁白里**的产品口径（2026-09-25 补）

**先把一句话写死**（这就是本节的全部内容，代码里的唯一出处是
`lib/dashcore/dash_role_layout.h`）：

| 角色（契约 §5） | 编译期 | 该显示哪一屏 | 主表 | 副表 | 开机标签 |
|---|---|---|---|---|---|
| **主板** = 右侧 | `LINK_ROLE == 1` | `Theme::screens[1]` | **车速**（`ArcKind::Speed`，蓝 `0x39C5FF`） | 进气温度（`ArcKind::Intake`，琥珀 `0xFFB020`） | `MASTER (RIGHT)` |
| **从板** = 左侧 | `LINK_ROLE == 0` | `Theme::screens[0]` | **转速**（`ArcKind::Rpm`，红 `0xFF5C5C`） | 水温（`ArcKind::Coolant`，绿 `0x7CFF6B`） | `SLAVE (LEFT)` |

**为什么值得单独一节**：这条对应关系**之前没有任何一行判据、也没有任何一行文档** ——
它只"活"在两个人的记忆与两处旁白里：
  · 开机标签的**文案**（`src/dash_ui.cpp` 的 `build_role_label()`：
    `MASTER (RIGHT)` / `SLAVE (LEFT)`）；
  · `src/ui_model.h` 的一句注释（"左屏（转速表）只看转速，右屏（速度表）只看车速"）。
⇒ 后果在 2026-09-25 **真的发生了**（车主原话）："**副表怎么也给你刷成速度表了**" ——
新板（`COM8`，刷的是 `esp32s3-rgb-slave`）上电显示的是**速度表**，而它该显示转速表。
**没有任何东西报错**：屏上有画面、弧在动、数字在跳 —— 只是**是另一块表**。

**为什么它会错（机制，值得记住）**：本轮的 RGB 驱动是**单屏**版本 ——
`g_left` 与 `g_right` 指向**同一个** `lv_display_t`（`src/dash_display_rgb.cpp`
的 `g_left = d0; g_right = d0;`，第二块屏到货后才分叉）。两块"屏"其实是同一块玻璃上的
两个 LVGL screen 对象，而**谁最后被 `lv_screen_load()` ⇒ 谁在上面**。
修之前 `dash_ui_init()` 是"第 s 屏用 `kScreens[s]`"⇒ **末屏恒为 `screens[1]`（速度表）**，
两块板看到的都是速度表。

**判据（落地）**：

1. **代码**：`lib/dashcore/dash_role_layout.h` 给出 `roleThemeIndex()` /
   `themeIndexForScreen(s)` / `faceIndexForScreen(s)`；`src/dash_ui.cpp` 里
   **四处**（建弧 / 建读数 / 扫表 / 渲染）一律走它 ⇒ 表盘与**表情/图片角色**
   （`face_stages.h` 的 `kFaceRoleId[screen][slot]`）**共用同一个下标**，
   不可能出现"转速表的弧配速度表的脸"。
   ★ 它**没有**改 `LINK_ROLE` 的语义（契约 §5 一行未动）、没有改链路协议、
   没有改面板驱动、也没有改两个 `lv_display_t` 的创建顺序。
2. **串口自证**（真机上唯一能看出"跑的是哪块表"的两行）：
   `layout: role=SLAVE last=LEFT/rpm+coolant slots=[RIGHT/speed+intake | LEFT/rpm+coolant]`
   与每秒那行尾部 `... face=… | role=SLAVE last=LEFT/rpm+coolant`。
   `last=` = 今天**唯一可见**的那一屏（末屏）。
3. **像素判据**（不靠人眼、也不靠日志）：
   `node tools/theme-editor/check-gauge-identity.js <左屏帧.bmp> <右屏帧.bmp>` ——
   两块表的主弧**颜色本来就不同**（红 vs 蓝），所以"哪一帧是转速表"是**数出来的**。
   实测（pcpreview，同一份代码、只把 `LINK_ROLE` 换成 0/1）：
   两条弧**互换了那一屏**（`r_*`：蓝 18005 → 红 20613）⇒ `GAUGE-IDENTITY-OK`。
4. ★ **反向判据**（防"两块屏是同一套"那个 bug 复发）：native 用例
   `test_role_layout_other_screen_is_complementary` 要求两屏**必须是不同的两套表**
   （`a != b` 且 `a + b == 1`）。

**边界（写清楚，别被读成"双屏已经成了"）**：今天两块玻璃上仍是**同一份画面**
（单屏驱动）⇒ 本节保证的是"**每一块板上、那一块屏显示的是这一角色该显示的那一屏**"；
第二块屏到货、驱动分叉之后，`slots=[…]` 里那两套表**各自都在**，映射不用再改。

#### 1.6.1 ★★ 表情**图片**的分组也必须走同一个下标（2026-09-26 补 —— 上一版漏掉的那一半）

**车主第二次实测的原话**（2026-09-26）：

> "**转速表的表情好像跟转速失去关联了，现在表情变化怎么感觉是随机变动的。**"

**含义**：副板（= 转速表那一屏）的**弧和读数已经跟着转速**，可**表情状态**还在按
**另一屏（车速）**的数据挑 ⇒ 他盯着转速看 ⇒ 表情像随机变（台上是模拟数据，车速本来就在乱扫）。

**机制（为什么 §1.6 那次只修了一半）**：`dash_role_layout.h` 把"**表盘**"那一半收进来了
（弧 / 读数 / 扫表 / 渲染），可"**表情图片**"那一半还拿 `faceIndexForScreen(s)` 的返回值
当**屏号**去索引两张按**屏槽位**排的表（`g_face_ok[2][…]` / `g_face_dsc[2][…]`，
它们在 `dash_ui_init()` 里按 `s` 填）。于是从板上那两屏的"**表号**"与"**屏号**"正好对调：

| | 从板末屏（`s=1`，今天唯一可见的那一屏） | 主板 |
|---|---|---|
| 表盘（`themeIndexForScreen`） | `screens[0]` = 左/**转速表** ✓ | `screens[1]` ✓ |
| 表情**数据源**（`v.face_left/right`） | `face_left`（只看转速）✓ | `face_right` ✓ |
| 表情**图片分组**（修之前） | **0 号 = 右/车速那 5 张** ✗ | 恰好 `s == ti` ⇒ 一直是对的 |

⇒ 车速在模拟里乱扫，而"脸"按车速的档位取图 ⇒ 车主看到的就是"跟转速没关系、随机变"。
★ **主板一直看着是对的**，所以这个 bug **只在从板上现形** —— 这正是它躲过一次修复的原因。

**判据（现在有三条机器判据 + 一条人眼判据）**：

1. **代码**：`lib/dashcore/dash_role_layout.h` 里 **表盘 / 表情槽位 / 表情图片 / 表情数据轴**
   四个下标写成**一处**式子：`themeIndexForScreen(s)` =
   `faceGaugeIndexForScreen(s)` = `faceImageIndexForScreen(s)`，
   而"这一屏的表情看哪条轴"由 `gaugeKindForScreen(s)`（`kGaugeRpm` / `kGaugeSpeed`）给出。
   `src/dash_ui.cpp` 里 `ScreenUi::face_group`（原 `ScreenUi::idx`）**只由它赋值**，
   `face_apply_image()` 也**不再**接受"屏号当组号"的参数。
2. **native 反向判据**：`test_role_layout_image_group_follows_gauge`（三名字恒等）、
   `test_role_layout_face_axis_follows_gauge`（轴与表同源、两屏轴必须不同）、
   `test_role_layout_redline_axis_is_the_rpm_one`（红区只可能落在按转速那条轴上）。
   ★ 这三条红了就是"弧看 A、脸看 B"复发。
3. **★ 像素判据（用车主自己的素材）**：
   `node tools/theme-editor/check-face-image.js <image.bin> <帧.bmp> <left|right>` ——
   把 image.bin 里那 9 张图逐个**按 alpha 合成**到帧的底色上、与帧逐点比，
   谁的差最小就是"屏上那张脸"；再断言它属于**期望的那一组**。
   ★ 与 `check-gauge-identity.js`（弧的**颜色**判据）分工不同：那一个判"哪块表"，
   这一个判"哪张脸" —— 车主报的两次 bug 正好各需要一条。
4. **人眼**：给车主的一句问话（"把转速推到红区 ⇒ 那张脸应该是红区的"）。

**★ 一条可判定的规则（车主能自己验，也是本节的验收话）**：
**转速表那一屏上，转速进红区时表情必须是红区那张脸**（`expression.cpp` 的红区阈值 5800）。
反过来这条更能一眼分开"跟转速 ✓ / 跟车速 ✗"：
**车速为零、转速在红区时，副板那张脸仍应是红区脸**（屏上/预览里把 `rpm` 推到 6200、
`speed` 留 0 就能造出来）。

### 2. 「数据不可信」提示

**触发条件（四个，全部用既有可得信息，一个新数据源都没有）**

| # | 条件 | 判据 | 出处 |
|---|---|---|---|
| ① | **数据来源回退到 `Sim`** | 车速/转速/水温/进气四格里有**任何一格**的来源是 `FieldSource::Sim` | `data_service` 已有的来源档位（`DataSourceStatus`） |
| ② | **VAN 断流** | 见过帧、但最近一帧的年龄 **> 3000 ms** | `VanSource` 的帧时间戳；3000 ms **刻意与数据层的 `kStaleMs` 同口径** |
| ③ | **数据冻结** | 帧还在来（年龄 ≤ 3000 ms）**且"发动机在转 / 车在动"**（`rpm > 500` 或 `speed > 0`），但车速与转速**连续 20000 ms 一个字节都没变** | 既有快照的数值（`VehicleState`） |
| ④ | **双板链路降到最低档** | `LinkTime` 的状态 = `SimFallback`（**仅从板**；主板没有 LinkTime） | `lib/link/link_time.h` 的三档 |

★ **优先级 = 上表顺序**（严重程度降序）：断流 > 来源回退 > 冻结 > 链路回退。
同时命中多条时**只显示最重的那一条**（屏上只有一枚角标；几条一起报等于都没报）。
★ 条件 ③ 只在"帧还在来"时判 —— 否则断流 20 秒会同时报成"冻结"，
屏上多一条无意义的理由（两个条件各自独立，见 `system_status.cpp` 的 `evaluate()`）。
★ 条件 ③ 有一个**前提**（2026-09-24 追加，消掉一类**假警报**）：
「值不变」只有在**发动机在转 / 车在动**时才可疑 —— 判据是
`rpm > kTrustEngineRunningRpm(500) || speed_kmh > 0`。
理由：**熄火通电在车里听歌**时车速恒 0、转速恒 0，这两个数**本来就该不动**；
没有前提的版本会在 20 秒后挂一个"数据不可信"，那是在冤枉一块好表
（假警报多了，真的那一次就没人看了）。
- `rpm` 那半条是**严格大于**：恰好 500 rpm **不算**"在转"（边界由
  `test_system_status.cpp` 的 `test_sys_freeze_rpm_boundary_500` 钉住）。
- **为什么还要车速那半条**：万一 VAN 冻在"转速读 0 但车其实在跑"那一帧，
  只看 `rpm` 会把**真故障**漏掉 ⇒ 车在动的时候，静止的数据一律可疑。
- ★★ **不含水温/进气** ✗：那两格 20 秒不变是**正常**的（热惯量那么大，
  一分钟不动都正常），把它们算进来判据会一直成立、角标常挂 = 又一个假警报。
★ 条件 ③ 的**代价**（**写出来，别当 bug 查**）：**停车且熄火时的真实冻结不再报**。
可以接受，三条理由：① 那种场合（熄火听歌、车库停着）显示通常也是关着的；
② 另外两条网照旧兜着 —— **①来源回退 `Sim`** 与 **②VAN 断流**：真坏了
（帧不来 / 数据层回落 Sim）照样报，只是"值冻住"这一条不再**单独**触发；
③ 反过来（熄火停车时挂一个"不可信"）是**必然发生**的假警报，比漏报更伤。
真车标定时若仍以"车主看着别扭"为准，先调 `kTrustFreezeWindowMs`（窗口），
不要去掉这个前提（去掉就是把上面那条假警报请回来）。

**形态（明确但不刺眼）**

- **一枚小角标**：表盘**右缘**固定位置（480 基准：x=340..456、y=205..241），
  内容是按理由给的三字母标记：`SIM`（来源回退）/ `VAN?`（断流）/
  `STALE`（冻结）/ `LINK`（链路回退）。
- **不是红屏、不是每秒闪**：边框颜色只用两档 —— 「来源/链路回退」灰蓝（信息级）、
  「断流/冻结」琥珀（要人看一眼）。**刻意不用红色**：红是"故障"的语气，
  而"数据不可信"是"这块表现在在演"，两者不是一回事。
- **一声轻提示**（每次"变坏"只响一次，见下）：走**既有 `Buzzer` 抽象**，
  时长 `kTrustBeepMs = 120 ms`，模式 `BeepPattern::Short`。
  ★ **不许做成长鸣**：2.8C 那颗蜂鸣器是**有源**的（只能开/关、不能变调，
  见「提示音说明」），且"持续高电平 2 秒把 3.3V 轨拉低 ⇒ 面板掉状态"
  这条机制尚未排除 ⇒ **单次哔 ≤ 300 ms**（`static_assert` 钉着）。
  ★ **可静音**，且静音开关**优先于提示音**（车主的选择 > 这一声提示）。

**去抖与限速参数（就是文档里这一组数，代码里同名的常量是唯一来源）**

| 常量 | 值 | 含义 / 为什么是这个数 |
|---|---|---|
| `kTrustSysDebounceMs` | **1500 ms** | 判"不可信"：条件**连续**成立这么久才认。1.5 s 能吃掉"偶发丢一帧"与贴帧的间隙，真断流时又足够快（人还没发现指针不动，角标先出来） |
| `kTrustSysRecoverMs` | **800 ms** | 判"恢复"：连续成立这么久就撤掉。★ **比变坏快**是刻意的 —— 数据回来了还挂着"不可信"是**反向的谎** |
| `kTrustFreezeWindowMs` | **20000 ms** | 数据冻结窗口（见上面的已知边界） |
| `kTrustEngineRunningRpm` | **500 rpm** | ★ 冻结判据的**前提**（2026-09-24 追加）：`rpm` **严格大于**它就算"发动机在转"（恰好 500 **不算**）。怠速约 700~900 ⇒ 500 以下一律当成"不在转"。★ 前提的另一半是 `speed_kmh > 0`（车在动），两半是**或**的关系 |
| `kTrustVanStaleMs` | **3000 ms** | VAN 断流阈值（与数据层 `kStaleMs` 同口径） |
| `kTrustBeepMs` | **120 ms** | 那一声轻提示的标称时长（≤ 300 ms 硬上限） |
| `kTrustBeepMinIntervalMs` | **5000 ms** | 两次"变坏"提示的最小间隔（数据在阈值上下抖时不会变成滴滴叫） |

**可恢复**：数据恢复后角标**自动消失**（`kTrustSysRecoverMs` 之后），
不需要任何人工操作；下一次真的再坏时**重新走完整的去抖窗口**、
并且**重新可以响一声**（受上面那条 5 秒限速约束）。

### 3. 诊断页

**入口**：**平时不显示**（整块容器建好就是 `HIDDEN`），只由**长按/组合键**唤出。

| 环境 | 入口 | 说明 |
|---|---|---|
| `pcpreview` | 键 **`K`**（或控制文件 `diag=1`） | 一个键干三件事：关着 → 打开第 1 页；开着且还有下一页 → 翻页；开着且在最后一页 → **关闭**（回到表盘）。退出也可以按 `X`/`Esc`（与"全部复位"同一个键） |
| **真机** | 串口单字符 **`d`**（2026-09-24 落地 ✓） | **这块板上没有可用的按键**（只剩 `GPIO0` = BOOT strap 与 `GPIO7` = I2C 的 SCL）⇒ 入口走**串口**：Type-C（板载 CH343P = `Serial0`/UART0 = `COMx`）本来就是看日志要接的那根线，**零额外引脚**。`d` 执行的**与预览 `K` 是同一个循环**：关着 → 打开第 1 页；还有下一页 → 翻页；在最后一页 → 关闭。**收到即回一行确认日志**：`diag: open page=1/2` / `diag: closed page=2/2`（与预览那一行逐字同格式） |
| **真机** | 串口单字符 **`m`**（2026-09-24 落地 ✓） | **静音开关取反 + 写 NVS**（掉电保存）⇒ 车主**不用按键**就能静音。回执 `mute: 1 (saved)`；复位后那行 `mute=1` 就是"掉电保存成立"的证据。诊断页第 1 页最后一行也是 `mute=`（**看**状态用） |
| **真机的编译边界** | 上面两条命令**只编进 `[env:esp32s3-rgb]`** | 门用的是**既有的** `DASH_DISPLAY_RGB` 宏（platformio.ini 里早就有、且只有那一条 env 定义）⇒ `[env:esp32s3]`/`[env:esp32dev]`（**VAN 抓帧盒**）与 `-vaninv`/`-vansniff` 的编译单元里**这两段代码一行都不存在**，行为逐字节不变 ✓（`platformio.ini` **没有新增任何 `-D`**）。★ 命令**只认"行首"**（这个口上当前没有未完成的一行）：回放行里的十六进制**本来就可能含 `d`**（例 `VAN 824 18F8271D000000`）⇒ 不这么判会悄悄吃掉回放数据。**唯一的例外**是"缓冲里那串字节不可能是回放行"（首字节不是 `V`/`v` —— 回放行的头三个字符必须是 `VAN`）：那多半是串口线上一颗杂散字节，这时也认命令（并把它清掉），否则那颗字节会把命令通道堵到下一个换行为止（上板用一颗 `x` 验过） |

**内容（全部取本地现有信息，零新数据源）** —— 两页：

| 页 | 行 | 数据从哪来（既有出处） |
|---|---|---|
| **1/2 数据这一路** | 车速 / 转速读数、四格的来源短名（`none`/`sim`/`obd`/`van`/`link`） | `DataSourceStatus`（与串口 `SRC …` 那一行同源） |
| | `van frames=` / `fcs=` / `edges=` / `age=` | 帧计数＝`main.cpp` 的 sink 计数；`age` 与 `SRC-VAN age` 那一行同源 |
| | `heap=` / `psram=` | 与示位标 `BEACON` 那行同源 |
| | `ui=` fps | 渲染节流（200 ms 档）的实测帧率 —— 它回答"这条节流有没有被卡住" |
| | `panel=` fps / `flush=` / `copy=` | 面板帧率与搬运耗时。★ **本轮 RGB 驱动还没有这个出口** ⇒ 显示 `panel=- (driver n/a)`（不是 0！0 会被读成"一帧都没刷"） |
| | `mute=` | 静音开关的当前状态（也是它的操作入口） |
| **2/2 链路与告警** | `link state=` / `age=` / `ticks=` / `gap=` / `miss=` / `offset=` | **从板**自己的 `LinkTime`（与串口 `link: …` 那一行同源） |
| | `obd rpm/cool/speed/intake` Hz、`010D=` 位图结论 | `DataSourceStatus` 的 `obd_*`（与 `SRC-Hz` / `obd: ECU 位图` 那两行同源）；**没接就显示 `obd n/a (no ELM327)`** |
| | `alert=` / `mute=` / `mask=` | 告警回执、静音开关、圆屏遮罩的像素数 |

★ **未知/没有的数据一律显示 `-` 或 `n/a`，绝不显示 0**：0 是一个**有意义的值**
（"现在就是 0"），把它当"没有数据"用，到车上会被读成"这一路是好的、只是恰好为 0"。

#### 诊断页的**可读性**约定（2026-09-24 晚，车主报"屏幕为什么黑了"之后定）

**背景**：车主发 `d` 打开诊断页后报"**整屏看起来是黑的**"。实测根因是那一版
的底是 **`0x0A0A0A` 铺满整屏**（`LV_OPA_COVER`）—— 圆屏上没有任何参照物，
于是只能读成"黑了"。落帧实测（`pcpreview`）：页区域平均亮度 **14.8**、
**95.3%** 的采样点亮度 < 20（正是历史上判黑的那个"96% 采样点 < 20"）。

**四条约定（都是硬要求）**：

| # | 约定 | 为什么 |
|---|---|---|
| ① | 底色**不铺满**：改成**内缩的圆角面板**（480 基准内缩 32、圆角 24），底色 `0x16212E` | 四周露出表盘底色 ⇒ 一眼看出"这是一页盖在表盘上的界面"，而不是"屏坏了/板子死了" |
| ② | **必须有可见描边**（琥珀 `0xFFB020`、2px） | **失败也要看得见**：哪怕一个字都没画出来，人也该看出"这里有一页界面" |
| ③ | 标题与页码用**大数字那一档**字体（480 档 48 号） | 车主追加："标题与页码尤其要显眼" |
| ④ | 诊断页的文本**一律 ASCII** | 本构建**只使能 Montserrat**（`include/lv_conf.h` 的 `LV_FONT_MONTSERRAT_*`）⇒ **没有 CJK 字形**。中文在这里不是"看不清"，是**一个字形都画不出来**（`dataTrustReasonText()` 那条中文短文因此**没有**用在屏上）。要中文先引入 CJK 字体（`source_han_sans_sc_16_cjk`，占 Flash）——**独立一单** |

★ **每个标签的尺寸必须显式写死**（绝不让 LVGL 自算）：第一版没给多行标签设尺寸
⇒ 自算高度把整屏容器撑出屏幕 ⇒ 一次整屏重绘把它自己盖满 ⇒ **整屏全黑**。

★ **几何自证日志**（打开时打**一行**，只打一次）——"页面上什么都没有"从此
能从串口一眼判出来，不必靠人眼猜：

```
diag: geom box=416x416 at 32,32 title=384x56 at 58,50 body=368x320 at 58,114 font title=m48/lh52 body=m18/lh21 page=1/2
```

（`box`/`title`/`body` 都是 **LVGL 里对象的实际宽高与左上角坐标**；`font` 是
**哪一档 + 行高** —— ★ 打的是这两个，**不是** `lv_font_t::name`：LVGL 9 把
实例字段留在私有头里，公开 API 拿不到字体名。）

★ **每次打开都从第 1 页开始**（车主："关闭后重开停在第 2 页这个别扭一并改掉"）：
复位写在**打开的那一次**（`dash_ui_diag_toggle()` 里），于是"关→开"与
"开机后第一次开"走同一条路径、只有一处判据。**预览的 `K` 与真机的 `d` 语义
保持一致**（两处都是那一个循环：
`main.cpp` 的 `diag_key_press()` 与 `loop()` 里的预览分支）。

**★ L11 合规说明（写清楚，免得以后有人以为违反了裁决）**

- §8 的 **L11** 定的是：主板「从板离线」**角标** v1 **不做** —— 从板在线状态
  **只进 `STATUS` 日志、不上屏**（owner 裁决，2026-09-22）。
- **诊断页不是常驻角标** ⇒ **不违反 L11**：它平时**完全不显示**，
  只由长按/组合键**主动唤出**，退出即消失。L11 禁的是"让从板在线状态
  **常驻**在表盘上"；诊断页是**主动查询**式的 —— 与"插上 USB-C 看串口"
  是同一类行为，只是换了个出口（串口那一行照旧存在，一个字节都没少）。
- 同理，**L13**（`STATUS` 超时门限 30 s、只用于日志）**没有被动过**：
  诊断页第 2 页读的是**从板自己的 `LinkTime` 状态**，**不是**主板对
  "从板在不在线"的判断；主板上那一格明文写着 `link - (master: log only)`。
- ⇒ **本轮没有新开 L 号、也没有改动任何既有 L 裁决**（L11/L13 一个字没动）。

### 3.5 ★ 仪表盘必须常亮（2026-09-24 晚，**产品要求**，与 §0 同一个地位）

> **这块屏是仪表盘，要常亮。** —— ✗
>
> 原话（业主，2026-09-24 晚，屏从厂里回来、准备上实车时）：
> "屏幕回来了，**上实车的时候可不能这样，这毕竟是仪表盘，要常亮的**。"

**为什么它是"产品要求"而不是"观感意见"**：屏幕黑掉**不是外观问题，是功能性失效** ——
车速、转速、告警、指示灯**全部看不到**。而这一条与 §0（"不是实测数据就必须看得出来"）
是**同一类**要求：§0 管的是"表在撒谎"，这一条管的是"表根本不在说话"。两条都写在
"上实车之前必须成立"的那一档里。

#### 3.5.1 触发它的事故（**2026-09-24 当晚，两次，都是同一类**）

| 那次 | 现象 | 当时的串口（复位前抓到的原文） | 怎么恢复的 |
|---|---|---|---|
| **① 自检当晚**（`docs/RGB-PANEL-2.8C.md` §13.8） | 车主报"屏幕突然黑掉" | `rgb: vsync=5900(+54/s) … timeout=0 …` + `206 dash ok` 每秒一行 ⇒ **驱动全正常** | 复位一次（面板重跑 41 步初始化） |
| **② 吃完饭回来**（`ACCEPTANCE.md` 本轮） | 车主回来发现**屏黑了、固件一直活着** | `rgb: vsync=68942(+54/s) … timeout=0 … fullrb=0/s` + 每秒一行 `206 dash ok`；`theme: 已加载`、`image ok: 9 张` 都在 | RTS 脉冲复位 ⇒ 立刻恢复（`vsync 26 → 349`） |

★★ **两次的签名完全一样**，而它把"黑屏"与"卡死"彻底分开：
**主循环卡死 ⇒ 屏上定格**（面板还在扫那块 framebuffer）**不会变黑**；
**变黑 = 面板丢了初始化（ST7701 要重跑 41 步）或背光没了**，而驱动计数、日志、
每秒心跳**一切正常**。⇒ 从此**不许**拿"黑屏"当"主循环被阻塞"的证据。

#### 3.5.2 三层防线（**这一节的全部内容就是这三层**）

| 层 | 防的是什么 | 落地在哪 | 现状 |
|---|---|---|---|
| **① 根因消除** | 我们自己**新引入**的诱因 | 运行期**不改共享 I2C 时钟**（那条总线上挂着 `LCD_RST`(EXIO1)/`LCD_CS`(EXIO3)）⇒ 候选机制 A **已删除**；真机**不许长鸣**（单次哔 ≤300ms）⇒ 不制造长时间大电流 | ✓ 已做 |
| **② 可检测故障 ⇒ 自愈** | 那颗扩展器的输出寄存器**被意外改写**（面板复位位就在里面）／背光占空被改 | `lib/dashcore/panel_guard.{h,cpp}`（判据，宿主机可测）+ `src/dash_display_rgb.cpp` 的四个注入回调。**稳态每 2 秒**回读对账；**一旦这一拍读到坏值（或读不到）⇒ 下一拍提前到 200 ms**，连续 3 个 200ms 窗口干净才回稳态（"修完之后接着盯"那段窗口从 2 秒压到 200ms）。不一致 ⇒ **按影子重写**；连续 3 次**读到坏值** ⇒ **自动重跑一次**初始化（有 60s 冷却期，**不是**周期性重跑；★ 读失败**不算**坏值，只让它多看两眼） | ✓ 已做，**②③ 都有真机证据**（发现耗时 17ms、复检间隔 190/201/200ms、`anomaly x3 -> auto reinit` 都实测到） |
| **③ 不可检测故障 ⇒ 现场恢复路径 + 取证** | ② **看不见**的那些（供电跌落导致面板丢状态、背光坏、别的东西干扰总线） | 串口命令 **`r`** = 重跑面板初始化 + 重发当前画面（**不用按物理 RST、不重启 MCU**，实测阻塞 208ms）；开机一行 **复位原因 + 复位累计次数**（`boot: reason=… n=…`）—— `BROWNOUT` 就是候选机制 B 的判据；★★ **2026-09-25 补：把"上一次"也留档** —— `prev=`（上一次的复位原因）+ `prev_up=`（**上一次运行了多久**）+ 守护四个计数的**跨重启累计**（`lib/dashcore/boot_persist.{h,cpp}`，见下面 3.5.5） | ✓ 已做，上板实测 |

★★ **②③ 的边界（不许过度承诺，写在最显眼处）**：**"玻璃到底黑了没有"软件无法直接感知**
—— ST7701 **没有状态回读脚**。第 ② 层查的是**因**（那个复位位被改写）与**另一条独立路径**
（背光占空），不是**果**（玻璃）。所以"屏黑了"这件事本身**永远需要一个外部判据**
（人眼 / 摄像头 / 车主在屏前）；软件能做的只有：把**最可能的那条因**变成可检测、
把**现场恢复**变成一条命令、把**"是不是电压掉了"**变成一行可查的记录。

#### 3.5.5 ★ 复位原因与"上一段运行时长"的持久化（2026-09-25，**这一节解决的是"事后能不能定性"**）

**先问一句：这一条防的是什么？** 不是防黑屏（那是 ①②③ 的事），而是防**"出过事之后
说不清是哪一种"** —— 这是本工程真踩过的一个坑，逐字记在案：

> **悬案**：累计启动次数在夜里从 **31 涨到 32**（多了一次），而打开串口只看到
> **当前**这次开机的 `boot: reason=POWERON (raw=1)`。**上一次的 reason 没有留档**
> ⇒ "那次到底是 USB 被断电（`POWERON`）还是板子自己掉电（`BROWNOUT`）"**判不出来**。

⇒ 三样东西进 NVS（命名空间 `dash`），开机那一行一起打：

| 留档 | 它回答什么 | 怎么来的 |
|---|---|---|
| **上一次的 `reason`**（名字 + 原始值） | 上一段运行是**怎么结束**的 —— `prev=POWERON` = 人插拔电/正常断电；**`prev=BROWNOUT` = 电压跌落**（= 候选机制 B 的判据）；`prev=PANIC`/`WDT` = 崩了 | 每次开机把**本次**的 reason 写进 NVS，于是下一次开机读到的就是"上一次" |
| **上一次运行了多久**（`prev_up=`） | ★★ **"长跑后断电"（正常）与"几分钟就重启"（异常）** | ★ **周期性心跳**：每 **10 分钟**把当前 uptime 写一次 NVS（掉电那一刻没人来得及记任何东西 ⇒ 只能运行期不断写） |
| **守护四个计数的累计**（`rd/fix/bl/anom`） | "某次夜里守护救过几回" —— 原来**一重启就归零**，正好在最需要它的时候丢掉 | 开机读回"上次为止的累计"当基线，搭心跳的车定期落盘；诊断页**同时显示本次与累计** |

**判据与代价（写在这里，免得以后有人"顺手改成每分钟"）**：

* 心跳周期 **10 分钟** = 144 次/天 ≈ **5.3 万次/年**；NVS 有**磨损均衡**（日志式 + 页轮转）
  ⇒ 可接受。**不要**做成每秒/每分钟 ✗（3100 万次/年，白耗寿命，而心跳只回答"上一段大概多长"）。
* `prev_up` 的**分辨率就是心跳周期**：上一次跑 27 分钟，下一次开机读到的可能是 20~29 之间的
  某个值（取决于最后一次心跳落在哪）⇒ 够分"长跑 vs 几分钟"，**不够**做秒级计时。
* 拿不到就写 `-`（**不是 0**）：`prev=-` = NVS 里**还没有**上一次（全新板子）；
  `(nvs-)` = 这一次**没写进去**（NVS 坏了）。两者是**两件不同的事**，别读混。
* ★ 这一层是**单侧**的：`prev=BROWNOUT` 能**证实**"电压掉到过 brownout 门限以下"；
  它一次都没出现**不能否定**机制 B（电压可能只跌到面板掉状态、没跌到 ESP32 的门限）。

**落地与判据层**：`lib/dashcore/boot_persist.{h,cpp}`（NVS 读写全是**注入的回调**、时钟由
调用方给 ⇒ 宿主机可测）+ `test/test_dashcore/test_boot_persist.cpp`（13 例）；设备侧只接
`Preferences`（`src/main.cpp` 的四个回调）与主循环里那一次 `tick()`。

#### 3.5.3 车上特有的风险（**硬件建议**，不是软件能解决的）

| 风险 | 车上长什么样 | 硬件建议 |
|---|---|---|
| **起动机瞬间的电压跌落** | 打火那一下 12V 轨可能掉到 6~8V，5V/3.3V 跟着塌 ⇒ **面板掉状态而 MCU 靠 BOR 撑住**（= 机制 B 的现场版） | 仪表盘供电入口要有**储能电容余量**（按"背光峰值 + MCU 峰值"算，不是按平均电流）；3.3V 轨与背光支路**分开滤波** |
| **振动** | 排针/杜邦线接触瞬断 ⇒ I2C 上出现半截事务 ⇒ **打错一个字节就是一次面板复位** | 该焊的焊（别只靠杜邦线）；I2C 两根线尽量短、必要时串小阻值 + 对地小电容 |
| **温度** | 夏天仪表台暴晒（舱内可到 70°C+）+ 屏背光自发热 ⇒ 液晶变慢/面板工作点漂 | 背光别长期钉在满档（本工程 50%）；留通风。★ **本工程的软件侧现在没有温度读数**：曾试过开机打 `tc=…C`（片上温度），代价实测 **+6,080 B flash**（把 IDF 的 `temperature_sensor` 驱动整个链进来 —— 这一版 arduino-esp32 3.3.9 在 S3 上走的就是那条路），一行日志换 6 KB 不值当 ⇒ **已砍**（想加回来的四步写在 `src/main.cpp` 的 `boot_note()` 里）。★ 而且**片上结温 ≠ 环境温度**；真要知道舱温，应该从 **VAN 的温度字段**取（那是另一单） |
| **蜂鸣器支路** | 有源蜂鸣器直接挂在扩展器的输出脚上 ⇒ 那一路的电流**全从 3.3V 轨取**，与面板共享同一条轨 | **串电阻限流**（按实测电流定值）+ **并电容**吸收开通瞬间；**不要**让它与面板共用一段细走线 |

★ 上面四条是**建议**，不是本工程的实测结论 —— 车载电气环境的量化（电流/瞬跌/温升）
只能上实车量，现在**没有**那些数据（`ARCHITECTURE.md` §8 的 **L6** 就是这一条的静态那一半）。

#### 3.5.4 判据与日志字样（**改这几行之前先改文档，反之亦然**）

```
boot: reason=POWERON n=3 (raw=1) up=218ms | prev=BROWNOUT (raw=9) prev_up=27min | hb=3
                                                          ↑ 上一次的复位原因 + 上一次跑了多久（2026-09-25）
panelguard: exio rd=0x04 shadow=0x05 -> rewritten (t=15113ms)  ← 第 ② 层抓到了并修回来了（带时刻）
panelguard: backlight rd=0 want=512 -> rewritten          ← 背光那一路（独立）
panelguard: anomaly x3 -> auto reinit                    ← 连续 3 次**读到坏值** ⇒ 自动重跑一次初始化
panelguard: exio rd err (no ack) -> retry next           ← 总线无应答（**不算**"被改写"，只进快速复检）
panel: reinit by cmd r (#1, 208ms; 41 步 + 重发 fb1; …)   ← 第 ③ 层：现场一条命令
hb: up=601s rd=301 fix=1 bl=0 anom=2 k=7                 ← 心跳（每 10 分钟一行，落盘一次；k=累计落盘次数）
inject: exio out 0x05 -> 0x04 (bit0, …, t=15096ms)       ← **只有测试构建有**（临时注入路径）
```

★ `panelguard:` 与 `inject:` 两行的 `(t=…)` 是**同一个 `millis()` 时基** ⇒ 相减就是发现耗时
（实测 **17 ms**）；`boot:` 那行的 `n` 是**上电/复位累计次数**（掉电不丢；用来发现"板子自己重启过"）；
`prev=`/`prev_up=` 是**上一次**的复位原因与运行时长（见 3.5.5，用来**事后定性**）。

诊断页第 1 页那几格（**黑屏风险变成可看到的数据**）：
`guard 84s rd=42 fix=1 bl=0 anom=1`（**本次**开机：`rd` 在涨 = 守护在跑；`fix` ≠ 0 = 真救回来过）、
`sum rd=210 fix=3 bl=1 anom=4 k=7`（★ **跨重启累计**，不随重启归零）、`n=33 hb=7`（第几次上电 / 心跳累计）。

### 4. 提示音说明（有源蜂鸣器：只开/关，不变调）★ **2026-09-24 已实现并上板**

**硬件结论（2026-09-24 实测 + 结构原因，两条都有）**：2.8C 板载那颗蜂鸣器挂在
**`TCA9554` 的 `EXIO8`**（I2C 扩展脚）上 ⇒ **没有 LEDC/PWM 通路** ⇒
**原理上不可能出音调**（车主换驱动频率听不出音调变化，实测印证）。
音量同样不可调 ⇒ 只能靠"**少响 + 可关**"。

**因此**：

1. 提示音只有**开/关**两个自由度：**时长（ms）+ 次数**。
   **不许**出现频率/音调/占空比参数（那会与实际硬件不符）。
2. **单次哔 ≤ 300 ms**，且**首版不做"长鸣"** ✗ —— 严重级用 **3 短哔**。
   原因不是保守：那次黑屏事故有两条候选机制，A（400 kHz 改共享 I2C 时钟
   打错字节 ⇒ 复位面板）已被去掉，但 **B（持续高电平 2 秒把 3.3V 轨拉低
   ⇒ 面板掉状态）仍未排除** ⇒ 在 B 排除前**不许出现长时间连续高电平**。
   生产用法（短哔）本来就不会触发它，所以这个限制不痛。
3. ★ **`BeepPattern::Long`（长哔）在真机上暂时不许用** ⇒ **`long` ⇒ 真机降级为
  3 短哔**（每次 ≤ 300 ms）。**待机制 B 排除后可恢复**。
   ★ **2026-09-24 已经落地（不再只是文档）**：`alerts` 的实现与 `patternFor()`
   的映射**仍然一个字没改**（门告警照旧映射到 `Long`）；降级动作落在**驱动那一层**
   —— `lib/dashcore/buzzer_exio.cpp` 的 `pulsesFor()` 把 `Long` 落成 **3 相**，
   而 `kMaxPulseMs = 300` 在**驱动内部**再夹一次（`clampPulse()`），
   序列里**不可能**出现超过 300 ms 的相。
   ★ 预览里 `BuzzerHost` 打印的仍是 `pattern=long ms=…`：那是**模式名**，
   不是它在这块板上的落法。
4. **静音状态掉电保存**：`Alerts::setMuted()` 是内存态（每次上电复位 ⇒
   车主每次都得重关）⇒ 本轮用 **NVS（`Preferences`，命名空间 `dash`、键 `mute`）
   落盘**，在 `setup()` 里读回、在开关动作时写入。
   ★ 口径：**默认有声**（不静音）；**静音是车主的选择**，要跨上电记住。
   ★ 预览端**刻意不落盘**：那会让"重新跑一次预览"继承上一次的静音，演示时容易误会。
5. **静音入口放诊断页**（不在主表盘上加控件 ✗ —— 那会变成第四个常驻元素）：
   `K` 进诊断页，第 1 页最后一行就是 `mute=`（**看**状态）；
   **真机上取反 + 保存**由串口命令 **`m`** 做（这块板上没有可用的按键，见 §3 的入口表）。
   ★ 静音走的是**既有那条路**（`Alerts::setMuted()` + `g_beep_muted` + NVS），
   驱动那一层**不知道"静音"这件事** —— 静音时调用点**根本不调 `beep()`**
   （见 `src/main.cpp` 里 `g_sys.beepDue()` 与 `g_alerts.beeping()` 那两处），
   所以"静音在真机上确实不响"是**判据层**保证的，不是驱动层猜的。

#### 4.1 提示音词表（**这块板上唯一还能表达的东西 = 节奏与次数**）

| 词 | 模式（`BeepPattern`） | 在这块板上的落法 | 谁在用 |
|---|---|---|---|
| **1 短哔** | `Short` | 1 × `Short`（默认 120 ms） | 「数据不可信」那一声轻提示（`kTrustBeepMs = 120 ms`） |
| **2 短哔** | —— | —— | ★ **还没有调用方**：词表先留着（有源蜂鸣器只有"几次"这一个自由度，两短哔是自然的下一档）；要加的时候**只改 `alerts` 的模式映射**，驱动不用动 |
| **3 短哔** | `Triple` / **`Long`（降级）** | 3 × ≤300 ms，中间隔 80 ms | 红区（`Triple` = 3 声）/ 门（`Long` ⇒ 降级 3 声） |
| **4 短哔** ★ | `Urgent` | 4 × ≤300 ms，中间隔 80 ms | 超速（`Urgent` = **4 声** —— `lib/dashcore/buzzer_exio.cpp` 的 `pulsesFor()` 是唯一事实来源） |
| **长鸣** ✗ | `Long`（**本意**） | ★ **禁止** —— 在真机上被降级成 **3 短哔** | 无（机制 B 排除前不恢复） |

* ★ **为什么 `Urgent` 是 4 声、不是"更响"**：音量不可调、频率不可调 ⇒ "更急"只能靠
  **更多声**。而 `Urgent`（**4** 声）与 `Triple`/`Long`（都是 **3** 声）在这块板上
  **只差一声** —— 声数就是全部的自由度，所以两者的区分度天然很弱。
  这是有源蜂鸣器的自由度决定的**已知代价**，不是 bug。
  要真正区分它们就得换硬件（飞线到一个能走 LEDC 的普通 GPIO），不在本轮范围。
  ★ **2026-09-24 更正**：本表原先把 `Urgent` 与 `Triple`/`Long` 并成一行"**3** 短哔"、
  并写成"听起来一样" —— 那是**文档错**：`pulsesFor(BeepPattern::Urgent)` 一直是
  **4 声**（`lib/dashcore/buzzer_exio.cpp`，native 用例 `test_buzzer_exio.cpp` 的
  `pulse_counts` 用 `ASSERT_EQUAL_UINT8(4, …)` 钉着）。这里按**代码**对齐。
* ★ **禁止长鸣的理由**：见第 2 条（机制 B 未排除）。**单次哔 ≤ 300 ms** 是硬上限，
  在 `lib/dashcore/buzzer_exio.h` 的 `kMaxPulseMs` 里，并且有
  `static_assert(kTrustBeepMs <= 300u)`（`system_status.h`）与 native 用例
  （`test_buzzer_exio.cpp` 的 ③）**两道**钉着。
* ★ **静音入口与掉电保存**：见第 4/5 条 —— 串口命令 `m`（取反 + 写 NVS），
  重启后由 `setup()` 读回（日志里那行 `mute: %d (loaded from NVS)` 就是判据）。

#### 4.2 落地形态（L14 的答复：**发声的是 2.8C 这一块，即右/主机板**）

| 件 | 在哪 | 说明 |
|---|---|---|
| 抽象（"什么时候该响"） | `lib/dashcore/alerts.*` / `system_status.*` | 判据、去抖、最短重复间隔、静音 —— **本轮一行没改** |
| 抽象（"谁发声"） | `lib/dashcore/buzzer.h` | `BuzzerNull`（不发声）/ `BuzzerHost`（预览打印）/ `BuzzerExio`（真机） |
| **真机驱动** | `lib/dashcore/buzzer_exio.{h,cpp}` | **本轮新增**：非阻塞多相序列 + 单次 ≤300ms 夹取 + `Long`⇒3 短哔 |
| **唯一写总线的地方** | `src/dash_display_rgb.cpp` 的 `dash_buzzer_set()` | 走**既有**的 `tca9554_set()` + 影子寄存器 `g_exio_out`（读-改-写） |
| 挂载点 | `src/main.cpp`（`#if defined(DASH_DISPLAY_RGB)`） | `g_buzzer = &g_buzzer_exio;` —— **只有真屏那一份构建有这一段** |

★ **L14 的答复（"由哪块板发声"）**：**发声的是 2.8C 这一块 —— 也就是右板（主机板）**。
判据是"手上只有这一块 2.8C"，而它**板上自带的**蜂鸣器挂在 `TCA9554` 的 `EXIO8`
（那颗芯片本来就在驱动这块屏的 20 根线里）⇒ **零额外引脚、零额外接线**，
所以**用不上** `EVENT 0x01` 那条还没定的上行。
★★ **另一块板（左/从板）有没有蜂鸣器：2026-09-25 已实测 —— 有**（L14 到此**完全关闭**）。
第二块 2.8C（`COM8`）到货后**上板实测**：它刷上从板镜像之后**真的会响**
（串口上那一串 `buzz: exio 0x05 -> 0x85 (mask 0x80)` / `回读=0x85(影子=0x85)`
证明**驱动路径通**；★ 而"到底响没响"**是人耳判据**，机器侧只到寄存器回读）。
车主原话（2026-09-25）："**这块新板的蜂鸣器响，不然我怎么会跟你说蜂鸣器会长鸣之后
画面卡死**" ⇒ 两块**同型号**板**都有**蜂鸣器、都在 `TCA9554` 的 `EXIO8` 上
（同型号 ⇒ 同构，本轮实测印证）。
★ **判据的分工写清楚**（免得以后被读成"寄存器证明了发声"）：
  · 机器侧能证明的：写进去的那一位**读回来一致**（`回读=0x85(影子=0x85)`）；
  · **只有耳朵**能证明"它真的出声"—— 本轮的结论**就来自人耳**。
★ ★ 顺带把这条实测**推翻了的一个旧假设**记在案：从板**也会响** ⇒
  "发声的只有主板"那种接线口径**不成立**（两块板各自都可能响；今天 `main.cpp`
  给两个角色都挂了 `BuzzerExio`，所以两块板上的告警都会响 —— 这是**现状**，
  不是本轮改的）。
★ 三条硬约束（单次 ≤300ms / `Long`⇒3 短哔 / 绝不长鸣）与"写总线只有一处"
（影子寄存器只能有一份，否则丢位 ⇒ 复位面板）在 `lib/dashcore/buzzer_exio.h`
的文件头里逐条写着，改之前先读那一段。

#### 4.3 自证与人工判据（这一功能为什么能被验收）

* **每一次写到扩展器都落一行**：`buzz: exio 0xXX -> 0xXX (mask 0x01…)`
  —— "有没有打到 `LCD_RST`/`LCD_CS` 那两位"从串口**一眼就能看出**
  （期望恒为 `0x05 -> 0x85` 或 `0x85 -> 0x05`，**只有 bit7 在动**）。
* **串口命令 `b`**：**响一声短的**（走 `Long` ⇒ 3 短哔那条降级，共 520 ms），
  回执 `buzz: test beep cmd=b(Long>3short) …` ⇒ 车主**按需**验证听感，
  不用等告警、不用等开机那 2.44 秒的 `trust: sim-fallback`。
  ★ 与既有 `d`/`m` **同一道门**（`DASH_DISPLAY_RGB`）：抓帧盒两个 env 里
  **一行都不存在**，行为逐字节不变。
* ★ **人耳那一条只能由车主给**（机上没有麦克风）：判据是"**听到 3 声短哔、
  不是一声长鸣、也不是只有一声**"，而串口那两行 `buzz:` 是**同时**要贴出来的客观对照。
  ★ **2026-09-24 已确认**：车主在真机上听到 **3 短哔**（原话「我听见了哔哔哔三声」）
  —— 与 `b` 的 `ms=120 x3 gap=80ms`（= `Long` 的降级）对得上，
  同拍 `vsync +54/s` / `copy_max≈1.3ms` / `timeout=0` / `fullrb=0/s`
  ⇒ **响了，而屏没坏**（详见 `ACCEPTANCE.md` 当天条目 ⑤-bis/⑤-ter）。
* ★ **台上会间断出声，那是正常的**：模拟数据周期扫量程 ⇒ 会扫进红区（>5800）
  ⇒ `alert=redline` 反复出现、蜂鸣器跟着间断叫。**不是长鸣**：
  两次之间至少 3000ms、每声 120ms（两道界都在 `lib/dashcore/alerts.cpp`）。
  **想安静就发串口 `m`**（取反 + NVS 掉电保存 ⇒ 重启之后仍然静音）。
  将来做实体按键时，静音入口放**诊断页**那条口径不变（本文件 §4 第 5 条）。

## 双板链路协议 v1 范围（2026-09-22，契约稿：只定范围与消息表）

**这节是什么**：给接下来"**主板发送侧 / 从板固件**"并行开工用的**接口契约** —— 只写范围与消息表（帧格式、消息、时基、失败兜底），不写实现、不碰代码。
**与相邻章节的关系**：「最终显示架构：两块 S3、一板一屏、一条 UART 串联（2026-09-21）」那一节定的是**为什么两块板、哪几根脚、什么线**；本节定的是**线上跑什么**。只有一处口径被本节**收窄**（不是推翻）：那节把"复用回放行格式"记为左板"零新协议"的起点，本节把回放行**降级为调试通道**、正常数据面改用 §2 的二进制帧 —— 两条通道在从板入口按**首字节**分流，既有实现（`van_replay.h` / `main.cpp` 的 `van_replay_poll()`）不动。
**写法**：**事实**（有出处的现状）与**建议**（要落地的契约）分开写；本节凡是没实测、没在文件里读到的一律标 `待确认`，并写清"待确认什么、怎么确认"—— 汇总在 §8。
**本轮只写文档**：不改代码、不改 `platformio.ini`、不动素材与 `tools/theme-editor/theme.json`。

### 0. 角色与物理层（事实基线，每条都有出处）

| 项 | 事实 | 出处 |
|---|---|---|
| 角色 | **右板 = 主板（A）**：挂 VAN 收发器（只听）+ 日志走**原生 USB-C**，渲染**右盘**（车速 + 进气温度）；**左板 = 从板（B）**：**不接任何 VAN 硬件**，只收那条 4 线缆，渲染**左盘**（转速 + 水温） | 「最终显示架构」+「接线定案」；左舵车 9004/9005 在中控/仪表罩右侧够得到 ⇒ 主板放右、VAN 走线最短 |
| 链路引脚 | 主板 **GPIO43**（UART 口 `TXD`）→ 从板 **GPIO44**（UART 口 `RXD`）；VAN RX 走 **GPIO15**，**特意避开 43/44** | 「接线定案」引脚表 + 那条"为什么挑 15、不挑 44" |
| 线缆 | **4 线：5V / GND / TX / RX**；**5V 是电源**（主板 12PIN `VBUS(5V)` → 从板）、**TX/RX 是 3.3 V 逻辑**、**GND 兼作电源回流与信号地**；★ **跨板只送 5V，绝不送 3V3** | 同上（每块板自己的 LDO 管自己那一路，送 3V3 等于一块 LDO 扛两个负载 + 倒灌） |
| 载体 | 链路用的就是 **UART0 的那对脚（43/44）** ⇒ 显示构建必须**关掉 UART0 文本日志**，日志只走原生 USB-CDC（否则日志文本会混进链路数据流）—— ✓ **2026-09-23 已在代码里收口**：`dash_log.h` 的 `DASH_LOG_UART0` 在带链路 PHY 的构建里默认 0 ⇒ 这个"必须"由编译期判据保证，显示 env 不需要写任何豁免宏 | 那节末尾的**待办（固件）第 1 条（已做）** + `lib/link/link_phy_uart.cpp` 的编译期闸门 |
| 显示分叉 | 每板**一块** 480×480（最终板 = 微雪 `ESP32-S3-LCD-2.8C`，ST7701S/RGB，屏那 20 根在板上走、不占排针）⇒ 显示侧只要**单屏**那一路（`esp32s3-rgb` 现在就是单屏版本；`esp32s3-spi` 是已退货的 240 档验证板） | 「最终显示架构」+「引脚预算」+ README 的环境表 |
| VAN 时序（主板侧是**硬实时**） | 槽时间 **8.25 µs**（实测，≈121 kbit/s）；空闲关帧门限 **70 µs**；帧内最长一段"没有跳变"**49.0 µs**；帧间最短空闲 **95.5 µs** | `van_wire.h` 的 `kTsNs`；`van_phy_gpio.cpp` 的 `kIdleCloseUs` 及其上下界 |
| 车速/转速帧 | **IDEN 0x824 / CMD 0x8**，BSI → 仪表，7 个数据字节，**≈79.7 Hz** | 「最终显示架构」把速率记为 ≈79.7 Hz；字段口径见下 |
| 字段口径 | 转速 = `data[0..1]` 16 位大端 **× 0.125**；车速 = **`data[2]` 单字节 × 2.56**（`kSpeedScale = 2.56`，2026-09-22 实测定标；取自蓝牙 ELM327 `010D` 回归，数据可分辨范围 **2.55~2.57**，**2.56 是取整**）；水温/进气温度**只有 OBD 有**（VAN 上没有这两项）；油量/挡位 = Sim | `van_source.h`（2026-09-20 定案字段、**2026-09-22 定标**）、`data_service.h` 的优先级说明 |
| 从板入口已有能力 | 从板本来就在解 `VAN 824 18 F8 27 10 00 00 00` 这种**回放行**（`parseVanReplayLine`），同类通路 2026-09-18 已在真板上验过 | `van_replay.h`、`main.cpp` 的 `van_replay_poll()`、「最终显示架构」那条"已知可用，不是推测" |

- ⚠ **一条口径提醒（本节依赖它，不说清就会写错字段）**：车速字段一律以 `van_source.h` 的 `kSpeedOffset = 2`（**单字节**）/ **`kSpeedScale = 2.56`**（2026-09-22 实测定标；取自蓝牙 ELM327 `010D` 回归，数据可分辨范围 **2.55~2.57**，**2.56 是取整**）为准 —— 2026-09-20 之前"`data[2..3]=车速×100 km/h`（大端）"的旧口径已被实测否掉，上面「软件分层」那行**本轮已按实测改准**。**本节下面所有载荷都按 `van_source.h` 写。**
- ⚠ **引脚口径提醒**：主板 **VAN RX = GPIO15** 是文档定案，但代码里默认是 `VAN_RX_PIN 16`（`van_phy_gpio.cpp`），`esp32s3-spi` 那个 env 覆盖成 18 ⇒ 最终板上必须 `-DVAN_RX_PIN=15`。**本轮不改代码**，只在这里记着。

**★ 本节对「接线定案」的一处新增（v1 需要上行，所以 4 线要双向跑）**

「最终显示架构」把链路写成**单向**（主板只发、从板只收）—— 那只覆盖了 A→B 的车辆数据。本节的 v1 还要 **B→A 的状态上报与事件**（§3 的 `0x30`/`0x40`，这是"单一日志出口"的基础），所以那对 4 线要**双向**用：

| 方向 | 主板脚 | 从板脚 | 线缆里那一根 |
|---|---|---|---|
| A → B（车辆数据 + 心跳，v1 的主流量） | GPIO43 输出 | GPIO44 输入 | `TX` |
| B → A（状态上报 + 事件，v1 的次流量） | GPIO44 输入 | GPIO43 输出 | `RX` |

- 线缆**不用改**（4 根里 `RX` 那根本来就定义了），改的是**两端的用法**：主板的 44 从"空着"变成**输入**，从板的 43 从"空着"变成**输出**。
- ⚠ 这**正好踩在「接线定案」记录的那个风险上**：43/44 是板载 USB-串口桥（CH343P）那一对，**桥的 TX 是推挽输出，和输入并线会"对打"**。当初 VAN 的 RO 就是为了这条挪去 GPIO15 的。那节只讨论了"主板拿 44 当输入"，而 v1 双向之后**两块板的 44 都成了输入**（每块板各有一个板载桥）⇒ 这条风险对**两块板**同样成立。
  **★ 该风险已按官方原理图判定为「不对打」（2026-09-23，原 L1 → 已关闭）**：板上有一颗 **`FSUSB42UMX`（U10，2:1 模拟开关）**，把 ESP32-S3 的 UART0（`IO43`/`IO44`）在 **CH343P** 与 **排针** 之间**二选一** —— 两个驱动源**不同时接到 43/44**。依据与出处见 §8 的 L1 行。
- 兜底（L1 若确认为"会对打"）：① 从板 RX 与主板 RX 都改走 **GPIO7**（引脚表里它的用途就是"备用"）；或 ② 放弃上行、从板状态只能插它自己的 USB-C 看 —— 那就没有"单一日志出口"了，要 owner 权衡。
  ★ **L1 已判"不对打"（2026-09-23，依据见 §8）⇒ 这条兜底 v1 不启用**：两块板的 44 都当输入用是安全的（板载桥走的是另一条支路，见 §8 L1）。

### 1. 物理层与发送侧约束

#### 1.1 波特率：**建议沿用 115200 8N1**（事实：这是那节已定的值，且同类通路在真板上验过）

| 波特率 | 1 字节（8N1 = 10 bit） | A→B `DATA`（13 B） | A→B `TICK`（12 B） | B→A `STATUS`（23 B） | v1 合计占空比 |
|---|---|---|---|---|---|
| **115200（推荐）** | **86.8 µs** | **1.13 ms** | **1.04 ms** | **2.00 ms** | **≈15%**（A→B ≈14%，B→A ≈0.4%） |
| 230400 | 43.4 µs | 0.56 ms | 0.52 ms | 1.00 ms | ≈7% |
| 460800 | 21.7 µs | 0.28 ms | 0.26 ms | 0.50 ms | ≈4% |

- **为什么是 115200**：① **预算够**（上表：把 v1 全部消息按 §3 的频率全速跑，也只占约一成半的线时）；② **下行不需要更快** —— 从板渲染是 5 Hz 节流（`main.cpp` 的 `last_ui_ms >= 200`），而且它**不做任何硬实时**；③ **上行几乎不占**（2 Hz、23 B）；④ 主板那一侧唯一有硬实时要求的是 **VAN 边沿采集**，所以"链路的中断/DMA 占用越低越好"，而 115200 是这条线上**已经被真板验证过**的速率（回放行 29 B ≈2.3 KB/s，占 11.5 KB/s 的两成）。★ 中断密度的定量影响**没实测** ⇒ **待确认 L2**。
- 留量：若 v2 要把从板的**文本日志**转发到主板 USB-C（§3 的单一日志出口再进一步），11.5 KB/s 会被日志吃掉大半 ⇒ 那时升 **460800**（46 KB/s）；**不建议** v1 上 921600 —— 921600 在 PINOUT 里是"抓帧应急"用的值（当时是日志把主循环拖住），不是链路需求，而更密的中断对主板 VAN 采集是纯风险（L2）。

#### 1.2 主板**发送侧必须非阻塞**（硬约束）

- **先例（实测，不是推测）**：PINOUT「已知的坑」记着 —— 115200 只有 ≈11.5 KB/s，VAN 帧一行约 48 字符，总线超过 ~230 帧/秒时 `Serial0.write` 会**阻塞**、把主循环一起拖住 ⇒ **丢边沿 → 坏帧变多，看起来像"FCS 约定不对"**。同一条路不可能拿来发链路帧。
- ① **不在中断里发**：ISR（`van_isr_thunk` / `onIsrEdge`）只做"读电平 + 读时间戳 + 入队"（既有口径：绝不在中断里跑几百微秒的解码）。发送一律在**主循环**里。
- ② **自有环形缓冲 + 不等待**：链路帧先进一个自有环形缓冲（**建议 ≥512 B**，够装下最坏突发：`DATA` 13 B + `TICK` 12 B + `STATUS`/`HELLO` 若干 + 余量），再由 UART 的硬件 FIFO/DMA 排出；入队前用 `availableForWrite()` 之类判断，**不够就丢这一帧**（丢弃由 `TICK.seq` 与从板 `frames_ok` 计数可见），**绝不忙等、绝不 `delay()`**。
- ③ **从快照发，不从回调发**：数据帧的内容取自主循环里 `g_data.update(now)` 之后的**快照**，而不是在 VAN 帧回调（`VanLogSink → VanSourceSink → VanSource`）里顺手发 —— 那条路径是"边沿 → 解码 → 数据层"的热路径，任何阻塞都会回传到解码器。

#### 1.3 **不得在 VAN 关帧窗口附近做长临界区**（硬约束）

事实（都有出处，§0 表里那四个数）：一个 TS = **8.25 µs**；帧内最长"没有跳变" = **49.0 µs**；关帧门限 = **70 µs**；帧间最短空闲 = **95.5 µs**；关帧判据是 `vanIdleCloseReady()`（**队列非空就不许关帧**，队列 2048 个边沿）；ISR 只入队，主循环 `tick()` 排空 + `finish()` 收尾。

- 主板**关中断 / 临界区**必须**远短于一个 TS**：目标是 **< 5 µs** —— 一个边沿丢一次，那一帧的 FCS 就必不过（症状是 `edges` 在涨、`frames` 不涨）。**"最长允许多少"没有实测** ⇒ **待确认 L3**。
- **绝不允许**把长耗时操作放进这段窗口（"距最后一条边沿 49.0~70 µs"这一区间 + `g_van_phy.tick()` 到 `finish()` 之间）：`delay()`、flash 写、阻塞写串口、LVGL 整屏重绘、任何"等对端回话"的同步等待。链路发送与解析都是**新增在这一段附近的工作**，所以它们必须"能随时被打断、且单次很短"（这正是 1.2 那三条的由来）。
- 每圈 `loop()` 要给队列排空留预算：既有口径是"队列满 = 主循环排空不及"（`dropped` 涨）；`vanIdleCloseReady()` 的注释里写得很直白 —— 主循环被 LVGL 拖住时"最后弹出的边沿可能已经老出几百微秒"，窗口很窄但代价是**丢整帧**。

### 2. 帧格式 v1（二进制，定长头 + 变长载荷 + CRC-15）

| 偏移 | 宽度 | 字段 | 取值 / 含义 |
|---|---|---|---|
| 0 | 1 | `SYNC` | 固定 **0x5A** |
| 1 | 1 | `VER` | 高 4 位 = **主版本**、低 4 位 = **次版本**；**v1 = 0x10** |
| 2 | 1 | `TYPE` | 见 §3 的消息表 |
| 3 | 1 | `LEN` | 载荷字节数；v1 合法范围 **4..16**（★ 下界 2026-09-23 由 5 更正为 4，理由见下），**>64 一律按坏帧丢**（不等载荷） |
| 4 | 1 | `ROLE` | **1 = 主板（右）、0 = 从板（左）**；每帧都带（角色冲突自检用，§5） |
| 5 | `LEN` | `PAYLOAD` | 见 §3 |
| 5+`LEN` | 2 | `CRC` | 16 位字段大端承载 **15 位** CRC（bit15 恒 0），内容 = `van::crc15_van_iso()` |

- **帧长 = 7 + `LEN`**（v1 按长度升序：`EVENT` **11 B**、`TICK` 12 B、`HELLO` 12 B、`DATA` 13 B、`STATUS` 23 B）。解析缓冲建议 ≥ 128 B。
- ★ **`LEN` 下界为什么是 4 而不是 5（2026-09-23 更正）**：§3 的 `EVENT`(`0x40`) 载荷只有 **4 B**
  （`evt_id u8` + `value u16` + `face u8`，见 §3 表），是 v1 里**最短**的一条消息 ⇒ 照原文「合法范围 5..16」实现，
  `EVENT` 会被接收侧**全体当 `bad_len` 丢掉**（上面那条重同步规则），而 §3 又要求 `EVENT` 可用 ——
  这是契约内部的一处**硬冲突**。按"下界由最短载荷决定"改成 **4..16**（`EVENT` 帧长 = 7 + 4 = **11 B**，即帧长下界 11）。
  实现侧已取 **`kLenMin = 4` / `kLenMax = 16`**（`lib/link/link_frame.h` 的 `lenInRange()`），与本行一致。
- **`SYNC` 为什么是 0x5A**：① 与从板入口要认的**回放行首字节 `'V'`（0x56）不撞**；② `01011010` 是交替位、没有长同电平段，空闲（高）之后第一个下降沿就能定边界；③ 不是 `0x00`/`0xFF` 这类"空闲/填充"值。
- **`SYNC` 刻意不进 CRC**：它只用于对齐 —— 帧头被噪声改掉时，这一帧会因为**找不到帧头**被跳过（重新找下一个 `SYNC`），而不是被当成好帧收下。
- **重同步**：CRC 不过或 `LEN` 非法 ⇒ 丢这一帧、**从 `SYNC` 之后一个字节继续找下一个 `SYNC`**，并计数（`crc_err` / `bad_len`）。**不清空已显示的数据**（宁可显示旧值，也不要闪 0）。

**校验：直接复用仓库里已有的 CRC（找到了，就在 `lib/dashcore/van_wire.h`）**

- 事实：`van::crc15_van_iso()` 已实现、已有宿主机单测、并已在**真实抓包上逐帧命中**（drive5min 17106/17106、sample 66/66）—— 多项式 `x^15+x^11+x^10+x^9+x^8+x^7+x^4+x^3+x^2+1`（掩码 0x0F9D）、初值 0x7FFF、输出取反、MSB-first、不反射。
- 建议：**覆盖范围 = 字节 1..(4+`LEN`)**（`VER`..载荷末尾），**不含 `SYNC`、不含 `CRC` 自己** —— 与 VAN 的 FCS 同一口径（不含 SOF、不含 FCS）。
- ★ 两处**别照搬**：① `fcsFieldFromCrc()` 那次左移（"最低位恒 0"）是 **VAN 线上 16 位 FCS 字段**的约定（那个 0 位是 EOD 的一半），链路帧不背这个包袱 —— 直接 2 字节大端存 15 位值；② **别用 `crc15()`**（0x4599 = CAN-15），头文件写明它不是本总线的 FCS（实测 0 命中）。
- 检错能力：该多项式有 **10 个非零项（偶数）⇒ 含 `(x+1)` 因子 ⇒ 奇数个位错必被检出**（这条自己数一遍就能核）；**其余已枚举实测（2026-09-22，L4 关闭）**：12..23 B **每个帧长**上 —— 1 位 / 2 位 / 3 位错**未检出均为 0**（3 位错同属奇数位错，也必检出）、长度 **2..15 bit 的突发未检出 0**、**16 bit 突发实测 1308 个**（每帧长 `8(L-2)-15`：65/73/…/153，图案恒为 `0xB9F1`，全部落在被覆盖的数据区内）、**最小汉明距离 = 4**。用例 `test/test_dashcore/test_link_crc_coverage.cpp`（`python -m platformio test -e native`）。
  ★ 枚举口径（不写清就会把结论说过头）：码字 = **整帧**（前 L-2 字节被覆盖 + 末尾 16 位字段）；按上面那条「`SYNC` 刻意不进 CRC」，**SYNC 那 8 位不在这张表里** —— 它们由"找不到帧头就重新找 `SYNC`"兜住。

**版本不匹配时的行为（v1 只做"上报 + 不匹配告警"）**

| 情况 | 行为 |
|---|---|
| **主版本不同**（`VER` 高 4 位） | 双方各打一行 `link: ver 不匹配(本机 0x10, 对端 0x..)`；`STATUS.flags` 里 `ver_mismatch = 1`；**不断链、不降级**：已知 `TYPE` 按 v1 继续解析，**未知 `TYPE` / 越界 `LEN` 一律丢帧并计数**，不清空已显示数据 |
| **次版本不同** | **不告警**（次版本只用来"加东西"）：新 `TYPE` 直接忽略；已知 `TYPE` 的载荷**只许加尾巴、不许改前缀** —— 于是新固件发的东西，老固件也认得前半截。★ **但"加尾巴"不得越过 `LEN` 上限 16**（§2 的合法范围）：`STATUS` 载荷**已经顶格用到 16 B** ⇒ 它**没有**加尾巴的余地，要加字段只能**升主版本**或另开一个新 `TYPE`；还有余量的是 `EVENT`(4→16)、`HELLO`/`TICK`(5→16)、`DATA`(6→16) |
| 对端版本高于本机 | 同上：向前兼容靠"忽略未知"，不靠协商 |
| v1 **不做** | 不做"协商出共同版本"、不做握手重传、不做能力/素材推送（§6） |

**与回放通道的分流（从板入口）**

- 事实：从板现在就在解回放行（`parseVanReplayLine`），而且 `main.cpp` 的 `van_replay_poll()` **两个串口都收**。
- 建议（分流器，**本轮不做**）：从板 RX 上按**首字节**分流 —— `'V'`(0x56) → 文本行（一直吃到 `\n`）、`0x5A` → 二进制帧（§2）、其余字节丢弃并计数。⇒ 贴行联调**不因为上了协议而失效**，而协议帧也不会被当成文本行解出垃圾。

### 3. 消息表 v1

**载荷怎么读（一条总则 + 两处位号，2026-09-23 补全 —— 原文只在 `DATA.rpm_raw` 上标过一次 `u16 BE`，两个 `flags` 也没给位号）**

- ★ **多字节字段一律大端（MSB-first）**：表里 `DATA.rpm_raw` 那处 `u16 BE` 是**举例、不是特例** ——
  `fw_ver` / `build_tag` / `tick_ms` / `uptime_ms` / `frames_ok` / `frames_dropped` / `crc_err` / `last_gap_ms` /
  `EVENT.value` **同此**。理由：协议里混用两种字节序没有任何好处，而仓库全线（VAN 的 IDEN/CMD/FCS、§2 的帧头、
  §2 的 `CRC` 字段）都是 MSB-first 大端。实现侧只留大端一份 `putU16/getU16/putU32/getU32`（`lib/link/link_msg.cpp`）。
  字节序写错**不会有任何编译期信号**，只会表现成"两板的 `tick_ms` 差了 16777216 ms"。
- ★ **两个 `flags` 的位号**：见下表 `DATA` / `STATUS` 两行（都按"高位在前"排，与上一条同口径）。

| `TYPE` | 名称 | 方向 | 频率（建议值） | 载荷 | 用途 | 超时 / 丢弃策略 |
|---|---|---|---|---|---|---|
| `0x01` | `HELLO` | 双向 | 上电 1 次；之后每 **5 s** 重发，直到收到对端 `HELLO` | `fw_ver u16`（固件版本，与协议 `VER` 分开）+ `build_tag u16` + `boot_reason u8`（5 B） | 版本协商 + **角色对账** + "两块板是不是同一份固件" | 幂等、可重复；无超时概念 |
| `0x10` | `TICK` | A → B | **50 Hz（20 ms）** | `tick_ms u32`（主板**单调**毫秒，从复位起算，32 位 ⇒ 49.7 天回绕）+ `seq u8`（5 B） | **时基 + 心跳**（车睡着、VAN 没帧时也照发，从板靠它判断"主板还活着"）+ 跨屏动画对齐 | 从板：**>100 ms**（5 个周期）没见 ⇒ 时间基准失效、回退本地时钟；**>500 ms** ⇒ 链路降级；**>3 s** ⇒ 回退 Sim（沿用 `data_service` 既有的 3 秒规则） |
| `0x20` | `DATA` | A → B | **跟随 VAN 0x824 到达（≈79.7 Hz）**，不另建定时器 | `rpm_raw u16 BE`（= 转速 × 8，即 VAN 线上原值，与 `kRpmScale = 0.125` 配套）+ `speed_raw u8`（原始计数；**1 计数 = 2.56 km/h**，2026-09-22 实测定标，数据可分辨范围 2.55~2.57、**2.56 是取整**）+ `coolant_raw u8`（= ℃ + 40）+ `intake_raw u8`（= ℃ + 40）+ `flags u8`（**每字段 2 位 = `FieldSource`**：0 None / 1 Sim / 2 Obd / 3 Van；★ **位号按本行字段出现顺序从高位排**：`rpm = bit7..6`、`speed = bit5..4`、`coolant = bit3..2`、`intake = bit1..0` —— 打包/取值只走 `dataFlagsPack()` / `dataFlagsGet()`，别手写移位）（6 B） | 驱动**左盘**（转速弧 + 大数字 + 水温弧 + 水温数字） | 同 `TICK` 那三档。`flags` 是刻意的：从板上**没有**任何本地源，所以"这个值到底是真值还是假数据"只能跟着数据一起过来（对应 `DataSourceStatus` 的字段级优先级） |
| `0x30` | `STATUS` | B → A | **2 Hz（500 ms）** | `fw_ver u16` + `uptime_ms u32` + `frames_ok u16` + `frames_dropped u16` + `crc_err u16` + `last_gap_ms u16` + `left_face u8` + `flags u8`（★ **位号**（2026-09-23 补）：`bit0` `ver_mismatch` / `bit1` `role_conflict` / `bit2` 无 `DATA` 超时 / `bit3` 温度弧无源；`bit4..7` 保留 = 0）（16 B） | **"单一日志出口"的基础**（见下）+ 链路质量 + `uptime_ms` 用来发现"从板在反复重启"（§7） | 主板：**>30 s** 没见 ⇒ 一行 `link: 从板无响应` + 计数（**只用于日志、不上屏** —— owner 裁决，2026-09-22，原 L13）；**不停机、照常发数据**（从板可能只是刚上电/重启/没接线） |
| `0x40` | `EVENT` | 双向（v1 实际只用 B → A） | 事件发生即发，**限速 ≥100 ms**（防抖） | `evt_id u8` + `value u16` + `face u8`（4 B） | 告警与档位变化，走日志，以及"由**主板的硬件**做"的事（如将来那个声音告警） | **不重传**：丢一条 `EVENT` 不会漏掉**持续状态** —— 当前档位每 500 ms 由 `STATUS.left_face` 兜底（刻意设计：**用状态帧兜事件**，省掉重传逻辑） |

**`STATUS.last_gap_ms` = v1 决定：字段保留、v1 一律发 0、将来要填需先有接收侧时钟（2026-09-23 定案）**

★ **本条口径：保留字段、v1 恒 0、不发明语义**（owner 口径，2026-09-23；**原文是"待确认"，现在改成 v1 决定**）。

- **定案**：`STATUS` 载荷里那个 `u16`（§3 表 `0x30` 行，偏移见 `lib/link/link_msg.h` 的
  `StatusMsg::last_gap_ms`）**保留** —— v1 里**一律发 0**（默认值就是 0，**没有任何生产者**）。
  **本轮不新写实现**（发送侧那一个字节的行为一个字都没变），**也不给 0 附加语义**
  （接收侧今天没有读它的代码，所以"0 = 未提供"这种解释**暂时不写**）。
- **为什么保留而不是删**：它是 §3 表里唯一的链路质量类字段，删了就要动载荷长度
  （`STATUS` 已经**顶格 16 B**，§2 那条"次版本加尾巴不得越过 `LEN` 上限"就是为它写的）
  ⇒ 删字段的代价比留着一个恒 0 的字段大，而恒 0 不占用任何带宽（它本来就在载荷里）。
- **为什么 v1 只能是 0（这是本题的物理原因，不是"懒得做"）**：要填它，
  **发送侧必须先有"接收侧自己的时钟"** —— 无论候选语义是哪个，量出来的都是
  "**从板**观测到的帧间隔"，而那需要一个**落在从板上的时间基准**；v1 的从板
  还没有这条落地代码（`LinkTime` 的偏移估计今天只在从板侧被喂，见 §4）
  ⇒ **这个字段在 v1 里没有生产者**，也就只能是 0。
- **将来要填的前置条件（先有引用侧时钟，再谈语义）**：① 从板的接收侧时钟落地
  （§4 那条"TICK 偏移估计"真正开始跑）；② 那时才定它是"最近一次帧间隔"（瞬时、
  抖得厉害、通常还要再平滑）还是"观察到的最坏间隔"（2 Hz 窗口内取 max，是链路质量
  指标）—— **两者实现不同，届时按"这个数拿来回答什么问题"选**（判据见下）。
- ★ **本条不占 L 号、也没新开 L 号**：它是对**已有字段**的语义缺口，不是新的电气/时序
  风险；§8 的 L 表是 owner 裁决用的编号表，照旧不自行占号（§8 表头的指引未改）。

**`EVENT.evt_id` v1 取值（全部落在仓库已有的概念上，不新增概念）**

| 值 | 事件 | 触发（= 仓库里已有的东西） | 方向 |
|---|---|---|---|
| `0x01` | 红区进入 / 退出 | 左屏转速档 `Face::Redline`（进 5880 / 退 5720，`expression.h`） | B → A |
| `0x02` | 超速进入 / 退出 | 右屏车速档 `Face::Overspeed`（进 >130 / 退 ≤127） | 类型号留给"两台表一起提示"的将来；主板自己就知道，v1 **不过链路** |
| `0x03` | 表情档位变化 | `kFaceLeftStates` / `kFaceRightStates` 那 5+5 档的**任意一跳**（载荷 `face` = 新档位） | 双向（各自报**自己那一屏**） |
| `0x04` | 数据源变化 | 字段源变了（Van ↔ Obd ↔ Sim 的 3 秒回退，`DataServiceStatus`） | B → A（主板自己那半自己知道） |
| `0x05` | 扫表完成 | 开机动画收尾（`BOOT_TOTAL_MS` 走完） | 双向（用于对齐"两屏什么时候开始显示真实数据"） |
| `0x06` | 模式切换 | **占位**：仓库里**没有**运行期"模式"这个概念（`ui_model.h` 的 `center_mode` 恒 1、`center_phase` 只是历史遗留的 2 秒相位，两者都没有消费方） | **v1 不做**（`0x06` 号段保留、不许挪作他用；将来要做再定载荷 —— owner 裁决，2026-09-22，原 L9） |

**"单一日志出口"在 v1 怎么落地**

- 从板**不往链路里写文本日志**（沿用那节的约束：这条 UART 是数据面）。从板的**结构化**状态由 `0x30`/`0x40` 带上来，主板把它们转成**自己 USB-C 上的一行**（建议前缀区分，例：`link: B uptime=… rx_ok=… dropped=… crc=… face=…`）。
- ⇒ **车上只看主板那一根 USB-C，就能同时看到 A（主板自己，靠既有的自检/`SRC`/`van:` 几行）与 B（从板）的关键状态** —— 这就是"单一日志出口"在 v1 的实现方式。
- 从板**自己的** USB-C 照旧可用（插上线就能看它的原始文本日志）；要让从板的**文本**日志也进同一根线，需要一条限速的 `LOG` 帧（`TYPE 0x50`，**v2 候选**，§6）。
- 反方向（A → B）**不需要**日志汇总。

**为什么 `DATA` 就这四个字段**

- 事实：面谱与渲染实际消费的只有 **4 个标量** —— `speed_kmh`、`rpm`（表情 + 主弧 + 大数字）、`coolant_c`（左屏副弧 + 数字）、`intake_c`（右屏副弧 + 数字）。`VehicleState` 里还有 `fuel_pct` / `gear` / `ign`，但 `fuel_pct` 只被拷进视图、**没有任何消费方**（`dash_ui.cpp` 不引用它），`gear` 的注释写着"屏不显示挡位（原表负责）"，`ign` 恒 `true`。
- 建议：v1 **不发** `fuel_pct` / `gear` / `ign`，也**不发** VAN 帧里的 `data[3]`（未知）、`data[4..5]`（里程/位移累计量）、`data[6]`（帧序号）—— 前三个没人消费；后三个当前也没人消费（取舍见 `data_service.h`）。将来要油量时，**加一个新 `TYPE` 或给 `DATA` 加尾巴**（次版本 +1，§2 的规矩允许），而不是现在先塞进来。
- 丢帧可见性：`DATA` 不带序号，丢帧从两处看 —— `TICK.seq` 跳变、以及"主板发出的 `DATA` 条数"对"从板报的 `frames_ok`"（粗判，不一一对应）。
- ★ **实现侧的一个已知缺口（本节只记不做）**：`data_service.h` 的 `FieldSource` 只有 None / Sim / Obd / Van 四档，**没有"主机 / 链路"这一档** —— 从板要把收到的 `DATA` 喂进它自己的 `data_service`，得先加一档（或绕过它、直接写视图）。协议本身不受影响（`flags` 那 2 位编码够用），但两位固件作者都要知道这件事。

### 4. 时基与超时

- **主板 tick 的形式（事实 + 建议）**：`tick_ms` = 主板**自己的单调毫秒**（`millis()`，从复位起算），32 位、49.7 天回绕；每帧带 `seq u8`（回绕/丢帧可见）；**校验由整帧 CRC 负责**（覆盖 `VER`..载荷，§2）⇒ 收到一个 `tick_ms` 就能信它。
- **从板怎么估偏移**：收到 `TICK` 时算 `d = (int32_t)(tick_ms - local_ms)`（有符号减法天然处理回绕），对 `d` 做**滑动最小 + 一阶低通**（串口单向延迟只有正抖动，取最小是标准做法）。目的只是让**两屏的相位一致**，不是绝对时间。
- **跨屏动画同步的用法**：从板 UI 就绪后，**等到下一个 tick 栅格**再启动自己的扫表动画（建议栅格 `tick_ms % 100 == 0`）⇒ 两屏扫表起点差 ≤ 一个 tick 周期（20 ms）。开机动画本来就**不受 5 Hz 渲染节流**（按主循环刷），所以它是唯一真正需要对齐的动画。★ 前提（owner 裁决，2026-09-22，原 L8）：两块板刷**同一份 `theme.json`** —— `BOOT_*` 就在主题里，两板不一致则扫表时长不同，这个对齐没有意义。
- ★ **目标精度：对同一 tick 的两板估计偏差 < 40 ms**（= 25 fps 一帧；这个量级的跨屏错位人眼看不出来）。**这是目标，不是实测** ⇒ **待确认 L5**（怎么确认：两板各打一行"扫表起点"的本地时间戳做对比，或同拍视频/在两屏同一位置放同相位图案拍照数帧）。
- **从板等不到主板 ⇒ 必须能自跑**：三级超时（`TICK` 那行：100 ms 失基准 / 500 ms 降级 / 3 s 回退 Sim）。从板有**自己的** `data_service`（只有 Sim 可用）、自己的 LVGL/主题/图片 ⇒ 主机断流**不等于黑屏**（沿用"行车中断线不黑屏"）。
- **主板等不到从板 ⇒ 降级但不停机**：只降级**日志**（一行 + 计数），**不改发送节奏、不停机** —— 主板的左屏本来就不归它渲染，它没有任何"因为从板不在而做不了"的事。屏上要不要加"从板离线"角标 ⇒ **v1 不做**：从板在线状态**只进 `STATUS` 日志、不上屏**（owner 裁决，2026-09-22，原 L11）。`STATUS` 超时门限取 **30 s**（不是 5 s），且**只用于日志、不上屏**（owner 裁决，2026-09-22，原 L13）。

### 5. 角色判定：编译期 env 与运行期自检各占什么位置

- **编译期 env = 唯一权威**（建议）：在既有显示 env 之上再分两个角色 env（都 `extends = env:esp32s3`、打开 RGB 驱动、**关掉 UART0 日志**、各自 `-DLINK_ROLE=1/0`）。
  ★★ **2026-09-25 已落地**（`platformio.ini` 新增两个 env，**只加两条 `-D`**、其余全部继承）：
  | 角色 | env | 关键 `-D` |
  |---|---|---|
  | 主板（A / 右） | `env:esp32s3-rgb-master` | `-DLINK_ROLE=1` + `-DLINK_PHY_UART=1` |
  | 从板（B / 左） | `env:esp32s3-rgb-slave` | `-DLINK_PHY_UART=1`（角色走 `link_role.h` 的**默认值 0**，**故意不写** `-DLINK_ROLE=0`） |
  - 两个 env 都 `extends = env:esp32s3-rgb` ⇒ 都是 **2.8C 显示固件**（面板 / 守护 / 蜂鸣器 / 诊断页全在）；
  - ★ 它们各自在**继承来的那串 build_flags 之后**再写 `-DLINK_PHY_UART=1`，把 `[env:esp32s3-rgb]` 里那条 `-DLINK_PHY_UART=0` 覆盖回来（`-D` 的合并语义是**后者覆盖前者**）——
    于是两个角色都**不可能**与日志抢 43/44：`dash_log.h` 的 `DASH_LOG_UART0` 见到**数值非 0** 的 `LINK_PHY_UART` 就置 0，而 `link_phy_uart.cpp` 那道 `#error` 会拦住任何"把日志又放回 UART0"的构建；
    ★★ **2026-09-25 实测修正**：`[env:esp32s3-rgb]` 原来写的是 `-ULINK_PHY_UART`，而**它不生效** —— `-U` 与 `${env:<父>.build_flags}` 挤在同一串里时，PlatformIO 解析 build_flags 会把 `-U` **丢掉**（`-D` 照收）⇒ 那份显示构建（以及所有 extends 它的）里 `LINK_PHY_UART` 一直是 1。它在 2026-09-24 没闯祸，只因为那时真 PHY 只在 `LINK_ROLE==1` 那一支里编、而显示构建的角色是 0；**两个角色都接真 PHY 之后它立刻变成真问题**（43/44 被日志与链路两边抢）。⇒ 现在改成显式 `-DLINK_PHY_UART=0`，并且**全仓库的判据一律写成 `#if LINK_PHY_UART`（数值）而不是 `#if defined(LINK_PHY_UART)`** —— "定义了但为 0"与"根本没定义"必须同义。
  - ★ 代价（**必须知道**）：这两份镜像的 Type-C / CH343P（`COMx`）上**一个字都不打** —— 日志与文本回放全走**原生 USB-CDC（GPIO19/20，只在 12PIN 上）**。台面上怎么同时拿到电和日志，见 `docs/LINK-TWO-BOARD.md` §2；
  - ★ 抓帧盒三个 env（`esp32s3` / `-vaninv` / `-vansniff`）与 `espressif32@7.1.3` 的钉法**一个字都没动**；`[env:esp32s3-rgb]` 本身也没动。
- ★★ **两个角色都带真 PHY**（2026-09-25 收口，本节的"从板 = 空壳"已作废）：
  - v1 要**双向**（§0 那条"本节对「接线定案」的一处新增"：B→A 的 `STATUS`/`EVENT`），所以"只有主板有 PHY"那种做法只能验下行；
  - 现在两侧用的是**同一个 `LinkPhyUart`、同一组脚（43 发 / 44 收）、同一个 115200 8N1** —— 契约 §0 的引脚口径本来就不分角色（`lib/link/link_phy_pins.h`）；
  - 门是**既有的** `LINK_PHY_UART`（不是 `LINK_ROLE`）：有它 ⇒ 真 PHY；没有 ⇒ `LinkPhyNull`（空壳，落在 pcpreview 与 `esp32dev` 那一档，语义是"链路静默、不卡主循环"）；
  - ★ **`LINK_ROLE` 仍旧是编译期唯一权威**：它决定"发什么、收什么怎么消费"，**不**决定"有没有 PHY"；
  - ★ 从板侧多了一行排水（`g_link_tx.pump()` + `g_link_phy.pumpTx()`）：v1 的 `DATA`/`TICK` **只由主板发**（§3），所以从板那个环今天恒空、这一调用是空转（不碰 UART）；留着它是为了"将来从板要发 `STATUS`/`EVENT` 时，改的是谁 enqueue、不是谁排水"；
  - ★ **回放口也跟着改了**（同一条判据）：`van_replay_poll()` 原来无条件从 `Serial0` 收文本回放行，那在"43/44 = 链路"的构建里就是**抢**（会把链路帧的字节当文本吃掉）⇒ 现在那一支由 **`DASH_LOG_UART0`** 一起看着，UART0 归链路时它**一行都不存在**。带链路 PHY 的构建里文本回放只走原生 USB-CDC。
- **运行期自检只报警、绝不改角色**：
  ① **角色冲突**：收到**对端 `ROLE` == 本机 `ROLE`** 的帧 ⇒ `role_conflict = 1` + 一行日志；该帧丢弃（两块板刷了同一份固件时，这是唯一能自证的信号）。**v1 定案：丢帧 + 报警**（owner 裁决，2026-09-22，原 L12）；★ **"只收不发"不必再等 L1**（L1 已于 2026-09-23 关闭：43/44 上不会对打，见 §8）⇒ **维持"丢帧 + 报警"，不升级**（软件层已够；真要两块同角色互发，也**不是**电气风险）。
  ② 主板侧：`-DVAN_PHY_GPIO=1` 但 `van: edges=` 恒 0，同时又收到了 `DATA` 帧 ⇒ 打一行"本机像从板"的提示。
  ③ 从板侧：本地看到 VAN 边沿（说明这块板才该是接了收发器的主板）⇒ 打一行提示。
- **v1 不追求"同构固件"（明确建议不做）**，理由：
  · 同构需要运行期探测"这块板接了 VAN 收发器吗"来定角色，而**探测失败（收发器没接/坏了/极性不对 ⇒ `edges=0`）与"这是从板"长得一模一样** ⇒ 最坏情况是两块都自认从板（都不发数据、两屏都停格）或两块都自认主机（都在发 `DATA`、左盘没人渲染）。
  · 编译期分叉的成本是**零**（同一份源码、一个宏两处分支），换来的是"角色永不猜错"。
  · **留位**（将来真要合并成一个 env 时需要的三样，今天就能定）：① 协议**每帧带 `ROLE`**（§2 第 4 字节）；② `HELLO` 里交换 `fw_ver`/`ROLE`；③ 把所有"我是谁"的判断收敛到**一个函数**里 —— 将来把它从宏改成变量，协议与帧格式都不用动。

### 6. v1 明确不做（各一句说明为什么留到以后）

| 不做 | 为什么留到以后 |
|---|---|
| 素材（主题 / 图片）流式推送 | 一张 192×192 表情 = **108 KB**（存储账在「最终显示架构」那节），115200 下要 **9.6 s** —— 现在两块板**各刷各的** `theme`（16KB）/`image`（8MB）分区就够了，车上不该为素材占链路 |
| 单 USB 口透传烧录从板 | 从板刷机走它自己的口就行（esptool 要真 EN/IO0 线，见 `platformio.ini` 的串口那段）；把"刷写"和"VAN 硬实时解码"绑在同一根线上，收益为零、风险不小 |
| 跨板共享内存 / 大 buffer | 两板之间只有**一根 UART**，没有共享总线；真要搬大块，先问"这块数据该不该在从板上" —— v1 的答案是只需要 **4 个标量 + 状态** |
| 图像/帧传输（把主板的画面送过去） | 从板有**自己的** LVGL 与屏：480×480×2 B = **450 KB** 一帧，115200 下要 **~40 s/帧**，方向本身就反了 |
| 从板文本日志转发（`TYPE 0x50`） | 11.5 KB/s 的文本会吃掉大半带宽（§1.1 的留量）；v1 的"单一日志出口"已由结构化 `STATUS`/`EVENT` 满足，真要看从板文本就插它的 USB-C |
| 版本协商 / 降级到"共同版本" | v1 只做**上报 + 不匹配告警**（§2）—— 两块板的固件本来就是同一批刷的，"协商"是给不同团队各自升级准备的复杂度 |
| 多主机 / 多从机 / 地址字段 | 就是**两块板、一条线**（那节已定）；加地址等于现在就为不存在的拓扑付解析成本 |

### 7. 失败模式表

| # | 失败 | 现象（看哪里） | 兜底行为 |
|---|---|---|---|
| 1 | **链路断**（`TX`/`RX` 任一根断或接触不良） | 从板：>100 ms 无 `TICK` ⇒ 时间基准失效；>500 ms ⇒ 降级；>3 s ⇒ 回退 Sim（表现是**弧停在某个位置不动**，不是黑屏）；主板：30 s 后 `link: 从板无响应`（只进日志） | **两块板都不停机**：主板右盘照常（VAN 数据是它自己的），从板左盘照常显示（>500 ms 降级 = **冻结最后值 + 表情退常态**，owner 裁决 2026-09-22，原 L10；>3 s 仍没恢复则回退 Sim）；两行判据：主板的 `link:` 与从板自己 USB-C 上的 `link:` |
| 2 | **版本漂移**（两板固件不同批） | 双方各一行 `link: ver 不匹配`；`STATUS.flags.ver_mismatch = 1` | **不断链、不降级**：已知 `TYPE` 照解，未知 `TYPE`/越界 `LEN` 丢帧计数（§2） |
| 3 | **上电顺序**（从板先 / 主板先） | 从板由主板 5V 供电 ⇒ 通常主板先起；若从板先跑，它按"等不到主板"自跑 | 主板起来后**自动恢复**（无需重启）。主板复位**不切 5V** ⇒ 从板不掉电，只是链路静默一段时间 |
| 4 | **从板无响应**（挂了 / 没刷 / 接线错） | 主板 **30 s** 一行 `link: 从板无响应` + 计数；从板那条 USB-C 什么都不打（没上电） | 主板**降级但不停机**：右盘照常 + **照常发数据**（从板随时可能回来）；屏上**不加**角标（v1 不做，owner 裁决 2026-09-22，原 L11） |
| 5 | **主板重启**（车上没有 USB 主机） | 从板立刻进入"等不到主板"那三档；主板起来后 `HELLO`/`TICK` 自动恢复 | 同上；若是**整车断电**（拔 12V / 关开关），两块一起掉电、一起扫表 —— 这是预期的，不是故障 |
| 6 | **角色刷反**（两块刷了同一份固件） | 收到 `ROLE` 与本机相同的帧 ⇒ `role_conflict = 1` + 一行日志；两块都在发 `DATA` | **丢帧 + 报警**（owner 裁决 2026-09-22，原 L12）；★ **不升级为"只收不发"** —— 原来那条"等 L1 的结论"已随 L1 关闭（2026-09-23：43/44 上不对打，两块同角色互发**不是电气风险**） |
| 7 | **5V 供电不足**（从板 + 480×480 背光峰值） | 从板 brownout 重启 ⇒ 反复扫表；主板收到的 `STATUS.uptime_ms` **反复归零**（这个字段就是为它准备的） | 主板不受影响；修法是加大 5V 支路 / 降背光（`dash_display_spi.cpp` 文末有 `set_backlight` 的建议接口）；电流上限的**静态那一半已定、实测仍待确认（L6）** |
| 8 | **从板收到半截帧 / 误码**（接触不良、共地不好） | `STATUS.crc_err` 涨；若一直涨但每帧都能重新同步 ⇒ 只是误码 | 丢帧计数 + 继续找下一个 `SYNC`；**不清空已显示的数据**（宁可显示旧值，也不要闪 0） |

### 7.5 ★★ 可靠性约定：**主循环不许被任何输入堵死** + **蜂鸣器必须有绝对上限**（2026-09-25 新增）

> 这一节的两条都是**产品要求级**的（与 §3.5「仪表盘必须常亮」同一个地位）：
> 一条管"**表不许停**"，一条管"**响声不许停**"。

#### 7.5.1 现场（车主原话，逐字）

> **"现在会长鸣一会儿，画面也卡住了"**（2026-09-25，新板 `COM8`，从板镜像）

**这一句把两件事锁成了同一件**：这块板上的蜂鸣器是**软开关**（写 `TCA9554` 的
`EXIO8`，**没有硬件定时**，见 §4.2）⇒ "到点关"那个动作**只在主循环转得动的时候**
才被执行；而画面也在主循环里渲染 ⇒ **画面冻住 + 响声不停 = 同一次"主循环被堵死"**。
★ 这一条与 §3.5.1 那条"黑屏 ≠ 卡死"是**互补**的：那一条说"面板黑掉时主循环还在跑"，
这一条说"主循环不跑时画面定格（但面板照旧在扫那块 framebuffer，**不会黑**）"。

#### 7.5.2 约定 ①：主循环单圈的收帧量**必须有上界**

* **判据**：链路收帧那一圈最多吃 `kLinkRxBytesPerLoop = 512` 字节
  （`src/main.cpp`），判据是 `LinkRxStats::bytes_read` 的**差值**。
* **为什么不能用"解出了几帧"当判据**：悬空 RX 脚上的形态是**一直在读、一帧都没解出来**
  （噪声字节里只要夹着一个 `SYNC`(0x5A)，后面那串连 `noise_bytes` 都不涨）
  ⇒ native 用例 `test_link_rx_bytes_read_counts_every_phy_byte` 把这条差异钉住。
* **为什么原来的写法没有上界**：`LinkRx::poll()` 自己的预算（默认 64 B）只封住
  "**一次调用**"；主循环用的是 `while (poll(...))` ⇒ **一圈**能吃多少字节由**线路速率**
  决定，不由我们决定。
* ★★ **一个实测出来的坑（差点写上板）**：`poll()` 返回 `false` 有**两种**意思 ——
  ⓐ "PHY 现在没有字节了"；ⓑ "**这一次调用**的预算用完了"。把 ⓑ 当 ⓐ 就退出
  = "每圈最多读 64 字节" ⇒ 连续字节流下环里**永远排不干净**（`rxLeft` 只增不减），
  症状是"链路看着像断了"。正确形状（`src/main.cpp` 的 `link_poll_bounded_*()`）：
  `false` 之后再看 `pending()`（缓冲里还有没解完的）与 `phy.available()`（环里还有货）
  —— 有一个为真就继续；两个都空才是"这一圈收完了"。
  native 用例 `test_link_rx_poll_false_budget_is_not_empty` 与
  `test_link_rx_per_loop_budget_leaves_rest_for_next_loop` 钉住这两半
  （**有界**，但**一帧不丢**）。
* **上界的取值**：512 B ≈ 115200 线速下的 **44 ms**。正常流量（主板 ≈80 Hz 的
  TICK/DATA，十几个字节/圈）离它差一个量级 ⇒ 它只在"线路出问题"时生效。
* **RX 脚上拉**（`lib/link/link_phy_uart.cpp` 的 `pinMode(rx, INPUT_PULLUP)`）：
  悬空输入 + 旁边 15 MHz 的 RGB 并口 = **持续伪字节流**；上拉把它钉在空闲高电平。
  ★ 只配 RX、**绝不把 43/44 配成输出**（`ARCHITECTURE §8 L1` 的残余 ⓒ 就是这条）。

#### 7.5.3 约定 ②：蜂鸣器有**绝对上限**（`kBuzzerSafetyMs = 2000`）

* **语义（只有一条）**：任何一次 `beep()` 起表之后，**从起表那一刻算起**墙钟超过
  **2 秒**还在响 ⇒ **无条件关掉 + 丢弃序列**（`BuzzerExio::safety()`，由主循环
  **每圈独立调**一次 —— 刻意**不写在 `tick()` 里面**：写在里面就防不住
  "`tick()` 压根没被调用"这种病，而车主遇到的**正是**这种病）。
* **它与既有那条"单次哔 ≤ 300 ms"（§4 第 2 条）的分工**：那条管**一段高电平**有多长
  （防候选机制 B：持续高电平把 3.3V 轨拉低）；这一条管**整条序列**多长
  （防"主循环停住 ⇒ 到点关没被执行"）。两条**都要在**。
* **2 秒这个数怎么来的**：上限必须**严于最长的一拍**（`Urgent` = 4×300 + 3×80 =
  **1440 ms**）⇒ 合法序列永远不会被误掐；又要**短到人耳听得出这是"卡死"而不是"告警"**。
* **判据**：native 三条（`test_buzzer_exio_safety_is_the_absolute_cap` /
  `..._safety_ignores_state_machine` / `..._safety_does_not_cut_legal_pulses`）
  + 串口每 5 秒那行尾部 `| buzz safety=N`（`N` 不为 0 = **真的发生过**"到点关没被执行"）。
  ★ 这个读数比屏上现象更硬：它把"主循环停过"变成**一个计数**。

#### 7.5.4 取证：主循环停顿探测（`loop: stalled` / `loop: n=…`）

* **每圈**记 `millis()` 差值；**≥ 200 ms** 就当场打一行：
  `loop: stalled <N>ms (n=<这一窗口第几圈> link rx bytes=… crc=… bad_len=… noise=…) probe_took=<打印这一行花了几微秒> prev=<本次复位原因>`
* **每 5 秒**一行**摘要**：`loop: n=<圈数> in <ms> (<圈/秒>) max=<最长的一圈> stall=<超阈值的圈数> | link rx bytes=… frames=…`
  ★ 摘要那一行是**刻意**加的：没有它，"有没有卡过"只能靠"**没看见** WARN 行"来推断，
  而那是**无法证明**的；有 `max=`/`stall=` 就是**正面读数**（`stall=0` 是有证据的 0）。
* **阈值为什么是 200 ms**：本构建有两处**合法**的长圈 —— 开机第一次整屏刷新
  （实测 **2535 ms**）与每 30 ms 一次整屏失效那一拍的重绘（实测 141~225 ms）⇒
  阈值压到几十毫秒会把正常路径打成噪音。而"卡死"是**秒级到永久**的。

#### 7.5.5 ★ 还没做：任务看门狗（Task WDT）—— **方案写在这里，默认不开**

**为什么没开（如实写）**：它要的判据是"**主循环多久没喂狗**"，而本机上**合法**的长圈
已经实测到 **2535 ms**；在"多长的圈算合法"这件事被量清楚之前打开它，风险是
**把一次正常的长圈变成一次重启**（那比"偶尔卡一下"更糟，而且会让人误以为是新的病）。
⇒ 本轮只做**探测**（7.5.4），把"到底会不会卡、卡多久"变成读数。

**要做的时候怎么做（照这个顺序，别跳步）**：

1. 把 7.5.4 的 `loop: max=` 读数**攒够**（跨几次上电、含开机那一次整屏刷新），
   得到"合法长圈"的上界 L（今天已知 L ≥ 2535 ms）。
2. 超时取 **max(2×L, 5 s)** —— 今天就是 **5 s**。
3. **只在主循环里喂狗**（`esp_task_wdt_add(nullptr)` + 每圈 `esp_task_wdt_reset()`），
   而且**在 `setup()` 末尾才订阅**：面板初始化那 41 步、主题/图片加载、第一次整屏刷新
   都在 setup 里，订阅早了它们会被当成"卡死"。
4. 这段代码写成 `#if DASH_TASK_WDT`（**默认 0**），并且**不新增 platformio env**
   （与"能不新增 -D 就不新增"同一条纪律）。
5. 打开之后**必须**上板看一眼：`boot: reason=… prev=…` 里出现 `TASK_WDT` = 它真的救过；
   而 `prev=POWERON` 一直不动 = 没被误触发。两边的读数都要留档。

#### 7.5.6 ★★ **根因（2026-09-25 当天补记）**：主循环会**阻塞在日志那一行**上

**这不是推测，是时间线算出来的**（同一块板、同一次上电、同一份镜像）：

| 时段 | 谁在读 `COM8` | 主循环读数（探测行原文） |
|---|---|---|
| 0 ~ 200 s | 采集脚本**正在读** | `loop: n=… (28862~36819/s) max=30~160ms stall=0` × **40 个窗口** |
| 200 s ~ 228 s | **没人读**（脚本退出、口关着；`tick_age` 从 195011 涨到 391634 ms） | —— |
| 228 s（重新打开串口） | 刚打开 | `loop: n=1 in 20120ms (0/s) max=20120ms stall=1` |
| 229 s | 在读 | `loop: stalled 7689ms (n=1 … link rx bytes=0 …) probe_took=0us` |
| 230 s 起 | 在读 | `loop: n=156442 in 5000ms (31288/s) max=126ms stall=0`（恢复正常） |

★★ **两段读数之间的算术把根因锁死了**：

* **没人读的那 28 秒里，主循环一共只跑了 2 圈**（两个窗口各 `n=1`）；
* 那两圈的时长分别是 **20120 ms** 与 **7689 ms**（合计 **27.8 秒** ≈ 整个窗口）；
* 而**同一次上电、同一个固件**在前 200 秒里能跑 **3.3 万圈/秒**；
* 那 28 秒里唯一**变了**的东西是"**USB 主机在不在读**"；
* 旁证（同一个文件里，`tick_age` 那一行）：两次读数的 `tick_age` 从 **391634** 涨到
  **399323** —— 差值 **7689 ms**，与那一次 `stalled 7689ms` **逐毫秒对上**
  ⇒ 那 7.7 秒里**主循环真的没有完成过任何一圈**（连 5 秒一条的 `link: sim …` 都没打出来）。

⇒ **结论：主循环会阻塞在 `dash_logf()` 的 `Serial.write()` 上**（`lib/dashcore/dash_log.h`
→ `HWCDC::write`），而 `dash_logf` 是**同步**调用的 —— 它一慢，"到点关蜂鸣器"那一步
就跟着慢。**车主那句"长鸣一会儿，画面也卡住了"的机制就是它。**

★ 与 `com8` 那条已知口径的关系：`HWCDC::write` 的默认 `tx_timeout_ms = 100`、
`max_consec_timeouts = 20` ⇒ 单次调用最坏 ≈ **2 s**（`cores/esp32/HWCDC.cpp`）。
实测到的 20 s / 7.7 s 说明"没人读"那一段是**连续多次**写都撞在超时上
（每 200 ms 一条状态行、每 5 秒一轮好几行 ⇒ 一路排队）。
★ 本轮**没有**在真机上把单次 `Serial.write()` 的耗时单独量出来（要串口在手里、
而"没人读"这个条件本身没法在**有人读**的时候复现）⇒ 这一条写在 ⑧ 里，不吹。

**为什么本单的三条防线正好对着它**（这也是它们现在的**排序理由**）：

| 防线 | 对这件事的作用 |
|---|---|
| **蜂鸣器 2 秒绝对上限**（§7.5.3） | ★ **直接兜住后果**：不管主循环被什么堵多久，高电平最多留 2 秒 |
| **停顿探测**（§7.5.4） | 把"堵了多久、堵在哪一段"变成**一行原文**（本单就是靠它算出来的） |
| **收帧有界 + RX 上拉**（§7.5.2） | 管的是**另一条**输入路径（悬空 RX 脚的伪字节流）—— 本单实测 `link rx bytes=0`，所以它**不是**这次的根因，但两条路都要封 |

**下一步（还没做，动之前先读这段）**：真正的修法是让**日志那一行也不再是同步阻塞**的。
三条候选（按"改动面小 / 风险低"排）：

1. **关掉 USB-Serial-JTAG 的 TX 阻塞**：`usb_serial_jtag_driver_config_t.tx_buffer_size`
   > 0 时 IDF 走**驱动模式**，把 TX 变成"有界缓冲 + 满了就丢"，而不是同步写
   （`usb_serial_jtag_write_bytes()` 从"等到写完"变成"写入缓冲"）。
   ★ 代价：**日志会丢**（没人读时丢旧的/新的），而"日志丢"对仪表盘是**可接受**的代价。
   ★ 必须写进文档：`USB_SERIAL_JTAG` 驱动装不上时 `Serial` 会**静默失效**，
   那就是"串口一片空白"——又回到 2026-09-18 那个坑，所以要**先验再上**。
   ★★ **本条仍未实施**（owner 明确要求本单只写方案）⇒ 方案、风险、退回步骤见
   **§7.5.7**。
2. **给日志加"有界节流"**：主循环路径上的 `dash_logf` 一律"每 N 秒最多一条"，
   或者把高频那几行挪到**低频的**那一档（`rgb:` 每秒那行本来就是诊断用的）。
   ★ 这条不改任何驱动行为、风险最低，但**治标**（写得少 ⇒ 堵得少）。
   ★★ **本条已做**（2026-09-26）：`206 dash ok` 与两行 `rgb:` 降到每 2 秒一条，
   见 §7.5.7 与 `src/dash_ui.cpp` 的 `g_ok_gate`。
3. **把日志移出主循环**：单独一个低优先级任务消费环形缓冲。
   ★ 改动面最大（`dash_logf` 要变成入队），且"任务里写 UART"与 §1.2 那条
   "不在 ISR/回调里发"的纪律要重新对一遍。
   ★★ **本条已做，但形态是"主循环里按预算排空"、不是"另起一个任务"**（2026-09-26）：
   理由写在 §7.5.7（最要紧的一条：多一个任务就多一处并发，而这里要的是"更少的
   不确定性"）。**环 + 入队**那一半完全按本条做（`dash_logf` 只进环、绝不写串口）。

★ 在做 1/2/3 之前，**7.5.3 那条 2 秒上限必须一直在**（它已经把"永远响"变成"最多响 2 秒"）。

#### 7.5.7 ★★★ 硬约定：**日志绝不许阻塞主循环**（2026-09-26 落地）

> 本节是 §7.5.6 的**修法**。7.5.6 量出了根因（没人读 `COM8` 时主循环 28 秒只转 2 圈），
> 本节把它**做掉**，并把"不许再犯"写成一条**硬约定**。

**① 约定本身（一句话，写在这里，改动日志那一层之前先读它）**

> **`dash_logf()` 不许有任何可能等待的调用。**
> 它只做两件事：格式化进环形缓冲、返回。环满就**丢整行并计数** ——
> **宁可丢日志，也不能堵主循环。**
> 真的写串口只有一处：主循环末尾的 `dash_log_drain()`，它**按预算**、
> 而且**写之前先问端口能不能写**（`availableForWrite()`；报 0 就一个字节都不写）。

★ 「丢日志」是**有意的取舍**，不是缺陷：日志是**诊断**通道，不是数据通道
（链路帧、车状态快照、蜂鸣器时序一个字节都不走这里）。诊断的价值在"有读数"，
不在"一条不漏"。**谁要改这条，先解释为什么"主循环卡住"比"少几行日志"更可接受。**

**② 怎么做的（`lib/dashcore/dash_log.h`，一个文件收口）**

| 环节 | 形状 | 为什么是这个形状 |
|---|---|---|
| **入队**（`dash_logf`） | `vsnprintf` → `Ring::write()`（纯内存拷贝） | ★ 环里**只有**整行放得下时才写；放不下**整行丢**+计数。**半行会把剩下的日志结构打乱**，排障时最费时间的就是这个 |
| **环** | `dashlogring::Ring`，`DASHLOG_RING_BYTES = 8192` 字节，静态内存（不碰堆） | 8 KB 够放 ~80 行的突发（实测就是"刚开口那一下"的补投量，见下面那两条）。★ 它**不是**"没人读时攒日志的地方" —— 没人读时它根本不涨（同见下） |
| **排空**（`dash_log_drain`） | 主循环**末尾**，`DASHLOG_DRAIN_BUDGET = 512` 字节/圈 | 与链路收帧那条 `kLinkRxBytesPerLoop = 512` **同一个理由**："一圈的工作量必须有上界"。115200 线速下 512 B ≈ 44 ms；"有人读"时 USB-CDC 是 MB/s 级 ⇒ 正常路径永远碰不到这个上限 |
| **"能不能写"** | `Serial.availableForWrite() > 0`（HWCDC 的 TX 环剩余空间） | 这是**唯一**能区分"写下去是微秒级"和"写下去要撞 100 ms × 20 次超时"的读数（见 §7.5.6 引的 `cores/esp32/HWCDC.cpp`）。报 0 ⇒ 一个字节都不写、**不重试** |
| **可观测** | 5 秒摘要行尾部多了 `log drain=… drop=… dropped=… blocked=… ring=…/… hwm=…` | `drop=`/`dropped=` = 这一窗口丢过多少**行**/**字节**；`blocked=` = 有多少圈撞上"端口报满"（**这是设计在工作，不是错误**） |
| **节流**（`dashlogring::RateGate`） | `206 dash ok` 与两行 `rgb:` 从 1 Hz 降到 **2 秒一条**（`g_ok_gate` / `rgb_log_gate`） | 治标那一半：写得少 ⇒ 堵得少。★ **只对周期性遥测**；守护 / 蜂鸣器 / 告警 / 诊断页 / 链路状态变化那几行**一律不许挂闸门**（它们的价值就在"变化那一刻"） |

★ **为什么不做"另起一个低优先级任务来排空"**（§7.5.6 候选 3 的原始形态）：
排空这一步**已经在按预算做**，把它挪进任务**不减少**任何一次写；
却要新增一处并发（"谁先动 `Serial`"、任务栈、优先级反转）——
而这条路上要的正是"**更少的不确定性**"。⇒ 环 + 入队的形状照抄，
"谁排空"留在主循环里（这一步是纯内存拷贝 + 一次 `availableForWrite()` 查询）。

★★ **实测出来的两条"环其实什么时候才会满"**（2026-09-26，原报告在
`docs/RGB-PANEL-2.8C.md` §18；**它决定了"丢日志"这件事的代价到底有多大**）：

1. **没人读的时候环并不涨**：设备很快就能判定"端口写不出去"
   （`availableForWrite()` 报 0 / `isCDC_Connected()` 为假），
   于是 `drain()` **一个字节都不写**就返回 —— 代价只是 `blocked=` 这个计数在涨。
   ★ 实测：把口关掉 90 秒，`log drain=` 那 90 秒里**一个字节都没动**
   （40542 → 51246 全部发生在**重新开口之后**的几秒内）。
2. **真正把环填满的是"刚开口/刚恢复"那几秒**：一波补投 + 节流前的积压
   （8 KB 的环一次就满）。
   ★ 实测：90 秒里设备一共产生了 **≈37 KB** 日志（10.7 KB 排出去 + 26.8 KB 丢掉），
   而 `drop`/`dropped` 的**增长全部落在开口后的第一个统计窗口**里。

⇒ 于是"丢日志"的实际代价是"**过渡那一下丢一批**"，而不是"全程都在丢"。
这也解释了为什么 `hwm` 停在 **8189**（8192 的环在剩 3 B 时已经放不下任何一行）。

★ **UART0 那一路**（只在 `DASH_LOG_UART0 = 1` 的构建里存在）**不进环**：
它由 `dash_log_drain()` 顺带发一份，同样先问 `Serial0.availableForWrite()`，
报 0 就丢这一份。理由：UART0 那条路只在"有人在 43/44 上收"时才通，
那种场合它的 `availableForWrite()` 是可靠的；把它也塞进同一个环会让
"环里排的到底是哪个口"变糊。

★ **与 §7.5.3 的关系没变**：那条 2 秒蜂鸣器上限**继续留着**。
它防的是"到点关那个动作没机会跑"，与本节的"日志不再制造长圈"是**两层**：
本节治因，§7.5.3 兜底。**两条都要在。**

**③ 第二步（★ 本单只写方案，未实施）—— 换 USB-Serial-JTAG 驱动模式**

> 对应 §7.5.6 候选 1。**本单没有实施**（owner 明确要求先报）。
> 它现在**不是必需的**：① 已经把"没人读 ⇒ 卡死"这条路堵死了（实测见 §7.5.7 ④）。
> 它的价值是"更上一层"：让**驱动自己**在没人读时丢字节，从而**连 `drain()` 这一步
> 的 `availableForWrite()` 查询都不需要**。所以它属于"锦上添花"，不是"救火"。

**要改什么**：把 `Serial`（HWCDC）从当前的"同步写 + 内部超时"切成 **IDF 驱动模式** ——
`usb_serial_jtag_driver_config_t{.tx_buffer_size > 0}` + `usb_serial_jtag_driver_install()`，
之后 TX 是"写入有界缓冲，满了就丢"，`usb_serial_jtag_write_bytes()` 不再等写完。

**风险（一句话）**：**驱动装不上 ⇒ `Serial` 静默失效 ⇒ 串口一片空白** ——
症状与 2026-09-18 那个坑（"以为固件没跑，其实是没让它跑"）**一模一样**，
而那条坑当时花了很久才排掉。具体三条：
ⓐ `driver_install()` 返回非 `ESP_OK`（缓冲区分配失败/已被占用）时，若代码没兜住，
`Serial.write()` 会变成**静默丢字节**（不是报错）；
ⓑ 驱动模式与 Arduino 的 `HWCDC` 对象**抢同一个外设**：`Serial.begin()` 之后
再装驱动、或装完驱动再 `Serial.begin()`，都可能把对方掀掉；
ⓒ 装完之后 `availableForWrite()` 的语义会变（变成"驱动缓冲区剩余"），
本节的 ① 仍然成立，但**读数含义不同** ⇒ 判据行要重新看一遍。

**怎么验（照这个顺序，别跳步）**：
1. **先写一个探针镜像**（一个新 env 或临时 `-D`，**不进交付档**）：只装驱动 + 每 5 秒
   打一行"我在、`log drain/drop` 是多少"，**不动别的任何一行**。
2. 烧上去、插原生 USB 口看串口：**判据只有一条** ——
   **开机那几行必须在**（`206 dash ok` / `boot: reason=…`）。
   只要开机那几行出得来，就说明"驱动装上了、`Serial` 没失效"。
3. 再跑本单那套三情形（读 / 不读 / 复读，步骤见 `ACCEPTANCE.md`）：
   `stall=0`、`max` 正常、`log drop=` 有读数。
4. **只有 1~3 全绿**才考虑换到交付档；换的时候**逐字节比对**"开关机两段日志"，
   确认没有一行消失。

**失败怎么退回（三条，按"最快能用"排）**：
1. **首选：把那个 `-D` 去掉重新编译烧回去**（本节的 ① 是**独立**的，
   不依赖驱动模式 ⇒ 退回之后"日志不阻塞"这件事**一个字都不会退步**）。
2. 若已经进了交付档：`git revert` 那一笔提交（它应当**单独是一笔**，
   与 ① 分开 —— 这也是"为什么要小提交"的一个具体理由）。
3. 退不回去又必须看日志时：**插板载 CH343 那个口**（UART0 / `Serial0`），
   它的日志路径**完全不受 USB 驱动模式影响**（它走 `HardwareSerial`）。
   ★ 这条只在 `DASH_LOG_UART0 = 1` 的构建里成立 —— 两个链路镜像里它是 0
   （43/44 让给链路了）⇒ 那两份镜像**没有这条退路**。

★ **什么时候值得做它**：等到"① 的 `log drop=` 在正常跑车时**经常**很大"
（那说明 8 KB 环经常满），或者将来要在没人读的情况下**长时间**留日志。

### 8. L1 的残余与「接线定案」的收尾

**v1 定案（owner 裁决，2026-09-22）** —— 原 L8~L13，**编号保留**（§1~§7 里引用 L8~L13 的地方照旧对得上）：

| # | 定案 | 来源 / 落地 |
|---|---|---|
| **L8** | **两块板刷同一份 `theme.json`**（写成**规定**：两板的 `theme` 分区内容必须一致，否则两屏配色/几何/开机时长不同**而不报错**）；`HELLO` 里带 16 位 `theme_tag` 的做法**留到 v2** | owner 裁决，2026-09-22（§4 的跨屏扫表对齐依赖它；代价：刷主题要刷两块板） |
| **L9** | `EVENT 0x06`「模式切换」**v1 不做**，**号段保留**（不许挪作他用；将来要做再定载荷） | owner 裁决，2026-09-22 |
| **L10** | 链路降级（`TICK` >500 ms）时从板的行为 = **冻结最后值 + 表情退常态** | owner 裁决，2026-09-22（>3 s 仍没恢复则按 §3 回退 Sim） |
| **L11** | 主板「从板离线」角标 **v1 不做**：从板在线状态**只进 `STATUS` 日志、不上屏** | owner 裁决，2026-09-22（⇒ 本轮不动素材/角色编号） |
| **L12** | 检测到角色冲突 = **丢帧 + 报警**；是否升级为「只收不发」**等 L1 的结论**再定 | owner 裁决，2026-09-22 |
| **L13** | `STATUS` 超时门限 = **30 s**（不是 5 s），且**只用于日志、不上屏** | owner 裁决，2026-09-22（"从板单独刷机/断电"算常态 ⇒ 门限放宽） |

**仍未确认（L2~L7）** —— ★ **L1 已于 2026-09-23 关闭**（结论与依据见下），编号保留、行内划掉。
★ **L14 已于 2026-09-24 关闭**（结论见下：**发声的是 2.8C 这一块 = 右/主机板**，走
TCA9554 的 `EXIO8`，已实现并上板）—— 编号保留，行内改写成结论。
★ 其中 **L6 已于 2026-09-23 做完"静态那一半"**（依据 = 2.8C 官方原理图 + 器件手册 + 本仓库既有的耗电口径；
见下面 L6 行）—— **没有关闭**，剩下一半（上板量电流/看瞬跌）**只能上板做**，照旧留在本表。
★ **没有新开任何 L 号**：本轮的蜂鸣器落地是 **L14 的收口**，不是新的待确认项
（唯一"还没实测"的那一点 —— **第二块 2.8C 上有没有同一颗蜂鸣器** —— 写在 L14 行里，
它是"等硬件到货"而不是"等裁决"，所以**不占新号**）。

> ★ **验证现状一句话（2026-09-25 更新，别读过头）**：**单板回环已经在车主那块裸 S3 上做过并通过**
> （`linkloop: PASS`）—— 它验的是**协议层 + PHY 层**在真实 UART 字节流上站得住（见 `docs/LINK-LOOPBACK.md`）；
> ★ **两个角色镜像（`env:esp32s3-rgb-master` / `-slave`）已经落地并各自编译 SUCCESS**，
> 从板侧也接了**真 PHY**（见 §5）—— 但**两块板一次都没对打过**：
> **第二块 2.8C 还没插上**，所以"跨板"那一半（电气、共地、5V、角色对账）**全部未实测**。
> **下一验证点 = 第二块 2.8C 到货/插上之后**（主板 `LINK_ROLE=1` / 从板 `LINK_ROLE=0`、43 → 44 交叉接线）：
> 台面步骤、判据、排查顺序与"它测不到什么"见 **`docs/LINK-TWO-BOARD.md`**（本单新增）。
> ★ 而**回环 env（`esp32s3-linkloop`）不是上车固件** —— 它只是那块裸 S3 上的验证固件，
> 上车的是 `esp32s3-rgb-master` / `-slave` 这两份。

★ **另一条"尚未编号"的待确认 → 已按 v1 决定收口（2026-09-23）**：§3 表下那条 `STATUS.last_gap_ms` 的**语义**
（"最近一次帧间隔" vs "最坏间隔"）**不是**待确认了 —— 口径定为**字段保留、v1 一律发 0、将来要填需先有接收侧时钟**
（见 §3 表下那一段）。它是对**已有字段**的语义缺口、不是新的电气/时序风险，所以**照旧不占 L 号、没新开 L 号**。

| # | 待确认什么 | 怎么确认 |
|---|---|---|
| ~~**L1**~~ | ~~两块板拿 **GPIO44 当输入**时，会不会与**各自板载 USB-串口桥（CH343P）的推挽 TX 对打**~~ **✓ 已关闭（2026-09-23，官方原理图判定「不对打」）**：**不会对打 —— 43/44 上只有"二选一"，没有"两个推挽并线"**。① **器件**：板上有一颗 **`FSUSB42UMX`（U2.8C 原理图里的 `U10`）**，是 **2:1 双向模拟开关（不是缓冲器，不额外驱动）**，wiki 对它的描述就是 *"UART selection chip: when the UART Type-C is connected, the 4Pin UART is disabled; when the UART Type-C is not connected, the 4Pin UART is enabled"*。② **连接关系（原理图 U10 的 10 个脚逐个读出）**：公共端 `D+`(1)/`D-`(2) ↔ 选择端 `HSD1±`(5/4)、`HSD2±`(7/6)；两根**0 Ω**（`R38`/`R39`，图上 `USB_RXD`/`USB_TXD`）把 `HSD2±` 接到 **CH343P（`U9`）的 `RXD`(5)/`TXD`(4)**；`HSD1±` 是 **`UART_RXD`/`UART_TXD`**（即 ESP32 的 `IO44`/`IO43`，见 §0 与 wiki 的 UART/12PIN 表）⇒ **`IO43/IO44` 一次只连到 CH343P 或排针的其中一边**，所以"桥的推挽 TX 与我们的输入并线对打"这个前提**不成立**。③ **`SEL`(10) 由 `UART0SEL` 驱动**：该网络在原理图上只有三个端子 —— **`IO0`（GPIO0）、10 K（`R45`）、10 K（`R44`）**，即由 GPIO0 选边（**极性未取值：图纸文字抽取看不出哪个是上拉** ⇒ 残余待确认）。④ **CH343P 的供电**：`VDD`(3) = **3V3**（板载 `ME6217C33M5G` 的 LDO 3.3 V）、`VBUS`(9) = **Type-C 的 `USB_5V`**，`UD+`(7)/`UD-`(8) ↔ **`USB_RXD`/`USB_TXD`** ⇒ **桥是自供电（板载 3V3），不是"仅 VBUS 供电"，所以它不受"插不插那根线"控制**；真正把它从 43/44 上摘下来的是**那颗开关**。⑤ **`SEL` 的默认值决定"上电/复位瞬间谁在 43/44 上"**：GPIO0 既是 UART 选边脚、又是 **BOOT strap 脚**，复位释放时由它决定开关倒向哪边 | **出处**：微雪官方原理图 PDF `ESP32-S3-Touch-LCD-2.8C_schematic_diagram.pdf`（1 页，946,158 B，2026-09-23 抓取；元件位号 `U9`/`U10`/`R38`/`R39`/`R44`/`R45` 与网络名 `ESP_RXD`/`ESP_TXD`/`USB_RXD`/`USB_TXD`/`UART_RXD`/`UART_TXD`/`UART0SEL` 逐个读到）+ 微雪 wiki 的 Onboard Resources #4/#5 与 UART/12PIN 引脚表。**残余待确认（上板实测，见下）**：ⓐ `SEL` 的**默认电平**（复位期间哪一路在 43/44 上）与 GPIO0 动作时的**切换时刻**；ⓑ 不插那根 UART Type-C 时 **44 对地电平**是不是干净的高（串 1 kΩ 试）；ⓒ 固件侧**不许把 43/44 配成输出**（含 RGB 屏那批脚与 UART0 日志的占用，见 §0"载体"那条） |
| **L2** | 链路波特率/中断密度对主板 VAN 采集的**定量**影响（115200 vs 230400 vs 460800） | 真板上做 A/B：只收 VAN（`van: edges/frames/dropped` 基线）vs 同时跑链路，比 `dropped` 与 `frames_fcs_ok` |
| **L3** | 主板**关中断/临界区最长允许多少**（推断必须 < 一个 TS = 8.25 µs，目标 < 5 µs） | 逐级拉长掩码时间，看 `edges`/`frames` 何时开始掉（用同一份实车抓包回放做基线） |
| ~~**L4**~~ | ~~CRC-15（0x0F9D）在 v1 帧长（12..23 B）上的**检错覆盖**：已知含 `(x+1)` ⇒ 奇数位错必检出；1~2 位错与突发错上限**未验**~~ **✓ 已关闭（2026-09-22 枚举实测）** | **实测**（native 单测 `test/test_dashcore/test_link_crc_coverage.cpp`，`python -m platformio test -e native`，Windows 台式机）：帧长 **12..23 B（96..184 bit）逐个枚举** —— **1 位 / 2 位 / 3 位错未检出都是 0**（3 位错同属奇数位错，必检出；"3 位错会非零"的猜测不成立）、**长度 2..15 bit 的突发未检出 0**、**16 bit 突发 1308 个**（每帧长 `8(L-2)-15`：65/73/…/153，图案恒为 `0xB9F1`，全部落在被覆盖的数据区内；理论只保证 ≤15 bit）、**最小汉明距离 = 4**（每个帧长都有一个经真实 CRC 复核的 4 位错见证）。口径：码字 = 整帧；**SYNC 不进 CRC 覆盖**，SYNC 位错由重同步兜住，不在本枚举内 |
| **L5** | 跨屏动画同步 **< 40 ms** 这条目标能不能达到（**是目标，不是实测**） | 两板各打一行"扫表起点"的本地时间戳做对比；或同拍视频/在两屏同一位置放同相位图案拍照量错位 |
| **L6** | 主板 5V 支路**能给从板多少电流**；主板刷固件/USB 重枚举时 5V 会不会**瞬跌**（从板会不会跟着重启） | ★ **静态那一半 2026-09-23 已做（下表的读法就是结论：这个上限不由板子决定）**，剩下的是**实测**：在**主板上装好从板 + 背光 80%** 的条件下量那根 5V 的运行电流与峰值；刷机时看那根 5V 有没有瞬跌（这块板子上就是"Type-C 的 VBUS 被重新枚举"这件事）。<br>**① 出处（静态依据，逐条可查）**：微雪官方原理图 PDF `ESP32-S3-Touch-LCD-2.8C_schematic_diagram.pdf`（1 页 / 946,158 B / `SHA256 = 01EAE811F2B777B3F919108ACB48C61E79734055F517A07D516185EF26299D76`，2026-09-23 重抓、与 L1 那次同一份）；同一份图里读到：`J9` = **SH1.0 12P**，其**第 6 脚**的网名是 **`USB_5V`**，而 `USB_5V` 就是 Type-C（`J3`）的 **VBUS**（`A4/B9`、`A9/B4`）；**12PIN 那一路与 Type-C 之间图上没有任何保险丝/二极管/PTC/开关/mΩ 级采样电阻**（板上唯一的 `U1 = ETA6098` 是**电池充电管理**，走 `BAT` → `J1`(PH1.25 2P)，不在这条路上）。<br>**② 所以 L6 的第一个问题有答案了**：**能给出多少电流不取决于主板**，因为 12PIN 的 `VBus` 是 Type-C VBUS 的**直通**（Type-C 的额定电流有多大，12PIN 的 5V 就有多大）× 主板 + 从板**两边加起来**的耗电 —— 而这个上限只有**上游那个 5V 源**知道：本项目选的是 **OBD 取电降压线 5V/2A**（`PINOUT.md`「供电」+ `PURCHASE.md`：ESP32-S3 待机 40~80 mA、开 WiFi 150~250 mA、**启动瞬间 500 mA 级尖峰** ⇒ 选 2A）。<br>**③ 一头（板上限）有硬数字**：这块板的 3V3 全部由 **`U5 = ME6217C33M5G`**（`VIN`(1) 直接吃 `USB_5V`）供给，**最大输出 800 mA**（微雪 wiki `Onboard Resources` **#9**：*"ME6217C33M5G Low dropout LDO, current (Max) 800mA"*；同族手册 *"最大输出电流：800 mA (VIN≥VOUT(T)+1.0V)"*）。3V3 上挂 = ESP32-S3R8(峰值 ~250 mA) + 480×480 背光 + CH343P + 触摸/IMU/RTC/扩展器 ⇒ 两块板**各自**都远在 800 mA 之内，5 V 那一侧更是只有它的 2/3 上下（LDO 降压，还有一部分功率变成热）。★ **跨板只送 5V 绝不送 3V3**（§0/「最终显示架构」那条）⇒ 从板那一路是**它自己的 LDO** 管，不吃主板的 3V3。<br>**④ 第二个问题（刷机/USB 重枚举时会不会瞬跌）没有静态答案，只能实测**：插上 USB-TO-UART 那个 Type-C 之后，主板 5V 的来源**整个换了一根** —— 从"车主那个 5V/2A 源"变成"PC 的 USB 口"（主机侧 500 mA 是常见上限），而两颗 S3 + 一块 480×480 背光的**峰值**正好落在这个量级上；**若**这时从板 brownout 重启，症状就是 §7 失败模式表**第 7 行**那条（`STATUS.uptime_ms` 反复归零）。⇒ 这一条**待实测**；本轮**不新增**任何"刷机时必须断开从板"之类的规定（那正是要实测才敢写的话）。★ 与 **L7** 的边界：L7 问的是"12PIN 那个 `VBus` **实际是多少伏**"（上板一量就有），本条问的是"**能给出多少电流**"（静态已能定：只取决于上游源）。 |
| **L7** | 12PIN 那个 `VBUS(5V)` **到底是什么**：PINOUT「开箱当天」有一条警告 —— 通用 S3 板的 5V 排针出厂状态**不一定是 5V**（有的板子是 LDO 输出端） | 到货先量那个脚对 GND（**先别接从板**），再按结论接线 |
| ~~**L14**~~ | ~~将来那个**声音告警**（README「下一步」#6：转速进红区 5800 响一声）由哪块板发声~~ **✓ 已关闭（2026-09-24，上板实测 + 车主听感）**：**发声的是 2.8C 这一块 —— 也就是右板（主机板）**，硬件走板上**自带的**那颗蜂鸣器（`TCA9554` 的 `EXIO8`，**零额外引脚、零额外接线**）⇒ **用不上** `EVENT 0x01` 那条还没定的上行。 | **两问都有答案了**：<br>**① 由哪块板发** ⇒ **2.8C 这一块（右/主机板）**。判据：手上只有这一块 2.8C，而它板载的蜂鸣器就在驱动这块屏的那颗扩展芯片上；左/从板**还没到货**，所以**不假设**它也有（同型号 ⇒ 理论上同构，但**到货后要按 §13 那套自检重跑一遍才算**，见「显示约定」§4.2）。<br>★★ **2026-09-25 补（L14 的最后一块拼图，现在完全关闭）**：第二块 2.8C（`COM8`）到货并刷上从板镜像之后，**它的蜂鸣器也真的会响** —— 判据是**人耳**（车主原话见「显示约定」§4.2），机器侧只到 `buzz: 回读=0x85(影子=0x85)` 那一步。⇒ **两块同型号板都有蜂鸣器**（实测，不再是从"同型号"推的）。<br>**② 有源还是无源** ⇒ **有源**（只会响/不响，**没有音调、音量不可调**）。实测见 `docs/RGB-PANEL-2.8C.md` §13.4/§13.6（车主：直流 5 轮 5 声"哔"、换驱动频率音调不变、持续拉高 2 秒是**一整段稳定音** ⇒ 有源；结构原因：扩展器输出脚上没有 LEDC/PWM 通路）。<br>**实现（2026-09-24 上板）**：`lib/dashcore/buzzer_exio.{h,cpp}`（非阻塞多相序列、单次 ≤300ms 夹取、`Long`⇒3 短哔、时钟与输出都靠注入 ⇒ native 可测）+ `src/dash_display_rgb.cpp` 的 `dash_buzzer_set()`（**唯一**写那颗芯片的地方，走既有 `tca9554_set()` + 影子寄存器做**读-改-写**）+ `src/main.cpp` 里 `#if defined(DASH_DISPLAY_RGB)` 的挂载与串口命令 `b`。<br>`alerts` 与 UI **一行都没改**（分层口径见「显示约定」§4.2 那张表）。|

### 8.1 ★★ 固件自己选 USB/串口「人格」（`FSUSB42UMX` 的 `SEL` = GPIO0）—— 2026-09-26

> **状态一句话：方案已写进代码（`lib/dashcore/usb_personality.h`），但默认 `USB_PERSONALITY_AUTO=0`
> = 一个寄存器都不碰；要不要真的在 `setup()` 里驱动 GPIO0，等 owner 拍板。**
> 纪律照 **§7.5.7**（"只写方案 + 报 owner"那一条），本节的形态也与 §7.5.5 一样。

**想要它做什么**：正常插电（**不按 BOOT**）也变成**原生 USB** 人格 ⇒ 于是同时得到
**Type-C 给电 + 给日志**，而 **43/44 空出来给链路**（"USB 占了 43/44"那个麻烦彻底消失）。

**极性（★ 出处是车主实测，不是猜）**：**按住 BOOT 插电 ⇒ 枚举成原生 USB**
（`VID_303A&PID_1001`）⇒ `SEL` 拉低 = 原生 USB、放开/拉高 = 板载 `CH343P`。
代码里是**一个常量** `dashusb::kSelectNativeLevel`（= 0），反了就改它。

**★ 风险（这是默认关闭的唯一原因）**：GPIO0 **同时是 BOOT strapping 脚**。
ESP32-S3 的复位序列会读 `GPIO_STRAP_REG` 决定"从 flash 启动"还是"进 ROM 下载模式"，
而那个寄存器的值就是 strapping 脚在**复位那一刻**的电平 ⇒ **我们一旦把它驱动成低，
下一次复位可能被读成"要进下载模式"**（现象：`boot:0x… (DOWNLOAD(USB/UART0))`、屏黑、app 不跑
—— 本项目实测过，见 `ACCEPTANCE.md` 的 `USB_UART_CHIP_RESET` 那条）。

**已查证的边界（不是所有复位都一样）** —— 出处：ESP-IDF v5.5「Application Startup Flow」
（[docs.espressif.com](https://docs.espressif.com/projects/esp-idf/en/v5.5/esp32s3/api-guides/startup.html)）
原文分两支：

- "For **power-on reset, software SoC reset, and watchdog SoC reset**: check the
  `GPIO_STRAP_REG` register if a custom boot mode (such as UART Download Mode) is requested."
- "For **software CPU reset and watchdog CPU reset**: configure SPI flash … and attempt to
  load the code from flash."（**不查 strap**）

数据手册那句更直接：strapping 的锁存器在 **power-on / RTC watchdog / brownout** 这几类复位时
**重新采样**。⇒ **"会重新采样的那几类"恰好都是车上真会发生的**（掉电、看门狗、欠压），
所以不能靠"CPU 复位不采样"这一条来免责。

**★ 好消息（同样必须写清）：不会变砖。** 进下载模式之后 esptool 照烧（下载模式正是它要的状态）、
`COM8`/`COM9` 那两口还在；退回一步**只要拔掉重插**（power-on 时 app 还没跑、没有谁在驱动 GPIO0
⇒ 不按 BOOT 就是正常启动）。⇒ 代价是"**可能要拔一次线**"，不是"砖"。

**评估过的三条"更安全的写法"（都没选，理由在这儿）**：

| 想法 | 为什么不行 |
|---|---|
| **只在需要时驱动** | USB 是"一直在"的，没有"需要时"这一说 |
| **主动复位前先释放** | 本项目**没有**主动复位（没有 `esp_restart()` 这条路），而**看门狗/欠压我们来不及释放** —— 挡不住真正危险的那一类 |
| **换一颗能锁存的开关** | 动硬件（且 `SEL` 已经焊在 GPIO0 上） |

⇒ 结论："更安全的那一版"**只能是"不开这个开关"** —— 这就是默认值为 0 的理由。

**做的时候会怎么做（代码已就位，一行 `-D` 即启用）**：

1. `setup()` 里、**日志起来之后**（那一行要自证）、**43/44 被 UART0 抓走之前**调一次；
2. GPIO0 配成 **`OUTPUT_OPEN_DRAIN`**（只会"拉低/放开"，不与板上上拉电阻和 BOOT 按键对打）；
3. 开机打一行 `usb: personality=… (GPIO0 …)` —— 它是"固件**要求**哪一边"在串口上唯一的证据，
   要与设备管理器里看到的**对上**才算证据；
4. 启用方式：给需要的那份 env 加 `-DUSB_PERSONALITY_AUTO=1`
   （本单**没有**动 `platformio.ini`，这一步属于"要 owner 拍板"）。

**四条判据（**做了才谈得上；现在一条都没有**，因为默认没开）**：
① 正常插电（不按 BOOT）⇒ 设备管理器里出现**原生 USB**；
② 开机日志那行"人格"与 ① 对上；
③ **43/44 上的链路照常工作**（至少要有"43/44 没被 USB 占"的证据：日志里 link 的 TX/RX 计数在动）；
④ 复位/重插多次、**从没进过下载模式**（每次贴 `boot: reason=… raw=…` 那一行）。
★ **判据 ④ 现在给不出来**：要证明"RTC 看门狗/欠压复位不会把它带进下载模式"，必须真去制造
那几类复位，而每一次尝试都有"板子停在下载模式、要车主去拔线"的代价 ——
**那不该在没有车主在场的台面上做**。



- **结论**：**43/44 上不存在"两个推挽并线"** ⇒ 两块板各自的 44 当输入是**电气上成立**的，v1 的 B→A 上行**不必**改走 GPIO7（§0 那条兜底不启用）。
- **什么时候仍可能出问题**（把结论停在证据允许的范围内）：
  1. **接上 USB-TO-UART 那个 Type-C 刷机/看日志时，链路的那两根会随之易主** —— 那颗开关会把 43/44 从排针切到 CH343P ⇒ **4PIN UART（含 12PIN 上的 TXD/RXD）此时被禁用**。⇒ **刷机时 A→B/B→A 自然断掉**；从板会走它自己的降级路径（>500 ms 冻结最后值、>3 s 回退 Sim，§7），**但这不等于"可以边刷边跑链路"** —— 刷机那条线本身就是 CH343P 的对手。
  2. **上电/复位那一段**：`SEL` 的默认值由 GPIO0（同时也是 BOOT strap）与 `R44`/`R45` 两个 10 K 决定，**我们的固件在 `setup()` 之前说了不算** —— 这一段谁在 43/44 上、会不会有几十毫秒的争用，**图纸读不出来，要实测**（下面 ⓐⓑ）。
  3. **两端同时把 43/44 配成输出**（软件错）时，43↔43 正是"对打"：一端输出、另一端也输出。⇒ v1 的**角色分工必须由编译期 `LINK_ROLE` 保证**（§5），别指望硬件兜。
- **还需要上板实测什么（最小残余风险）**：ⓐ **`SEL` 的默认电平**：板子空跑（不插 UART Type-C、不接排针）时量 `IO0`/`UART0SEL` 与 **44 对地电平**，并在复位瞬间用示波器看它有没有抖；ⓑ **串 1 kΩ 试**：链路接上后两板能正常收发（能收就是没人对打）；ⓒ **插上 UART Type-C 再重复一次**，确认此时**链路按预期失效**（而不是变成半死不活）。

---

### 8.2 ★★ 排针 43/44 什么时候真的通向 ESP32 —— **本款（非触控 `ESP32-S3-LCD-2.8C`）的口径**（2026-09-27）

> **状态一句话：机制已由「官方 wiki 原文 + 车主台面实测」两路独立证据定案；
> 而"由谁驱动那颗开关"这件事，本款**没有**自己的原理图 ⇒ 只有 Touch 版的读数，
> 明确标**不适用**、不拿来当本款的结论。**
>
> ★ 本节**更正**了 §8.1 与「链路协议 v1」§0 里的一条口径（那条写的是
> "`SEL` 由 GPIO0 驱动、网络 `UART0SEL`"）—— **更正留档**见下面第 4 条。

**1. 本款到底是哪一块板（这是一条必须写清的事实）**

车主的板子是 **`ESP32-S3-LCD-2.8C`（非触控）**，而**不是** Touch 版。两条出处：

| 出处 | 原文 / 读数 |
|---|---|
| 设备端开机日志（真板） | `rgb: 板=微雪 ESP32-S3-LCD-2.8C(非触控) ST7701 RST=EXIO1 CS=EXIO3 BL=GPIO6/PWM20000 @50%` |
| 官方文档站 | 非触控版有**自己的**文档页与资料页：<https://docs.waveshare.com/ESP32-S3-LCD-2.8C> 、<https://docs.waveshare.com/ESP32-S3-LCD-2.8C/Resources-And-Documents> |

**2. 那条"Type-C 插入 ⇒ 4 脚 UART 被禁用"在**本款**上由什么实现 —— 出处与原文**

- **出处①（官方 wiki，本款页面，权威原话）**：<https://docs.waveshare.com/ESP32-S3-LCD-2.8C>
  的 **Onboard Resources** 第 4 条，原文逐字：
  > "**FSUSB42UMX** UART selection chip: when the UART Type-C is connected, the 4Pin UART is
  > disabled; when the UART Type-C is not connected, the 4Pin UART is enabled"

  同一页 **Interface Description** 那张 12PIN 表逐字给出 43/44 的归属：
  > "`TXD` | TXD (GPIO43) | UART data transmit or can be used as a regular GPIO"
  > "`RXD` | RXD (GPIO44) | UART data receive or can be used as a regular GPIO"

  并且第 17 条（UART Header）也写着同一件事：
  > "This interface is not available when the USB TO UART Type-C interface is connected"

- **出处②（车主台面实测，2026-09-27）**：这是**本款真板**上的直接读数，与出处①逐条吻合 ——

  | 测点 | 读数 | 它证明了什么 |
  |---|---|---|
  | 主板（**Type-C 拔掉**）12PIN 第 9 脚（`TXD` = GPIO43） | **3.11 ↔ 2.95 V 跳动** | 排针这一侧**是通的**、且在发数据 ⇒ "不插 Type-C ⇒ 4Pin UART 有效"✓ |
  | 副板（**Type-C 插着**）同一根线的那一针 | **同样量到跳动** | 信号**物理上到了排针** ✓ |
  | 副板自己的日志 | `link rx bytes=0` + 屏上 `SIM` 角标常驻 | ★ 信号到了针上、**芯片却收不到** ⇒ 排针的 43/44 与 ESP32 之间**是断开的** |
  | 通断档（断电）主板 9 ↔ 副板 10 / 主板 9 ↔ 副板 9 | **响 / 不响** | 线是**交叉**的且是通的（排除"线接错"这一路） |

  ⇒ **两路证据独立同向**：那颗 **`FSUSB42UMX`** 在**本款上也在**，而它选边的**输入是
  "UART Type-C 插没插"**（不是我们固件、也不是排针上有没有信号）。

- ★ **"由谁驱动它"这件事：本款查不到，如实说明**（不猜）：
  - 官方**没有**为非触控版单独提供原理图 —— 非触控版**资料页**给出的那份 PDF
    （<https://files.waveshare.com/wiki/ESP32-S3-LCD-2.8C/ESP32-S3-Touch-LCD-2.8C_schematic_diagram.pdf>）
    与本仓库既有引用的 **Touch 版**那**同一份**（946,158 B、
    `SHA256 = 01EAE811…99D76`，2026-09-27 重新下载核对，**逐字节相同**）；
    实测把 `Touch` 字样换成 `LCD` 的两个 URL 都是 **404** ⇒ **本款没有自己的原理图**。
  - ⇒ **Touch 版原理图上读到的那些东西（`U10`/`UART_SEL`/`R44`/`R45`）一律标
    「Touch 版口径，本款不适用」，不作为本款的结论。**
  - ⚠ 顺带记一条**我在本单里读到、但不能用**的东西（留在档上免得后人重走）：
    Touch 版原理图里 `UART_SEL` 那个网络的**几何**读出来是 `R44`(标注 **1K**) 一端接
    `VBUS`、另一端与 `R45`(**10K**，另一端接 GND) 在 `UART_SEL` 上分压、再进 `U10` 第 10 脚。
    这与上面第 3 条那个"按 Type-C 插没插选边"的语义**方向一致**（VBUS 在 ⇒ 切到 CH343P），
    但：① 它是 **Touch 版**的图；② 同一张图上 `R44` 的**阻值文字**（`1K`）与
    本仓库 §8.1 之前记的"两个 10 K"**不一致**。两者都**不构成本款的结论**，
    只作为"将来若拿到本款原理图，先核这两处"的线索。
  - ★★ **2026-09-27 追加：这块板上有两个 Type-C ⇒ "UART Type-C" 指的是哪一个，有答案了。**
    官方页面 Onboard Resources 同时列着**第 10 项 `USB Type-C Port`**（直连**芯片原生 USB**，
    即 `GPIO19/20`）与**第 18 项 `USB TO UART Type-C Port`**（接**板载 `CH343P`**）。
    ⇒ 第 4 条那句 `when the **UART Type-C** is connected …` 指的**就是第 18 项那个口**
    （**USB TO UART** Type-C），**不是**第 10 项那个 `USB` 口 ——
    同页第 17 条把话说全了："UART Header: This interface is not available when the
    **USB TO UART Type-C** interface is connected"。
    ⇒ ★ **很可能就是"`UART` 口的 VBUS 检测在驱动 `FSUSB42` 的选边脚"**：
    插 `UART` 口 ⇒ 43/44 归 CH343P（排针 4Pin UART 被禁用）；插 `USB` 口 ⇒ 43/44 留在排针上。
    ★ **仍标"待确认"（不是结论）**：① 本款没有原理图 ⇒ 驱动源（VBUS 检测 / 数据连接检测 /
    别的开关）**仍然无法从图纸回答**；② "两个口的 5V 是不是同一个网"同样没有本款图纸。
    ⇒ 两条判据与**按顺序跑**的步骤写在 `docs/LINK-TWO-BOARD.md` **§9.2b / §9.6**（待上板）。
    ★ 它**顺带作废**了"外部 5V 从 12PIN 灌 / 充电宝 / 抓帧盒当电源"那一整套绕法
    （两块板各插各自的 `USB` 口即可）—— 同样**待上板判定**后才算数。

**3. ★ 结论（本款、可执行的那一条）**

> **用 12PIN 的 `VBus`(5V) 给板子供电、Type-C 不插 ⇒ 排针的 43/44 与 ESP32 是通的、
> 可用。** 反过来：**只要那一块板插着 UART Type-C，它自己的排针 43/44 就是断开的**
> （与信号有没有到针上无关）。

- 这条**不是**推论：出处①的 wiki 原话就是"not connected ⇒ 4Pin UART is enabled"，
  而出处②在**本款真板**上量到了正向的那一半（拔掉 Type-C 的主板，第 9 脚在跳动）。
- ★ **检测的是"Type-C 连接器这一侧"，不是"板上的 5V 轨"**：
  1. wiki 原话写的是 **"the UART Type-C is connected/not connected"** —— 判据是**连接器**；
  2. 实测反证：主板是**用 12PIN 的 VBus 供电**的（Type-C 拔掉、板子在跑、第 9 脚在发）
     ⇒ **"5V 轨有电"这件事本身并不会把排针关掉** —— 否则主板那一侧也不该通，
     而它通了。
  - ⚠ 边界（把结论停在证据允许的范围内）：本款没有原理图 ⇒
    "**具体是哪一根网络（Type-C 的 CC / VBUS 分压 / 别的）**在驱动那颗开关"**仍然未知**；
    上面第 3 条只回答"什么条件下通"，不回答"电气上怎么实现的"。
    要回答后者，要么拿到本款原理图，要么在真板上量那颗 `FSUSB42UMX` 的选边脚
    （插/不插 Type-C 各量一次）—— **待上板**，本单没做（本单不许开串口/动板子）。

**4. ★ 更正留档（撤回/更正必须留痕，不许静默改掉）**

| 位置 | 原来写的 | 现在改成 | 依据 |
|---|---|---|---|
| §8.1 标题与正文 | "固件自己选 USB/串口人格（`FSUSB42UMX` 的 `SEL` = **GPIO0**）" | ★ **那条是 Touch 版口径，本款不适用**：本款机制已由 §8.2 的 wiki 原文 + 实测定案为"按 Type-C 插没插选边"；`lib/dashcore/usb_personality.h` 里"`SEL` 挂在 GPIO0 上"那句同此更正（**代码与默认值一个字都没动**，只更正注释里的出处归属） | 本款资料页**没有**自己的原理图（§8.2 第 2 条），GPIO0 那句来自 **Touch 版**的图 |
| 「链路协议 v1」§0 的 L1 行 ③ | "`SEL`(10) 由 `UART0SEL` 驱动：该网络只有三个端子 —— `IO0`（GPIO0）、10 K（`R45`）、10 K（`R44`）" | 同上：**Touch 版口径**，本款不适用；且同一张图上 `R44` 的阻值文字是 **1K** 而不是 10 K（§8.2 第 2 条那条 ⚠） | 同上 |
| §8.1 的启用前提 | "给需要的那份 env 加 `-DUSB_PERSONALITY_AUTO=1`" | ★ **本款不需要它**：本款只要**不插 `UART` 口**（两个 Type-C 的哪一个，见第 2 条那段 2026-09-27 追加）排针 43/44 就通（§8.2 第 3 条）⇒ 那个开关（以及它"GPIO0 是 BOOT strap"的风险）**在本款上没有存在的理由**，`USB_PERSONALITY_AUTO` 保持默认 0 即可 | §8.2 第 3 条 + `docs/LINK-TWO-BOARD.md` §9.2b |
| §8.2 本文（同日追加，同表留档） | "由谁驱动它这件事：**本款查不到**" | ★ **仍然查不到驱动源**，但**"说的是哪个口"有答案了**：官方页面第 10 项 `USB Type-C Port`（原生）与第 18 项 `USB TO UART Type-C Port`（CH343P）⇒ 第 4 条的 "UART Type-C" 指**第 18 项那个口**，★ **很可能**就是它的 **VBUS 检测**在驱动 `FSUSB42` 的选边脚 —— **标"待确认"**（没有本款图纸），判据见 `docs/LINK-TWO-BOARD.md` §9.6 | 官方本款页面 Onboard Resources 第 4/5/10/17/18 条原文 |

★ **这次更正没有动任何代码行为**：`usb_personality.h` 的常量、默认值、
`main.cpp` 里那一行开机日志的**取值口径**都没变（改的只有注释里的**出处归属**）。
