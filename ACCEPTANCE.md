# 验收

- [x] 工程能编译（esp32dev 已实测；构建路径需纯 ASCII，见 ARCHITECTURE.md）
- [x] 宿主机单元测试全绿（python -m platformio test -e native，当前 **注册 132 例 /
      130 通过 / 0 失败 / 2 跳过**，其中 2 例需环境变量否则跳过；★ **退出码不是判据**，
      看摘要里有没有 `[FAILED]` —— 理由见 `ARCHITECTURE.md` 的测试节）；网页端另有 5 个 Node 检查脚本
      （`test-face-stages.js` 429 项、`test-image-blob-build.js` 362 项、
      `test-theme-json.js` 259 项、`test-gauge-geometry.js` 71 项、
      `syntax-check-pages.js` 页面脚本语法）
- [x] **主题文件的颜色两种写法都能读**：`0xRRGGBB` 与十进制。
      固件解析器一直两种都吃，但编辑器用裸 `JSON.parse` ——
      于是"导入 `theme-default.json`"会报一句和颜色毫无关系的语法错
      （`Expected ',' or '}' after property value`），
      而那个文件正是给用户当模板用的。现在编辑器先经 `theme-json.js`
      把值位置上的 `0x` 转成十进制再解析（只动冒号后面的，不碰字符串），
      `test-theme-json.js` 用真实的 `theme-default.json` 做回归，
      并断言"直接 JSON.parse 确实会失败" —— 免得这条测试变成空转。
- [x] VAN 线路层协议栈（van_wire.h/.cpp）：SOF 识别、4B5B（E-Manchester）解码、
      IDEN/CMD 拆分、CRC-15、帧尾收尾；编码器 + 解码器往返自洽
- [x] **VanPhyWire 整链回归**：GPIO 边沿 → 4B5B → 帧字节 → FCS 校验 → VanPacket
      → VanSource（车速/转速）。黄金向量取自公开真实抓包的 IDEN/CMD
      （8C4/C、8D4/C、5E4/C、4D4/E），连续多帧不丢字节。
      零依赖寄存器，`onEdge(t_us, level)` 即可喂真实中断。
- [x] VanPacket 扩到 **15 位 IDEN**（能表达 0x000/0xFFF 保留值）+ cmd + ack + fcs_ok
- [x] 回放语法扩到 15 位 IDEN 与显式 CMD（"VAN <iden> [cmd] <data...>"）
- [x] VanSourceSink 适配层：物理层 → 数据源的接线打通（main.cpp 已接）
- [x] **运行时主题（改配色不用重编译固件）**：`theme` 分区（`0x210000`/16KB）
      + JSON 解析（手写扫描器，不依赖 newlib 的 float 解析）+ 值域钳制；
      加载失败**自动回退默认主题**，串口打印原因。
      实测：`THEME_FILE=theme-demo.json` 与默认主题渲同一帧，6/6 采样点、
      3600/3600 采样像素都不同；损坏/截断的 JSON 被拒后仍能出 232/181 帧。
- [x] **图片资源（放 flash 分区，改图不用重编译固件）**：
      `image` 分区（`0x254000`/**1MB**）+ `esp_partition_mmap` 只读映射
      （像素不进 DRAM）+ `lv_image_dsc_t` 组装；空分区/坏镜像一律拒收并降级。
      并且**真的接上了**（`src/image_load.cpp` 由 `setup()` 调用）——
      曾经漏接：`imageBlobLoad()` 写好了却没人调，刷了图片在设备上完全静默。
      实测（pcpreview，`IMAGE_BLOB` 指向 JS 生成的 1088 字节镜像，输出 262 帧）：
      `image ok: 5 张,数据 372 字节,镜像 1088 字节`。
      这证明"JS 打包 → 宿主机加载 → 索引可用"整条链通了。
      设备端的 `esp_partition_find_first`/`mmap` 分支仍需上板确认
      （见下面那条待办里的串口输出）。
- [x] **表情：每屏一套、各看各的表**（用户要求）
      左屏（转速表）**只看转速**：怠速/巡航/运动/高转/红区；
      右屏（速度表）**只看车速**：常态/巡航/运动/**超速（>130 km/h）**。
      **水温不参与表情**（只驱动水温弧与水温数字）—— 用户提的理由是容量与机能，
      实测也确实如此：水温若参与要再加 4 张图，而 image 分区只有 1MB。
      `Face` 枚举是两屏共用的档位名，哪一屏有哪些状态由
      `kFaceLeftStates` / `kFaceRightStates` 明确列出，并有测试断言
      "列出的状态必须有图、没列出的必须没有"（两处不一致只会让某张脸永不出现，
      不会报错）。
- [x] **进气温度做成速度表的副表**（用户实测 OBD 支持 010F 后提出，2026-09-17）
      用户原话："发现了进气温度这个选项，能否加入到速度表模块作为它的分表存在
      （类似于水温表在转速表的位置）"。于是两块表现在左右对称：
        左屏（转速表）：外圈转速弧（缺口朝下）+ 内圈水温弧（缺口朝上、镜像）
        右屏（速度表）：外圈车速弧（缺口朝下）+ 内圈进气弧（缺口朝上、镜像）
      落实的地方（一处都不能少，否则"数据有了但屏上没有"）：
      · 数据层：`VehicleState.intake_c`；OBD 轮询表加 **010F**；`data_service`
        按字段独立超时（前两路断了不影响它，反之亦然）；假数据也给一路
        （环境温度慢漂移 + 热浸），否则预览里看不见这条弧。
      · 主题层：`ArcKind::Intake = 3`（**枚举只能往后加** —— 它就是要存进
        theme.json 的 kind 数字）；量程 `intake_min_c/max_c = 0/80`
        （环境温度 → 堵车热浸的窗口；冬天零下时弧趴在 0，数字照实显示负数）；
        读数 `intake_color = 0xFFB020`（琥珀）/ `show_intake` / `intake_cy = 384`。
      · UI 层：`dash_ui` 里把"大数字跟哪条弧"的规则从"第一条不是水温的弧"
        放宽成 **`is_aux_kind()`（水温或进气都算副表）** —— 否则
        `[Intake, Speed]` 顺序的屏会让速度表的大数字变成进气温度。
      **实测证据**（pcpreview 落帧，右屏第 140 帧 17/17 通过）：
        进气温度带（琥珀字）命中 200 像素；墨迹外框 x[219..258] y[378..390]
        （与左屏水温数字同一位置，证明"对称"）；弧带命中 899 像素、
        数字最低点 y=390 < 弧内沿 y=398；镜像端点核对
        左端 170° = `#ffb220` 点亮、右端 15° = 轨道；
        左屏反向核对"**不该有**进气温度数字" = 0 像素（右屏同理不该有水温数字）。
      **顺手修掉一个真 bug**：`parseObdLine()` 原来是**硬编码**只认 `41 0C`
      与 `41 05` 两种前缀（注释写着"解析层不区分 PID"，是错的），
      所以 010F 的数据喂进来解析直接失败 —— 而且很安静（返回 false，
      上层继续等下一帧，表现为"进气温度永远是假数据"）。
      已改成表驱动（`kSupportedPids`），以后再加同类 PID 只动那张表。
      另加一条"ECU 回 `NO DATA` 时必须保持 invalid"，防止把不支持误读成 0℃。
      **补记（用户当天就发现的漏项）**：第一版只加了弧和读数，
      **漏了阶段表** —— 用户当场指出"阶段里面缺失了进气温度低中高的选项"。
      也就是说：弧画得出来，但阶段模拟里根本没有"进气低/中/高"可点，
      那条弧在模拟器里走不起来、也验不了。现已补上 3 条用例
      （进气 20 / 40 / 65℃，都落在默认量程 0..80 内、彼此差 ≥10℃），
      阶段表从 11 条变 **14 条**（转速 4 + 车速 4 + 水温 3 + 进气 3），
      两个编辑器也补上了"按 kind 3 算弧进度"与"右屏画进气读数"两条分支 ——
      少任何一条，格子上都是"点了没反应"。
      **教训**：给一条新弧配"能点出来的档"才算完 ——
      这与当初"惊喜档选不出来"是同一类问题，两次都是用户先看出来的。
- [x] **万用表 / S3 板 / SN65HVD230 / OBD 一分二线到货后能立刻做的事**（2026-09-18）
      到货当天做完三件"纯准备"，全部在宿主机上验证过，板子插上就能用：
      · **S3 编译目标**：新增 `[env:esp32s3]`（N16R8：QIO flash + OPI 8MB PSRAM）
        与 `partitions-s3.csv`（16MB 表，image 分区 **1MB → 8MB**）。
        ★ 两份分区表的 **theme/image 偏移刻意相同**（0x210000 / 0x254000），
        所以网页编辑器印的那套 esptool 命令**两块板通用**，不用记两套数字。
        实测：`pio run -e esp32s3` 通过，RAM 37.1% / Flash 65.2%（app 分区 1MB）。
      · **开机自检**（`src/main.cpp` 的 setup）：上电打印
        `chip / flash / psram / heap / image 分区` 五行。它们是
        "板子是不是 N16R8"、"PSRAM 起没起来"（双 480×480 缓冲的前提）、
        "有没有烧错分区表"的唯一一眼判据。
      · **VAN 硬件接收链路**（收发器一到就能抓车速，这是本次到货最大的解锁点）：
        `van_edge_queue.h`（ISR→解码器之间的环形缓冲，2048 个边沿）、
        `van_phy_gpio.{h,cpp}`（GPIO16 中断只入队；tick 排空 + 总线空闲 **70µs**
        关帧 —— 原为 300µs，2026-09-19 按实测改小，见文末「关帧门限」那条）、
        `main.cpp` 的 `VanLogSink`（每一帧按 **van_replay 的行格式**打到串口 ——
        车上抓的日志能直接粘回来回放）。
        开关 `-DVAN_PHY_GPIO=1`（已加进 `[env:esp32s3]`）；没开时串口会明说
        `van phy: stub(没启用 GPIO 接收…)`，**不会静默地什么都不做**。
      **实测（宿主机）**：native 105 例（103 通过 / 2 跳过；新增 3 条边沿队列用例：
      队列路径与直连路径解出同一帧、溢出丢新且计数不覆盖已读数据、复位）；
      Node 318+156+71+**194**+11 全绿；esp32dev / esp32s3 / pcpreview 三个目标都编过。
      上电前的万用表量法与接线顺序见 `PINOUT.md` 的「开箱当天：先量，再接」。
      **顺手抓掉两个真会咬人的坑**：
      ① 给 `partitions-s3.csv` 写注释时用了"★"（UTF-8 = E2 98 85）——
        PlatformIO 用 `open(csv)`（**不指定编码**）读它，中文 Windows 按 GBK 解码，
        于是**固件已经链接成功**却在最后一步 checkprogsize 抛 UnicodeDecodeError，
        看起来像"编译失败"。那个文件自己的注释里就写着"keep this file pure ASCII"，
        我先违反了。现在 `test-image-blob-build.js` 有守卫：两份分区表必须纯 ASCII、
        无 BOM，且 theme/image 偏移一致、S3 的 image 必须更大。
      ② 开机自检用了 `ESP.*` 与 `esp_partition.h` —— pcpreview（宿主机）没有这些，
        预览构建直接编不过。已用 `ARDUINO` 判定圈成设备专属
        （`DASH_DEVICE_SELFTEST`），同一份 main.cpp 两端都能编。
- [x] **S3 第一次真正上电：固件在跑，而且立刻抓出一个真 bug**（2026-09-18 当天）
      结论先说：**固件一直是好的**。白天"刷完串口一片空白"是刷写/调试通道的坑
      （原生 USB 口的软复位 + pyserial 默认拉 DTR/RTS，见下面那一节），
      换成板上 **UART 口（CH340）** 之后，复位后第一个字节就能看到完整开机日志：
      ROM → app 启动 → 自检五行 → `BEACON` 每秒一步 → `SRC` 每 5 秒一行，
      表情按模拟数据在 `sport/redline/cruise/idle` 之间正常推进。
      自检当场抓出的问题：**`psram : 0 KB 可用 / 0 KB 总`** —— 漏了
      `-DBOARD_HAS_PSRAM`（缺它时核心会主动把 `CONFIG_SPIRAM` `#undef` 掉），
      补上后 `8189 KB` ✓。这正是"自检五行"当初加进来的理由：
      换板子/换芯片后第一眼就能看出硬件到底认没认全。
      顺带做完的三件事：
      · 日志改**双通道**（`lib/dashcore/dash_log.h`）：USB-CDC 与 UART0 都发，
        一根线插哪个口都看得见；串口贴帧回放也两个口都收。
      · 加**示位标**（`BEACON`，esp_timer 驱动、每秒一行、带"走到第几步"）——
        主循环卡死也照打，一次就能区分"没跑起来"和"卡在某一行"；
        同时把上电后每秒重打完整自检那段刷屏删掉了（它会淹掉 VAN 帧）。
      · 抓开机日志的脚本进了仓库：`tools/serial-capture/capture.py`
        （把"DTR=IO0、RTS=EN、且必须在 open() 之前设"这套知识固化下来）。
       **实测**：esp32dev / esp32s3 / pcpreview 三目标全 SUCCESS；
      native 105 例（103 通过 / 2 跳过）；S3 上电后
      `psram=8189KB flash=16MB image=8192KB`、heap 259KB。
- [x] **桌面联调：VAN 解帧链在真板上跑通**（2026-09-18，收发器还没接）
      用 `tools/serial-capture/replay.py` 往 UART 口持续贴
      `VAN 824 18 F8 27 10 00 00 00`，设备端那行从 `speed=sim` 变成：
      ```
      SRC speed=van rpm=van coolant=sim intake=sim | v=100.0km/h 799rpm 80.4C 27.8C
      ```
      车速正好 **100.0 km/h**（0x2710 × 0.01，与 `van_source.h` 的
      `kSpeedIden=0x824 / kSpeedOffset=2 / kSpeedScale=0.01` 完全对得上），
      同一帧的转速字段也一起变成 `rpm=van 799rpm`。
      *（注意：0x824 是本项目的**假设**，实车 IDEN 必须实测确认 ——
      见下面的实车必验清单。）*
      顺手记下两条必须先知道的坑（都写进工具注释里了）：
      ① **开串口那一下会复位板子**（CH340 的 DTR/RTS 接在 EN/IO0 上），
         而 Arduino 的 `Serial0.begin()` 会**清空 RX FIFO** ——
         开机那几百毫秒里贴进去的帧直接丢掉。所以工具先等 2.5 秒再发。
      ② 数据源有 **3 秒新鲜度窗口**（`data_service.cpp` 的 `kStaleMs = 3000`），
         而 `SRC` 行 5 秒才打一次 —— 只发一帧很容易"过期了才轮到打日志"，
         看着像没生效。真车上本来就是连续帧，所以工具每 0.5 秒重发一次。
      同时能看到 GPIO 收帧那路的 1 Hz 诊断：
      `van: edges=1 frames=0 fcs_ok=0 dropped=0(队列0) 待收=0`
      —— 收发器还没接，那 1 个边沿是浮空脚上的噪声，正常现象。
- [x] **桌面验证收发器"活着"：摸 CANL 抬计数，静置不抬**（2026-09-18，上车前最后一道桌面关）
      桌上没有总线，`frames` 必然是 0，但 `edges` 能回答"收发器到底通没通"：
      手指碰 / 拔插 `CANL` 端子 5~6 下，串口那行从 `edges=15` 变成 `edges=334`；
      而**静置 15 秒一个边沿都不涨**（漂移 ≈ 0）→ 那一大截只可能来自那几下 →
      **通路确认**：收发器有电、RO 确实在驱动 GPIO16（`RX` 就是接收输出）。
      ★ 它**只**证明"线通、芯片活"，不证明极性/速率/协议 —— 那三样只能上车看
      `edges` 和 `frames` 谁在涨（PINOUT.md「C. 上车步骤」那张表）。
      顺手把这条判定法写进 PINOUT.md 的桌面自检（原来那条写的是"以车上为准，别在
      桌上纠结"，等于把这 30 秒的确定性白扔了）。
- [x] **抓帧工具链补齐（发现两个会让这趟白跑的问题）**
      用户要去车上抓帧，先按 PINOUT.md 的命令逐条复核，发现两处**照抄必失败**：
      ① `python tools/serial-capture/capture.py` 会 `ModuleNotFoundError: No module
         named 'serial'` —— pyserial 3.5 只装在 `.pio-pylibs` 里（实测系统 python
         里没有），命令里必须带 `PYTHONPATH`；
      ② 这台机器是 **Windows PowerShell 5.1**：`>` 重定向写出的是 **UTF-16LE**，
         抓回来的日志没法直接分析。
      做法：`capture.py` 增加 `--out`（自己写 UTF-8 无 BOM）、`--until-frames N`
      （抓够就收工）、`--baud`（逃生门，见下）、`--selftest`（9 项，已通过），
      并新增一键脚本 `tools/serial-capture/capture-van.ps1`：把 `PYTHONPATH`、
      编码、端口/时长/路径、以及"上车先确认的 4 条"全部写死，
      跑完直接打一张 `好帧/坏帧/每个 IDEN 的数量和样例` 的小结 ——
      **车上当场就知道有没有白跑**。已用真板在 COM4 实跑两遍（8s / 15s）验证。
      另外两处口径更正：
      · 旧文写"用充电宝供电 + 串口抓帧"是**自相矛盾**的 —— 裸板没有 SD 卡，
        充电宝供电就没人记录日志。正确的是：笔记本**拔掉市电充电器、只用电池**，
        地环流风险只来自"市电充电器 + 车 12V"这一种组合（车上 12V 转 USB 也安全）；
      · 旧文的完整性检查命令 `Select-String -Pattern 'fcs_ok=1'` **数不出任何东西**
        —— 那行里的 `fcs_ok` 是累计个数不是标志位，且只在计数变化时才打印。
      顺带修掉一个自己踩的坑：控制台默认代码页 GBK(936) 里没有 `✓`，
      `print` 会在**打小结那一刻**抛 `UnicodeEncodeError`（跑完一趟才发现日志没存成）
      → 脚本开头 `reconfigure(errors='replace')` 兜底。
- [x] **右屏第 4 档从"惊喜"改成"超速"**（用户提出："这个惊喜挡测试的时候
      是无法选出来的，是做什么用的"）
      原来它是**急加速瞬态**：相邻两次调用速度差 > 15 km/h/s 就亮 400ms。
      查下来两个问题都是真的：
      ① **模拟器里点不出来** —— 它不是按车速分的档，阶段表里没有它的位置
      （阶段模拟只有"车速低/中/高"三个按钮），所以用户试用时根本选不到它；
      ② **实车基本到不了** —— 15 km/h/s ≈ 4.2 m/s²，206 CC 自动挡 0-100 约 11s
      （均值 ≈ 9 km/h/s），那张图导进去几乎永远不会亮。
      假数据里它只在第 6..11 帧勉强擦过阈值（波峰斜率 15.75 刚好过 15）。
      改成**超速**：进入 `> 130`、退出 `<= 127`（迟滞，免得定速巡航压在
      130 上下时脸来回跳）。这样车速组和转速组一样是 **4 档 = 4 个状态**，
      每个状态都有对应的一条阶段用例、都能在模拟器里点出来。
      **改名不改号**：图片角色号仍是 8，已导出的 `image.bin` 不受影响；
      主题键名 `eye_surprise` 也保持不变（改键名会让用户的 theme.json 静默丢字段）。
      **实测证据**：假数据波峰现在有 90 帧在超速档（第 8..96 帧），
      落帧核对 `r_0030 overspeed` 13/13 通过；同帧左屏是红区，两屏第 4 档同时可见。
      C 侧 4 条新用例钉住阈值/边界/迟滞/复位，另有一条
      `test_acceleration_does_not_change_face` 防止有人按加速度把瞬态加回来。
- [x] **每屏只被自己那一路驱动**（可执行定义 + 两端测试）
      转速组的右屏恒为常态、速度组的左屏恒为常态、水温组两屏都不动；
      `test_expression.cpp` 还把车速从 0 扫到 210 断言左屏不变、把转速从
      怠速 900 扫到表盘上限 **7000** 断言右屏不变。
      ★ 2026-09-22 改准：这里原文写 **6000**，与代码对不上 —— `test_expression.cpp`
      的循环是 `for (float r = kRpmIdleNominal; r <= kRpmMax; r += 250.0f)`，
      而 `lib/dashcore/vehicle_state.h` 里 `kRpmMax = 7000.0f`（表盘上限）；
      所以扫到的是 **7000**，6000 是表盘上限改成 7000 之前留下的旧值
      （那一次改动就是 `d4d72b7`「修正表盘上限：kRpmMax 6000 → 7000」）。
      **实测证据**（pcpreview 落帧，8 张表情各一个颜色）：
      第 120 帧左屏是"常态红 #ff0000"、右屏是"巡航橙 #ff8100" ——
      同一瞬间两块表显示**不同的**状态；左屏整段只在 红→黄→绿→青 之间变
      （纯转速），右屏整段只在 蓝→白(超速)→紫→橙 之间变（纯车速），
      两边从不同步。
- [x] **「弧压在背景图之上」已补上像素级覆盖**（外部复审指出时确实没验，现已验）
      起因:弧带(半径 181..205)与当时夹具的 240×240 底图(离圆心最远 169.7 在角上)
      **不重叠** —— 那条"弧带点"验的其实只是"这一圈有东西"。
      修法:把 make-test-blob.js 的底图改成**满屏 480×480**(中心品红标记跟着挪到
      234..246,仍在屏幕正中),于是弧带上每个点都落在底图上。
      代价与连带改动:弧**缺口**处读到的从主题底色变成底图色,
      朝向检查的判定跟着改成"缺口 = 底图色 #000062"。
      **实测证据**（check-preview-frame.js 新增一条,读真实落帧）:
      沿弧带半径 200 在 135°..405° 内取 9 个角度(避开 45°..135° 的缺口),
      **9/9 全部不是底图色** —— 点亮的红 #ff5d5a 与半透明轨道 #080c4a 都读到了,
      说明弧是画在底图**上面**的(若被底图盖住,这一圈都会是 #000062)。
      这条与数值无关(满量程读出点亮色、怠速读出轨道色,两种都算通过)。
      左屏第 140 帧 16/16、右屏第 140 帧 13/13、右屏第 0 帧(表情未显形)10/10。
      顺带把"这一圈真的画了弧"也钉住了 —— 原来那个采样点正好取在弧的端点(45°),
      读到的是边界混合色,证明力很弱。
- [x] **表盘朝向：起点在左下、缺口在正下方**（用户看出"表的方向是不是要向右转 90 度"）
      **固件是对的**：实测落帧沿弧带扫一圈 —— 135°（7:30）处开始点亮，
      60°~120°（正下方）是底色，12 点钟有弧。这与汽车仪表惯例一致
      （下方留缺口给指针轴与里程）。
      LVGL 的约定从源码确认：`lv_arc.c` 里 `x = r·sin(angle+90)`、`y = r·sin(angle)`
      → 0°=3 点钟、顺时针；而 canvas 的 `arc()` 约定完全一样，所以不需要任何偏移。
      **错的是两个编辑器的预览**：都写成 `(d - 90)`，把整块表**逆时针转了 90°**
      （起点跑到 4:30、缺口跑到正右方）。已改成不加偏移。
      两端都有测试：`test-gauge-geometry.js`（71 项，查编辑器源码 + 默认主题几何）
      与 `check-preview-frame.js` 的「表盘朝向」检查（读真实 BMP，断言正下方是缺口、
      正上方有弧）。故意把偏移改回去，前者立刻红两项。
- [x] **水温弧镜像(从左端起涨)**（用户要求："位置对了，但是涨幅方向反了，得做一下镜像"）
      横比:两条弧开口相反(转速朝下、水温朝上);纵比:水温弧从左端(9 点钟)起涨。
      实现:`ArcStyle` 新增 `reverse`(1 = 从 end 端起涨),固件里由 `arc_set_progress()`
      统一决定"固定哪一端" —— 建屏、开机扫表、正常渲染三处都走它(不再是三份手算角度)。
      主题 JSON 里的字段名就是 `reverse`，两个编辑器都有「涨幅反向（镜像）」开关，
      预览也认它(不认的话固件镜像了、预览还是反的)。
      **实测证据**(固件落帧,水温 89℃):弧带左端 170° 是点亮色 #7bff6a、
      右端 15° 只是轨道 #181818 —— 点亮区确实从左端往右长。
      测试:`test_coolant_arc_is_mirrored`(C,钉住默认值 + 另两条弧是 0)、
      `test_reverse_defaults_to_zero`(老主题缺字段 → 0)、
      `test-gauge-geometry.js`(水温弧 reverse=1 + 85℃ 时点亮区在左半边)、
      `check-preview-frame.js` 的「水温弧从左端起涨」（读真实 BMP）、
      以及三处默认值对账里新增的 reverse 比对。
- [x] **本机记住的配色不会挡住新的默认值**（用户实测撞到的坑）
      编辑器把配色记在本机（刷新不丢），但只存"整份主题"时，固件默认值一改就被
      旧记录盖住 —— 用户刷新后看到"什么都没变"（水温弧 145→330 改成 0→180 时）。
      现在记录是 `{v:2, base, theme}`，`base` = 存那一刻的默认值，读回来做逐字段三方比对：
      用户改过的保留、没碰过的跟当前默认值走。
      旧格式记录（无基线）备份到 `206dash.theme.v1` 后忽略，并在页面上说明原因 ——
      留着它就会永远挡住新默认值。
      `test-theme-json.js` 里把用户的 bug 完整演了一遍（用页面里真实的 ARC_FALLBACK
      当新默认值、旧默认值当基线、只改一个颜色），断言水温弧跟着新默认值走、
      用户的颜色不丢。另断言两页的记录里都带 `v:2`/`base`、都调 `mergeDefaults`、
      都有旧记录迁移键。
- [x] **两条弧开口方向相反**（用户要求："水温表可以做成上方开口吗，跟转速表同向感觉有点怪"）
      转速弧（外）**135→405**：开口在正下方，拱在表盘上方；
      水温弧（内）**0→180**：开口在正上方，兜在表盘下方。
      **实测证据**（固件落帧沿弧带径向扫）：
      6 点钟方向上只有水温弧（r 158..166），转速弧不在（r 182..204 是底色）；
      10:30 方向上只有转速弧，水温弧不在 —— 一上一下，不再同向。
      `test-gauge-geometry.js` 的「水温弧：开口在正上方」一节把它钉住
      （含"两条弧开口方向必须相反"这条断言）。
      顺带记一笔取舍：LVGL 的弧只能从 start 顺时针画到 end，"开口朝上"就要求
      覆盖区落在下半圆，唯一干净的写法是 [0°,180°]，代价是水温弧的**填充方向
      从右往左**（要 C 在左 H 在右就得放上半圆、又变回同向，只能挑一个）。
- [x] **`radius` 的语义 = 弧的"外沿"半径**（LVGL 把弧画在盒子内、外沿贴盒边，
      `width` 往里长）—— 这是这一轮扫帧时量出来的：
      radius 205 / width 24 的弧落在 **181..205**（几何值：外沿 205、内沿 205-24=181；
      扫帧时最内那行是半亮混合色，一度量成 182），不是 193..217。
      固件一直是对的（盒子尺寸就是 `radius*2`），但**两个编辑器的预览**是按
      "带宽居中在 radius"画的，等于整体外移了半个带宽（外圈差 12 像素）——
      而"表情压没压到弧"正是靠这个预览判断的。已改成画在 `radius - width/2`，
      `test-gauge-geometry.js` 断言这一点（注释里提到的不算数，它会先去掉注释）。
- [x] **转速分档按实车地标**（用户实测：点火怠速 900 / 稳定巡航 2000 / 上限 6000）
      ⚠️ **2026-09-18 更正**：那条"上限 **6000**"是错的 —— 用户确认**表盘到 7000**
      （下面那条 `kRpmMax` 7000 → 6000 的改动就是照着这个错前提做的，
      已改回 **7000**）。后果与当时担心的相反：不是"弧填不满"，而是
      **6000 转以上弧就不动了**（真实 7000 转时弧早已满格）。
      教训：**表盘量程要问看表的人**，别从"发动机断油约 6500"倒推表盘刻度 ——
      这两件事本来就可以不一致（断油 6500 而表盘 7000 是很常见的组合）。
      顺带说明：红区阈值 5000 也是按 6000 的刻度标的，表盘更正后偏早
      （正常 kickdown 就能亮），这一条在"5+5 分档"那轮一并重排。
      · 表盘刻度上限 `kRpmMax` 7000 → **6000**：弧现在能在上限处画满
        （旧值会永远停在 86%，看着像"表没画完"）。
      · 分档 1800 / 3500 / 5000：怠速 900 落在常态（并给起步留余量）、
        巡航 2000 落在巡航、红区在**上限前 1000 转**开始报红。
        旧阈值（2500/4500/6000）在真车上等于"巡航显示常态脸、红区只有断油才亮"。
      · 阶段表的转速四档直接改用实车地标（900/2000/4200/6000），
        `test_rpm_stages_use_real_landmarks` 断言三个地标各自落在对的那一档。
      · **假数据源也改了**：转速不再绑在车速上（旧算法最高只到 4980，
        红区永远演不到），改成独立慢波扫 怠速→上限 ——
        预览帧因此能走完 常态→运动→红区→运动→巡航 的完整循环
        （实测左屏出现 青(红区) 32 帧、绿(运动) 11 帧、黄(巡航) 13 帧、红(常态) 30 帧）。
- [x] **图片角色支持双屏两套、每屏 4 张**：`ImageRole` 现在 9 个在用 ——
      背景 + **左屏 4 个状态（转速表）** + **右屏 4 个状态（速度表）**。
      **屏与表的对应按法系车来（标致 206 实车）：左屏 = 转速表，右屏 = 速度表**，
      水温表在转速表上。别按"左车速右转速"的日德习惯改回去。
      **保留编号 2/5/7/9/10/11/14/15/16/19/20 一律不复用**
      （分别是当年的开机帧、左屏惊喜、右屏红区、开机图、眨眼图、冷车/过热图）——
      复用会让别人已导出的 image.bin 静默变成另一个角色。新角色从 21 开始。
      编号是"界面下拉框 / 打包器 / 固件"三方共用契约，三方都被钉死
      （C 的 `test_role_ids`、`test_face_role_ids_match_stages`，JS 的
      `test-face-stages.js`、`test-image-blob-build.js` 角色编号一节），
      往返测试还核对中文名。
      **开机画面不用图片**：它是程序化的（`boot_anim.cpp` 的淡入 + 扫表 + 显形），
      `boot_stagger_ms` 已让两屏错峰启动，观感上就是两个不同的开机过程；
      用位图要多占 144KB 和一套时序逻辑，不值。
- [x] **数字读数（转速/速度大数字 + 单位 + 水温数字）**：
      大数字 48 号在表盘正上方、单位 18 号紧随其下、水温数字在转速表底部；
      颜色/字号/位置/开关全部来自主题的 `readout` 段（可运行时改，不用重编译）。
      "大数字跟哪一路"由**弧**决定（该屏第一条非水温弧），不看屏幕序号。
      **实测证据**（pcpreview 落帧 + `check-preview-frame.js` 读像素）：
      左屏 数字墨迹 x[183..294] y[55..88]（4 位转速）、单位 x[223..256] y[104..117]、
      水温 x[220..259] y[378..390]；右屏 数字宽 66（3 位车速）、**无水温数字**；
      开机扫表期间三处命中 0 像素（数字栏是空的）。
      踩过的坑：`lv_label_create()` 的**默认文本是 "Text"** ——
      不显式清空的话，开机时表盘上会出现两个 "Text"，而"带里有没有白像素"
      这种检查是发现不了的（Text 也是白的）；所以检查里加了**墨迹外接框**。
- [x] **表情阶段表在固件与网页两端一致**：
      固件侧 `lib/dashcore/face_stages.h` 是唯一事实来源
      （16 条用例 + 每屏状态集合 + 缺图降级链 + 槽位→角色编号），
      `test_face_stages.cpp` 逐条断言 `face_update()` 对**左右两屏**的输出；
      网页侧 `tools/theme-editor/face-stages.js` 是它的镜像，
      `test-face-stages.js` **解析 face_stages.h** 后逐字段对账（429 项断言）。
      理由：表情导入页的"阶段模拟"是用户刷图前唯一能看到的证据，
      网页那份和固件那份不一致，预览就是在骗人。
- [x] **每组阶段只变自己那一维**（用户试用时抓出来的规则）：
      点"速度·中"时**转速表不能跟着动**，否则画面里两个表同时变，
      根本看不出这一档改了什么。
      第一版违反了：速度组的转速写成 1500/2600（想借转速凑出巡航/运动脸），
      结果点速度档时转速弧和转速表的表情一起变。
      其实"速度≥30/≥90"这两条阈值自己就能给出巡航/运动，不需要借转速。
      现在速度组固定 rpm=900（怠速）、转速组固定 speed=0、非水温组固定 85℃，
      由 `test_stage_values_are_consistent` + `test_only_own_gauge_moves_own_screen`（C）
      与 `test-face-stages.js` 的"每组只变自己那一维 / 每屏只被自己那一路驱动"（JS）
      两端钉住。
- [x] **缺图降级链**：两屏各 4 个状态里只导入一部分也能正常显示 ——
      运动缺图退巡航、超速缺图退运动……每个状态都一定能退到常态；
      一张都没有才回落到程序化占位表情。左右屏各查各的（两套差分图不能顶替，
      编号表里"这屏用不到的状态"写成 0，初始化时直接跳过）。
      **实测证据**：测试图只含 3 张老表情时，"运动"确实显示成了红区那张
      （左屏 #ffff00），"常态"显示常态那张（#ff0000）—— 降级链按预期工作。
- [x] **JS 打包器 ↔ 固件解析器的往返一致性测试**（`tools/theme-editor/test-image-roundtrip.ps1`）：
      Node 侧 362 项断言 + 用 JS 打镜像（覆盖全部在用角色）+ 让
      **固件自己的解析器**逐字段、逐行、逐字节对账。
      这个核对实测抓出四个真 bug，全都是编译期看不出来的：
      索引项名字用了上一轮循环的遗留变量（每项名字都变成最后一张）、
      像素循环漏 x 偏移（每行重复第一个像素）、
      `offset` 是 u16 把镜像卡在 64KB、结构体对齐算错（44 vs 48）。
- [x] 固件侧 P0 准备就绪（**未上板，仅静态验证**）：
      `setup()` 会打印 `206 dash ok`（放在最前，后续初始化失败也能看到）；
      每 5 秒打印 `SRC speed/rpm/coolant | v=..km/h ..rpm ..C` —— 带数值，
      因为桩驱动丢弃画面，"假数据扫表"只能靠串口确认；
      sim 车速按 `0.5*(sin(t*0.15)+1)*210` 扫满 0→210（周期约 42 秒，
      实测 5 秒间隔能看到明显变化）。以上字符串已确认存在于
      `.pio/build/esp32dev/firmware.bin` 中。
      上电后还会多两行便于定位：`theme: ...` 和
      `image ok: N 张,...` / `image none: 无图片资源,背景用主题纯色`。
- [x] 刷到板子并观察上述输出（**2026-09-18 已在 S3 上确认**）：
      `206 dash ok` / 自检五行 / `theme: 分区为空,用默认主题` /
      `image: 镜像无效或未刷入,不用图片资源`（还没刷图片资源，属正常）/
      `van phy: gpio 就绪 RX=GPIO16(RO)` / 每 5 秒的
      `SRC speed=sim rpm=sim coolant=sim intake=sim | v=176.6km/h 5963rpm 87.0C 28.3C`
      （车速按 42 秒周期扫 0→210 ✓）。表情跟着走：
      `206 dash ok  spd= 58% rpm= 69% coolant=85C face=sport/sport`。
      当时跑的还不是最终固件 —— 详见上面「S3 第一次真正上电」一条。
      （经典 ESP32 那份 `esp32dev` 仍未上板实测，但它本来就是廉价回归目标。）
      若之后刷了 image.bin，这里应变成 `image ok: N 张,...` ——
      那是"分区表 + mmap + 格式"三者同时正确的证据。
- [ ] 真屏到货：锁分辨率（改 ui_theme.h 的 THEME_DISPLAY_RES）→ 写实驱动
      （dash_display.cpp，#error 保护）→ 删 DASH_DISPLAY_STUB；板子换 S3+PSRAM
- [ ] **把图片画到真屏上**（`dash_ui.cpp` 的图层已接好，**未上真屏**）。
      宿主机预览（pcpreview）已经逐像素验证过：背景图铺满整屏、**弧压在背景图之上**、
      表情图压在最上层、透明通道生效、缺图按降级链退、数字读数位置正确；
      但"真屏驱动取像素的路径"仍要等实驱动写完才算数。
- [ ] 真屏上核对**弧的端点**（外部复审点出的唯一未覆盖环节）。
      预览走的是宿主机 LVGL，端点没有圆头收尾；真实屏 + 真实驱动下，
      弧两端可能因抗锯齿/圆头**胖 1~2 像素** —— 也就是"起点那一格到底亮没亮"。
      看两处：① 转速弧起点（7:30 方向，0 转时应当只有轨道）；
      ② 水温弧**镜像**后的起点（左端，默认 `reverse=1`）。
      先观察再决定：`arc_set_progress()` 里"哪端固定"是唯一判定点，
      **不要**为了端点好看去预改它。
- [ ] 实车 K 线 OBD：010C/0105/**010F**/**010D** 持续更新，断线 3 秒回退假数据
      ★ **车那一侧验过了**（2026-09-17，蓝牙 ELM327 + 手机 App 走 EOBD）：
      转速/水温都能实时读到，并且发现 ECU 还支持**进气温度(010F)** 与
      **车速(010D)**。
      **车速那条的判据**（2026-09-18 用户补充了测试条件，很关键）：
      测试是在**地库**里做的 —— 那里**没有 GPS 定位**；松刹车后车挪**不到一米**，
      车速读数立刻跟着变。这种响应只可能来自车轮转速传感器，GPS 在这种
      速度/信号条件下不可能有读数。
      *（我先前一度按"手机 App 的车速默认走 GPS"把这条降级为待验 ——
      那是拿通用习惯去否定一次特定条件下的实测，判据本身比习惯有力得多。
      教训：记录实测结论时要**把测试条件一起记下来**，否则后人只能靠猜；
      这条现在补上了"地库 + 亚米级响应"两个条件。）*
      剩下的是**固件这一侧**要跑通 —— 得等 4 号 USB 有线版到货，
      因为蓝牙版连不上 S3（S3 只有 BLE，没有蓝牙经典/SPP），
      原因与分工见 PURCHASE.md 的「蓝牙版实测结论」。
      到车上仍要直接用 `010D` 问一次确认（手机 App 走的路径未必就是这个 PID；
      固件这边做成了"先问 0100 位图，支持才轮询"，插上就会自己报）。
- [x] **OBD 车速(010D)这条路先铺好，等适配器到货就能验**（2026-09-18）
      起因是用户问了一句很准的话："有线 ELM327 要是能读全部数据，
      为什么还要 SN65HVD230 抓帧？" 查下来两个事实：
      ① 文档里"车速只能从 VAN 取"讲的是**物理层**（OBD 座上没有 VAN 引脚），
         不等于"OBD 读不到车速" —— 这两件事在文档里被混成了一句，已改正
         （用户随后补充的测试条件更直接：地库 + 亚米级响应 ⇒ OBD 上确实有车速，
         见上面那条实车清单）；
      ② 代码里**根本没有车速的 OBD 通路**（轮询表只有 0C/05/0F），
         所以之前"车速靠 VAN"一半是设计，一半是没写。
      于是把这条路补上，但**不是简单往轮询表里塞一个 PID**：
      · **先问 0100 位图**（ECU 自己声明支持哪些 PID），只有 0x0D 置位才轮询它。
        理由就是 `obd_source.h` 里那笔"时隙账"：K 线是排队共享的窄管子，
        每多一个时隙，转速/水温/进气各慢 ~25%；而"ECU 不支持"的代价更大
        （ELM327 要等到超时才回 NO DATA，白扔一个时隙）。拿不到位图就按
        原来的三路跑，**不猜**。
      · 优先级定成 **车速 Van > Obd > Sim**：VAN 上的车速是广播（零 K 线成本），
        优先用它，把时隙留给转速；OBD 是兜底（VAN 没接时不掉回假数据）。
      · 每个字段加**实测刷新率**（Hz，每秒结算），随 `SRC-Hz` 行打到串口，
        另外 0100 的结论只报一次（`obd: ECU 位图 0x… -> 车速(010D)支持/不支持,
        车速轮询已开/关闭`）—— 到车上第一眼就看这两行，用数据决定
        "要不要花时间做 VAN 抓帧"。
      `-DOBD_SPEED_PID=0` 可强制关掉车速那一路（对比两种刷新率用）。
      ★ 优先级是**按"更快的源优先"定的**，不是按"哪个更权威"：
      VAN 是广播、OBD 要轮询。等车上量出 `SRC-Hz` 之后再回头看这个决定 ——
      若把两条慢弧（水温/进气）降频（`obd_source.h` 里预留的 kSlowEveryNTurns
      那个旋钮）之后 OBD 车速能到 3~5Hz，那"OBD 优先、VAN 做补充"也成立。
      用数说话，别用偏好。
- [x] **快路/慢路降频：温度让位给转速和车速**（2026-09-18，用户提出）
      用户的原话："进气温度和水温确实可以先降频，这两参数确实不需要太快速的
      更新温度"。做成 `kSlowEveryNTurns = 4`：
      快路（转速 0x0C，ECU 支持时加 0x0D 车速）每轮都问，
      慢路（水温 0x05、进气温度 0x0F）每 4 轮问一次 ——
      一个周期的序列是 `0C 0D 05 | 0C 0D 0F | 0C 0D | 0C 0D`。
      ★ 顺手抓出一个**比降频本身更大的浪费**：`PollWait` 那一格原先
      **只会等满 250ms 超时才离开**，收到响应也不提前走 ——
      也就是每格固定 250(等)+200(间隔)=450ms，不管 ECU 其实 30ms 就答完了，
      算下来只有 ~2.2 请求/秒，K 线的时间有三分之二被扔掉。
      现在按 `waiting_pid_` 认领**当次请求**的响应、当即回到间隔态
      （没响应才走超时那条 —— 这正是超时值必须留着的原因），
      间隔也从 200ms 收到 80ms。
      预估（**等车上 SRC-Hz 实测替换**）：转速/车速 ≈2.2~3.6Hz、
      水温/进气 ≈0.55~0.9Hz；不支持车速时转速 ≈3.6~5.7Hz。
      改动前每字段只有 ≈0.7Hz —— 也就是快 3~4 倍。
      **实测（宿主机）**：native 115 例（113 通过 / 2 跳过；新增 3 条：
      快路/慢路周期序列**逐格对字符串**（不能用 `sent()`，那只看"发过没有"，
      顺序错了照样通过）、不支持车速时的序列、以及"收到响应 100ms 内就发下一条"
      （旧实现这条必然失败，等于把上面那个浪费钉死）。
      ★ 这两条序列用例第一版挂了，原因又在测试侧：我把响应**提前**塞进去，
      于是请求还没发、响应先被解析掉；状态机不认（不是当次请求），
      每格白等 250ms 超时，测出来的节奏是假的。改成"按刚发出的请求喂响应"
      之后才对 —— 与"喂数据必须对齐状态机"这条老经验是同一回事。
      **实测（宿主机）**：native 112 例（110 通过 / 2 跳过；新增 7 条：
      位图位序两端各钉一个、位图不被当数据帧、支持/不支持两种轮询表、
      位图超时不卡链、010D 解析、刷新率结算、车速三级优先级、
      刷新率到得了 SRC 行）；esp32dev / esp32s3 / pcpreview 三目标 SUCCESS。
      ★ 这次四条用例先挂了，其中**两条是我的测试数据想当然写错的**
      （0xBE 的最高位其实是 1、0x13 的最低位其实是 1），另两条是刷新率
      结算窗口没对齐。教训与"阶段表那次"一样：位图/位掩码这种东西，
      **两端各钉一条**（最高位 + 最低位），中间靠推理。
- [ ] 实车 VAN：0x824 车速帧格式验证（不符先用 configureSpeedFrame 现场改）
- [x] **表情分档扩成"每屏 5 档"**（2026-09-18，用户定：转速 5 + 车速 5）
      用户给的边界是"转速表上限 7000、速度表上限 210"，并按 **TU5JP4（1.6 16V）
      + AL4（4AT）** 的实际活动范围重排分档。设计依据（推算，待实车抽验）：
      AL4 齿比 2.72/1.50/1.00/0.71 + 主减 4.14 + 195/55R15 ⇒
      `转速 ≈ 车速 × 齿比 × 36.9`：
      · 日常全在 **1600~3400 转**（4 档 60km/h≈1570、100≈2620、130≈3400；
        3 档 90≈3300；AL4 正常升档点 2000~2500）
      · 全油门才上得去：1 档 50≈5000、60≈6000；2 档 80≈4400、100≈5500
      · **断油 6300（用户实测 2026-09-18）** → 红区从 **5800** 起（留 420 转提前量）
      **分界刻意避开日常落脚点** —— 1600~3400 之间一个分界都不放，
      否则 4 档巡航会贴着边界抖（这是这一轮最主要的设计约束）。
      最终五档：
      | 屏 | 档位 | 阈值（进 / 退） |
      |---|---|---|
      | 左（转速） | 怠速 / 巡航 / 运动 / **高转** / 红区 | 1500(1580/1420) · 3500(3580/3420) · 4500(4580/4420) · **5800(5880/5720)** |
      | 右（车速） | 静止 / **市区** / 快速路 / 高速 / 超速 | 30(33/27) · 65(68/62) · 95(98/92) · >130(≤127 退) |
      加这两档的理由：左屏 4000（扭矩峰）与 5750（功率峰）是两种体感，
      1/2 档全油门真能走到 4500+；右屏原来 30~90 合成一档，
      等于把"市区跟车"和"环路 90"画成同一张脸。
      **实现**：新槽位 High=5 / City=6（**追加在末尾**，枚举号就是降级链下标），
      新角色号 **21（左·高转）/ 22（右·市区）**（编号从 21 起接，保留号不复用），
      阶段表 14 → **16 条**，每屏状态 4 → 5，槽位 5 → 7，降级链 5×5 → 7×5。
      迟滞这件事也一起做了：新增 `face_ladder.h` 的纯函数 `ladderStep`
      （**每条边界**都有迟滞，不再只有超速那条有）——AL4 的锁止/解锁差 200~300
      转、定速巡航 ±1km/h，没有迟滞脸会来回切。
      **实测（宿主机）**：native 114 例（0 失败 / 2 跳过；新增
      `test_face_ladder_hysteresis`：进入门槛、退出门槛、红区、车速各方向都钉住）；
      Node **428**（face-stages 从 318 涨上来）/ 156 / 71 / 211 / 11 全绿；
      esp32dev / esp32s3 / pcpreview 三目标 SUCCESS。
      ★ 这一轮踩到两个坑，都值得记：
      ① **槽位顺序 ≠ 档位顺序**：`kFaceRoleId` 按槽位排（两屏共用一套槽位名），
         而网页的 `states` 按该屏档位从低到高排 —— 左屏加了"高转"之后两套顺序
         就分叉了（高转槽位是 5，却是该屏第 4 档）。按**下标**比对的对账测试
         立刻报"角色编号不一致"，而两边其实都对 → 改成**按状态名**比对。
      ② **对账测试的守卫数字没同步**：它先说"face_stages.h 排版被改了"，
         把"预期数字没更新"这个真原因盖住了。现在守卫会打出实测数字。
- [ ] 实车核对**车速**分档（数值按 TU5JP4 + AL4 推的，边界位置要上车看）：
      30/65/95 三条边界是否"切得太频繁"、130 超速是否合适。
      到车上按这个看一眼：市区 50 该是"市区"、环路 80~90 该是"快速路"、
      高速 110~120 该是"高速"、怠速/挪车该是"静止"。
      不合适就改 `expression.cpp` 顶部的 kSpeedCityFrom/kSpeedFastFrom/kSpeedHighFrom，
      别忘了同步 `face_stages.h` 的阶段表（Node 对账测试会提醒）。
- [ ] 实车核对转速分档的**手感**（数值按 TU5JP4 + AL4 推的，边界位置要上车看）：
      1500 起巡航、3500 起运动、4500 起高转、6000 起红区。
      开起来如果觉得"上匝道加速时脸切得太频繁"，就把 kRpmSportFrom 往上抬；
      如果觉得"红区提醒太晚"，就把 kRpmRedlineFrom 往下降。
      这三个数在 `expression.cpp` 顶部，改完 `face_stages.h` 的转速四档要跟着改。
- [x] **圆弧两端端帽不对称（2026-09-21 落帧实测修正）**：用户看到的"弧的起点是圆的、终点是方的"。
      根因：`src/dash_ui.cpp` 的 `build_arcs()` 只给 `LV_PART_INDICATOR` 设了 `arc_rounded`
      （原第 148 行），**轨道那半边 `LV_PART_MAIN` 从没设过**；而 `lv_obj_remove_style_all`
      又把 LVGL 主题里 indicator 的圆头一起删了，于是 MAIN 落回属性默认值 0 = 平头。
      四条弧（左：转速主弧＋水温副弧；右：车速主弧＋进气副弧）都出自这一个函数，所以四条全中。
      修法只有一行：补 `lv_obj_set_style_arc_rounded(arc, true, LV_PART_MAIN);`
      （厚度／角度／半径／颜色一行没动；主题层 `ArcStyle` 本来就不暴露 `arc_rounded`，
      所以这条只能落在 `build_arcs()` 里，也不存在按屏覆盖）。实测（pcpreview 落帧，
      沿半径逐角度量墨迹宽度）：弧尾外伸 **0.00°／0.15° → 3.45°／1.65°**，
      与弧首 3.60°／2.10° 同量级；剖面由"整宽硬断"变成半圆收细
      （24→21.8→19→11→0px，与 w/2 端帽模型逐点吻合）。两屏 `check-preview-frame` 各 17/17。

## 两处措辞更正（审查指出，实测确认）

**1）"主题做成只读省了 36KB DRAM" —— 说法不准确，已改。**

实测 `sizeof(Theme) = 256` 字节。主题结构体本身从来不是内存大头，
所以"只读指针"省不下那 36KB。真实情况是：主题若是**可写全局**，
LVGL 就无法把样式对象折叠进 rodata（可写全局会阻止 const 折叠），
于是链接期多出约 36KB —— 那个数字来自链接 map 对比，
**成分是 LVGL 的样式对象，不是主题数据**。

正确表述：*把主题做成只读，才能让 LVGL 把样式对象放进 rodata；
主题数据只有 256 字节，与这个数字无关。*
这个取舍作为**原则**保留（资源放 .rodata、运行时只留指针），
但不要再写成"已经省下 36KB"。

附带一条观察（审查提到，属实）：从 theme 分区解析出来的那份在 DRAM，
与 .rodata 里的默认主题是**两份**。256 字节无所谓，但这是个陷阱，
已在 `ui_theme.h` 的注释里写明，不要当成"只留一份"来理解。

**2）CRC 多项式：`0x4599` 是 CAN-15，不是 VAN/TSS463 的。**

实测自检：CAN-15（0x4599、初值 0、不取反）对 `"123456789"` 给出 `0x059E`，
与 CAN-15 的公认校验值一致 —— 也就是说现在线上用的这条**确实是 CAN 的**。

已按 TSS463/Graham 描述补上独立实现 `crc15_van_iso()`
（多项式 x^15+x^11+x^10+x^9+x^8+x^7+x^4+x^3+x^2+1，初值 0x7FFF，发送前取反），
**但不替换** `crc15()`：两条都还没被真实抓包证实。

> 位掩码记一笔：完整掩码是 **0x8F9D**（含 x^15 那一位），
> 寄存器只有 15 位故实取 **0x0F9D**。我手算时漏了 x^15 写成 0x7F8D，
> 被 `test_van_iso_poly_mask` 当场抓住 —— 又一次"别手算位掩码"。

**结论：两条多项式都复现不出公开抓包的 FCS，而且问题不在于选哪条。**
把 5 帧真实导出（DATA/FCS 分界未公开，故遍历所有分界）×
所有 15 位多项式 × 初值/取反/左右移 全枚举，**能同时打中 5 帧的组合数 = 0**。
所以建模层面（字节内位序 / CRC 覆盖范围 / 4B5B 位到字节的映射）
至少有一处与线上不一致 —— 换多项式解决不了。见
`test_van_wire.cpp` 的 `test_van_iso_crc_against_public_frames`。

## 分区表（`partitions.csv`，改这里必须同步改三处常量）
| 名称 | 类型/子类型 | 偏移 | 大小 | 谁在用 |
|---|---|---|---|---|
| `nvs` | data/nvs | `0x9000` | 20 KB | WiFi 等 |
| `otadata` | data/ota | `0xe000` | 8 KB | OTA 状态 |
| `app0` | app/ota_0 | `0x10000` | 1 MB | 固件（现占 62.6%，含 18/48 号字体） |
| `app1` | app/ota_1 | `0x110000` | 1 MB | OTA 备份槽（**必须与 app0 等大**） |
| `theme` | data/0x40 | `0x210000` | 16 KB | 运行时主题 JSON |
| `spiffs` | data/spiffs | `0x214000` | 256 KB | 未用（本项目没有文件系统） |
| `image` | data/0x41 | `0x254000` | **1 MB** | 图片资源（mmap 只读） |
| `coredump` | data/coredump | `0x354000` | 64 KB | 崩溃转储 |

严丝合缝排到 `0x364000`（3.39 MB / 4 MB），无重叠、无空洞。

三处必须一起改：`lib/themetool/theme_store.h` 的 `THEME_MAX_BYTES`、
`lib/themetool/image_blob.h` 的 `IMAGE_PARTITION_BYTES`、
`tools/theme-editor/image-blob-build.js` 的 `PARTITION_BYTES`
（以及 README 和 `esptoolCommand()` 里的刷写偏移 `0x254000`）。

**这张表踩过的坑：**

1. **分区重叠**：`image` 一开始写成 `0x3DC000 + 0x20000`，正好越过
   `coredump` 的 `0x3F0000`，构建报 `CSV Error: Partitions overlap`。
   空洞不会报错，重叠一定报 —— 所以宁可排满也别留缝。
2. **两个 app 槽必须等大**。从 1.25 MB 降到 1 MB 是为了腾出 512 KB 给
   `image`；但 app0/app1 一旦不等大，OTA 升级会失败。
3. **128 KB → 1 MB 的直接原因**：图片资源。去掉开机图后需要的是
   **1 背景 + 两屏各 4 个状态的表情**（一共 8 张）；240×240 时 8 张表情要
   1.35 MB、240 背景再要 113 KB —— **1 MB 仍然装不下**，所以：
   · 降到 **192×192**：8×108 + 72 = 936 KB → **能装下整套**；
   · 或者按"能放多少放多少"往里塞（先背景 + 两屏常态 = 约 450 KB），
     缺的状态自动降级 —— 所以"一张都没有也能跑 + 缺图有降级链"是硬需求。
   这也解释了为什么表情状态要压到每屏 4 个：14 个状态那版无论怎么调尺寸
   都装不下一整套（而 8 张至少有一个能全放下的工作点）。
   换 S3（16 MB flash）后把 `image` 分区放大到 4 MB 就能全用 240×240，
   分区表改这里（`partitions.csv`）即可，代码不用动。
   逐帧满屏开机动画仍然放不下：480×480 × 12 帧 × 2 屏 = 10.5 MB，
   比整块 flash 还大 —— 所以开机画面走程序化扫表，不碰图片资源。

## 图片格式（`image.bin`）的两个关键决定

- **`offset` 是 u32，不是 u16。** 曾经是 u16，把整个镜像的像素数据卡在
  64 KB 以内 —— 一张 480×480 背景就要 450 KB，1 MB 分区等于白给。
  而且它**不报错**：偏移溢出后设备端拿到错误的像素位置，画面是花的，
  但日志一切正常。
- **索引项 44 字节、没有任何对齐空洞。** 这条是被测试逼出来的：
  手算对齐错了两次（一次漏算两个 u32 之间的洞、一次多算尾部填充），
  两次都是 `test_entry_field_offsets` 先发现。现在 C 侧用
  `offsetof`/`sizeof` 断言、JS 侧用字节断言，把同一张表各钉一遍，
  往返测试再逐字节对账。

## S3 板子开箱：为什么"刷成功但串口一片空白"（2026-09-18 全天实测）

第一次刷板，`Upload SUCCESS` 之后串口监视器**一片空白**，看起来像固件没跑。
真相是**四件事**叠在一起，其中三件与固件无关 —— 全部查清并已修：

1. **这块板子有两个 USB 口，插哪个口差别是天壤。** 设备管理器实测：

   | 口 | 芯片 | 它到底是什么 |
   |---|---|---|
   | 标着 **UART** 的 | **CH340**，`VID_1A86:PID_7523` → `COM4` | **真 UART0** + **真 EN/IO0 复位线** |
   | 标着 **USB** 的 | `VID_303A:PID_1001` | USB-Serial-JTAG（原生 USB，**不是** UART 桥） |

   **结论：刷写、监视、排障一律走 UART 口。** 它的复位是真的（EN 拉低再放开），
   所以刷完的 `--after hard_reset` 之后 app **真的会启动**；ROM、二级 bootloader、
   panic 的日志也全在这条线上，复位后**第一个字节**都抓得到
   （见 `tools/serial-capture/capture.py`）。

2. **native USB 口那条路会把芯片留在 ROM 下载模式。**
   USB-Serial-JTAG 的"复位"不是拉 EN 电线，而是一个**软复位请求**；
   esptool 用 `USBJTAGSerialReset` 表达它（`tool-esptoolpy/esptool/reset.py`）：
   ```
   DTR=True   # Set IO0   ← IO0 拉低 = 下载启动
   RTS=True   # Reset. Calls inverted to go through (1,1) instead of (0,0)
   ...        # 最后 RTS/DTR 都 False
   ```
   实测后果：**每一次** esptool 复位（刷完的硬复位、监视、探测）都把芯片送进
   `boot:0x0 (DOWNLOAD_BOOT)`，程序根本没机会跑。自证材料两条：
   · 上传后立刻 `esptool --before no_reset read_mac` **秒同步** —— 只有还在
     下载模式才可能；
   · `GPIO_STRAP_REG(0x60004038)` = `0x00000000`，即"复位时 IO0 被判定为低"，
     而 `GPIO_IN_REG` bit0 = 1、`IO_MUX_GPIO0_REG(0x60009004)` 的 `FUN_IE` = 1
     —— IO0 现在**是高电平**。那个 0 是 esptool 自己造成的，不是板子的问题。

3. **pyserial 打开串口时默认拉高 DTR/RTS**（`serialutil.py`：
   `_rts_state = _dtr_state = True`）→ **开监视器这一下本身就是一次
   "复位进下载模式"**。所以 `monitor_rts = 0` / `monitor_dtr = 0` 必须留着。
   （经典 ESP32 那边不设这两行：它的 DTR/RTS 是真正的自动复位线，而且经典板子的
   自动复位电路会让"两个都拉高"只是复位、不进下载。）
   ★ 想"打开就复位、顺便看完整启动日志"，就得自己按顺序拉线 ——
   `tools/serial-capture/capture.py` 干的就是这件事。

4. **`Serial` 曾经绑到 TinyUSB 的 `USBCDC`，写进去石沉大海。**
   `cores/esp32` 里的映射与直觉相反：
   ```
   HWCDC.h :  #if ARDUINO_USB_MODE         → extern HWCDC Serial;   ← 要的是这条
   USBCDC.h:  #if CDC_ON_BOOT && !USB_MODE → extern USBCDC Serial;
   ```
   也就是 **`ARDUINO_USB_MODE` 必须为 1**（板子定义本来就写着）。我先前"顺手"
   覆盖成 0，`Serial` 就成了 TinyUSB —— 它在这块板子上没接管 USB 外设，于是所有
   printf 都没了，**而芯片其实跑得好好的**。
   *教训：别读代码猜宏，让编译器回答* —— 用
   `static_assert(std::is_same<decltype(Serial), HWCDC>::value)` 这种探针，
   两个取值各编一次，答案立刻明确（本次就是这么定的案）。
   现在只留 `-DARDUINO_USB_CDC_ON_BOOT=1`，那两行
   `-UARDUINO_USB_MODE` / `-DARDUINO_USB_MODE=0` **不要再加回来**。

### 定位这类"静默不启动"的手段（以后照这个顺序来）

1. **先看 UART0**（UART 口）。ROM 会打 `rst:` 和 `boot:` 两个数字：
   `boot:0x8 (SPI_FAST_FLASH_BOOT)` = 正常从 flash 启动；`boot:0x0` = 下载启动。
   这一行就把范围砍一半。
2. **绕开 USB 拿"程序到底跑没跑"的铁证**：让固件在 `setup()` 第一行往 flash
   某个角落写一行字，再用 esptool 读回来。串口空白时，这是唯一能区分
   "根本没跑"和"跑了但 USB 不吐"的办法 —— 本次就是靠它把锅从固件上摘掉的。
3. **再看 flash 里的实际内容**，不信磁盘上的文件：分区表逐项核对（`0x8000`）、
   `boot_app0.bin` 与 framework 里的文件**逐字节**对比（`0xE000`）、
   bootloader/app 的 `0xE9` 镜像头（注意 S3 的 bootloader 在 **0x0**，
   不是经典 ESP32 的 0x1000）。
4. **最后才怀疑固件**：`coredump` 分区全 `0xFF` = 没崩过；示位标
   （`BEACON`，见 `src/main.cpp`）每秒报一次"走到第几步"，卡在哪一眼可见。

### 顺带查出来的真 bug：PSRAM 根本没开

自检第一次能打出真话时是 `psram : 0 KB 可用 / 0 KB 总`。原因不在板子
（esptool 认到 `Embedded PSRAM 8MB (AP_3v3)`），而在
`cores/esp32/esp32-hal-psram.h`：
```c
#ifndef BOARD_HAS_PSRAM
#undef CONFIG_SPIRAM
#endif
```
**没有 `-DBOARD_HAS_PSRAM`，核心会主动把 PSRAM 编掉**，`memory_type` 配得再对
也没用。补上之后 `psram : 8189 KB 可用 / 8189 KB 总` ✓ —— 8MB PSRAM 是
"双 480×480 整屏缓冲"的前提，这条一漏后面全白干。

### 日志改成双通道

`lib/dashcore/dash_log.h`：`dash_logf()` 一次格式化、**两个口都写**
（USB-CDC + UART0）。理由就是上面那个坑 —— 手头只有一根线时，插哪个口都得
看得见；而且 UART 口是唯一能拿到 bootloader / panic 日志的通道。
（经典 ESP32 上没有 `CDC_ON_BOOT`，`Serial` 与 `Serial0` 是同一个 UART0，
所以那边按宏判断只写一次，不会重复。）

**验证记录**（S3 N16R8 + CH340，2026-09-18）：
```
ESP-ROM:esp32s3-20210327
rst:0x1 (POWERON),boot:0x8 (SPI_FAST_FLASH_BOOT)
mode:DIO, clock div:1
entry 0x403c98d0
206 dash ok
chip  : ESP32-S3 rev0, 2 核 @ 240 MHz
flash : 16 MB
psram : 8189 KB 可用 / 8189 KB 总
heap  : 259 KB
image : 分区 8192 KB @ 0x254000(编译期口径 8192 KB)
van phy: gpio 就绪 RX=GPIO16(RO),空闲 300us 关帧
theme: 分区为空,用默认主题
image: 镜像无效或未刷入,不用图片资源
BEACON  1  step=6(loop: 刚开始一轮)  uptime=1s heap=259KB psram=8189KB flash=16MB
SRC speed=sim rpm=sim coolant=sim intake=sim | v=176.6km/h 5963rpm 87.0C 28.3C
```

- [x] **外部审核（Grok，审到 b1d920a）提的实质问题：帧可能被提前关掉**（2026-09-18 修）
      审核指到 `van_phy_gpio.cpp`：关帧用的是"距最后一条**已喂给解码器**的边沿多久"，
      而队列里可能还压着同一帧的后续边沿 → `finish()` 会把帧当场截断，
      表现是 **`edges` 在涨、`frames`/`fcs_ok` 不涨**。
      核实：**方向对，触发条件比描述的更窄**（排空是"先全排空、再判空闲"，
      普通卡顿时最后弹出的边沿总是刚弹的，微秒级前）——
      真会踩到的是**排空预算吃满**那条路（`budget = kCapacity` → 2048 个边沿
      ≈ 16ms 总线活动），此时最后弹出的边沿可能已老出几百微秒而队列里还有新的。
      代价却是单向的：多等一个 tick（几毫秒）换一整帧数据，所以照改。
      改法：判据抽成**宿主机可测**的纯函数 `vanIdleCloseReady()`
      （`frame_pending && queue_empty && 空闲超阈值`），`van_phy_gpio.cpp` 调它，
      ISR 继续只入队。新增两条用例：判据的四条边界 + **把截断场景整个复现**
      （同一帧分两次喂，中间用判据问"能关吗"，必须答不能，最后才关成功）。
      *（生成设备专属代码没法在宿主机测，所以"抽纯函数"这一步不是洁癖，
      是唯一能在 CI 里钉住它的办法。）*
- [x] **外部审核提的口径问题：编辑器还按 1MB 卡**（2026-09-18 修）
      固件 `[env:esp32s3]` 早已放宽到 8MB，而 `image-blob-build.js` 写死
      `1024*1024` —— **S3 上多出来的 7MB 从网页根本导出不进去**，
      提示还写着"超过 image 分区(1024 KB)"，把人往"把图改小"的方向误导。
      改法（照审核的意见"要目标板开关，别只改 C 宏"）：
      · `image-blob-build.js` 加 `TARGETS`（classic 1MB / s3 8MB）、
        `targetInfo()`、`partitionBytesFor()`；老名字 `PARTITION_BYTES` 保留为
        经典板的值（不破坏已有调用）。
      · 图片编辑器「占用」栏加**目标板下拉**，页面里所有分区数字都改问 `partBytes()`
        （原来 4 处各自读常量）。
      · 命令行加 `--target classic|s3`，usage 里列出两块板。
      · ★ 顺带修掉两个连带 bug：**刷写命令的 `--chip` 要跟着目标板**
        （拿 `--chip esp32` 刷 S3 会被 esptool 当场拒掉）；
        **spec.json 的 UTF-8 BOM**（记事本/PowerShell 存出来就带 BOM，
        `JSON.parse` 报 `Unexpected token ''`，完全看不出是 BOM）。
      · 测试：`test-image-blob-build.js` **直接读两份 csv** 对账
        （`TARGETS` 的大小必须等于分区表里 image 行），另外钉住
        `--chip` 两块板各是什么、未知目标板要报错。
      **端到端验证**：造一个 1.2MB 的包（8 张 280×280）——
      默认目标下 119.8% 被拒（exit 3，并提示可切 S3），`--target s3` 下
      15.0% 正常产出（exit 0，刷写命令为 `--chip esp32s3`，偏移仍是 0x254000）。
- [x] **审核提的五处口径/注释过期**（2026-09-18 逐条核实后改）
      · `obd_source.h` 头一段还写"轮流请求 010C/0105/010F"（降频后不是这样了）；
      · `obd_source.cpp` 位图超时注释"按默认三路"（现在是"快路只有转速"）；
      · `ARCHITECTURE.md` 阶段表"10 条用例"（实际 14）、
        `tools/theme-editor/README.md` "9 条阶段用例"（同上）；
      · `main.cpp` 的 `van phy` 注释"当前为桩"（`-DVAN_PHY_GPIO=1` 时就是真收帧）；
      · `data_service.h` 优先级行把水温/进气也写成 `Obd > Van > Sim` ——
        这两项 VAN 帧里根本没有，只有 `Obd > Sim`。
      另外把编辑器 README 的「1MB 能放什么」一节改成**按目标板**讲
      （经典板 1MB / S3 板 8MB），并写清两份分区表偏移相同、只有大小不同。

## 实车必验清单（van_wire 的未定项，到货后逐条确认）

> **2026-09-19 更新：第 1~4 条已用真实抓包（drive5min 17106 帧 + 仓库切片 66 帧）
> 定案，下面的原文保留作历史记录 —— 结论与证据见文末那条「FCS/CRC 与帧布局定案」。
> 第 5 条那个尾巴（背靠背帧被并进同一帧）也已在同一天修掉：关帧门限按实测从
> 300µs 改成 70µs —— 见文末最后一条「关帧门限」记录。**

1. **FCS 约定**（★ 已定案：poly 0x0F9D / init 0x7FFF / 输出取反 / MSB-first，
   覆盖 IDEN+CMD+DATA，16 位大端字段 = crc<<1；17106/17106 + 66/66 帧通过）：
   公开描述是"CRC-15、覆盖 IDEN+CMD+DATA、多项式
   x^15+x^14+x^10+x^8+x^7+x^4+x^3+1"，但用 VanAnalyzer readme 的 5 帧真实
   导出做全枚举（32768 个多项式 × 6 种字段拆解 × 3 种位序变换 × 2 种字节序）
   都复现不出其 FCS。到货后抓一帧原始位流重新定位。
   —— **真因是字段边界（FCS 18 槽 + IDEN 低 8 位先行的打包），不是多项式。**
2. **字节内位序**（★ 已定案：MSB-first）：现按 MSB-first（与 VanAnalyzer 实现一致）。
3. **SOF 槽数**（★ 已定案：**10 槽裸图案 0000111101**，17106/17106 命中；
   旧笔记里"VanAnalyzer 按 8 TS"那条作废）：规范写 10 TS（0000111101），
   VanAnalyzer 按 8 TS 整字节处理；本实现取后者（能与公开真实抓包对得上）。
4. **IDEN 宽度**（★ 已定案：线上就是 **12 位 IDEN + 4 位 CMD**，
   字节流 `[IDEN>>4, (IDEN&0xF)<<4|CMD]`；bit12..14 线上不存在）：
   线上两个字节只承载 **12 位** IDEN + 4 位 CMD。
   规范字段是 15 TS（bit12..14 落在 SOF 区域内），当前布局没有它们的
   位置 —— 结构里保留了 15 位 iden，但字节编解码只搬运 12 位。
   公开样例是 0x8C4，本项目假设 206 实车为 0x824 —— **已实测确认 0x824**。
5. **ACK 位与帧尾**：解码器**不再**用"8 个连续 recessive"当帧尾
   （帧内合法数据会带出 8 个连续 recessive，实测 8A 22 5A 这帧就有一段，
   照它收尾会在帧中途截断）。现在帧的完整性**由 FCS 校验判定**，
   总线空闲只负责复位。实车要确认 ACK 位行为（`Stats::frames` 与
   `ackDominant()` 可观测）。
   —— **ACK 结构已定案**：仅 `cmd bit2=1 且 RTR=0`（0xC/0xE）的帧在 FCS 后有 2 槽
   （recessive + dominant）；解码器的 ACK 判据已按它重写（旧实现恒为 false）。

## 构建环境备忘

- 本机用便携 zig 作宿主机编译器（`.tools/zigbin` 转发桩）；zig 有全局缓存
  （`%LOCALAPPDATA%\zig`），出现"改了代码但行为不变"时先清它。
- PlatformIO 的 native 测试产物在 `.pio/build/native`，怀疑构建陈旧时整目录删掉重来。
- 工作副本在中文路径（`C:\Users\张九思\206Dash`），**构建必须在纯 ASCII 副本
  （`C:\Users\Public\206dash\Neru-s-206-dashboard`）上做**，两侧用 robocopy 同步。
  中文路径下 ESP32 工具链会报 `g++ Invalid argument`。
- **`tools/theme-editor/test-image-roundtrip.ps1` 必须存成带 BOM 的 UTF-8**。
  Windows PowerShell 5.1 读没有 BOM 的 .ps1 会按 GBK 解释，脚本里的中文
  会变成乱码进而**破坏字符串引号配对**，报
  `Missing argument in parameter list`。用 `edit`/`write` 改完这个文件后要补 BOM：
  `[System.IO.File]::WriteAllBytes($p, [byte[]](0xEF,0xBB,0xBF) + $bytes)`
- 同理**别用 `Get-Content` 去看 UTF-8 文件**：PS 5.1 按 GBK 读，显示乱码
  但文件本身是好的（这次差点因此去"修"一个没坏的 manifest）。
  要确认内容请用 read 工具或 `[System.Text.Encoding]::UTF8.GetString()`。

## RGB 并口屏驱动骨架 + 一个"买屏前必须知道"的发现（2026-09-18）

用户确认两块屏**还没买**，板上只有 2.54mm 排针（没有 FPC 座）。这一轮的产出是
"先别买错" + "动手时从哪开始"。

**1）实驱动骨架落地**：新增 `src/dash_display_rgb.cpp`（`-DDASH_DISPLAY_RGB=1`，
新 env `[env:esp32s3-rgb]`），提供与桩/预览**同一组接口**，所以 `dash_ui.cpp`
一行未改。里面已经写好：LVGL→面板的区域 flush（`esp_lcd_panel_draw_bitmap`，
只搬变化区域）、每帧完成回调（只在中断里记数）、每秒一次的 `rgb: frames=…/s`
诊断（与 VAN 那条 `edges/frames` 同思路）、以及 ST7701S 的 3 线 SPI 初始化通路。
**屏到手后只需要填文件顶部三块**：引脚、初始化命令表、时序（各项都注明了从哪来）。
编译已验证：`esp32s3-rgb` SUCCESS（RAM 46.2% / Flash 69.3%）。

**2）★ 关键发现：这一版框架的 `esp_lcd` 是旧精简版，双屏方案被它卡住。**
`framework-arduinoespressif32` 自带的 `esp_lcd_panel_rgb.h`（4 个芯片目录同一份、
120 行）只有 `esp_lcd_new_rgb_panel` + 单个 `on_frame_trans_done` + `fb_in_psram`；
**没有** `num_fbs` / `esp_lcd_rgb_panel_get_frame_buffer` /
`esp_lcd_rgb_panel_register_event_callbacks` / bounce buffer（那些是 IDF 5.x 的）。
而"两块 480×480 共用一条 16 位总线、轮流发帧"**恰好需要那三样**：
DMA 得能在两块帧缓冲之间自动切换。旧 API 下面板只有**一块** fb，
硬做会出现"两块屏轮流显示对方的画面"（每帧约 16ms 的鬼影）。
⇒ 双屏两条出路（都不推翻现有代码）：
  ① **换构建**拿到 IDF 5.x 的 esp_lcd（新 pioarduino 平台 / 直接 ESP-IDF 工程）
     → 一元 RGB 总线 + 硬件 1→2 选通（16 位缓冲 + /OE 选通脚）成立，每屏 30Hz。
     **推荐**：本文件的时序/初始化/LVGL 绑定都能留。
  ② 两块屏各占一组数据线 → 需约 40 根 ✗ S3 没有。
这也解释了为什么"先把 13 号验证板买来跑通"是对的：它是**带屏的整板**，
0 根线，验证的是帧率/内存/观感，与这个 API 缺口无关。

**3）买屏信息**（详见 `.md` 第十一节与回复）：官方型号 `ESP32-S3-Touch-LCD-2.1`
（平面）/ `-2.1B`（2.5D 曲面）；淘宝按关键词搜（商品链接会话绑定、会失效，
关键词耐用）；核对清单：480×480、**RGB 并口**、ST7701S、S3 + 8MB PSRAM、
板内已有 FPC 座。
## 显示驱动:SPI 分支也备好了（2026-09-18，双屏两条路都能走）

用户看 13 号板商品图发现该型号的屏"**一组 SPI 同时控制 SD 卡与屏幕**"、副板是
**SH1.0 14PIN** —— 14 根线塞不下 RGB565 的 20 根信号，所以那个型号是**自带 GRAM 的
SPI 屏**。于是补上 SPI 分支，让"屏到手无论哪种接口都能直接跑"。

**新增** `src/dash_display_spi.cpp`（`-DDASH_DISPLAY_SPI=1`，新 env `[env:esp32s3-spi]`）：
· 两块屏**共享 SCK/MOSI**、各自 CS/DC/RST/BL ≈ **10 根**，**不需要转接板**；
· 每屏一个 `lv_display` + 一条 480×40 的 SRAM 缓冲（各 38KB，不占 PSRAM）；
· flush = 写窗口(CASET/RASET) + RAMWR 送像素，**异步**（`on_color_trans_done`
  里调 `lv_display_flush_ready`）；
· 背光走 **LEDC PWM**（20~40mA 不能 GPIO 直驱），默认 80%；
· 每秒一行诊断：`spi: 左 flush=…(+…/s …KB/s ~…fps) | 右 …` ——
  "SPI 到底够不够用"这个问题由它回答（与 VAN 的 edges/frames 同一思路）。

★ **查证到的一件事**：这一版框架的 `esp_lcd/include` 下**只有**
`commands/io/ops/rgb/vendor`，**没有** st7789/ili9341 那些现成 panel 驱动 ——
所以 SPI 分支是"自己发命令 + 送像素"的**通用实现**（CASET/RASET/RAMWR 这一套
覆盖 ST7789/ILI9341/GC9A01/NV3041 等）。这在本题里反而更合适：init 序列本来
就得从卖家例程抄，代码里空表会**明确报一行**（"命令表为空 → 屏不会亮"），不静默。

**编译实测**：`esp32s3-spi` SUCCESS，RAM **63.8%** / Flash 69.5% ——
比 RGB 版高 17 个百分点，因为两块屏各自的 38KB 绘制缓冲占了 SRAM。
真机若紧张：把缓冲降到 480×20，或把缓冲挪到 PSRAM（SPI flush 每次都要读一遍，
PSRAM 会慢一点，但 40MHz 的 SPI 本来就不是瓶颈）。

**两条路的差别（都有骨架了）**：
| | SPI（新） | RGB（上一轮） |
|---|---|---|
| 接线 | ≈10 根，飞线即可 | ≈21 根 + 转接板 |
| 双屏在本框架 | ✅ 直接可行（两块屏各自 CS） | ❌ 旧 esp_lcd 缺 num_fbs 等 |
| 整屏刷新 | 受带宽限制（~27/13fps） | 无上限 |
| 上手速度 | 快（只要 init 序列） | 慢（时序 + 选通 + 可能换 IDF 5） |
## 相关文档

- `PURCHASE.md`：采购清单（含到货后的验证动作、安全提示）
- `PINOUT.md`：引脚预案 + **「接线两段」**（VAN 到车上的接法、极性待定）
- `tools/serial-capture/capture.py`：抓 UART 口日志，尤其是**复位后的完整开机日志**
  （监视器做不到这件事，原因见上面「S3 板子开箱」那一节）
- `tools/serial-capture/replay.py`：往设备贴 VAN 回放帧，桌面联调解帧链用
  （收发器没接、车不在手边时唯一的端到端验证手段）
- `tools/theme-editor/README.md`：两个编辑器（主题 / 图片）的用法、
  二进制格式、分区偏移、限制
- `ARCHITECTURE.md`：模块划分与数据流

- [x] **实车抓帧成功：拿到真 VAN 波形，"槽时间"实测是 8.25µs 而不是 8.00µs**（2026-09-18）
      路径：裸 S3 + 收发器 → 仪表连接器 5/10 脚（双绞线，对地 2.82V/4.05V 且浮动）
      → 逻辑分析仪（Saleae Logic，4MHz）夹模块 RX → 导出跳变 CSV
      → `tools/van-decode/edge_stats.py` 协议无关统计。
      30 秒 / 104,180 个边沿 / 平均 **3463 边沿每秒**（与固件 `edges` 的 3400/s 吻合 ✓，
      说明**固件的边沿采集没丢**）。关键数字：
      · 跳变间隔量化在 **1×/2×/3×/4×/5× 槽**（52%/15%/8%/21%/2%）—— 就是这类编码 ✓；
      · 拟合槽时间 = **8.250µs → ≈121kbit/s**，而固件按 **8.000µs / 125kbit/s** 解码
        → **偏 3.1%**（32 个位就累计一个整槽）✗ —— 这解释了 `frames=0`；
         **8.00µs 只是规范标称值，已作废**（`lib/dashcore/van_wire.h` 的 `kTsNs` 已改成 8250ns，见下一条）。
      · 帧结构：30 秒 **1708 帧**、帧长中位 **1001µs**、每帧 24~148 边沿（中位 58）、
        帧间隔中位 **14.2ms** → 57 帧/秒 × 60 边沿 = 3420 边沿/秒 ✓ 自洽。
      用户的现场观察也印证："拧钥匙/点火/双闪/关门时波形有明显变化" → 真数据 ✓
      （此前"这是 ~1.7kHz 干扰"的判断据此推翻）。
      数据留存：`C:\Users\Public\206dash\van_capture_dm.csv`（30 秒全量）、
      仓库 `tools/van-decode/sample-diffmanchester.csv`（1.2 秒切片，可离线回归）。
      下一步：用这份真实边沿序列**离线**解出帧结构（SOF/IDEN/CMD/FCS），
      改对 `van_wire` 的槽时间与位解码，再回车上验证 `frames > 0`。
- [x] **VAN 帧结构解出 + 定位到车速字段**（2026-09-19，5 分钟实车抓包 drive5min.csv）
      输入：`C:\Users\Public\206dash\drive5min.csv`（34MB / 300.06s / 1,119,627 边沿）；
      对照：`van_capture_dm.csv`（30s / 104,181 边沿，静止）。工具：
      `tools/van-decode/decode_frames.py`（新增，复用 edge_stats.parse_csv）。
      **先修了一个 bug**：`parse_csv` 对"秒"时间列做 int(t*1e6) 截断，
      0.25µs 量化被抹成整µs → 8.25µs 的槽永远量不出来。现在秒/毫秒列保留
      4MHz 细网格（tick=0.25µs）。这是本次能定量化的前提。
      **帧结构（数据可证）**：
      · 最小跳变间隔 **2.0625µs**；间隔量化在它的 1/2/3/4/5 倍
        （52.7/20.1/8.9/14.1/2.6%）⇒ **位槽 = 8.25µs ≈ 121kbit/s**。
        固件的 8.00µs 偏 **3.1%** → 每 32 位漂一个整槽 ⇒ `frames=0` 的直接原因。
      · 帧间空闲门限 17µs ⇒ 300s 共 **17106 帧**（228 帧/秒）。
      · 每帧都以 **SOF = 0000111101（10 槽）**开头 —— **17106/17106 帧 100% 命中** ✓
      · 之后 **IDEN 15 槽 + CMD 5 槽**，再接数据区 ⇒ 16 个报文族
        **★ 报文表里的裸槽数字不是 IDEN 的值**：IDEN 与 CMD 都是 4B5B 编码的，
        丢掉每组第 5 槽才是真值 —— 例：裸 15 槽 `0x44A9` → **4B5B 后 IDEN = 0x824**、
        裸 5 槽 `0x11` → **CMD = 0x8**。固件里**只认 0x824**（`van_source.h` 的
        `kSpeedIden`），**绝不要把 0x44A9 写进固件** —— 那是同一帧的线形，不是字段值。
        （`decode_frames.py` 现在两个都打印，并把这条换算写成了自检。）
        （4B5B 后是 0x824/0x8、0x464/0xC、0x4FC/0xC、0x664/0xC、0x4DC/0xC …）——
        与 30s 静止抓包的报文集**完全一致** ✓。
      **车速字段（数据可证）**：报文 **4B5B 后 IDEN=0x824 / CMD=0x8**（实测 79.7Hz），
      字段在**裸槽坐标**的帧内 **bit 71..77**（7 位；等价写法：bit 71..78 这 8 槽 `>> 1`，
      两者逐帧同值。**数据区不能照搬 IDEN 那套 4B5B 字节视图** —— 已穷举验证不成立）：
      · 全程**单调平滑**；静止抓包里该字段**恒为 8**（唯一值）✓
      · 本段轨迹：停车 8 → 32s 41 → 40..48s 平台 46 → 末端 81
      · 42 个不同值；**每一步多少 km/h 仍未定标**（见 ①）
      **★ 仍属假设/未定（不许当结论用）**：
      ① **标度未定** —— 无地面真值（没读车速表）。以前按"巡航 46 ⟹ ≈23km/h"
         猜过"0.5km/h 一位"，那只是**推测**；代码里**不内置任何 km/h 换算**
         （只打印原始计数），等标定跑（见文末「标定记录格式」）用斜率定标度、截距定零偏。
         ★ 2026-09-20 更新:**这条定速跑不再要求** —— 刻度先按 1.0 上屏、用表盘脸的档位
         (30/65/95/130)复核;以后顺手有 OBD 日志时采几个 010D 散点即可定标,
         见本文文末「上屏定标决定」那条。
      ② **量程没定**：本段最高只到 81，字段 7 位上限 127（或更宽），无法判定。
      ③ 倒车/U 段该字段**仍在上升、没回零** —— 与"U 型掉头"不符，可能我截取的
         不是纯车速、或该报文在低速/倒车时另有语义。
      ④ 位序（现按 MSB-first）、IDEN 的 15 位拆分、FCS 的存在与覆盖范围**均未验证**
         （没做 CRC 反推）。
      ⑤ rpm 未定：候选 bit 36/13 位（idle≈1180, max 7662）与 bit 37/12 位
         （idle≈1112, max 3566），都还没定标。
      **下一步(顺序不能反)**：
      **① 先改槽时间 → 停车点火验 `frames > 0`(这是"改对了"的判据)；
      ② 标定跑只负责标度/零偏** —— 槽时间没改对之前跑标定没有意义
      (占空比/相位都不对时抽出来的字段不可信)。现况：
      · 槽时间已改：`lib/dashcore/van_wire.h` 的 `kTsNs = 8250u`(实测 8.25µs，
        `kBitRateHz = 125000` 只作规范标称保留)；位解码/帧结构用真实边沿离线回归过了
        (`test/test_dashcore/test_van_real_capture.cpp`：真实切片 SOF 命中 62 次/
        750 字节，且含 4B5B 后 IDEN=0x824 的报文)。
      · **但 `frames` 是否 > 0 还定不了**：`FrameParser` 只把 FCS 校验通过的帧算成
        `frames`，而 **CRC-15 多项式/覆盖范围仍未定**(工作单明确不许动) ⇒
        真实帧的 FCS 对不上时 `frames` 仍是 0。**这一趟就是去取真实帧把 CRC 定下来**。

      **明日跑车范围(写死，别扩)**：
      · 表显 **0 + 20/40/60/80 km/h 各稳几秒**(每一档停住、速度稳定，方便和原始计数对齐)；
      · **不开门、不打双闪**(减少与车速无关的报文变化，便于对字段)；
      · **不再量槽时间**(已实测 8.25µs，这趟不重复)；
      · **不把找转速 / FCS / 倒车符号塞进同一趟** —— 那几件事各有前置条件，
        混在一起会让失败原因分不清(这趟只回答"标度/零偏"和"取真实帧定 CRC")。
      ★ 2026-09-20 更新:**上面这趟定速跑不再要求**(见文末「上屏定标决定」)——
      先用 1.0 上屏、用表盘脸的档位复核;顺手有 OBD 日志时采几个 010D 散点即可定标。

      **标定记录格式(代码里先不写死斜率，只按这个格式记)**：
      一行一条，字段顺序固定，逗号分隔；只记**时间戳 + 表显 km/h + 原始计数**：
      ```
      时间戳,表显km/h,原始计数
      2026-09-20T10:03:12,0,8
      2026-09-20T10:03:40,20,52
      2026-09-20T10:04:10,40,96
      ```
      · `时间戳`：ISO8601 本地时间(秒级足够)；
      · `表显km/h`：读数(停车那一刻写 0)；
      · `原始计数`：`decode_frames.py` 打印的**原始值**，不要手工换算；
      · 每档至少 5 秒稳定，取该段**中位数**(别取端点，避免加速瞬间)。
      收车后：`原始计数 → 表显km/h` 做线性拟合，**斜率 = 标度、截距 = 零偏**，
      最高那一档给量程；拟合结果再写回文档(以及 `van_source` 的 `configureSpeedFrame`)。
      在那之前，代码里**不许出现任何 km/h 换算常数**(脚本只打印原始计数)。
      ★ 2026-09-20 更新:这条格式只在**顺手有 010D 散点**时用;固件也已不再受"不许出现
      任何 km/h 换算常数"约束 —— 刻度按 1.0 上屏(见文末「上屏定标决定」)。

- [x] **★ FCS/CRC 与帧布局定案：真实帧终于被收下了（2026-09-19，drive5min 17106 帧 + 仓库切片 66 帧）**
      输入：`C:\Users\Public\206dash\drive5min.csv`（300s / 1,119,627 边沿 / 17106 帧）
      + `tools/van-decode/sample-diffmanchester.csv`（1.2s / 66 帧，两条**独立**抓包互证）。
      新工具：`tools/van-decode/fit_fcs.py`（下次换抓包直接跑它复核；`--search` 可重搜约定）。

      **帧布局（槽 = TS = 8.25µs；全部是实测，不是规范抄来的）**
      | 字段 | 槽数 | 内容 |
      |---|---|---|
      | SOF | 10 | 固定 `0000111101`，**裸槽**（不走 4B5B） |
      | IDEN | 15 | 3 个 4B5B 组 = **12 位**（线上就这 12 位） |
      | CMD | 5 | 1 个 4B5B 组 = 4 位（EXT/RAK/R-W/RTR） |
      | DATA | 10n | 每字节 2 组 |
      | FCS | **20** | 4 个 4B5B 组 = **16 位 = 15 位 CRC + 1 个固定 0 位** |
      | EOD | **0** | FCS 末字节的 bit0(=0)与紧随的编码位(=0)= 一对 dominant（违约），**不额外占槽** |
      | ACK | 2 | 第 1 槽 recessive + 第 2 槽 dominant；**只在 CMD bit2=1 且 RTR=0（0xC/0xE）的帧上有**；没有时全 recessive 并进空闲 |
      | EOF | 8 | 8 个连续 recessive（与空闲连成一片，不在跳变表里单列） |
      ⇒ **帧的槽数 = 50 + 10n (+2 若被应答)**。实测各族逐帧吻合：0x824/0x8=120(7 字节,无 ACK)、
      0x464/0xC=102(5 字节,有 ACK)、0x4FC/0xC=162(11)、0x524/0x8=190(14)、
      0xE24/0x8=220(17)、0x564/0xE=322(27)、0x4EC/0xF=50(0 字节,请求)、`0x984/0x8`=100(5)……
      **★ 旧模型的 FCS「18 槽」是错的**（18 不是 5 的倍数，本身就说明字段分界错）。

      **编码位规则（比"每 5 槽丢第 5 槽"更强，也是定位帧尾的唯一可靠判据）**：
      每个 5 槽组 = 4 个数据位 + **第 4 位的反相**；正常组永远含一次跳变，
      而**全帧恰好最后一组违反这条**（= EOD）。17106/17106 帧成立。

      **FCS 约定（定案）**：`crc15_van_iso()` —— 多项式 **0x0F9D**
      （x^15+x^11+x^10+x^9+x^8+x^7+x^4+x^3+x^2+1，完整掩码 0x8F9D）、
      初值 **0x7FFF**、**输出取反（^0x7FFF）**、**MSB-first 不反射**；
      覆盖 **IDEN(12 位)+CMD(4 位)+全部数据**，按线上字节流喂入，
      **不含 SOF、不含 FCS 自己**。线上是 16 位**大端**字段 = `crc << 1`（最低位恒 0）。
      **实测：drive5min 17106/17106 帧、仓库切片 66/66 帧全部通过**（两条独立抓包）。
      ★ 结论修正：**问题从来不在多项式**。旧代码复现不出公开抓包 FCS，是因为
      ① 按 FCS 18 槽切字段、② 用 `IDEN 低 8 位先行` 的字节打包 —— 喂给 CRC 的
      字节根本不是线上那几个字节。同一份抓包在边界切对之后，同一条多项式立刻全中。

      **线上字节流（BitDecoder 吐出来的那串，也是唯一正确的打包）**：
      `[IDEN>>4, (IDEN&0xF)<<4|CMD, DATA…, FCS_hi, FCS_lo]`
      —— 即 `idenByte1/idenByte2` 的旧实现（IDEN 低 8 位先行）是**错的**，
      与真实抓包解出的 `0x82 0x48 ⇒ IDEN=0x824` 对不上（`test_van_real_capture.cpp`
      的注释③早就记下了这条不一致，这次按实测改掉）。

      **固件改动**：`lib/dashcore/van_wire.h/.cpp`
      · FCS 校验换成 `crc15_van_iso` + 16 位大端字段（`fcsFieldFromCrc`/`fcsCrcFromField`/
        `fcsFieldWellFormed`）；`parseFrameBytes` 改按真实字节布局、候选长度**从长到短**扫
        （长优先：真帧长就是缓冲里那条；短优先会把假命中概率累积起来）。
      · `idenByte1/idenByte2/idenFromBytes/cmdFromByte2` 按实测打包重写。
      · 编码器 `encodeFrame` 同步（CRC/FCS 字段/EOD 用 `patchLastSlotDominant()` 把
        FCS 末字节的编码位写成 dominant；ACK 只在 `cmdExpectsAck()` 时写）。
      · **ACK 位**：旧实现在真实帧上**恒为 0**（EOD 锚点一进窗口就被自己的 recessive 清掉）；
        现在按实测尾部形态认：`[≥2 dominant 的 EOD][1 recessive][1 dominant 的 ACK]`。
        新增用例 `test_chain_ack_flag_follows_cmd` 钉住 0x8/0xC/0xE/0xF 四种。
      · `Frame::fcs_le` 保留但恒 false（字段序已定，16 位大端）。

      **实测（宿主机）**：native **119 例（116 通过 / 2 跳过，0 失败）**
      —— 基线是 118/115/2，本次 +1 例且不减少。
      `test_van_real_capture.cpp` ③ 从"钉住 frames=0"翻成"钉住 FCS 必须过"：
      真实切片整链 **frames=61 / fcs_ok=61 / 回调包 61（其中 IDEN=0x824 车速帧 23 个）**，
      并断言 `frames == fcs_ok`（黄金值写死在用例里）。
      esp32s3 编译 SUCCESS。

      **仍未定 / 已知限制（别当结论用）**
      ★ ① **已修（2026-09-19，见文末最后一条「关帧门限」）**：关帧门限从 300µs
         改成 **70µs**，背靠背帧不再并帧。★ 顺带更正下面原文里的一个数：
         "两帧只隔 **25µs**"是**错的** —— 25µs 是"按 300µs 门限切段"之后在段**内**
         量到的间隔（那是帧内间隔）。实测**最短帧间空闲 = 95.5µs**（12 槽），
         300µs 门限下真正被并掉的边界有 **481 个 < 300µs**。原文保留作历史记录：
       ~~① **背靠背帧会被并进同一帧**：本切片实测有两帧只隔 **25µs**，而关帧判据是
          "空闲 > 300µs"（`van_phy_gpio.cpp` 的 `kIdleCloseUs`）⇒ 第一帧仍能解出
          （FCS 全中），但**第二帧丢失**，且 `ack` 位可能取自后一帧的帧尾
          （实测 61 帧里 4 帧如此：1 少认 / 3 多认；旧实现是 51 帧不符）。
          真实抓包里这种紧邻是少数，但**要彻底修就得改关帧判据**，本轮没动。~~
          —— 修完之后同一份切片：61 帧 → **66 帧**，ack 不符 4 → **0**。
      ② IDEN 的 bit12..14 **线上不存在**（只有 12 位）；规范的"15 位 IDEN"在本总线上取不到。
      ③ 车速字段的**标度/零偏仍未标定**（见上面那条），字段位置也仍按裸槽坐标记。
         ★ 2026-09-20:字段位置已钉死为 `data[2]` 单字节、停车 = 0;刻度先按 1.0 上屏、
         用表盘复核(见文末「上屏定标决定」)。
      ④ 上述布局只在**这两份抓包**（206 舒适总线、同一辆车）上验证过；
         换车/换网络（body 总线 62.5kbit/s）要重跑 `fit_fcs.py`。

- [x] **★ 关帧门限 300µs → 70µs：背靠背帧不再被并成一帧（2026-09-19，先量后定）**
      起因：`van_phy_gpio.cpp` 的 `kIdleCloseUs = 300` 是**帧结构定案之前**拍的
      保守值。帧结构定案之后两头都看得见了 —— 300µs 会把间隔小于它的两帧并进
      同一个解析器缓冲，丢一帧（包的 `ack` 还会取自后一帧的帧尾）。
      新工具：`tools/van-decode/gap_stats.py`（不预设门限：先出"间隔按槽折叠"的
      直方图找**空档**，再按门限切段、逐段重算 FCS 复核，最后报帧间空闲分布）。

      **输入**：`C:\Users\Public\206dash\drive5min.csv`（300.0s / 1,119,625 条跳变 /
      17106 帧，槽时间拟合中位 8.2551µs·p10 8.1076·p90 8.3212）
      + 仓库切片 `tools/van-decode/sample-diffmanchester.csv`（1.2s / 66 帧）。

      **measure-first：门限被夹在两个实测数之间（两份抓包结论一致）**
      | 量 | drive5min | 仓库切片 |
      |---|---|---|
      | **帧内最大跳变间隔**（一段没有跳变的同电平） | **49.0µs**（6 槽，1232 处） | 49.0µs（6 槽） |
      | 7~11 槽（53.6~94.9µs）直方图 | **0 个样本（空档）** | **0 个样本（空档）** |
      | **帧间最小空闲** | **95.5µs**（12 槽） | 96.0µs（12 槽） |
      ⇒ 门限必须落在这个空档里：`49.0 < kIdleCloseUs < 95.5`。
      **帧间空闲分布（drive5min，17105 个帧边界）**：min 95.5 · p1 95.8 ·
      p5 608.8 · p25 7720.0 · 中位 14432.2 · p75 24913.5 · p90 31572.8 ·
      p99 49251.0 · max 51659.0（µs）。
      · **< 300µs：481 个（2.81%）** ← 旧门限下正在丢的帧边界数
      · < 150µs：424 个 · < 100µs：276 个 · **< 50µs：0 个**
      （仓库切片：67 个帧边界里 <300µs 有 5 个、<50µs 0 个。）
      **旧门限到底丢多少**（同一份抓包、同一套 FCS 复核）：
      · 70µs：17106 段 → 单帧段 **17105**、**并帧段 0**；
      · 300µs：16625 段 → 单帧段 16159、**并帧段 481**（离线逐段复算 = 946 帧不能
        干净解出；固件实收每处并帧丢 1 帧 —— 切片上实测 61/66）。

      **取值 70µs 的理由**：落在空档里且接近两侧的几何中点
      （√(49.0×95.5) = 68.4）；距下界 +21.0µs（1.43×）、距上界 −25.5µs。
      · 为什么不用 60µs（它也在空档里）：60µs 只容得下 7.2 个槽的同电平等价，
        一个 **8 槽**（66µs）的帧内段就会被误判成帧尾；70µs 容得下 8.4 槽，
        余量能兜住槽时间的实测漂移（8.1076~8.3212µs）。
      · 为什么不贴着上界取（比如 90µs）：真机上关帧由主循环 tick 触发，只有 tick
        落在帧间空闲那一段时间里才关得掉；门限离 95.5µs 越近，"能关掉"的窗口
        越窄（70µs 给最短边界留了 25.5µs 的窗口）。

      **改动**
      · `lib/dashcore/van_phy_gpio.cpp`：`kIdleCloseUs` 300 → **70**，注释整段重写成
        上面这组实测数（旧注释只讲"300µs 留足余量"，已删）。判据 `vanIdleCloseReady`
        本身**没动**（只换了传进去的常数）。
      · `tools/van-decode/gap_stats.py`（新增）：本次所有数字的复现工具。
      · `tools/van-decode/decode_frames.py`：修了一个**潜伏 bug** —— `load_frames`
        里 `(t - last) * tick_us`，而 `edges_from_levels` 已经把时间换算成µs了，
        于是文档写的"帧间空闲门限 17µs"**实际一直是 68µs**（碰巧落在空档里才没出事）；
        现在两边都改成 70µs ± 一次换算，与固件同一个区间。改完重跑：仍是
        **17106 帧 / SOF 命中 17106/17106**（与改前逐字一致）。
      · `tools/van-decode/fit_fcs.py`：只改注释 —— 帧内最长同电平是 **6 槽 ≈ 49µs**
        （原文写 4 槽 ≈ 33µs，与实测不符），门限 60µs 仍在空档里，值不动。
      · `ARCHITECTURE.md` / 本文「实车必验清单」导语 / FCS 定案条目的「仍未定①」：
        把"背靠背帧仍开着"改成已修，并更正那里的"两帧只隔 25µs"（那个数是按 300µs
        切段后量到的**帧内**间隔；真实最短帧间空闲是 95.5µs）。

      **测试**
      · 新增 `test/test_dashcore/test_van_phywire.cpp ::
        test_back_to_back_frames_decode_as_two`：合成两帧、帧间空闲 = 门限 + 1µs，
        走固件那条路（入队 → 排空 → `vanIdleCloseReady` 判关帧）⇒ 必须解出 **2 帧**；
        同一条边沿流按**旧的 300µs** 判则只解出 **1 帧**（把"并帧 = 丢一帧"钉成
        可执行的证据，也保证这条用例挡得住"把门限放大"的回退）。
      · `test_van_real_capture.cpp`：本地 `kIdleCloseUs` 对齐 70µs，黄金值按新门限
        重取 —— 同一份切片 **frames 61 → 66、fcs_ok 61 → 66、回调包 61 → 66、
        IDEN=0x824 23 → 24，`ack` 不符 4 → 0**（原来只能钉 "≤6"，现在钉 **0**：
        没有并帧了，每个包的 ack 都来自它自己那一帧的帧尾）。
      · 效果：`vanIdleCloseReady` 那两条用例里的门限也改成 70（它们只是拿门限做
        边界算术，值本身不敏感，但别让 300 留在测试里像个"现行值"）。
      · 运行 `python -m platformio test -e native`：
        **120 例 / 117 通过 / 2 跳过 / 0 失败**（基线 119/116/2/0，本次 +1 例）。
        `test_face_stages.cpp` 那个**既有**崩溃（进程 exit 3）与本次无关，未动。
      · `python -m platformio run -e esp32s3`：SUCCESS。

      **仍未定 / 注意**
      · 门限只在**这两份抓包**（206 舒适总线、同一辆车）上量过；换车/换网络
        （body 总线 62.5kbit/s）必须重跑 `gap_stats.py` 重取，**不是按比例缩放**。
      · 板上的真实收益还取决于主循环 tick 落不落在帧间空闲里（窗口 = 帧间空闲 −
        门限）；本次只把"门限本身"放到实测允许的最优区间，tick 周期没动。
      · 其余文件里出现的"空闲 300us 关帧"全是**历史日志样本/自检夹具**
        （`PINOUT.md`、`tools/serial-capture/sample-log.txt`、抓包脚本的 `_SAMPLE`），
        不是依赖 —— 固件下次开机打的是"空闲 70us 关帧"。

- [x] **★ 车速字段标定 + 转速字段定位(2026-09-20,两份实车抓包定案;车速定标 0.01→1.0)**
      起因:上一轮把车速字段的位置按裸槽坐标记下来了(裸槽 71..77、"低 7 位"、
      "整个字节 >>1"),但**标度、量程、单位全没定**,转速字段更是没找到。
      本轮拿两份新抓包 + 一份蓝牙 OBD 日志把这两件事做完。

      **输入(只读,已复制到 `C:\Users\Public\206dash\`)**
      · `idle.csv` = 点火+怠速 30.1s / 105,679 行 / 逻辑分析仪 4MHz 边沿导出;
      · `drive.csv` = 驾驶记录 600.1s / 68MB / 同款导出;
      · `bt.zip` = 蓝牙 ELM327 导出的两份 CSV(见下面「BT 日志」那条)。

      **先修的不是字段,是坐标 —— 本轮把"字节布局"钉死(否则后面全是白算)**
      上一轮记的"裸槽 71..77"**不在数据区**:按实测布局,数据字节 4 的 8 个数据位
      落在裸槽 `[70,71,72,73,75,76,77,78]`,所以 71..77 是它的 bit6..bit0,
      而帧首 30 槽(SOF 10 + IDEN 15 + CMD 5)后面紧跟的才是 data[0]。
      判定方式**不是推理,是两条硬自检**:
      · **往返自检**:用固件自己的 `encodeFrame()` 造帧(它按 `putByte` 写
        `[d7..d4 s][d3..d0 s]`),再用同一套"每 10 槽 1 字节、第 5/10 槽是编码位"
        的边界读回来 —— 7 个数据字节**逐字节一致**;
      · **FCS 全帧命中**:`crc15_van_iso` 覆盖 IDEN+CMD+DATA,在**怠速抓包
        1704/1704 帧**、**行驶抓包 34051/34051 帧**全部命中 ⇒ 字节流就是线上
        字节流,不存在第二种自洽读法。
      附带更正:上一轮"数据区不能按 4B5B 折字节"的结论**只对了一半** ——
      能折,但锚点必须从**帧首槽 0** 起(与 SOF 的 10 槽边界对齐),不是从 SOF 之后。

      **① 转速字段:IDEN 0x824 / CMD 0x8 的 `data[0..1]`,16 位大端,×8**
      · 点火瞬间:起车前 raw **恒为 0**(前 127 帧唯一取值就是 `{0}`),
        t=6.376s 跳到 6152(=769.0rpm,起动机拖动),峰值 10692(=1336.5rpm),
        然后落回怠速;
      · 怠速平台(去掉前 20 帧瞬态,453 帧):raw 6230..7570、**均值 7237.6 /
        sd 215.0** ⇒ ÷8 = **904.7rpm / sd 26.9**(地面真值 900rpm,偏差 **0.52%**);
      · ÷4 会得 1809rpm(差 101%)、÷2 得 3619rpm —— 都排除;
      · 行驶抓包 raw 5988..47100 ⇒ **748..5888rpm**,全程在断油 6300 以下、
        表盘 7000 以内 ⇒ 量程自洽(超过 6300 的候选就不是转速,这条它也过);
      · 所以固件里 `kRpmOffset = 0`(16 位大端)、`kRpmScale = 0.125f` 这两个值
        **本来就对**(来自公开文档),本轮把它从"照抄文档"变成**实测钉住**。
      · 排除项:0x464/0xC 的 `data[0..1]` 只有在**加速时**才从 0 跳到 4096
        (不是平滑怠速平台);0x4FC/0xC 的 `data[4..5]` 静止时也非 0 ⇒ 都不是转速。

      **② 车速字段:IDEN 0x824 / CMD 0x8 的 `data[2]`,单字节,1 计数 = 1 km/h**
      · **停车判据不是拍的**:同一族的 `(data[4]<<8|data[5])` 是**单调不减**、
        只在行驶时增长的**位移累计量 S**;S 完全不动 + rpm 落怠速带 ⇒ 车停着。
        行驶抓包里这样的窗口有 5 个(0-7s / 78-117s / 214-241s / 547-554s /
        565-570s,合计 ~85s),**data[2] 在这些窗口内恒为 0**(个别帧 1,抖动);
      · **它是速率不是累计量**:对 `data[2]` 与 `dS/dt` 做 OLS ⇒
        `data[2] = 0.1390·(dS/dt) − 0.31`,**R² = 0.9942**,残差 **σ = 0.698**;
      · **两条独立积分量互证**:`∫data[2]dt` 与 `ΔS` 的分段比值在 10 个 60s 段里
        稳定在 **0.036~0.039 m/count**(末段 0.031,那是收车怠速段,分母小);
        整段 ∫data[2]dt = 1.646km(若 data[2] 是 km/h)⇒ 1 count ≈ **3.7cm**;
      · **量纲自洽**:`|Δdata[2]|/Δt` 的 p99 = **19.9 km/h/s**(max 21.0)
        < 21.6 km/h/s(乘用车加速度物理上限);
      · **有效字段宽度 = 8 位(不是 7 位)**:行驶抓包 data[2] 取值 **0..39**
        (40 个不同值),**没有"恒偶"性质**(奇数值 1/3/5… 都出现过,偶数
        6966/11955)—— 所以上一轮"低 7 位 / 恒偶"是**停车切片**的现象,
        不是字段性质;`data[2]` 与 `data[3]` 的一阶差分相关性 −0.954,两者像是
        同一物理量的两个不同分辨率副本,但 **data[3] 不参与车速**(已加用例钉住);
      · **满量程**:8 位 ⇒ 0..255 km/h,本趟实测只到 39(市区短途),**没观察到
        wraparound 也没有饱和**;190km/h 的极速在 8 位里够用(7 位会被卡在 127);
      · **停车与倒车**:停车时 data[2] **恒 0**(不做偏置);倒车/掉头段**没有
        单独的符号位或负值** —— 全程 data[2] ≥ 0,倒车在总线上与前进同码
        (与"表显只有正向刻度"一致,但**"倒车到底是不是 0"本轮没验证**:抓包里
        没有能独立判出倒车的字段,这条留作实车确认)。

      **③ 转向灯/双闪:IDEN `0x4FC` / CMD 0xC 的 `data[5]`,是个位域(2026-09-22 实测解出)**
      · **`0x04` = 左转向灯、`0x08` = 右转向灯、`0x0C` = 双闪**(= 两位**按位或**)。
        双闪恰好是 `0x04|0x08` 这一点，独立证明它是**位域**而不是三个枚举值。
      · 怎么测的:点火后依次 左→右→双闪，各拨一次。`data[5]` 出现三段方波:
        左转 9 次闪(0x04)、右转 12 次(0x08)、双闪 9 次(0x0C);
        每段内部 **0x04/0x08/0x0C 与 0x00 交替**，非零段只持续 1~3 帧。
      · **闪烁全周期 = 0.80 / 0.804 / 0.79 秒 ⇒ 约 1.24~1.27 Hz**，三段一致
        (同一只闪烁继电器)。★ 比常见的 1.5 Hz 标称**略慢**，这是本车的实测值。
      · 采样说明:`4FC` 只有 4.7 帧/秒，所以闪烁波形是**欠采样**的 ——
        0.80s 是**相邻两次"亮"的间隔**(全周期)，不是半周期;若按半周期解读会
        误得 0.63 Hz(与车灯明显不符)。这个坑记在这里，免得下次重算一遍。
      · 另有一处旁证:从"右转"拨到"双闪"的途中，行 1518~1520 出现过一次短暂的
        `0x04`(左) —— 正是拨杆经过左位的过渡，与操作路径吻合。
      · ★ **结清一条旧告诫**:上面「明日跑车范围」第 2 条写的
        **「不开门、不打双闪」(减少与车速无关的报文变化)** —— 当时只知道"双闪会
        改动报文"，但**不知道是哪一族哪个字节**。现在这条落实了:就是
        `0x4FC` 的 `data[5]`。以后抓包时既可以**主动避开**它，也可以
        **故意拨一下当作已知的时间标记**。
      · ★ **`data[5]` 是个多路位域，不止转向灯（2026-09-22 第二轮实测又解出两位）**:
        同一轮里按住"灯杆"的不同档位，`data[5]` 出现了**持续不闪**的另外两个值 ——
          `0x80`（bit7）持续 **8.1 秒**、`0x40`（bit6）持续 **13 秒**（中间断了 1.7 秒）
        **「持续 vs 闪烁」是区分两类灯的硬判据**:
          闪烁（`0x04`/`0x08`/`0x0C`）= 继电器驱动的**转向灯/双闪**；
          持续（`0x40`/`0x80`）= 开关直接控制的**稳态灯**（大灯那一类）。
        ⚠ **具体哪一位对应仪表盘灯、哪一位对应近光/远光尚未单独定** ——
          本轮灯杆是「一路推上去」连续操作的，两次持续段之间没有逐档独立确认。
          要定死得再做一轮**逐档停住、每档两次开关**的采集。

      **③之二 `4FC.data[0]` 的 bit5:危险警告灯(双闪)继电器状态**
      · 全程 `data[0] = 0xC0`，**只在双闪时段变成 `0xE0`**（57 帧，92s–99.8s），
        与 `data[5] = 0x0C` **精确同步** ⇒ **双闪一次改两个字节**:`data[5]` 的两位 +
        `data[0]` 的 bit5。后者更像「警告灯继电器已激活」的**状态回读**，
        而不是灯本身的指令位（因为它只在双闪时置位，左右转向时不置位）。
      · 可直接用作**已知时间标记**:抓包时按一下双闪，`data[0]` 与 `data[5]` 会同时动。

      **③之三 实测:刹车灯与雨刮【不在这条总线上】(2026-09-22，排除法定案)**
      · 做法:按固定顺序做完整一轮（全关基线→仪表灯→近光→远光→**刹车踩到底**→
        **雨刮二/三档各放一次**→双闪→喇叭），逐帧记录。
      · 结果:刹车与雨刮那一段（**约 57–91 秒，共 35 秒**）在**全部 12 个族的每一个字节**上
        **零变化** —— 没有任何取值只出现在该窗口内。
      · **已排除的解释**:不是手刹（手刹全程拉着，在差分里是常量，不影响归因）、
        不是操作问题（owner 确认刹车踩到底、雨刮实际在动、顺序无误）、
        不是抓包失效（**同一方法在同一轮里正确解出了双闪的 `0x0C`**，见上）。
      · ⇒ **结论:刹车灯与雨刮不由这段 VAN 广播**。合理解释是它们走
        **本地硬线**（BSI 直接驱动继电器）或**另一段 VAN（车身段）**。
      · ★ 对项目的意义:任何「想从这条总线读刹车/雨刮」的设想都应先按**不可行**处理，
        除非换 VAN 落点重抓。

      **③之四 灯位定死(2026-09-22 第三轮:逐档停住 + 每档两次开关)**
      · `4FC.data[5]` **bit7 = `0x80`** → **灯杆第 1 档 = 仪表盘灯**
        两次:34.7–40.6s(5.9s) 与 46.3–54.1s(7.8s)
      · `4FC.data[5]` **bit6 = `0x40`** → **灯杆第 2 档 = 近光**
        两次:59.6–64.6s(5.0s) 与 69.5–74.5s(5.0s)
      · ★ **为什么这一轮才算定死**:上一轮灯杆是「一路推上去」连续操作的，
        两次持续段之间没有逐档独立确认；本轮**逐档停住、每档开关两次**，
        同一档位两次给出**同一字节值** ⇒ 归因干净。
      · ⚠ **远光未解（且已排除"闪远光"这条路）**:灯杆第 3 档**没有**在 `data[5]` 里产生新位。
        owner 补充说明:本轮远光是**直接按压灯杆**打开的（不是先进近光档再按压进入常开），
        属**瞬时/闪远光**。据此按 0.25 秒粒度重扫过 75–85s 窗口的**全部族全部字节** ——
        除门信号（82.2s 起）外只有两个背景量（`824.data[0]` 的 `1B`/`1C` 计数器、
        `8A4.data[0]` 每约 0.47s 的固定交替），**没有任何远光相关变化**。
        ⇒ 结论:**闪远光不在这条总线上**（与刹车/雨刮同类，见 ③之三）。
        远光的**常开**模式本轮没测到，**仍未解** —— 要测得先进近光档再按压。

      **③之五 门状态位:已定位（一次改动三个族），但【左右门无法分辨 = 未解】**
      · 76–125s 窗口内三族字节**同步**跳动：
        | 族.字节 | 门关 | 门开 |
        |---|---|---|
        | `4FC.data[1]` | `0x00` | `0x01` |
        | `524.data[0]` | `0x00` | `0x02` |
        | `524.data[9]` | `0x01` | `0x18`（全程静息是 `0xFF`） |
        | `8A4.data[6]` | `0x8E`（静息） | `0x8F`/`0x90` |
        `524.data[0]` 与 `4FC.data[1]` **逐帧同步** ⇒ 同一个事件的两种编码。
      · ★ **关键旁证**:同族的 `4FC.data[0]` 与 `data[5]` 在整个门测试期间**完全没动**
        （b0 全程 `0xC0`、b5 全程 `0x00`）⇒ `data[1]` 是**独立于灯光的门状态位**，
        不是灯光位域的残留。owner 也判断「车门开关应该在 VAN 线上」，实测证实。
      · ⚠⚠ **「左右门能不能分辨」= 未解。本节早先写的「它是单一状态位、双门车够用」
        已被第四轮实测推翻，别沿用（2026-09-22 同日修正）**。
        起因:owner 提出一个有力的反证 —— **车上主控屏能显示左右门**。
        若屏能分辨左右，那个信息就必然在车上某处存在；而我只看了 `4FC.data[1]`
        一个位（四次全是 `0x01`）就下了"单一状态位"的结论，**下得太早**。

        **第四轮实验（专门为分辨左右设计）**:
          协议:静置 15s → **只开左门** 开/关/开/关(各 10s) → **空档 15s**（两门都关）
                → **只开右门** 开/关/开/关(各 10s)
          owner 执行得很干净，四段脉冲边界:
            左1 11.19–25.13s / 左2 36.43–49.54s
            ── 29 秒干净空档（这一轮不再有"间隔污染"，上一轮的伪命中就是它造成的）──
            右1 78.68–91.59s / 右2 103.37–115.49s
        判据与结果:
          (a) **段内取值恒定 且 四段呈 [L,L,R,R] 且 L≠R** → **零命中**；
          (b) **左门取值集合 ∩ 右门取值集合 = 空** → 只命中 `4DC.data[4]`，
              但它的左=`[8D..92]`、右=`[93..96]` 是**相邻连续区间** ⇒
              那是**单调递增的计数器**（左门测得早、值自然小），**伪命中，已排除**。

        **同时撤回的另一条推测**:早先记「`8A4.data[6]` 带门侧信息（左 `8F`/右 `90`）」
        —— **也错**。第四轮它四段**全是 `0x92`** ⇒ 那是个**慢变量**（随温度/时间漂移），
        不是门位。**两条撤回都记在这里，别再从旧提交里捡回来。**

        **还发现一件更根本的事**:`4FC.data[1]` / `524.data[0]` 在**每段脉冲内部
        都在跳变**（段内 2 种取值 `00`↔`01`、`00`↔`02`）——
        所以把它们描述成「门开期间恒为 1」是**过度简化**。它们更像门状态的
        **瞬时/边沿信号**，不是稳态位；我划分脉冲边界的做法本身也不够严谨。

        **结论（定性，不是"已解"）**:就我们抓的这段总线而言，
        **找不到能区分左右门的字节**。三种可能无法从数据分辨:
          ① 左右信息**不经这段总线**（BSI 只广播"有门开着"，左右走另一段 VAN/独立线）；
          ② 它在某个未解族里，但**编码不是简单位域**（状态机值等），现有判据抓不到；
          ③ 需要更长的单次开门或更严谨的边界划分才能稳定。
        **但有一条是确定的**:owner 的屏能显示左右 ⇒ 信息在车上存在,只是不在我们能读到的地方。
        下一步待 owner 确认:**那块能显示左右门的屏是原车还是后装、走 VAN 还是另接线** ——
        若它也挂在这段 VAN 上却仍能分辨，说明信息一定在这段总线上（我们漏了），值得再挖。

      · 关于**顶篷**:owner 说明**敞篷电脑独立控制**，不走这条舒适段。
        唯一影响车内警报的是**敞篷钩的开关状态**，以及**钩打开状态下超过 10 km/h 会报警**
        —— 将来若要做顶篷相关功能，注意这个 10km/h 门限。本轮**没测顶篷**。

      **③之六 方法上的两条经验(本轮得出，以后照用)**
      · **「两次开关给同一值」是廉价而有效的自校验** —— 建议以后每个开关都做两次；
        只要两次值不同，就说明有别的因素在动，结论先别下。
      · **用「同族其他字节有没有跟着动」判独立性** —— 门位与灯位同族却互不干扰，
        靠这一步才敢说 `data[1]` 是门而不是灯光位域的残留。

      **④ VIN 在总线上明文广播:IDEN `0xE24` / CMD 0x8(2026-09-22 实测解出)**
      · `0xE24` 的 **17 个数据字节恰好就是 17 位 VIN**，逐字节可打印 ASCII:
        `VAN E24 56 46 33 32 44 4E 46 55 52 32 57 30 30 35 34 35 30`
        = **`VF32DNFUR2W005450`**（`VF3` = 法国·标致、`2` = 206）。
      · **静态**:全程 101 帧，`data` **只有 1 种取值** —— 是常量广播，不是状态量。
      · ★★ **一处更正（别沿用我最初的说法）**:先前我把 `0xE24` 和 `0x5E4` 拼起来
        当作 VIN，**那是错的**。复核后:`0xE24` 自己就是完整的 17 字节 VIN，
        **不需要拼任何东西**;而 `0x5E4` 只有 **2 字节**（`00 01` / `20 1E`，有 2 种取值），
        拼上去只会得到不可打印的控制字符 —— 它和 VIN **无关**，是独立的一族。
        教训:字节数先数清楚（`17 == 17` 本身就是"不用拼"的证据）。

      **⑤ 总线全貌:一次 110 秒实车抓包（点火 + 左右转向 + 双闪）观测到的全部帧族**
      这是目前唯一一份"总线上到底有哪些帧、各多少"的实测汇总（此前仓库只记了
      16 族的**存在**与帧长，没有按 IDEN 的速率/占比）。采集条件:车通电并点火、
      怠速、无行驶;逐帧打印开启。
      | IDEN | CMD | 帧数 | 速率 /秒 | 占比 | ACK | 备注 |
      |---|---|---|---|---|---|---|
      | `824` | 8 | 1977 | 18.0 | 35.2% | 无 | 转速/车速/里程/序号 —— **已解** |
      | `464` | 12 | 1043 | 9.5 | 18.1% | 有 | 未解（仓库已排除它不是转速） |
      | `4FC` | 12 | 557 | 5.1 | 9.1% | 有 | `data[5]` = **灯光位域**：bit2/3=左/右转向、bit2+3=双闪、**bit7=仪表盘灯、bit6=近光**；`data[1]` = **门状态位（但左右门无法分辨，见 ③之五）**；`data[0]` bit5 = 双闪继电器 —— **部分已解**（远光未解） |
      | `4DC` | 12 | 515 | 4.7 | 9.4% | 有 | 未解（`data[4]` 有 208 种取值，像连续量） |
      | `664` | 12 | 480 | 4.4 | 8.5% | 有 | 未解 |
      | `524` | 8 | 206 | 1.9 | 3.5% | 无 | `data[0]` = **门状态**（与 `4FC.data[1]` 逐帧同步）；`data[9]` 门开段为 `01`/`18`、静息 `FF` —— **部分已解** |
      | `8A4` | 8 | 209 | 1.9 | 3.7% | 无 | `data[6]` 跟随门状态（静息 `8E` / 门开 `8F`/`90`）；`data[0]` **逐帧交替** `8F`/`AF` —— **部分已解** |
      | `9C4` | 12 | 206 | 1.9 | 3.7% | 有 | 未解（**2 字节**，见下面那条不一致） |
      | `5E4` | 12 | 194 | 1.8 | 3.4% | 有 | 未解（2 字节，2 种取值） |
      | `E24` | 8 | 101 | 0.9 | 1.9% | 无 | **VIN —— 已解** |
      | `4EC` | 15 | 99 | 0.9 | 1.9% | 无 | 0 字节请求帧 |
      | `4D4` | 15 | 98 | 0.9 | 1.7% | 无 | 0 字节请求帧 |
      | `564` | 14 | 7 | — | — | 有 | 27 字节，稀有 |
      | `8C4` | 12 | 7 | — | — | 有 | 2 字节，稀有 |
      | `984` | 8 | 1 | — | — | 无 | 5 字节，本趟只出现 1 次 |
      · 总帧率 ≈ **54 帧/秒**（实测每 5 秒 262~272 帧），`fcs_ok == frames`（100% CRC）。
      · 结构规律:高 2 族（`824`/`464`）占 **53%**，中 3 族（`4FC`/`4DC`/`664`）再占 27%，
        其余是低频族 —— 与"动态量高频、静态量低频"的直觉一致。
      · **这一趟还顺带定了一条否定结论**:刹车灯与雨刮**不在这条总线上**（见 ③之三）——
        35 秒的有效操作在 12 个族的每一个字节上零变化。
      · **⚠ 待核实的一处不一致**:仓库别处记 `0x984/0x8` = **5 字节**，
        但本趟实测 `9C4` 是 **2 字节**（`00 80`）。两者都过了 CRC ⇒
        要么是**笔误（`984` ↔ `9C4`）**，要么是**两个不同的族**。未澄清前
        别把"16 族"那张表的 IDEN 当准。

      **⑥ BT 蓝牙 OBD 日志:不是 GPS,但也**不能**当速度地面真值**
      · 内容:`2026-09-18 15-31-35.csv`(1177 行/40 列,15:36:35→15:53:07,16.5 分钟)
        与 `2026-09-20 18-06-41.csv`(370 行/27 列,18:07:03→18:12:49,5.8 分钟);
        **编码是 GBK、不是 UTF-8**(按 UTF-8 读列名会全乱码,要按 936 读);
      · 列里全是标准 OBD-II PID 的**十进制已换算值**:发动机转速(rpm)、
        车速(km/h)、节气门位置(%)、进气歧管绝对压力(kPa)、进气温度/冷却液温度
        (°C)、氧传感器电压(V)、正时提前(°)、短期/长期燃油修正(%)、计算负荷(%);
        另有 app 自算的油耗/花费/里程列。**没有 PID 编号列**,但列名与取值就是
        OBD 的形态 ⇒ 判为 **ELM327 读 OBD(车速那列对应 PID 010D)**,
        **不是 GPS 推导**(GPS 不会给出节气门/氧传感器/燃油修正这些 ECU 内部量);
      · **为什么不能当速度真值**:两份日志的"车速"列 **223 个样本 max 2km/h
        (只有 5 个非零)** 与 **95 个样本全 0** —— 那两段车**根本没动**
        (18:06 那份的"行驶距离"列 max 0.003km)。它们只能做**转速过桥**:
        转速列 203 个样本 818..3791rpm、**中位 900.0**、850..950 区间有 **176 个**
        ⇒ 与 ① 的怠速平台 904.7rpm 互证,坐实"1 计数 = 1/8 rpm"。
      · 结论:**速度的绝对标度仍然没有地面真值** —— 本轮给的"1 计数 = 1 km/h"
        是**最自然读法 + 物理自洽**(见 ② 的四条证据),不是实测标定。

      **⑦ 改动**
      · `lib/dashcore/van_source.h`:字段说明整段重写成上面这组实测数;
        `kSpeedOffset` 注释改成"data[2],单字节 km/h";`kSpeedScale` 注释 0.01→1.0;
        `configureSpeedFrame` 注明 `speed_offset` 指向**单字节**。
      · `lib/dashcore/van_source.cpp`:`kSpeedScale` **0.01f → 1.0f**;车速读取从
        "`data[off..off+1]` 16 位大端 × scale"改成"**单字节** `data[off]` × scale"
        (判据 `speed_offset_ < pkt.len`);转速路径**未动**(实测已对)。
      · `tools/van-decode/sample-speed-824.csv`(新增,36KB):0.6s / 2168 条真边沿,
        取自 `drive.csv` t=259.30..259.90s 的**行驶段**(车从 13km/h 加到 14km/h、
        转速 2587→2744rpm);格式与 `sample-diffmanchester.csv` **逐字一致**
        (表头 `Time [s],Channel 0`、逗号后**没有空格** —— 测试里的 `parseRow`
        只取 `comma[1]`,多一个空格就会把电平读成 0、整链 frames=0;这个坑本轮
        真踩了一次,工具生成的切片也要按这个格式写)。
      · `test_van_real_capture.cpp`:新增 ④
        `test_speed_field_constant_pinned_on_real_capture` —— 真边沿 → SOF → 4B5B
        → FCS → VanPacket → **VanSource** → `speedKmh()/rpm()`,黄金值
        **整段 33 帧 / 0x824 12 帧 / 末帧 speed=14.0 / rpm=2744.5**,并把
        `kSpeedOffset=2 / kSpeedScale=1.0 / kRpmOffset=0 / kRpmScale=0.125`
        四个常量一起钉住(改常量立刻变红)。`PacketRecorder` 加了 `forward`
        成员(实车链路上那个 sink 就是 VanSource)。
      · `test_van_source.cpp`:新增 `test_van_speed_is_single_byte`(只改 data[3]
        车速必须不变 —— 钉住"别退回 16 位读法");`test_van_speed_and_rpm`
        改用 `data[2]=0x64`(单字节 100km/h);`test_van_clamp_bad_speed` 改成用
        `configureSpeedFrame(..., 2.0f)` 把 0xFF 推到 510km/h 才越界(默认 scale
        下 8 位字段最大 255,永远碰不到 300 的钳制 —— 也说明那条钳制现在只在
        有人调大 scale 时才有用)。
      · `test_data_service.cpp` / `test_van_phywire.cpp` / `test_van_replay.cpp`:
        所有用旧 16 位写法造车速的夹具改成单字节(`0x27 0x10` → `0x64`、
        `0x13 0x88` → `0x32`),回放行字符串同步改。

      **⑧ 测试**
      · `python -m platformio test -e native`:**122 例 / 119 通过 / 2 跳过 /
        0 失败**(基线 120/117/2/0,本次 +2 例);`test_face_stages.cpp`
        那个**既有**崩溃(`test_stage_table`,进程 exit 3)与本次无关,未动。
      · `python -m platformio run -e esp32s3`:**SUCCESS**(Flash 66.9%)。

      **⑨ 仍未验 / 已知限制(别当结论用)**
      ★ ① **车速的绝对标度仍是"最自然读法"**:1 计数 = 1 km/h 有物理自洽支撑
         (R²=0.994、积分比恒定 3.7cm/count、加速度上限),但**没有一次"表显定速"
         的地面真值**。要彻底闭环必须做一次标定跑:表显稳在 20/40/60/80km/h
         各几秒,拿真值对 `data[2]`(记录格式见本文「标定记录格式」)。
         ★ 2026-09-20 更新(见文末「上屏定标决定」):**这条定速跑不再要求** —— 刻度先按
         1.0 上屏、用表盘脸的档位复核;以后顺手有 OBD 日志时采几个 010D 散点即可定标。
      ★ ② **倒车段未识别**:抓包里没有能独立判出"倒车/掉头"的字段,所以
         "倒车时 data[2] 是不是 0 / 有没有符号"这条**没验证**。
      ★ ③ **data[3] 的物理含义未知**(与 data[2] 反相相关、疑似低分辨率副本);
         `data[4..5]` 判为位移累计量(单调、只在行驶时涨),但 1 count = 3.7cm
         这个数是从"若 data[2] 是 km/h"反推的,**依赖 ★①**。
      ★ ④ 本轮的字段坐标只在**这两份抓包**(206 舒适总线、同一辆车)上验证过;
         换车/换网络必须重跑(工具:`tools/van-decode/`;本轮完整流程 =
         "往返自检 + FCS 全帧命中 + 停车窗口 + OLS 回归 + 积分互证")。
      ★ ⑤ 蓝牙日志按 **GBK(936)** 读,不是 UTF-8;两份都是**静止**记录,
         以后要拿它当速度真值必须先确认"车速列非零"。

- [x] **★ 上屏定标决定:先按 1.0 上屏、"看表盘"复核,不再要求 20/40/60/80 定速跑(2026-09-20,用户定)**
      用户的原话意思:**转速已经闭环**(怠速 904.7rpm vs 地面真值 900rpm,偏差 0.52%);
      车速这边的**字段位置(0x824 的 `data[2]`)、速率语义、停车为 0、单字节**也都闭环了。
      **不再要求四段定速(20/40/60/80)的标定跑**:先用 1.0 上屏,错一倍从表盘能看出来;
      下次哪趟顺手有 OBD 日志,采几个 **PID 010D 散点**就能定标(不为此单独安排跑车)。
      010D 那条路早就铺好了(见上文「OBD 车速(010D)」那条),有线 ELM 接上就能问。

      **为什么这样能接受**
      · 未知量只剩**一个乘性常数**:data[2] 已证明与车速成比例(对 dS/dt 回归 R²=0.9942)、
        停车窗口恒 0、加速度上限自洽 ⇒ 错的只可能是"1 计数 = 几 km/h",
        不是字段位置错、也不是零偏;
      · **2 倍这类错误不可能看不出来**:表盘不只有指针 —— 脸的档位比指针更显眼。
        专门跑一趟定速,只是把这个常数钉到小数点后一位,收益配不上成本。

      **★ "用眼睛核"的判据(后来人直接复用这一条,不用重新推)**
      车速脸的阶梯是按 **30 / 65 / 95 / 130 km/h** 设计的(`expression.cpp` 的
      `kSpeedCityFrom / kSpeedFastFrom / kSpeedHighFrom / kSpeedOver`,进档另加 3km/h 迟滞),
      所以**脸的变化本身就是刻度尺**:
      · 若真值是 **2 km/h/计数**(固件按 1.0 上屏 ⇒ 读数只有真车速的一半):
        **"市区"脸要等到真车速约 60km/h 才出现**(30 那条边界翻倍;算上 3km/h 迟滞是 66)
        —— 市区正常开到 40~50 还一直挂着"静止/挪车"脸,即判刻度**偏小一倍**;
      · 若真值是 **0.5 km/h/计数**(读数翻倍):**挪车就跳"市区"脸**(真车 17km/h 就进),
        真车约 34 报"快速路"、约 49 就报"高速" —— 即判刻度**偏大一倍**;
      · 每条边界都是 2 倍放大的:30→60、65→130、95→190(超速那条同理)——
        **对上任意一条边界就能反推常数**,不必每档都验;
      · 指针只作辅助:表盘满量程 190km/h,若平常快速路巡航指针就压到 3/4 以上,
        同样说明刻度偏大;
      · 判据只需要"和路上真实车速对比一次"(限速牌/跟车),**不需要定速、不需要稳几秒**。

      **本轮只改文字,不动行为**
      · `lib/dashcore/van_source.h/.cpp`:给 `kSpeedScale` 加了一行注(1.0 = 当前上屏值、
        绝对刻度待表盘复核),**值没动**(仍是 1.0f);字段读取、脸档阈值一行没改;
      · 与旧结论的关系:2026-09-19 那条的 ★① 与「明日跑车范围」里的定速跑要求、
        以及 2026-09-20 那条 ⑥★① 的"必须做一次标定跑",**都已被本条取代** ——
        旧条目原文保留(历史记录),只在原文旁加了指向本条的短注;
      · 旧结论里"绝对刻度没有地面真值"这条**性质没变**:本条定的是**怎么验**(看表盘),
        不是"已经验过" —— 谁也不许把 1.0 写成已实测标定。
      · `python -m platformio test -e native`:**122 例 / 119 通过 / 2 跳过 / 0 失败**
        (与基线一致;`test_face_stages.cpp` 那个既有崩溃仍是进程 exit 3,未动);
        `python -m platformio run -e esp32s3`:**SUCCESS**。

- [ ] **★ 排队:车辆状态驱动的声音告警(2026-09-20,用户排期;本条只记规格,一行实现都没写)**
      需求:车上某个状态越线时**响一声**。第一个条件用转速进红区(项目阈值 `kRpmRedlineFrom = 5800`,
      断油实测 6300),以后可以扩到超速(>130)一类。板子(微雪 ESP32-S3-DualEye-Touch-LCD-1.28)
      自带 **ES8311 + 功放 + 喇叭座**(套件含 8Ω 2W 喇叭),**没有蜂鸣器** —— 声音只能走 I2S。

      ⚠ **2026-09-22:DualEye 已退货** ⇒ 本条引用的 **ES8311 / 功放 / 喇叭座**与
      **`VAN_RX_PIN=18` 实测**,**硬件基础已不在手上**;"I2S 是唯一路线"这个结论**先保留**,
      但要等 **2.8" 级最终板**定型后按**新板的音频硬件**重新确认。(**原规格文字一字未删**,注记只加在这里。)

      **① 两条路线:默认合成、可选上传覆盖**
      · **默认 = 固件现场合成**:I2S 直接吐一小段方波/正弦(按下面④的短节奏),**零素材、零 flash 占用**,
        不依赖任何分区里有没有东西 —— 刷完固件就该响;
      · **可选 = 上传自定义音频覆盖它**:用户自己那一段刷进分区,固件优先放它;
        没刷/读失败/格式不认识 → **降级回合成音**,不静默也不哑(与主题、图片分区空着时的降级同一条思路)。
      · 为什么默认不塞一条现成 wav:默认音占的是**所有人的**分区,而合成音是 0 字节;
        一条 96000 B 的默认素材换不来什么,却要一直付分区和刷写两份钱。

      **② 上传格式与体积上限(写死,以后不各说各话)**
      · 格式:**WAV / PCM(fmt = 1) / 16-bit / 单声道 / 8~16 kHz**。只有这一个理由:**设备端不要解码器** ——
        44 字节头读掉就是裸 PCM,可以边读边喂 I2S,不用 malloc 一整段、不占 PSRAM。
      · 上限:**单个文件 ≤ 3.0 秒 且 ≤ 96000 字节**(两个都卡;96000 是**含 WAV 头**的文件大小,
        ≈93.8 KiB,也就是口语里的"96 KB")。算术:16 kHz × 2 B × 3 s = 96000 B ⇒ 16 kHz 下先撞字节数
        (96000 − 44 = 95956 B ≈ 2.998 s);8 kHz 下 3 s 只要 48000 B ⇒ 先撞时长(字节上限够 6 s)。
      · **MP3 是"以后",不是现在**:它要带一个解码器(体积 + RAM 都要付),128 kbps ≈ 16 KB/s,
        同样 3 秒比 WAV 小 6 倍 —— 但下面的余量根本不缺这点空间,现在不付解码器的代价。
      · 表外的采样率/位深/声道数一律拒收,**不做重采样**(校验放浏览器那页,见 theme-editor 的 README)。

      **③ 上限的算术(全部取仓库里的数,不是估的)**
      分区(`partitions-s3.csv`,逐行核对过;开机自检打的 `image : 分区 8192 KB @ 0x254000` 就是它):
      · `theme` = **0x4000 = 16 KB** @ `0x210000`;
      · `image` = **0x800000 = 8 MiB = 8388608 B** @ `0x254000` —— 与 `[env:esp32s3]` 的
        `-DIMAGE_PARTITION_BYTES=(8u*1024u*1024u)`、`image-blob-build.js` 的
        `TARGETS.s3.partitionBytes` 三处一致 ✓(经典板那份 `partitions.csv` 仍是 1 MB,偏移相同)。
      表情占用(常量与 `HEADER_SIZE` 都取自 `tools/theme-editor/image-blob-build.js`):
      · 镜像头 = 12 + 32×44 = **1420 B**(32 个槽现在用 11 个:1 背景 + 10 表情);
      · 表情是 RGB565A8 = **3 B/px**(2 B 色 + 1 B alpha 平面),背景是 RGB565 = 2 B/px;
      · 一套 = `FACE_COUNT_PER_SET` = **10 张**(两屏各 5 档);`FACE_CANVAS_MAX` = 320、`FACE_SIZE_RECOMMENDED` = 300;
      · 300 那档:300²×3 = **270000 B/张** → 10 张 = **2700000 B**;
      · 320 那档:320²×3 = **307200 B/张** → 10 张 = **3072000 B**;
      · 满屏背景 480×480 RGB565(两屏共用,role 1)= 480²×2 = **460800 B**。

      | 表情尺寸 | 10 张表情 | +1420 头 +480 背景 = 满配镜像 | 占 8 MiB | **空闲** |
      |---|---|---|---|---|
      | 300(推荐) | 2700000 B | **3162220 B ≈ 3.02 MiB** | 37.7% | **5226388 B ≈ 4.98 MiB** |
      | 320(几何上限) | 3072000 B | **3534220 B ≈ 3.37 MiB** | 42.1% | **4854388 B ≈ 4.63 MiB** |

      ⇒ 按**最坏情况(320 满配)也还剩 ≈4.63 MiB**:一条 96000 B 的声音是它的 **2.0%**,
        放满 4 条(384000 B)占 **7.9%**,之后仍剩 **4470388 B**(≈4.26 MiB)给更多的脸/背景。
        "≤96 KB"就是这个基础上定的 —— 小到**怎么加都不会把表情挤出去**,同时 16 kHz 下够 3 秒。
      · 另有一块没用上的空地(实现时可以把声音放这儿,**不改本条的格式与上限**):`spiffs` 在
        **两块分区表上都是** `0x214000` / `0x40000` = **256 KB**,而项目根本没有文件系统 ——
        单这一块就装得下 2 条满额声音(2 × 96000 = 192000 B,余 70144 B)。
      · ★ **容器还没定**(说清楚,别让后来人以为定了):`image.bin` 格式里**没有"裸音频"这一种
        cf/role**(cf 是 LVGL 颜色格式,role 21 起才是留给新角色的约定),所以"进 `image.bin`、
        还是进 `spiffs`、还是单开一段"是**实现时**要定的事。本条钉的是**格式与上限**:
        不管最后放哪,96000 B / 3.0 s 这个闸门不变,超了就在导出/刷写前拒收。

      **④ 防烦人策略(一并定死,不留到实现时再想)**
      · **迟滞**:进 5800(与 `kRpmRedlineFrom` 同值)/ 退 5600 —— 抖在阈值上不会连响。
        (注意:红区**脸**的边界是 5880 进 / 5720 退,声音的 5800/5600 **故意不是同一对** ——
         脸的边界要躲日常落脚点,声音要"真到了就响"。**2026-09-20 用户确认:就按 5800/5600
         这一对,不用和脸对齐。**)
      · **固定短节奏**:**3 短音、间隔 0.3 s**(用户给的例子),永远不是长鸣;上传音比这一拍长就截断。
      · **一次进入只响一次**:过 5800 响一轮,必须掉到 5600 以下再上来才再响(不做周期性重响)。
      · **能关**:一个开关关掉后一声不响(进主题字段还是编译开关,实现时定)。

      **⑤ 现在到哪一步 / 依赖什么(别当已完成)**
      · 本轮**只落文档**:`README.md` 的「下一步」加了这一条,`tools/theme-editor/README.md` 的
        「还没做」写了编辑器那一侧以后要长什么样;没有新增/修改任何源码、没碰构建。
      · 依赖一:**ES8311 初始化 + I2S 通路**(现在代码里一行都没有)。引脚出处已在仓库里 ——
        `src/dash_display_spi.cpp` 的板载占用清单写着 10-11 = I2C(两块触摸 + ES8311/ES7210)、
        12-16 = I2S 音频;与两块屏的 SPI2(SCK 41 / MOSI 42 / CS 47,38 / DC 45 / RST 48,8 /
        BL 46,39)**没有任何重叠** —— 声音不抢屏的脚。
      · 依赖二:**板子得先真的拿到转速**。板上 VAN 收发器还没接(`VAN_RX_PIN=18` 那条待实测),
        所以红区触发暂时只能用 `tools/serial-capture/replay.py` 回放/假数据在桌面验。
        ⚠ **2026-09-22:DualEye 已退货** —— `VAN_RX_PIN=18` 那条实测要等 **2.8" 级最终板**
        (见本条开头那条注记);"先用回放在桌面验"这件事不受影响。
      · 事件源取哪一路也待定:转速三路(VAN > OBD > Sim)都可能跨过 5800,告警应当跟**最终上屏的
        那一个值**(和脸用同一个数),不要另取一路 —— 实现时钉死。

> **显示架构定案（2026-09-21）**：最终形态 = **两块 ESP32-S3、一板一屏一块 2.8" 圆屏（480×480）、两板之间一条 UART**（主选 2× 微雪 `ESP32-S3-LCD-2.8C`，备选 2× 裸 N16R8 + Dwin `LI48480T028BA3098`）—— 决定 / 理由 / 被否掉的路 / 引脚预算 / 待确认全写在 `ARCHITECTURE.md` 的「最终显示架构：两块 S3、一板一屏、一条 UART 串联（2026-09-21）」一节。
> **引脚表更正（2026-09-21）**：`PINOUT.md` 原文把 PSRAM 占脚**写反了** —— 事实是**八线(OPI) PSRAM（就是本项目买的 N16R8）硬件上固定占 GPIO33-37**，那 5 根不可用；只有**四线(Quad)PSRAM**（N16R2/N8R2）**才不占**（所以原来的"排 SPI 用 35/36/37、买四线才要挪脚"恰好是反的，照它接线会接到 PSRAM 的脚上）。据此改正 `PINOUT.md` 开头型号说明的「GPIO33-37 仍可用」、屏 SPI 原排 35/36/37 的三行（改「脚位待定」）与「约束」①，并同步 `PURCHASE.md` 里同一处反向说法（「买的时候别买错」与「补 14 号」两段）。屏 SPI 的实际脚位不在此刻拍 —— 等**微雪 ESP32-S3-LCD-2.8C 原理图**，即 `ARCHITECTURE.md`「最终显示架构：两块 S3、一板一屏、一条 UART 串联（2026-09-21）」一节的**待确认 #1**。

> **左右分工更正（2026-09-21）**：`ARCHITECTURE.md`「最终显示架构：两块 S3、一板一屏、一条 UART 串联（2026-09-21）」一节的**主从左右对调** —— 由「左板主、右板从」改成**右板 = 主**（挂 VAN 收发器 + 日志 USB-C，渲染右盘车速 + 进气温度）、**左板 = 从**（不接 VAN 硬件，只收那条 4 线线缆，渲染左盘转速 + 水温），链路方向随之变成**右 → 左**；理由是车是**左舵**，VAN 那对线（9004/9005）在中控、仪表罩**右侧**够得到，主放右边 **VAN 走线最短**。**屏上内容与 4 线线缆（5V/GND/TX/RX，GND 兼作信号地）不变**。

> **引脚定案 + 两条待确认关闭（2026-09-21）**：同一节新增**主选板（微雪 `ESP32-S3-Touch-LCD-2.8C`，owner 给的厂商图）引脚表**与**两板之间那条 4 线缆的定义** —— VAN 收发器 **RX → 主板 GPIO15**（特意避开 43/44：那是板载 USB-串口桥 CH343P 的一对，桥的 TX 是推挽输出，与收发器 RO 并线会**对打**；43/44 当**输出**可以、当**输入**才有这个风险）；板间链路 = **主板 GPIO43(TX) → 从板 GPIO44(RX)**、115200 8N1 **单向**；**日志各走各的原生 USB-C**（19/20）；从板 5V 取 12PIN 的 **`VBUS(5V)`**；**GPIO7 备用、GPIO0（BOOT/strapping）不用**；线缆 4 根 = **5V / GND / TX / RX，5V 是电源、TX/RX 是 3.3 V 逻辑、GND 兼作电源回流与信号地** —— **跨板只送 5V 绝不送 3V3**（免得一块 LDO 扛两个负载/倒灌）。**待确认 #1（微雪板自由 GPIO 数）与 #2（机械三项 + 孔的真实最小内径）双双关闭**：#1 由厂商图解决（主板 2 根、从板 1 根，够且不抢 BOOT 键/原生 USB）；#2 按 owner 判断「**仪表盘内部空间肯定足够**」**不再测量**（`ARCHITECTURE.md`「机械尺寸」一节的"待量"同步作废）；#3（Dwin 多出的 3.45 mm 在哪条边）降为**只对备选（裸 Dwin 屏）有意义**，#4（表仓塑料桥）作废。另记一条**待办（未做）**：显示那两个 env 要关掉 `lib/dashcore/dash_log.h` 的 **UART0 日志**（`Serial0.begin()`）—— UART0 已让给 43/44 那条板间链路，日志文本会混进链路数据流，日志改走原生 USB-CDC；本轮**只记不做**。

- [x] **★ 240 档验证板（微雪 ESP32-S3-DualEye-Touch-LCD-1.28）验证完成并已退货；手上的显示硬件归零（2026-09-22，owner 亲口确认）**

      **事实（owner 确认，本条只记已确定的事实）**
      这块板承担的是"**真屏 240 档**"的验证 —— 字号随分辨率缩放、图片素材导入
      （白底字看不见的 bug、弧线端帽 bug）、面谱分档，都在它上面**验过并被接受**；
      该板**已于 2026-09-22 退货**，机器不在手上。**退货原因 owner 未说 —— 本条不记、不猜。**

      **退货 ≠ 结论作废**：`[env:esp32s3-spi]`、`lib/themetool/ui_theme.h` 的 240 字号档、
      `tools/theme-editor/spec-240.json` 等**继续保留并仍然有效**（240 档的代码一个字没删，
      也不该删）—— 变的只是"**现在手上没有这块硬件**"这一件事。

      **· 手上的显示硬件归零**
      现在只剩**裸 S3 抓帧盒**（**无屏**，用途是 VAN 抓帧，见 `PINOUT.md` 的「抓帧盒」一节）；
      要上屏得等 **2.8" 级圆屏**到货 —— 最终显示架构 = 两块 S3、一板一屏、一条 UART，
      见 `ARCHITECTURE.md` 的「最终显示架构：两块 S3、一板一屏、一条 UART 串联（2026-09-21）」一节。

      **· `esp32s3-spi` 仍可编译，但无硬件可上屏**
      环境、分区表与 `src/dash_display_spi.cpp` 的驱动**一行代码没动**，编译目标照旧存在；
      本条影响的是**验证面**（有没有屏可上），不是"240 档的代码要删"。

      **· 对后续上屏验证的影响（都要等真屏到货）**
      凡是"上真屏看"的项都停在这一条上：真屏上的字号 / 素材观感、SPI flush 那行每秒诊断、
      `esp_partition_mmap` 的设备端分支、`VAN_RX_PIN=18` 的板上抓帧、表情与背景的实际观感。
      在那之前只剩三条**不带屏**的路：`pcpreview` 落帧读像素、`-e native` 单元测试、
      `tools/serial-capture/replay.py` 桌面回放。

      **· 文档侧同步（本轮只动文档：4 个 .md + 工具页 `image-editor.html` 的提示文字；代码逻辑 / 配置值 / 素材未动 —— 同日追加的 3 处注释文字见下）**
      `README.md`（板子那段改成"曾经用于验证、**2026-09-22 已退货**" + 补当前硬件实情；
      环境表 `esp32s3-spi` 那一行同步）、`PURCHASE.md` 十一之二的选型分析**加日期注记**
      （**分析原文一字未动、选型结论维持原判**：2.8" 级圆屏）、
      `tools/theme-editor/README.md` 与 `image-editor.html` 的操作指引加一句
      "该板已退货，本节保留供同类板参考"（**步骤一条没删**）。
      ★ **同日追加（owner 裁决）**：又改了 3 处**纯注释**里的"现在实配"断言 ——
      `src/dash_display_spi.cpp`、`platformio.ini`、`tools/theme-editor/image-blob-build.js`，
      各改成"240 档验证用的板…**2026-09-22 已退货**…本套配置 / 这一档**保留且仍然有效**"；
      **只动注释文字，未动任何代码逻辑 / 配置值 / 素材**。
      另按同一次裁决：`README.md` 的「下一步」第 1 条改成"已在裸 S3 抓帧盒完成抓帧 + 新板要重做"，
      第 2 / 6 条各加一句日期注记（要等 2.8" 级最终板）；**声音告警那条（上面 `- [ ]` 排队条）**的开头
      与它的「依赖二」各加一句 ⚠ 注记（规格原文一字未删）。

- [x] **★ 三件收尾（2026-09-22）：链路协议 v1 契约稿入档 + 车速字段口径更正 + CRC-15 检错覆盖实测**
      **① 双板链路协议 v1 契约稿写进 `ARCHITECTURE.md`**（`f7a5744` + `acf7dde`）：
      新节 `## 双板链路协议 v1 范围`，**九个子节**（§0 角色与物理层 … §8 待确认 L1~L14），
      **只定范围与消息表**（帧格式 / 消息表 / 时基 / 失败兜底），**既有段落一行未改**
      （`f7a5744` = 202 行纯新增、0 处删除；`acf7dde` = 再补 1 行，记 `data_service.h` 的
      `FieldSource` 没有"主机/链路"档这条实现侧缺口）。**行span**：落笔时 **463~664 行**
      （`acf7dde` 时；`6401d54` 在 §8 补了 L4 注记后为 **464~666 行**，文件共 666 行）。
      与上一节的关系只**收窄**一处：上一节把"复用回放行格式"当作左板"零新协议"的起点，
      本节把回放行降级为**调试通道**、数据面走 §2 的二进制帧，从板入口按**首字节**分流
      （`'V'` = 回放行 / `0x5A` = 链路帧）；既有实现（`van_replay.h`、`main.cpp` 的
      `van_replay_poll()`）**不动**。
      **② 车速字段口径更正**（`d89b0df`，只改 `ARCHITECTURE.md` 一行）：「软件分层」那行原写
      `data[2..3]=车速×100 km/h（大端）`（公开文档的旧口径），与 2026-09-20 的实车抓包定案
      **矛盾** ⇒ 按文件改准：`data[2]` **单字节**、**1 计数 = 1 km/h**、`kSpeedScale = 1.0`、
      `data[3]` 含义未知（绝对刻度仍无地面真值，按表盘脸的档位复核）。转速那半句本来就是对
      （`data[0..1]=转速×8`），未动。
      **③ CRC-15 检错覆盖实测：关闭 §8 的 L4**（`6401d54`）：新增
      `test/test_dashcore/test_link_crc_coverage.cpp`（**5 条用例**），对 v1 帧长
      **12..23 B（96..184 bit）逐帧长全量枚举**（1/2/3 位错 + 长度 2..16 bit 的全部突发，
      约 **5.57×10^7** 个图案；判定走 CRC 的仿射线性，1/2 位错再用真实 `crc15_van_iso`
      **全量**复核）：
      · **1 位 / 2 位 / 3 位错未检出全部 = 0**（3 位错属奇数位错，`(x+1)` 因子保证必检出）；
      · **长度 2..15 bit 的突发未检出 = 0**；
      · **16 bit 突发未检出合计 1308 个**（每帧长 `8(L−2)−15` = 65/73/…/153），**全部落在被
        CRC 覆盖的数据区内**、图案**恒为 `0xB9F1`**（= `0x8F9D` 的位反转）；碰到末尾 CRC
        字段的 16 位突发**一个都没被放过**；
      · **最小汉明距离 = 4**（每个帧长都有一个用真实 CRC 复核过的 4 位错见证）。
      口径：码字 = 整帧；**`SYNC` 不进 CRC 覆盖**（它错由"重新找 `SYNC`"兜住），不在本枚举内。
      **实测（宿主机，Windows 台式机）**：native **128 例 / 125 通过 / 2 跳过 / 0 失败**
      （本次基线 **123/120/2/0**，+5 例）。★ `test_face_stages.cpp` 那个**既有**崩溃仍在
      （`panic: index 4 out of bounds for type 'Face[4]'`，进程 **exit 3**）——**它不是失败**：
      摘要里 `[FAILED]` = 0，PlatformIO 只是按进程退出码把环境标成 `ERRORED`；
      **进程退出码不能当判据**。两条跑测纪律（新用例注册顺序、`-v` 与 printf/`fflush`）
      已写进 `README.md` 的「编译 / 测试 / 刷机」。
      ★ **同日更新（2026-09-22）：那个"既有崩溃"已经修掉了** ——
      · `e97b13a`：`test_driven_screen_differs_within_group` 里的 `Face seen[4]` 改成容量由
        `face_stages.h` 的 `kFaceStageCount` 推导（**不再写死档数**），断言一条没动。
        崩溃消失 ⇒ 注册在它后面的 4 条用例**第一次真的跑起来**，当场红了 2 条（见下），
        `exit 3` 也随之消失（退出码变成 Unity 的失败条数 2）。
      · `a75b94a`：那 2 条红是**同族的"写死 4"**，都改成以 `face_stages.h` 为唯一依据 ——
        `test_fallback_chain` 的 `k < 4` / `a < 4` / `b < 4`（只扫前 4 列，Idle 落在第 5 列的
        Sport / Redline / High 三行永远扫不到）改成按行宽推；`test_screen_state_sets` 的
        `4`（`kFaceStatesPerScreen` 早就是 5）改成"声明值 ↔ 左右数组真实长度"。两条转绿，
        而且覆盖面比原来更全（降级链第 5 列以前从没被查过）。
      · **现状实测**：Unity 自报 **132 Tests / 0 Failures / 2 Ignored**（进程退出码 **0**）、
        PlatformIO `132 test cases: 2 skipped, 130 succeeded`、环境 **`[PASSED]`** ⇒
        注册 / 通过 / 失败 / 跳过 = **132 / 130 / 0 / 2**。
      · ⇒ "**新用例必须注册在 `face_stages` 之前**"这条纪律**已无必要**（保留为来龙去脉，
        见 `README.md`）；`-v` 才转发 `printf`、中文输出会打乱用例统计这两条**仍然有效**。
      · 顺带把 `ERRORED` 的含义写清（`ARCHITECTURE.md` 测试节）：PlatformIO 的 native runner
        只看**非零退出码**，而退出码还有个坑 —— Unity 用"**失败条数**"当退出码，**2 在
        Windows 上正好撞上 `signal.Signals(2)` = SIGINT** ⇒ "2 条用例红了"会被报成
        `Program received signal SIGINT (Interrupt)`、环境标 `ERRORED`（修前的 exit 3 不是
        Windows 信号，才报 `Program errored with 3 code`）。**`ERRORED` ≠ 崩了**。

- [x] **★ 链路协议 v1 定案：`ARCHITECTURE.md` §8 的 L8~L13 六条裁决完毕（2026-09-22）**
      **来源：owner 裁决，2026-09-22**（六条照写、**编号保留** —— §1~§7 里引用 L8~L13 的
      地方照旧对得上）；落地提交 **`e545be4`**（只改 `ARCHITECTURE.md`：**+22 / −15 行、
      37 行变动**，未动代码 / 测试 / `platformio.ini` / 素材）。
      §8 因此从"整节待确认"变成**两张表**：**「v1 定案」6 行（L8~L13）** +
      **「仍未确认」8 行（L1~L7、L14）** = **14 条编号**，节标题改为「待确认与 v1 定案」，
      原表里 L8~L13 六行删掉（L4 那条已关闭，仍按既有风格留在表里、带删除线）。
      逐条一句话（**这就是 v1 的口径**，细节与代价见 §8 的新表）：
      · **L8** 两块板**刷同一份 `theme.json`**（写成**规定**：两板 `theme` 分区内容必须
        一致，否则两屏配色 / 几何 / 开机扫表时长不同**而不报错**）；`HELLO` 里带 16 位
        `theme_tag` 的做法**留到 v2**（代价：刷主题要刷两块板；§4 的跨屏扫表对齐依赖它）；
      · **L9** `EVENT 0x06`「模式切换」**v1 不做**，**号段保留**（不许挪作他用）；
      · **L10** 链路降级（`TICK` > 500 ms）时从板 = **冻结最后值 + 表情退常态**
        （> 3 s 仍没恢复则按 §3 回退 Sim）；
      · **L11** 主板「从板离线」**角标 v1 不做** —— 从板在线状态**只进 `STATUS` 日志、
        不上屏**（⇒ 本轮不动素材 / 角色编号）；
      · **L12** 检测到角色冲突 = **丢帧 + 报警**；要不要升级为「只收不发」**等 L1 的结论**；
      · **L13** `STATUS` 超时门限 = **30 s**（不是 5 s），且**只用于日志、不上屏**
        （"从板单独刷机 / 断电"算常态 ⇒ 门限放宽）。
      连带把直接引用这六条的**六处**改一致（只改这些句子、未重排整节）：§3 的 `STATUS` 行
      （>5 s → **>30 s**）、§3 的 `0x06` 行（留号 → **v1 不做、号段保留**）、
      §4 跨屏动画同步（补 L8 前提）、§4 主板等不到从板（角标 v1 不做 + 门限 30 s）、
      §5 ① 角色冲突（**丢帧 + 报警**）、§7 失败模式表第 1 / 4 / 6 行。
      **仍未确认的 8 条**（L1~L7、L14：GPIO44 与板载桥对打、链路波特率对 VAN 采集的影响、
      临界区上限、跨屏同步 < 40 ms 目标、5V 支路电流、12PIN `VBUS` 真身、声音告警由哪块板
      发声）仍留在 §8 的「仍未确认」表里，每条都带确认办法。
      **实测（本条目落笔时的文件状态）**：`ARCHITECTURE.md` 共 **684 行**，§8 占
      **660~684 行**（「v1 定案」表 **666~671 行 = 6 行**、「仍未确认」表 **677~684 行 = 8 行**）。

- [x] **★ 收发器接线口径更正：`D` / `TX` 接 3V3（不是悬空）+ 6 脚模块的 `RS` 板内已接地（2026-09-22）**
      **起因**：owner 手上的模块是 **6 脚**（丝印 `3.3V GND RX TX CANH CANL`，**owner 2026-09-22
      实测：只有这 6 个脚**），**没有 `RS`（芯片第 8 脚）那一脚**；而 `PINOUT.md` 原文把
      `D` / `TXD`（发送输入）写成**悬空** —— 这两处都要改准。
      **结论（两条，就是本次改动的口径）**
      · **`D`（模块丝印常写 `TX` / `TXD` / `DI`，芯片第 1 脚）→ 接 3V3，不是悬空**：`PINOUT.md`
        改了 **6 处**（主引脚表 `VAN TX` 那行、约束 ③、「第一段」接线表、【抓帧盒】A 接线表、
        4/6 脚模块对应表、`RX`/`TX` 判定法），另在【② 认脚位】与 B 节第 5 条各补/改一句；
        `ARCHITECTURE.md` 的「接线定案」两处、`PURCHASE.md` 的「SN65HVD230 上板」接线清单
        与杜邦线那一行同步。**"我们只收不发"这个目的说明全部保留**。
      · **6 脚模块的 `RS` 在模块内部已接 GND ⇒ 用户无需外接**：`PINOUT.md`【抓帧盒】一节新增
        「6 脚模块」一段 —— 含 **SN65HVD230 芯片脚位表**（1=`D` 2=`GND` 3=`VCC` 4=`R` 5=`Vref`
        6=`CANL` 7=`CANH` 8=`RS`）与**万用表确认办法**；原 `RS`/第 8 脚那条（接 GND = 高速、
        接 VCC = 待机）保留。
      **为什么（`D` 不能悬空）**：`D` 是**驱动输入** —— **低电平 = 显性 = 收发器主动把总线拉低**；
      悬空时这根脚被噪声拉低，模块就会**主动干扰被测的 VAN 总线**（我们只监听，绝不能发）。
      `D` 拉**高** = **隐性 = 不驱动** ⇒ 接 3V3 是从**物理上**排除"发"的可能。
      **事实 / 推断分开（不冒充实测）**
      · **事实**：芯片脚位（TI 标准封装）；`RS` 接 GND = 高速模式、接 VCC = 待机；**owner
        2026-09-22 实测：手上这块模块只有那 6 个脚**。
      · **推断（待确认）**：这块 6 脚模块把 `RS` 在板内接 GND（芯片第 8 脚没引出）。
        **确认办法**：断电，蜂鸣档量**芯片第 8 脚 ↔ 第 2 脚（`GND`）**是否导通 —— 导通即落定；
        不导通（`RS` 可能悬空或接到了别处）**先别上车**：悬空的 `RS` 电平不定，收发器可能随时
        进待机或受斜率限制。
      · **接 3V3 之前先认准哪根才是 `D`**（个别厂家把 `RX`/`TX` 反着标）：断电用蜂鸣档量模块
        `TX` 脚 ↔ 芯片第 1 脚，通即丝印是正的；反着标就把 `RX` 接 3V3、`TX` 接 GPIO16 ——
        **别把 3V3 顶到 `R`（输出）上**。这一步**在桌面做、别在车上试**（脚位没定时 `D` 是悬空的）。
      **改动面（本轮只动文档文字 + 注释文字；代码逻辑 / 配置值 / 素材一行未动）**：本文件加本节；
      `PINOUT.md`（+54/−11）、`ARCHITECTURE.md`（+3/−1）、`PURCHASE.md`（+4/−2）、
      `lib/dashcore/van_phy_gpio.h`（+3/−1，**注释**）、`lib/dashcore/van_phy.h`（+1/−1，**注释**）、
      `src/main.cpp`（+1/−1，**注释**）、`tools/serial-capture/capture-van.ps1`（+2/−2）与
      `capture-van-nopy.ps1`（+3/−3，**只改 `Write-Host` 提示文字**，统计与自检未动）；
      未跟踪的 `tools/theme-editor/theme.json` **未碰**。
      ★ **同轮没动的两处（列给 owner，等裁决）**：
      ① **`platformio.ini` 未动** —— 它第 196 行注释只写「RO → GPIO16,DE/RE → GND」，**不含**
      "`D` 悬空"那句旧口径，所以本轮按"不改 `platformio.ini`"处理；要不要顺手把注释补成
      「`D`/`TX` → 3V3、`RS` → GND」**待定**。
      ② **`PINOUT.md`【抓帧盒】A 节「共地」那段的 ✓ 建议**仍写「把模块 `GND` 也接到**车的 GND**
      （OBD 座 4/5 脚或仪表侧搭铁）」，而 owner 这次的口径是**抓帧盒 USB 供电、车上只接 CANH/CANL
      两根差分线、不接车辆地** —— 两处对不上（事实与建议分属不同来源），**本轮未动那一段**、
      怎么接**待确认**。

- [x] **★ 采集工具扩容一轮（2026-09-22 晚）：整帧落盘 + 温度地面真值 + OBD 不再阻塞 VAN**（车主当场确认进气温度那一路）
      **起因（车主原话）**：「进气温度呢 那个可是表的一部分」—— 右屏速度表那道**内圈弧就是进气温度**
      （见本文件 2026-09-17 那条：`intake_c`、量程 0/80℃、颜色 `0xFFB020`），
      而这半边的真值**一次都没在车上量过**。

      **① 进气温度的地面真值（本轮实测拿到）**
      · 蓝牙 ELM327（COM6）/ `ATSP0`：**`010F` → `41 0F 6F` = 0x6F − 40 = 71 ℃**；
      · 同一次读数：`0105`（水温）→ `41 05 80` / `41 05 84` = **88 / 92 ℃**，
        `010C` → `41 0C 0E 17` = **901.75 rpm**（怠速那一路早已闭环）；
      · **车主当场确认：「71℃ 正常」** —— 因为装的是**冬菇头**（高流量滤芯，非原厂进气箱），
        进气温度常年偏高属于正常现象。⇒ **以后不许把「进气温高」当传感器或解码 bug 查。**
      · 口径澄清（车主「1 2 吧」= 两条都记，不是二选一）：表上那道弧的值**只有 OBD 这一个来源**，
        VAN 上目前没有任何已解出的进气温度字段；而「VAN 上到底有没有副本」这个问题
        **本轮仍然没验**（见 ④ 第 1 条）。两条都要落到文档里，别把「没验」写成「没有」。

      **② 上一版采集脚本的问题：问不了「VAN 上有没有温度副本」**
      · 旧的 `van_drive.csv` **只挑 `data[0..2]` 三列**落盘 —— `data[3]` / `data[4..5]`
        **根本没记**，于是事后想问什么都问不了（历史抓包里 `data[3]` 出现过 `0x30` 与 `0x01`
        两种值，也正因为没落盘而无法复核）；
      · 板子其实**本来就把整帧打全了**（`VanLogSink`：`VAN 824 1C 1E 27 …`，`cmd`/`ack` 打在行尾注释里）
        ⇒ **不用重新刷固件**，只把采集放宽。**采集脚本永远比改固件便宜，数据先囤着。**
      · ★ **索引口径按实测核对过、不是照抄注释**：那行日志 hex 的第 i 个就是 `data[i]`，
        **不带 cmd 字节** —— 旧 CSV 的 `data0` 列实测是 `0x1C`（转速高字节），
        而 `1C 1E` / 8 = 903.75 rpm 与怠速真值 900 对得上，**cmd 是 8 而不是 0x1C**。
        写错过一次（把 `data[0]` 当命令字节 → 速度列整体错位一位），自查后改正。

      **③ VAN 帧结构（实测样本 `VAN 824 42 40 0D 9D 92 08 55`，怠速 / 熄火滑行）**
      · `data[0..1]` = 转速 ×8（16 位大端，`0x4240`/8 = 2120 rpm）；
      · `data[2]` = **车速单字节**（`0x0D` = 13 ⇒ 13 × 2.56 = **33.28 km/h**，标度已定，
        见上一轮 c596351 那条）；
      · `data[3]` = 未知（与车速反相相关，疑似低分辨率副本）；`data[4..5]` = 位移/里程累计量（16 位大端）；
      · `data[6]` = 帧序号。

      **④ 本轮改了什么（只动采集脚本；固件、`lib/`、测试夹具、配置值一行未改）**
      1. `tools/serial-capture/drive-log.ps1` —— VAN 侧**整帧落盘**（`iden/len/cmd/ack/d0..d6/spd/rpm/seq`），
         并把 `0x4FC`（灯/门，现成的**已知时间标记**）一并收进同一张表；
      2. 同脚本 OBD 侧加 **`0105`（水温）+ `010F`（进气）**，并显式解析 `℃ = A − 40`；
      3. ★ **OBD 轮询不再阻塞 VAN**：原版是「发一问 → sleep 120ms → 死等最多 2500ms」，
         这两三秒里**完全不读串口**（115200 下驱动缓冲一两秒就满，多余的字**默默丢掉**）。
         加了 2 个 PID 后盲区变 4 × 2.5s ⇒ 重构成**同一个 15ms 循环里先读干 VAN 再走 OBD 状态机**，
         超时 2500 → 1200ms。实测 VAN 吞吐 **0.75 → 4.4 条/秒**；
      4. ★ **OBD 结算条件改成「收到 `>` 提示符」**：踩过一次 —— 327 先吐半截 `SEARCHING..`，
         那**一个字符**就让「有字节」成立、于是在真数据到达前就结算，
         实测 **209 条里 179 条失败**。改成等提示符后：**64 条采样 0 失败**；
      5. 收尾打一行**采集质量**（VAN 条/秒 + OBD 失败率，失败 >20% 直接红字告警），
         不让「静默降级」再骗一次。

      **⑤ 验证**
      · `drive-log.ps1 -SelfTest`：全部通过（新增 `d3`、`datalen`、短帧越界哨兵 `-1`、
        `data[0]` 是转速高字节、`0105`/`010F` 的 ℃ 换算共 5 条断言）；
      · 实车空跑（点火 ON、车不动）双路 **20 秒**：OBD **64 条 / 0 失败**，
        CSV 两列口径正确（`iden=824` 才有 `spd/rpm`，`4FC` 那些行留空）。

      **⑥ ★ 板子固件怎么烧（★ 本条 2026-09-22 深夜更正过一次，见文末「本条更正」）**
      · **实车能解帧的构建就是 `[env:esp32s3]`**（**不带** `VAN_RX_INVERT`）：
        `python -m platformio run -e esp32s3 -t upload --upload-port COM3`。
        判据：开机那行 `van phy:` **不该**出现「已翻转」；出现了就是**烧错版本**，
        先量再动线（`9004/9005` 的对调**早就做过**，见 `PINOUT.md` ⑤ 与 227 行）。
      · **别烧 `[env:esp32s3-vaninv]`**：那个宏是「线还没换、拿不准极性」时的**对照手段**
        （`platformio.ini` 141~143 行写得很清楚：确认后要**回到 `esp32s3`**）。
        在**已经对调过**的线束上用 `VAN_RX_INVERT=1` = **反两次 = 又反了**。
        实测对照（同一辆车、同一根线，只差固件）：
        | 构建 | 边沿/s | 帧/s | 15 秒内 `0x824` 行数 |
        |---|---|---|---|
        | `[env:esp32s3]`（正确） | **3,541** | **48.6** | **259**（≈17.3 条/秒） |
        | `[env:esp32s3-vaninv]`（反两次） | 2,400~2,700 | ~0 | 0 |
      · 行驶抓包当时烧的就是 `[env:esp32s3]`：`C:\206dash-build\esp32s3\firmware.bin`
        = **740,352 字节**，二进制里**不含** `VAN_RX_INVERT=1` 那个串；
        `esp32s3-vaninv` 是 **740,368 字节**、含该串 ⇒ **两个 binary 就能自证烧的是哪一版**，
        不用猜。
      · 板子 `[env:esp32s3-vansniff]`（`VAN_SNIFF=1`）是排障用的：它每帧多打好几行，
        实测把 VAN 输出压到 **0.75 条/秒**（安静版同条件 4.4 条/秒）⇒ **正式采集别用它**。

      **⑦ 已知限制（别当结论用）**
      · 采集速率**正常时是够的**：安静构建实测 **48.6 帧/秒 全部帧、其中 `0x824` ≈17.3 条/秒**；
        `115200` 下 17.8 × 46 字节 ≈ 820 B/s，离 11.5 KB/s 的上限差得远，**不是链路的锅**。
        （我先前写的「0.75~4.4 条/秒」是**极性反了 + 板子在跑 `vansniff` 构建**时的读数，
        不代表正常状态 —— 已按上面的对照实测更正。）
      · ★ **口径更正（2026-09-22 复核时发现，本条 ⑦ 初版把两个数混用了）**：
        「**17.8 帧/秒**」是那份行驶抓包的 **CSV 行速率**（6561 行 / 368.2 秒 = 17.82），
        **不是总线帧速率**。由同一份 CSV 的帧序号列 `seq`（`data[6]`）算：
        它模 256 的递增量累加只有 **4146** ⇒ **总线帧速率 ≈ 11.3 帧/秒**；
        `6561 / 4146 = 1.58` ⇒ 串口侧把同一帧**打印了约 1.58 次**（旧列宽脚本的重复输出）。
        上面那句「与总线上的 17.8 帧/秒一致」是**行速率与帧速率混用**；
        另外「总线全貌表」记的 `0x824` **18.0 帧/秒**是**点火怠速**条件下的总线帧速率，
        与 11.3 也不是同一件事（整车状态不同）。
        ⇒ **引用这个数时先说是哪个口径**（行/秒 还是 帧/秒），两处实测本身都没错。
        采集吞吐的结论不受影响（820 B/s ≪ 11.5 KB/s，全程 `fcs_ok == frames`）。
        详见本条末尾的 2026-09-22 独立复核。
      · 复测纪律：判「采集够不够」**只看 `frames/s` 与 `824/s` 两个数**，别看边沿数 ——
        极性反了边沿照样涨（`PINOUT.md` 30 行）。★ 并且**要区分行/秒与帧/秒**（见上）。
      · 一次**双路行驶抓包还没做过**（新格式 + 010F 同跑）：默认只记录 VAN，
        要温度真值必须显式带 `-ObdPort COM6`；
      · 本轮**仍未解出**：`data[3]` 物理含义、左右门/倒车/远光那几个老问题（本文件前面各条照旧）。

      **⑧ 本轮行驶抓包已入档（就是车速标定 2.56 的那两份，别再找不着）**
      · `tools/serial-capture/drive-2026-09-22-van.csv` —— 6561 行 / 368.2 秒，
        `0x824` 约 **17.8 条/秒**（车主点火 ON，板子跑 `esp32s3-vaninv`）；
      · `tools/serial-capture/drive-2026-09-22-obd.csv` —— 1198 行真值（`010D`/`010C` 各 599 条，
        **成功 599/599**）；`010D` 最大 **100 km/h**，对应 `spd_count` 最大 **39**（39 × 2.56 = 99.84）；
      · ★ 这两份是**旧列宽**（`spd_count,data0,data1,rpm_count,seq`，没有 `data[3]` 以后），
        所以**验证不了进气温度那条**；要验必须用本轮的脚本**重新跑一次双路**。
      · 时间轴**同源**（两个串口接同一台笔记本），列 `t_s` 是同一个 Stopwatch 的秒数 ⇒
        两份文件可以按 `t_s` 直接对齐，**不需要**对墙上时间。

      **⑨ ★ 本条更正（2026-09-22 深夜，车主当场纠正）**
      · **被更正的是 ⑥ 的初版**：初版写「实车能解帧的是 `esp32s3-vaninv`、
        `PINOUT.md` 的对调还没做」——**两条都错**。车主指出「测试前已经把线对调了」，
        复核后**车主是对的**：`PINOUT.md` 227 行**本来就记着**对调已完成
        （11 秒 492 帧、`fcs_ok == frames`、CRC 100%），是我把一条**已完成的实测**
        当成了未办事项。
      · 错因（写下来免得再犯）：我烧完 `esp32s3-vaninv` 后抓到「极性已翻转 + 帧数极低」，
        就把这个**自己造成的现象**反推成"车上的线没换"。正确顺序是
        **先查记录（`PINOUT.md` ⑤/227 行已经写明对调完成）再下结论**；
        而且二进制大小（740,352 = 无宏）当场就能自证行驶抓包烧的是哪一版。
      · 后果与现状：我一度把板子烧成 `vaninv`（= 反两次），VAN 采集归零并据此写错了文档；
        **现已烧回 `[env:esp32s3]` 并实测恢复**：边沿 3,541/s、帧 48.6/s、15 秒 259 条 `0x824`。
        **板子当前运行的就是正确构建**，不需要任何人再动线束。
      · 教训（给后来人）：**「边沿涨、帧不涨」的第一处置仍然是对调线**（`PINOUT.md` 229 行），
        但在这辆车上**对调已经做过了** —— 所以现在再出现「帧不涨」，
        先怀疑**固件烧错版本**（`van phy:` 那行有没有"已翻转"）或**总线在睡眠**，
        **不要再动那两根线**。

- [x] **★ 2026-09-22 全天汇总（台式机侧 + 笔记本侧）＋ 当晚实车数据独立复核**
      **本条是"汇总视角"，不是又一份日志** —— 上面 1601 / 1641 / 1696 / 1725 / 1767 行那几条
      各记一件事、各自只覆盖自己那半天；本条把它们**并到一张时间线上**，
      **且不重复上面的内容**（要看细节回原文）。后半段是**当晚两份 CSV 的独立复核**，
      数字全部由复核脚本从文件里直接算出来。核对时 **HEAD = `eba319e`**。

      **时间线（时间与 Machine/Role 都取自提交信息，不是推测）**

      | 时间(2026-09-22) | 侧 | 结果 | 依据提交 |
      |---|---|---|---|
      | 09:42 | 台式机 | 「笔记本到 GitHub 没有通道」是**错结论**，实测已修好：笔记本 DNS 干净、remote 换 SSH over 443、两端 `id_ed25519` 指纹一致 | `cb32e4d` |
      | 09:46 | 笔记本 | OBD 一直是**注释状态**（`OBD_RX_PIN/TX_PIN` 两个宏根本不存在）⇒ 定义 17/18 + `Serial1.begin(38400)` | `8733380` |
      | 09:47 | 笔记本 | 笔记本侧提交加 `Machine/Role` 来源标记 + `lt-commit.ps1` | `80eee92` |
      | 10:03 / 10:05 | 台式机 | 240 档验证板 DualEye **已退货**，手上显示硬件归零；三处注释断言与 README「下一步」按裁决改口径 | `07b9a80` `ab5b52d` |
      | 10:59 / 11:00 | 台式机 | `ARCHITECTURE.md` 新增**「双板链路协议 v1 范围」**契约稿（帧格式/消息表/时基/失败兜底 + `待确认` L1~L14 成表）；并补一条实现侧缺口 | `f7a5744` `acf7dde` |
      | 16:34 | 台式机 | **CRC-15 检错覆盖枚举**：帧长 12..23 B **全量枚举**（1/2/3 位错、2..16 bit 突发）⇒ 关闭 §8 的 **L4** | `6401d54` |
      | 16:40 | 台式机 | 三件收尾入档（协议 v1 / 车速字段口径 / CRC 覆盖实测） | `b59f372` |
      | 16:42 | 台式机 | §8 的 **L8~L13 定为 v1 定案**（owner 裁决），待确认表只剩 L1~L7 与 L14 | `e545be4` |
      | 16:50 | 台式机 | 修掉 **`Face[4]` 越界崩溃**（容量改由 `kFaceStageCount` 推导）；崩溃消失后**暴露出两条被它掩盖的隐藏失败** | `e97b13a` |
      | 16:54 | 台式机 | 两条同族「写死 4」修掉（行宽/状态数一律由 `face_stages.h` 推） | `a75b94a` |
      | 16:55 | 台式机 | **native 首次真绿**：注册 **132 / 通过 130 / 失败 0 / 跳过 2**、环境 `[PASSED]`、**进程退出码 0** | `a75b94a` `36937d5` |
      | 16:55 | 台式机 | 文档口径改准：崩溃叙述、转速扫描上限 6000→**7000**（`kRpmMax` 实际值） | `46a5c11` `a8ec4ed` |
      | 16:56 | 台式机 | 收发器接线口径改准：**`D`/`TX` 接 3V3**（不是悬空）+ 6 脚模块 `RS` 板内已接地 | `dd4d7fc` |
      | 21:21 | 笔记本 | 有线 ELM327 **实测判定为坏**（AT 全通但 K 线读不到 ECU）+ 两个探针脚本 | `11a3ada` |
      | 21:46 | 笔记本 | VAN 接收**极性接反**：加编译期翻转开关，实测 `frames 0 → 492`、`fcs_ok == frames`（**CRC 100%**） | `d512d7c` |
      | 21:50 | 笔记本 | 极性实测确认：「`edges` 涨、`frames=0`」**第一处置就是对调那两根线** | `7ffd8ff` |
      | 22:00 | 笔记本 | 真 bug：**`sink` 从没被转发到解码器**（两份 `sink_`，基类 `setSink` 非虚）—— 帧行打不出来 / 数据到不了 `VanSource` 的真因 | `7f77963` |
      | 22:09 | 笔记本 | 嗅探改为**全量 IDEN 输出** ⇒ 车通电未打火 30 s：**1556 帧 / 12 种 IDEN** | `5f43d1d` |
      | 22:16 | 笔记本 | 转向灯/双闪：**`0x4FC` 的 `data[5]` 是位域**（`0x04`/`0x08`/`0x0C`） | `ee15e9e` |
      | 22:19 | 笔记本 | **VIN 在 `0xE24` 明文广播**（`VF32DNFUR2W005450`）+ 总线全貌实测表 + 一处自我更正 | `f839a7d` |
      | 22:36 | 笔记本 | 灯光位域再解两位 `0x40`/`0x80`；**刹车与雨刮不在这条总线上**（排除法） | `1a5d69b` |
      | 22:44 | 笔记本 | 灯位定死：**bit7 = 仪表盘灯 / bit6 = 近光**（逐档停住，两次开关印证） | `2826610` |
      | 22:47 | 笔记本 | 门状态位定位（一次改动三个族）＋ **定案：闪远光不在这条总线上** | `9ad3d10` |
      | 22:57 | 笔记本 | **撤回**「单一门状态位」结论 ⇒ 左右门可分辨性 = **未解**（附第四轮判定与**两条撤回**） | `fa583f4` |
      | 23:11 | 笔记本 | 新增 `drive-log.ps1`（行驶采集，**双串口同源时间戳**） | `1e32b83` |
      | 23:20 | 笔记本 | 双串口实测修通：OBD 初始化时机 / 读窗口 + **VAN 失联告警** | `9fc0226` |
      | 23:31 | 笔记本 | 车速标度定为 **2.56**（首次有地面真值，**推翻**「1 计数 = 1 km/h」） | `c596351` |
      | 23:54 | 笔记本 | 采集扩容：整帧落盘 + 温度真值 `010F` + **OBD 不再阻塞 VAN**；两份 CSV 入档 | `901d67a` |
      | 23:59 | 笔记本 | **更正上一条**：线早就对调过了，该烧的是 `[env:esp32s3]` | `eba319e` |

      **两台机器的分工（看时间线就能看出来，这里只说结论）**
      · **台式机** = 文档/契约/测试主场（09:4x~16:5x）：链路协议 v1 契约稿与 L4/L8~L13 裁决、
        native 套件修绿、文档口径更正。**没有碰实车**。
      · **笔记本** = 实车主场（21:2x~23:5x，带 `Machine: MikamoNeru / Role: laptop-agent`）：
        极性 → sink → 嗅探 → 协议解码 → 采集工具 → 车速标定 → 数据入档。
      · 两边当晚靠 `push` 合流，**没有互相阻塞**。

      **★ 独立复核一：VAN 侧（`drive-2026-09-22-van.csv`，只读脚本重算）**
      · **列宽 = 旧列宽**：`t_s,wall_time,spd_count,data0,data1,rpm_count,seq`，
        物理 **6562 行** = 表头 1 + 数据 **6561** 行 ⇒ 与 ⑧ 行记的 6561 条一致；
      · **时间跨度 368.207 s**（6.14 分钟），`t_s 0.002 → 368.209`，
        `wall_time 23:21:51.467 → 23:27:59.652`（**该列没有日期**）；
      · **行速率 17.82 行/秒**；`t_s` 只有 **598 个不同的值** —— 每个值下平均 **10.97 行**
        （数据是**成簇**到达的），簇间隔中位数 0、最大 **1.207 s**（全程只有 1 处 > 1 s）；
      · ★★ **这份 CSV 里只有 `0x824` 一种 IDEN**，**没有 `4FC` 的行** ⇒
        采集时跑的**不是** `901d67a` 那种"整帧"列宽，而是扩容前那一版脚本
        （`1e32b83` 的旧列宽，只收 `0x824`）。**所以"出发前/到达后各按一次双闪"那两个
        已知时间标记根本没进日志** —— 这一趟能标定成功，靠的是双串口时间戳同源，
        不是双闪标记（`c596351` 自己也把这条记成了设计疏漏）。
        ⇒ **"用双闪当时间标记"这个做法在数据里没有被验证过**，只在脚本注释里写着；
        要真用起来得用扩容后的脚本重跑一趟。
      · **帧序号（`seq` = `data[6]`）**：在 0..255 上滚动，全程**递减 16 次**（回绕）；
        **模 256 递增量累加 = 4146** ⇒ 采集期间总线上大约产生了 **4146** 帧 `0x824`；
      · ★ **所以「17.8 帧/秒」是 CSV 行速率、不是总线帧速率**：`6561 / 4146 = 1.58`
        ⇒ 串口侧把同一帧打印了约 1.58 次。**真正的总线帧速率 ≈ 11.3 帧/秒**。
        （⭐ `1842` 行那句「与总线上的 17.8 帧/秒一致」是**口径混用** —— 18.0 帧/秒
        来自总线全貌表且是**点火怠速**条件，11.3 来自**这一趟**的 `seq` 推进。
        两者都是实测，但**说的不是同一件事**，引用时先分开。
        采集吞吐本身**没问题**：17.8 × 46 字节 ≈ 820 B/s，离 115200 的 11.5 KB/s 差得远。）
      · **CRC**：这份 CSV 是**解帧后**的产物，**不含** `frames` / `fcs_ok` 两列，
        也**没有任何坏帧标记行** ⇒ **从这份文件只能确认「解出来的帧都被收下了」，
        不能独立复核 CRC 通过率**。CRC 100% 的证据在提交信息里（`d512d7c` 的
        `frames=492 fcs_ok=492`、`eba319e` 引 `PINOUT.md` 227 行的 11 秒 492 帧）
        与总线全貌表的 `fcs_ok == frames`，**不在数据文件里**。
      · **车道/转速字段的实测分布**：
        - `spd_count`（`data[2]`）：**0..39**，40 个取值；**0 占 3616/6561 = 55.1%**
          ⇒ 这趟有一半以上时间车是停着的（与 368 s 里前 55 s 与后 110 s 的 `010D` 恒 0 吻合）；
        - `rpm_count`（`data[0..1]`）：5450..42912 ⇒ ÷8 = **681.25 .. 5364.0 rpm**，
          **中位数 904.50 / 均值 1416.10**；
        - ★ **怠速 rpm = 898.5 rpm（<1200 rpm 的 4375 个样本的中位数）**，
          与转速那路的地面真值 **900 rpm** 一致（偏差 **0.17%**）；
        - `rpm_count` **没有一行是 0**（全程 6561 行都 > 0）⇒ 整趟都在点火状态。
      · ⚠ **旧列宽的一处名字陷阱**（复核时差点读错）：`data0` / `data1` **不是**
        `data[0]` 的两种编码，而是**转速的两个字节** —— 逐行验过
        **`data0 × 256 + data1 == rpm_count`，6561/6561 行全部成立**。
        所以旧 CSV 里的 `data0 = 0x1C` 是**转速高字节**，不是命令字节
        （与 `901d67a` 那条"旧 CSV 的 data0 实测是转速高字节"完全一致）。

      **★ 独立复核二：OBD 侧（`drive-2026-09-22-obd.csv`，只读脚本重算）**
      · **文件的解析有个坑**：`raw` 字段里含**换行**（`\r\n` 与**裸 `\r`** 混用），
        所以**物理行数 ≠ 记录数**。文件 **4793 行**（含空行）= 表头 1 + **1198 条逻辑记录**；
        `git --stat` 数的是**物理行**，所以 `901d67a` 那行的 `1199 +` 与 ⑧ 行记的
        "1198 行真值" **两个都对**（一个是物理行、一个是记录数）。**文件本身没有错**
        （`WriteAllLines` 的 `\r\n` 叠加字段内自带的 `\r\n`），但**用普通行计数或
        默认的 `csv` 模块读它都会读歪**（裸 `\r` 会被当记录分隔符）——
        按 `\r\n` 切记录、再按正则取字段才稳。
      · **采到的 PID 只有两个**：**`010D` 和 `010C`**，各 **599 条**，
        **成功 599/599（100%，零失败）** ⇒ 与 ⑧ 行记的「成功 599/599」一致。
        ★ **没有 `0105`（水温）与 `010F`（进气温度）** —— 那两个 PID 是 `901d67a`
        才加进脚本的，而本趟行驶抓包用的是**旧脚本** ⇒
        **"VAN 上有没有进气温度副本"这个问题，本轮数据确实答复不了**
        （与 ⑧ 行记的限制一致，现在从数据里也独立确认了）。
      · **`010D`（车速真值）**：**599 条全有效**，取值 **0..100 km/h**，76 个不同值；
        **0 占 313 条、非零 286 条**（`c596351` 说的"286 个非零配对数"就是这个数）；
        分档分布：`0-9 → 368 条`、`10-19 → 62`、`20-29 → 61`、`30-39 → 19`、`40-49 → 14`、
        `50-59 → 12`、`60-69 → 11`、`70-79 → 13`、`80-89 → 14`、`90-99 → 22`、`100 → 3`。
      · **`010C`（转速真值）**：**599 条全有效**，**714 .. 5268 rpm**，中位数 **905.75**；
        取怠速段（<1200 rpm，385 条）**中位数 898.25 rpm**；
      · ★ **没有断流**：599 个采样点均匀铺满整个 368.2 s（每 10 s 桶 15~17 个），
        全程零失败、零 `NO DATA`/`SEARCHING`。⇒ `9fc0226`/`901d67a` 那两处
        （初始化时机 + 结算判据改成等 `>` 提示符）在**本趟真实行驶数据上是有效的**。
      · ⚠ **每条记录里其实有两个 ECU 应答帧**：`raw` 字段里 `41 0D`（或 `41 0C`）
        出现 **2 次**，**599/599 条都是**；脚本按**第一个** `41 xx` 取值
        ⇒ 真正被采用的那个字节是**较旧**的一帧（两条的子帧值确实常常不同，
        例如 `41 0C 0E 0B` / `41 0C 0E 0B` 与 `41 0C 0E 0E` / `41 0C 0D F8`）。
        **对定标的影响见下**。

      **★★ 独立复核三：车速标度 2.56 的验算（本条的结论：2.56 被数据支持）**
      · **时间戳列的含义与精度（先读脚本再对齐）**：`drive-log.ps1` 的 `t_s` 是
        **同一个 `[Diagnostics.Stopwatch]` 的秒数、3 位小数（ms 分辨率）**；
        两个串口接同一台笔记本 ⇒ **两份文件同源、可直接按 `t_s` 对齐**，
        不需要对墙上时间（`wall_time` 只有 `HH:mm:ss.fff`、**无日期**）。
        **验证**：两份文件的时间戳集合**共享 598 个**，OBD 另有 **1 个**独有
        （`t_s = 366.339`，恰好落在 VAN 那段 1.207 s 的间隔里）——
        **VAN 侧没有任何独有时间戳** ⇒ 同源成立，这个说法是**真的**。
      · ⚠ **但"同一 `t_s`"不等于"同一总线时刻"**：OBD 那一行是在**应答结算时**落盘的
        （收全到 `>` 或超时才结算），所以它对应的总线时刻比 `t_s` **早约 0.2~0.4 s**
        （`010D`/`010C` 实测）；再加上上面那条"取的是两个子帧里**较旧**的那个"，
        又加一层滞后。**这一层偏移会明显影响拟合的 R²，但不影响斜率。**
      · **配对与回归**（x = VAN `spd_count`，y = OBD `010D`）：

        | 配对方式 | n（计数>0 且 km/h>0） | 自由截距斜率 | R² |
        |---|---|---|---|
        | 取该 tick **最后一帧**（最接近 OBD 的真实读时刻） | **284** | **2.5549** + 0.3010 | **0.998385** |
        | 该 tick 的**众数**计数 | 281 | 2.5526 + 0.3062 | 0.994368 |
        | 取该 tick **第一帧** | 280 | 2.5475 + 0.4735 | 0.989125 |

        **过原点**（最后一帧配对）：**y = 2.5679 x**，R² = 0.998340；
        自由截距的**斜率 95% 置信区间 = [2.5429, 2.5669]**（se = 0.0061）。
      · **残差（2.56 那个候选）**：**RMSE 1.175 km/h、MAE ≈ 0.86、max |残差| 3.73 km/h**，
        残差中位数 0.057。VAN 计数本身的量化（±0.5 计数 = ±1.28 km/h）+ OBD 的 1 km/h 量化
        ⇒ **理论上限 σ ≈ 0.79 km/h，实测 1.18 km/h** ⇒ **残差已经和"1 个计数就是一个刻度"
        的量化极限同量级**，没有系统性弯曲。
      · **候选对比（同一批 284 对）**：`1.0` RMSE **27.59** km/h（MAE 21.03、最大 62）
        ⇒ **「1 计数 = 1 km/h」被彻底否掉**；`2.5` RMSE 2.49；`2.55` RMSE 1.55；
        **`2.56` RMSE 1.20**；`2.571` RMSE 1.20；`2.6` RMSE 1.59。
      · **分档均值单调**（独立于回归）：按 OBD km/h 分档取 VAN 计数均值 ——
        `0-9 → 2.12`、`20-29 → 9.05`、`40-49 → 16.57`、`60-69 → 24.82`、
        `80-89 → 32.64`、`100 → 38.67` ⇒ 干净的线性编码。
        （与 `c596351` 里那张表的差：`0-9` 我算 2.12 / 它记 1.93，
        `100+` 我算 38.67 / 它记 38.33 —— **差在分档口径与配对方式**，
        但**单调性与量级完全一致**，不影响结论。）
      · **覆盖**：计数 **1..39**（最大 39）、真值 **0..100 km/h**（最大 100）；
        计数 = 39 那 4 个点上 OBD 真值是 **98 与 100 km/h** —— 即 39 × 2.56 = 99.84 的
        **两侧都有**，**没有**出现"计数钉在 39 而真值继续涨"的平台
        ⇒ **至少到 100 km/h 为止没有饱和迹象**（130 km/h 以上仍**未验**）。
      · **⇒ 结论：2.56 是被本数据支持的**，可以放行。**但要说清它是"取整"而不是"分辨"**：
        数据能分辨的只是【标度 ≈ **2.55~2.57**】这个窄带（自由截距 2.5549、
        过原点 2.5679）；**2.56 落在两者之间，是一个方便且统计上无可指责的取整**，
        2.56 与 2.571 的 RMSE 差 **0.0004 km/h，完全不可分辨**。
        ⇒ **不必改代码**；但**别把"2.56 是精确值"写成结论**。
      · ★ **一个值得记下的巧合（`推断（待确认）`，未证实）**：**2.56 = 256/100**。
        若底层是「8 位字段、每 **0.64** km/h 一个刻度」，标度 2.56 会自然浮现 ——
        这也顺带解释了为什么**取整成 2.56 反而 RMSE 最小**。
        但**它与"标度 2.56 的真实来源是回归拟合"并不矛盾**：
        `c596351` 给的是拟合值（自由截距 2.5552 / 过原点 2.571），
        **"256/100"是事后才注意到的**（`van_source.cpp` 与提交信息里都没写这条理由）。
        **确认方式二选一**：① 找一段**极慢速**（1~3 km/h）与 OBD 逐点对照，
        看最小可分辨步长是不是 0.64；② 跑到 130 km/h 以上看饱和点在哪。
      · ★ **交叉复核（另一条独立通道）**：拿**转速**做同样的对齐 ——
        两份文件共享的 598 个时间戳上，VAN `data[0..1] × 0.125` 与 OBD `010C`
        即便**不做任何偏移校正**，绝对差**中位数只有 12.5 rpm**；
        只取"OBD 车速为 0（车停、转速稳定）"的 3601 个样本，
        **中位数差 5.75 rpm，两边怠速中位数完全相等（都是 898.75 rpm）**
        ⇒ **`kRpmScale = 0.125` 又被一份独立数据支持**。
        ⚠ 但这份数据的**时间错位**要记下来：转速那路的最优对齐在 **+0.8 s**
        （0.8 s 处 R² = 0.9989、中位差 7.0 rpm；0.0 s 处 R² = 0.9574、中位差 16.0 rpm），
        **比蓝牙答询+结算的 0.2~0.4 s 还大** —— 说明"同一个 `t_s`"在两路上对应的
        总线时刻并不严格一致（可能一路的打印队列在排队）。
        **转速变化快，所以它把这个错位放大了**；车速变化慢，所以掩盖住了。
        ⇒ 本条的标度结论**不受影响**（斜率对配对方式稳在 2.548~2.555），
        但**任何用这两份文件做精细时序分析的尝试，都必须先处理这层错位**。

      **独立复核四：与提交信息/既有文档的对账**
      · **对得上的**：`010D`/`010C` 各 599 条、成功 599/599、非零 286 条、
        VAN 6561 行 / 368.2 s、`spd_count` 最大 39、`010D` 最大 100 km/h、
        两份同源可对齐、旧列宽验不了进气温度 —— **逐条一致**；
      · **对不上的（三条，都已在上文写清，**未擅自改代码或数据**）**：
        1. 🔴 **`ARCHITECTURE.md` 里 4 处仍写 `kSpeedScale = 1.0` / "绝对刻度未定标"**，
           而 `c596351` 已把代码改成 **2.56** ⇒ **文档与代码互相矛盾**。
           落点：`ARCHITECTURE.md` **83 行**、**504 行**、**507 行**、**596 行**
           （`d89b0df` 抄进去的是当时正确的旧值，**同日晚上被 `c596351` 推翻但没回改**）；
           `README.md` **22 行 / 89 行 / 99 行**同理（"1 计数是否恰好 1 km/h 没有地面真值"
           + "按表盘复核刻度"那套口径**已经作废**，且 DualEye 已退货 ⇒ 表盘那条路也走不通了）。
           ⚠ **`ACCEPTANCE.md` 1641 行那一条（16:40 落的）里 `kSpeedScale = 1.0` 同样是
           当时的真值** —— 它是**带日期的历史记录、不是错**，**不要删**；
           但**这一条（新汇总）就是它的后继**，两处一起读才不会误用。
        2. ⚠ **`lib/dashcore/van_source.h` 的注释块 27~32 行现在是残句、前后矛盾**
           （`c596351` 改这段时只替换了其中一行，把原句切断了）：
           28 行说"**已定（2026-09-22）：1 计数 = 2.56 km/h**"，
           紧接着 29~32 行却是旧口径"**先按 1.0 上屏、用表盘脸的档位复核
           （30/65/95/130）**"；而且 29 行是个**孤句**（`"车速"列全是 0 —— 车没动，
           见 ACCEPTANCE.md`），它开头那个左括号**永远没有对应的右括号**
           （原句是"……没有地面真值（BT 蓝牙日志那两份的『车速』列全是 0 —— 车没动，
           见 ACCEPTANCE.md）"，被 `c596351` 的替换从中间切开，只剩后半截）。
           ⇒ 这是**注释**问题（**不影响编译、也不影响运行**），但**会误导下一个读它的人**；
           按 `d89b0df`/`b59f372` 的先例，注释口径本来也是要跟着改准的。
        3. ⚠ **`1842` 行「17.8 帧/秒」是口径混用**（行/秒 vs 帧/秒），见复核一。
      · **撤回的两条如实保留**：复核确认它们都在文件里、且都写清了"撤回的是什么、
        为什么撤、怎么继续验" —— `ACCEPTANCE.md`
        **1312~1313 行**（「左右门能不能分辨 = 未解」＋"本节早先写的「单一状态位」已被
        第四轮实测推翻，别沿用"）、**1331~1333 行**（`8A4.data[6]` 那条）＋
        **1340~1347 行**（三种可能 + 下一步待 owner 确认那块屏）。
        **两条都在这里，没有被悄悄抹掉。** ⭐ 并且 **`f839a7d` 里还有第三条自我更正**
        （VIN 不需要与 `0x5E4` 拼、`0xE24` 自己就是完整 17 字节，见 `1359~1368` 行），
        **同样保留**。
      · **数据文件的完整性**：两份 CSV 都**没有** `#VAN_STALL_FROM` 之类的标记行，
        `t_s` 也没有跳变 ⇒ 采集期间 **VAN 没有失联、OBD 没有断流**。

      **★ 复核用的一次性只读脚本**（跑在仓库外，**未入库、跑完即弃**）：
      直接按 `t_s` 对齐两份 CSV、算 IDEN/CRC/分布/三档配对回归/滞后扫描。
      结论全部可由本文件与两份 CSV 复算。

      **★ 本轮新入档的协议参考**：`VAN-PROTOCOL.md`（仓库根目录）——
      把散在上面各条里的**协议结论**收成一份独立参考（物理层/位层、帧目录表、
      已解字段明细、实测定案"不在这条总线上"的、未解清单**含两条撤回**、复现方法、
      与实现的关系）。**它是参考、不是日志**；「谁哪天做了什么」仍以本文件为准。
      本文里凡是引用"VAN 协议"的地方**都可以指向它**。

      **★ 独立复核没有做的事（划清边界，别把本条读过头）**
      · **没有复核 CRC 通过率**（两份 CSV 都不含 `frames`/`fcs_ok`）——
        这方面本条只转述提交信息与 `PINOUT.md`，**不是独立实测**；
      · **没有复核总线全貌表（12 族/速率/占比）** —— 那份数据（逐帧打印的原始日志）
        **不在仓库里**，只以表格形式记在 `1374~1390` 行。本条的 12 族数字全部引自它，
        **没有独立数据可验**；
      · **没有复核 `0x984` / `0x9C4` 那个不一致**（§6.2 第 7 条），也没复核
        `VAN-PROTOCOL.md` 里各族的字节长度（同上，原始日志不在仓库）。
        上面「帧长公式逐族吻合」这套话**引自 `van_wire.h` 文件头**，本轮**未复算**。

      **★ 本条更正（2026-09-22 深夜，owner 指示"现在就改"）**
      上面「独立复核四」列的三条不一致里，第 1、2、3 条**都已在同日修掉**
      （只改文档文字与注释文字，**逻辑 / 配置值 / 常量 / 测试 / 数据一律未动**）：
      · `ARCHITECTURE.md` **83~84 / 504 / 507 / 596 行** —— 4 处 `kSpeedScale = 1.0` /
        "绝对刻度未定标"改成实测定标口径（`kSpeedScale = 2.56`，取自蓝牙 ELM327 `010D`
        回归，数据可分辨范围 **2.55~2.57**、**2.56 是取整**）；
      · `README.md` **22 / 99 行** —— 那条"绝对刻度还没有 / 1 计数是否恰好 1 km/h 没有
        地面真值 / 先按 1.0 上屏看表盘"的**现状断言**改成定标后的口径，并把「下一步」里
        **已作废**的"上车后用表盘复核车速刻度"划掉、改成指向真值源
        （`drive-log.ps1 -ObdPort` 采 `010D` + `VAN-PROTOCOL.md` 的「车速」一节）；
        并在「最近进展」补一条 2026-09-22 的**定标**记录（新条目在上、2026-09-20 那条
        历史记录保留不动）；
      · `lib/dashcore/van_source.h` 的注释块 —— 28~32 行那个**残句/前后矛盾**（28 行说
        "已定 2.56"、29~32 行却还是"先按 1.0 上屏看表盘"、且 29 行左括号没闭合）
        重写成**自洽**的一段：说清 2.56 的实测定标来源（`010D` 回归、599/599、
        284 对非零、R²=0.9984）、**数据只能分辨 2.55~2.57 ⇒ 2.56 是取整**、
        并给出原始数据与文档落点；旧口径归到 `☆ 历史(已被 2026-09-22 实测取代)`
        一行里。**`kSpeedScale = 2.56f` 这个常量本身、默认值、逻辑一行未动。**
      · 上面列的第 3 条（本条 `1842` 行的"17.8 帧/秒"口径混用）也已按"行速率 17.82/s
        vs 总线帧速率 ≈11.3/s（由 `seq` 递增量 4146 算出）是两个口径"改准，
        并写明是旧列宽脚本 **1.58×** 重复打印造成的。
      · ⚠ 按 owner 要求**没有动**带日期的历史条目：本条 `1641` 行那一条里的
        `kSpeedScale = 1.0`（16:40 落的）、`1212` / `1416` / `1473` 等 2026-09-20 的
        历史条目**全部原样保留** —— 它们是**记录**，不是现状断言；**本汇总条目就是它们的后继**。
      · ⚠ 同样**没有动**代码/工具里的历史叙述与过时注释（不属于"文档文字"）：
        `tools/van-decode/decode_frames.py` 的 47 / 315 / 326~327 行、
        `tools/serial-capture/drive-log.ps1` 的 5 行、`test_van_real_capture.cpp` 的 11 行
        —— 这些在工具/测试源码里，**改它们就超出"只动文档与注释文字"的边界**，留待需要时另议。
        （其中 `decode_frames.py` 那三处仍在说"绝对刻度按 1 计数 = 1 km/h 上屏"，
        是**已知的过时说明**，只影响打印出的提示文字、不影响它算出的原始计数。）

- [x] **★ 进气温度/水温在 VAN 上有没有副本 —— 本轮筛查:现有数据**结构性地**答不了(不是没翻到),并给出车上一次能真正回答它的采集方案（2026-09-23）**
      **本条只做两件事**:①把"答不了"用数字钉死;②给出可照做的下一步。
      **没有改任何代码/测试/配置/数据** —— 只读脚本写在 `%TEMP%`、跑完即弃,**唯一的仓库改动就是本条目**。
      核对时 **HEAD = `458a533`**。**与 2026-09-22 汇总条目不重复**:那条复核的是"车速标度/采集质量",
      本条只回答"温度副本",并把"12 族"的原始日志去向一并查清。

      **① `drive-2026-09-22-van.csv` 能答什么(只读重算,数字全部来自文件本身)**
      · 258,159 字节 / 无 BOM / **6,562 物理行 = 表头 1 + 数据 6,561** / 每行**恰好 7 列**;
      · 列名 = `t_s, wall_time, spd_count, data0, data1, rpm_count, seq` —— **就这 7 列**:

        | 列 | 非空 | 去重基数 | 最小 | 最大 |
        |---|---|---|---|---|
        | `t_s` | 6561 | **598** | 0.002 | 368.209（跨度 **368.207 s**） |
        | `wall_time` | 6561 | 2592 | 23:21:51.467 | 23:27:59.652（**无日期**） |
        | `spd_count` | 6561 | **40** | 0 | 39（**0 占 3616/6561 = 55.1%**） |
        | `data0` | 6561 | **145** | 21 = `0x15` | 167 = `0xA7` |
        | `data1` | 6561 | **128** | 0 = `0x00` | 254 = `0xFE` |
        | `rpm_count` | 6561 | **2762** | 5450（= 681.25 rpm） | 42912（= 5364.0 rpm，**全程无 0**） |
        | `seq` | 6561 | **256** | 0 | 255 |

      · ★ **`data0 × 256 + data1 == rpm_count` 逐行成立 6561/6561,零反例** ⇒ `data0`/`data1`
        **就是 `data[0]`/`data[1]`(转速的两个字节)**,不是"命令字节";旧列宽的名字陷阱照旧成立;
      · `seq`(= `data[6]`):模 256 递增量累加 **4146**,其中 **4523 行增量与上一行相等**(重复打印)、
        2037 行有增量、**递减 16 次**(回绕)⇒ `6561/4146 = 1.58` 倍重复、总线帧速率 ≈ **11.3 帧/秒**
        —— 与 2026-09-22 汇总条目**逐项一致**(本次是独立复算,不是转抄);
      · ★★ **结构性结论(本条最硬的一条)**:这 7 列 **与 `0x824` / CMD `0x8` 的载荷一一对应** ——
        `data[0..1]` → `data0`/`data1`、`data[2]` → `spd_count`、`data[6]` → `seq`。
        文件里**没有 `iden`、没有 `len`、没有 `cmd`/`ack`、也没有 `data[3]`/`data[4]`/`data[5]`**
        ⇒ **一个能写"别的 IDEN"的列都不存在**。
        ⇒ **该文件结构上不含除 `0x824` 以外的任何 IDEN**。这不是"翻了没找到",而是
        **"表结构装不下"** —— 温度字段在这份文件里**没有被记录过**的任何可能。
      · 同一趟的 `drive-2026-09-22-obd.csv`:1,198 条逻辑记录 = **`010D`/`010C` 各 599 条**,
        **`0105`(水温)/`010F`(进气)出现 0 次** ⇒ **连温度真值也没有**。
        ⇒ 两条合起来:**这一趟既没有"别的 IDEN",也没有"温度在变"的真值** —— 答不了,而且是结构性地答不了。

      **② 全库其它 VAN 数据:清点结果(含"有没有温度变化时段"的判断)**

      | 文件 | 是什么 | 帧数 / 覆盖时长 | 出现的 IDEN | 有温度变化时段? |
      |---|---|---|---|---|
      | `tools/serial-capture/drive-2026-09-22-van.csv` | 解帧后 CSV | 6,561 行 / 368.2 s | **只有 `0x824`**（结构性，见①） | **没有**（且无 `data[3..5]`） |
      | `tools/serial-capture/drive-2026-09-22-obd.csv` | OBD 真值 CSV | 1,198 条 / 368.2 s | PID 只有 `010D`/`010C` | **没有**（`0105`/`010F` 各 0 条） |
      | `tools/van-decode/sample-speed-824.csv` | 逻辑分析仪**原始边沿** | **33 帧** / 帧覆盖 **0.1485 s**（时间列跨度 0.59 s） | **12 族**：`824=12 464=6 4FC=3 664=3 4DC(C)=2 E24=1 4D4=1 9C4=1 5E4=1 8A4=1 4DC(8)=1 524=1` | **没有**（0.15 s） |
      | `tools/van-decode/sample-diffmanchester.csv` | 原始采样（8 通道，只有 ch0 在跳） | **68 帧**（67 帧 SOF 命中）/ **0.2975 s**（时间列跨度 1.19 s） | **12 族**：`824=24 464=12 664=6 4FC=6 4DC(C)=3 9C4=3 4DC(8)=3 4EC=2 8A4=2 5E4=2 524=2 4D4=1 E24=1` | **没有**（0.30 s） |
      | `tools/serial-capture/sample-log.txt` | **测试夹具**（与 `capture.py` 的 `_SAMPLE` 同内容，11 行） | 5 好帧 + 1 坏帧 / 无时间 | `824`×3 / `8C4`×1 / `8A1`×1 | **没有**（不是抓包） |

      · ⇒ **仓库里没有任何一段数据包含"温度发生变化"的时间段**。两份切片都是**位级原始边沿**、
        覆盖只有 **0.15 s / 0.30 s** —— 这个时间尺度上**任何温度都不可能有可观测变化**。
        所以**不硬凑结论**:它们既证明不了、也反驳不了"VAN 上有温度副本"。
      · ★ **副产品(对 2026-09-22 汇总条目"没有复核总线全貌表"的细化)**:这两份切片的 IDEN
        **种类集合一致 = 12 种**,且与 `5f43d1d` 那份 30 秒观测的 12 族**逐族相同**
        ⇒ **"总线上有这 12 族"这件事在仓库里有独立旁证**;但**各族的速率/占比**仍然算不出来。
        ⚠ 因此:全貌表的**原始逐帧日志(110 s 那份)不在仓库,只有汇总表**
        (`ACCEPTANCE.md` `1370~1390` 行 + `VAN-PROTOCOL.md` §3);
        `git status --ignored` 也没有任何被忽略的日志(只有 `.pio/`、`.vscode/*`、`__pycache__/`)。
      · 仓库外也顺手查了:`.dsh-drop/<会话>/` 有 3 个 `van_capture_1.txt`(2026-09-19):
        **VAN 帧行 0 条**、全是 `speed=sim rpm=sim coolant=sim intake=sim` 且 `SRC-Hz … 0.0`
        ⇒ 那是**桌面空跑**记录,**不可用**。

      **③ 参照物:VAN 上**没有**已知水温字段 —— 这一条要更正提问的前提**
      · `lib/dashcore/van_source.h`:`VanSource` **只**取两路 —— 车速(`kSpeedIden = 0x824`、
        `kSpeedOffset = 2`、`kSpeedScale = 2.56f`)与转速(`kRpmOffset = 0`、`kRpmScale = 0.125f`);
        **没有任何温度常量、没有温度成员**;`van_source.cpp` 的 `onPacket()` 也只写 `speed_kmh_`/`rpm_`。
      · `VAN-PROTOCOL.md` §3 的帧目录表(12 族)**没有解出任何一个温度字节**;
        §6.2 第 8 条原文:「**VAN 上有没有进气温度副本 —— 本轮仍然没验(不是「没有」)**」。
      · `lib/dashcore/data_service.h` 41 行:「**水温/进气温度只有 Obd > Sim**(这两项 VAN 帧里没有,
        没有第二来源)」;`data_service.cpp` 52 行同口径;`ARCHITECTURE.md` 505 行:「水温/进气温度
        **只有 OBD 有**（VAN 上没有这两项）」;`ACCEPTANCE.md` 1779 行:「VAN 上目前没有任何
        已解出的进气温度字段」。(⚠ `ARCHITECTURE.md` 47 行那句历史优先级 `转速/水温 Obd > Van > Sim`
        是**过时口径**,`ACCEPTANCE.md` 785~786 行已点名)
      · ⚠ 唯一像温度的候选 **`8A4.data[6]`** 是**已撤回的假设**(`VAN-PROTOCOL.md` §6.1 撤回②):
        四段实测**全是 `0x92`**,只被描述为"慢变量(随温度/时间漂移)",**从未确认是温度**
        —— **不许把它当"已知水温字段"用**。
      · ⇒ **VAN 上没有已知的水温字段(也没有已知的进气温度字段)**,
        所以"拿 VAN 上已知水温当'温度确实在变'的参照"这条**在现有知识下不存在**。
      · ⇒ **但参照物是有的,在 OBD 侧**:`0105`(水温)、`010F`(进气),`℃ = A − 40`,
        而 `drive-log.ps1` **本来就同时采这两个**,且 VAN 侧与 OBD 侧**同一个 `Stopwatch` 时基**
        —— 2026-09-22 把车速标度定成 2.56 用的就是这套(599/599、284 对、R²=0.9984)。
        **下一步就用它当参照**,不需要 VAN 上有温度字段。

      **④ 车上确切的下一步(可照做)**

      **(1) 固件 —— 烧 `[env:esp32s3]`;而且"全量 IDEN 输出"不需要新固件**
      · ★ **更正一处口径**:"全量 IDEN 输出"有**两个**东西,别混:
        ① **逐帧全 IDEN 打印**(`VAN %03X <data...>   # cmd=.. ack=..`)在 `VanLogSink` 里
           (`src/main.cpp` 170~190,172 行的注释就是「先全打」),**默认构建就有、不受任何宏控制**;
        ② `5f43d1d` 改的是 **`van_sniff_report()` 那份按 IDEN 计数的汇总**(`sniff: 共 N 帧 / M 种 IDEN`),
           它在 `#if VAN_SNIFF` 里(`src/main.cpp` 101~163)⇒ **只有 `[env:esp32s3-vansniff]` 才有**。
      · ⇒ **要抓"所有 IDEN 的整帧",烧 `[env:esp32s3]` 就够了,不必重编**(`5f43d1d` 也已在 HEAD 里)。
      · ⚠ **别烧 `[env:esp32s3-vansniff]`**:实测把 VAN 压到 **0.75 条/秒**(安静版 4.4 条/秒),那是排障用的;
        ⚠ **别烧 `[env:esp32s3-vaninv]`**:`eba319e` 已更正 —— **线早就对调过了**,再翻一次 = **反两次**(帧数 ≈ 0)。
      · 判据:开机那行 `van phy:` **不该**出现"已翻转"。当时的自证尺寸是 **740,352 字节 = 无宏**、
        `vaninv` 是 **740,368 字节**;重编后以 `van phy:` 那行为准。
      · 命令:`python -m platformio run -e esp32s3 -t upload --upload-port COM3`
        (端口按实际改;`[env:esp32s3]` 带 `-DVAN_PHY_GPIO=1`,是真收帧的构建。)

      **(2) 怎么跑:冷车 → 热车 → 暖风 → 空调,每段 1~2 分钟,记下起止时刻**
      · 用手机秒表(或记 `HH:mm:ss`)记**每段的起止时刻**;全程**不熄火**,变量一次只动一个;
      · 建议阶段表(总长约 8~10 分钟):

        | 段 | 做什么 | 时长 | 期望(物理) |
        |---|---|---|---|
        | ① 冷车 | 冷启动后怠速（**点火前先读一次 OBD `0105` 并记数**） | 1~2 min | 水温 = 环境温度（≈20 ℃），`010F` 也低 |
        | ② 热车 | 继续怠速到水温稳定 | 1~2 min | 水温升到 **88~92 ℃**（2026-09-22 实测真值） |
        | ③ 暖风 | **暖风最大 + 风量最大**（外循环） | 1~2 min | **进气温度升**（机舱热浸）；水温可能**略降**（热量被抽走） |
        | ④ 空调 | **A/C 最大制冷**（可外循环） | 1~2 min | **进气温度降** |
        | ⑤ 复现 | 再回到 ③（暖风）做一次 | 1~2 min | 与 ③ 同向 —— **第二轮才好排除巧合** |

        ⇒ **③④⑤ 是本题的关键**:它在"温度确实在变"的同时给了**两个相反方向**的激励。

      **(3) 抓取命令**
      · **主抓(推荐 —— 唯一能同时给出温度真值的)** 双路、同一时基、VAN 侧收**全部 IDEN**:

        ```powershell
        # 双路:VAN(板子 CH340 那个 UART 口,通常 COM3)+ 蓝牙 ELM327(配对后实测是 COM6)
        powershell -ExecutionPolicy Bypass -File tools\serial-capture\drive-log.ps1 `
            -VanPort COM3 -ObdPort COM6 -Minutes 12
        # 产出:tools\serial-capture\van_drive.csv  (列: t_s,wall_time,iden,len,cmd,ack,d0..d6,spd,rpm,seq)
        #       tools\serial-capture\obd_drive.csv  (pid=0105/010F 的 value 就是 ℃ 真值)
        ```
        · ⚠ 它的头注释写"只记 `0x824` 与 `0x4FC`",但**实际过滤是 `^VAN ([0-9A-F]{3}) …`**
          (`drive-log.ps1` 289 行)⇒ **所有三位 IDEN 都会被收下**,这正是我们要的;
        · ⚠ 它的 `d0..d6` **只落 7 个字节** ⇒ `524`(9 B)、`E24`(17 B)、`564`(27 B) 会被**截断**
          (`len` 列能看出来)。对 `824`(7B)/`464`(5B)/`4FC`(4B)/`4DC`(4B)/`664`(4B)/`9C4`(2B)/`5E4`(2B)/`8A4`(7B)
          **够用**;

        > ★ **本条更正（2026-09-23，只加注、不改上面原文）**：上一行括号里那几个字节数是**错的**，
        > 是照抄 `VAN-PROTOCOL.md` §3 表当时**没有出处的 `长度 n` 列**，而该列有 4 行与本文
        > 1015 行的槽级实测矛盾。**实测值**（后面已按它重算）：`4FC` = **11 B**、`4DC` = **7 B**、
        > `664` = **11 B**、`524` = **14 B**；`824`(7)/`464`(5)/`E24`(17)/`9C4`(2)/`5E4`(2)/`8A4`(7) 原值即实测值。
        > ⇒ 因此**被 `d0..d6` 截断的族是 `4FC`/`664`（11 B）、`524`（14 B）、`E24`（17 B）、`564`（27 B）**；
        > `9C4`/`5E4` 只有 2 B，**根本不会截断**。逐行结论与证据见 `VAN-PROTOCOL.md` §3.0。
        · OBD 侧 4 个 PID 轮询 ⇒ 每个温度 PID 约 **0.5~0.9 Hz** ⇒ 每段 90 秒有 **~50~80 个真值样本**,够用。
      · **加抓(题面点名的脚本;拿不截断的整帧原文)** —— 在**同一趟之后**再跑一遍(同一个口,不能同时开):

        ```powershell
        # ★ 不要加 -UntilFrames:它一凑够 N 个好帧就提前收工,会把后面的阶段整段截掉。
        # ★ 按 -Seconds 跑满(VAN 输出 ≈54 帧/秒 ⇒ 12 分钟约 4 万行、文件 ~2MB)。
        powershell -ExecutionPolicy Bypass -File tools\serial-capture\capture-van-nopy.ps1 `
            -Port COM3 -Seconds 720 -Out tools\serial-capture\drive-2026-09-23-van-raw.txt
        # 产出:上面那个 txt(逐行原文:整帧、坏帧、van: 诊断、SRC),收尾打一张按 IDEN 的好/坏帧小结
        ```
        · 这一步的价值是**载荷不截断 + 人可读 + 可直接回放**(格式与 `van_replay` 一致);
          代价是**没有 OBD 真值、每行没有时间戳** ⇒ 只能靠阶段墙钟时刻对齐,**判断力明显弱于主抓**。
        · ⇒ **两样都做最稳;只做一样就做主抓。**

      **(4) 抓回来怎么判 —— 判断标准(先过入场券,再看候选,最后看重复)**
      · **(a) 入场券:这趟到底有没有"温度在变"的窗口**
        `obd_drive.csv` 里 `pid=0105` 的序列必须**从冷车到热车变化 ≈ +60~70 ℃**
        (冷车 = 环境温度,热车 = 88~92 ℃,见 2026-09-22 实测)。
        **若全程变化 < 10 ℃ ⇒ 这趟无效**(没热起来 / 采样错 / PID 没应答),
        **判"无效重跑",不是"VAN 上没有副本"** —— 这两件事不能混。
      · **(b) 对齐**:`0105`/`010F` 按 `t_s` 取出、5 点中位数平滑;再把 VAN 每个
        `(IDEN, 字节下标)` 的序列按 `t_s` 对齐(两份文件**同源时基**,不用对墙上时间)。
      · **(c) 先做减法**
        1. **丢常量**:整趟取值 ≤ 2 种的,丢;
        2. **丢计数器**:全程**单调不减**的(含 `824.data[4..5]` 里程、`4DC.data[4]` 那类),丢
           —— 这是上次 `4DC.data[4]` **伪命中**的教训(`VAN-PROTOCOL.md` §6.1 撤回①);
        3. **丢开关**:只有两三个取值、且**跳变与阶段边界无关**的位域,丢。
      · **(d) 主判据:段间均值位移(比相关系数更可信)**
        对每个候选字节算 `Δ = (mean(段④) − mean(段③)) / sd_pooled(段③,④)`;
        **要求 |Δ| ≥ 3,且方向符合物理**:
        · 暖风(③)⇒ 进气温度**升** ⇒ 候选**升**;空调(④)⇒ 进气温度**降** ⇒ 候选**降**;
        · 若是水温副本,则应与 `0105` **同向**(③ 段可能略降)。
      · **(e) 辅判据:与参照的相关 / 滞后**
        同一 `t_s` 上算 `r(候选, 010F)`(找进气副本)或 `r(候选, 0105)`(找水温副本),
        并在 **0~60 s** 内扫滞后取最大 |r| —— 温度是**慢变量、必然滞后**,
        **最优滞后为 0 且 r 极高的快变字节要怀疑**(更像计数器或其派生量);
        物理上合理的滞后是**几秒到几十秒**。要求 **|r| ≥ 0.8**。
        ⚠ **但相关系数不能单独定案**:段数只有 5 段、序列强自相关 ⇒ 有效样本远小于名义样本数,
        `r` 容易虚高。**所以 (d) 是主判据,(e) 只作交叉印证。**
      · **(f) 标度自洽(强证据)**:若候选是干净的仿射编码,试着用**同一个** `y = a·x + b`
        贯穿全程拟合 `010F`(或 `0105`),看残差是否 ≈ **1~2 ℃**(典型编码 `℃ = A − 40`)。
        **能给出小残差的固定 a、b ⇒ 基本可以定案**;拟合不出稳定 a、b 的,别硬套。
      · **(g) 必须重复**:把 ③(暖风)在 ⑤ 再做一遍,**两轮都命中且同向**才算。
        ⚠ 候选空间有 **12 族 × 最多 7 字节 ≈ 80 个 (IDEN, 字节)**,只命中一次、或只有单段命中的,
        按**巧合**处理。
      · **⇒ 什么情况下判"未解"(不许给弱结论)**
        1. (a) 不通过 ⇒ 判**「本轮无效」**(不是"没有副本");
        2. **没有任何字节同时通过 (d) 的方向 + 3σ**(③升 与 ④降 都要)⇒ 判**「本轮仍未解」**
           —— 这**不等于"没有"**,只说明**在这段总线的这些字节里、用这个实验没看出来**;
        3. 命中者**同时**是全程单调的 ⇒ 判**计数器/累计量**,不是温度;
        4. 段间位移落在噪声里(|\Delta| < 3)、或只有零星一两个字节勉强过线 ⇒ **判噪声,不要写进结论**;
        5. 最优滞后在两次重复里**乱跳**(第一轮 5 s、第二轮 40 s)⇒ 判**巧合**;
        6. 只有单段(只暖风或只空调)命中 ⇒ **判未解**,补做另一段再谈。
        ⇒ 落到「仍未解」时,就照 §6.2 第 8 条那种口径写成**未解**,
          **不要**写成"VAN 上没有温度副本" —— 后者要**穷举所有族并逐个排除**,本轮做不到。

      **⑤ 本条没做的事(划清边界)**
      · **没有**改任何代码/测试/配置/数据;只读脚本在 `%TEMP%`、跑完删掉;**仓库改动只有本条目**;
      · **没有**复核「总线全貌表」的**速率/占比**(原始逐帧日志不在仓库,见 ②);
      · **没有**去车上验证任何字节 —— (4) 是**方案**,不是结论;
      · **没有**把 `8A4.data[6]` 当温度字段(它是撤回过的假设)。

---

- [x] **★ VAN 帧目录表的「长度 n」与 `data[i]` 下标对齐（2026-09-23；改掉 4 行自相矛盾的 `n`，16 族逐个有槽级实测）**
      **起因**：对外评审指出 `VAN-PROTOCOL.md` 的**帧长 `n` 与已解字节下标打架** ——
      表里标了 `n`，同一行却引用 `data[i]` 而 `i ≥ n`（评审点名 `4FC` 与 `524`）。
      **核对时 HEAD = `e08b47e`**。**只改文档**：`VAN-PROTOCOL.md` + 本条目（含 2026-09-23 那条的加注），
      代码 / 测试 / 配置 / 数据文件 / `platformio.ini` **一行未动**。

      **① 先把"权威口径"定下来（不是先改表）**
      · 判定顺序：**先信实测槽数** `n = (槽数 − 50 − ack×2) / 10`（帧长公式，`van_wire.h` 文件头），
        再看被引用的最大下标（字号段在专门实验的操作窗口内动过 ⇒ 那个字节必然存在）;
      · 下标的权威在**代码**里，三处一致（本轮逐个读过）：
        `van_source.h` 的 `struct VanPacket{ uint8_t data[28]; uint8_t len; }`（CMD 是单独的 `cmd` 成员，
        **不在 `data[0]`**）；`van_wire.cpp` 的 `frameToPacket()` 是 `for (i = 0; i < n; ++i) out->data[i] = f.data[i]`
        （**左闭右开**）；`van_source.cpp` 的 `onPacket()` 读 `data[2]`/`data[0..1]` 前先判 `... < pkt.len`；
        `src/main.cpp` 的 `VanLogSink` 打出 `VAN %03X <data[0]> … <data[len-1]>`（`for (i = 0; i < pkt.len; ++i)`）。
      · ⇒ **约定写死一行**：`data[0]` = IDEN/CMD 之后的第 1 个数据字节，`n` = 数据字节数，
        **合法下标恰好 `0..n-1`**，与 `VanPacket::data` 是**同一套下标、同一个字节**（已写进 `VAN-PROTOCOL.md` §2.8）。

      **② 16 族逐个测 `n`（两条独立口径，逐族一致）**
      · **口径 A**：本文 1015 行那条 2026-09-19 帧布局定案（已有、slot 级）——
        `4FC/0xC=162 槽(11)`、`524/0x8=190(14)`、`E24/0x8=220(17)`、`564/0xE=322(27)`、`984/0x8=100(5)`……
      · **口径 B**：本轮用仓库现成工具**重跑同一份抓包**（`tools/van-decode/decode_frames.py` 的
        `load_frames`/`frame_to_slots`/`split_sof`/`n15_of`/`n5_of`；输入 `C:\Users\Public\206dash\drive5min.csv`）
        —— **17106 帧、SOF 命中 17106/17106**，16 族槽数**各自只有一个取值**：
        `824/8 5978帧 120槽`、`464/C 3001 102`、`4FC/C 1506 162`、`664/C 1479 162`、`4DC/C 747 122`、
        `4DC/8 747 120`、`524/8 614 190`、`8A4/8 607 120`、`5E4/C 602 72`、`9C4/C 597 72`、`E24/8 299 220`、
        `4EC/F 298 50`、`4D4/F 296 50`、`564/E 168 322`、`8C4/C 162 72`、`984/8 5 100`。
        ⇒ `n` = `7,5,11,11,7,7,14,7,2,2,17,0,0,27,2,5`（按上式反解，与 ACK 列 16/16 自洽）。
      · 再加**仓库里两份切片**复算：`sample-diffmanchester.csv`（68 帧）与 `sample-speed-824.csv`（33 帧），
        `4FC` 一律 162 槽、`524` 一律 190 槽、`E24` 一律 220 槽 ⇒ **同一族跨三份独立抓包零分歧**。
        ⚠ 切片里 `4FC` 有 1 帧 **149 槽**（不是 10 的倍数）= **切片首尾被切掉的半帧**，
        不能拿它反推长度；判据是"同一族内槽数是否为**单值**"。

      **③ 逐行结论：4 行原本打架（全部改 `n`、不改下标）；另外 12 行原值即实测值**
      | 行 | 原 `n` | 改为 | 打架在哪 / 依据 |
      |---|---|---|---|
      | `4FC` | 4 | **11** | 同一行引用 `data[5]`（需 `n ≥ 6`）而标 `n=4` ⇒ 162 槽 − 50 − 2 = 110 ⇒ 11 |
      | `4DC` | 4 | **7** | `data[4]` 有 208 种取值（需 `n ≥ 5`）⇒ 122/120 槽（有/无 ACK）⇒ 7 |
      | `664` | 4 | **11** | 未解族、无下标引用，但长度本身与 162 槽不符 ⇒ 11 |
      | `524` | 9 | **14** | 190 槽 − 50 = 140 ⇒ 14（引用的 `data[9]` 只是**不再越界**：`9 < 14`） |
      · **没改的 12 行**：`824=7`、`464=5`、`8A4=7`、`9C4=2`、`5E4=2`、`E24=17`、`4EC=0`、`4D4=0`、
        `564=27`、`8C4=2`、`984=5`（11 个数字 12 行，`4DC` 有 `0xC`/`0x8` 两行同为 7）。
      · **`E24` 另有一条"硬"自证**：`n=17` 且载荷恰好 17 个可打印 ASCII = **17 位 VIN**
        （`VF32DNFUR2W005450`）⇒ 下标只能是 `data[0..16]`，**下标/字节数/字符串长度三者对齐**。
      · **没有被改动的历史条目**（带日期的记录一律不动）：1174 条的 ①②③（`824` 那族本来就是 7 字节）、
        1236~1244 的 `4FC.data[5]`、1360~1368 的 VIN、1304~1306 的门位表 —— 逐条核过，**与实测不冲突**；
        唯一冲突的是 2026-09-23 那条里**照抄表值**的 4 个字节数，已按仓库既有体例**加更正注记**（不改原文）。

      **④ 落进 `VAN-PROTOCOL.md` 的改动**
      · **§2.8（新增）**：`data[i]` 下标约定 + 四处代码出处 + `E24` 自证 + 「遇到打架时的判定顺序」；
      · **§2.3**：`n` 的定义指到 §2.8；Data 行写 `data[0] … data[n-1]`；补 `n = (槽数 − 50 − ack×2)/10`
        这条**反解公式**（它才是本轮定 `n` 的手段）；§2.5 覆盖范围写成 `[idenByte1, idenByte2, data[0] … data[n-1]]`；
      · **§3.0（新增）**：口径 A/B 两表 + 改法表 + 没改的 12 行 + 「`n`（2026-09-19 槽级）与帧数/占比（2026-09-22）
        两类证据各管各的」的边界说明；
      · **§3 表**：4 行改 `n` 并标 ★；`E24` 行补"自证下标约定"；**§6.2 第 7 条**改成
        「待确认的是 `984`↔`9C4` **是不是同一族**，不是 `n`」（两条 `n` 各有实测）；
        **§8.1 + 引用索引**各补一行下标约定/帧长实测的落点。

      **⑤ 边界（别把本条读过头）**
      · **没有**用 2026-09-22 那趟（表里帧数/占比的来源）复核 `n` —— 它开的是逐帧打印而**逐帧原文不在仓库**
        （本文 2211 行已查过），所以本轮**绕开它**，槽数是从 `drive5min.csv` 与两份切片重新量的;
      · **没有**新增任何 `待确认` 的 `n`：16 族全部有槽级实测；仍 `待确认` 的只有
        `984`/`9C4` 的**族身份**，以及本文各处既有的字段语义类待确认项（远光/左右门/倒车/饱和……）;
      · 只读脚本写在 `tools/van-decode/_tmp_*.py`，**跑完即删、未入库**（`tools/van-decode/` 无残留）。

      **未动**：`tools/theme-editor/theme.json`（未跟踪，未碰、未提交）。

---

- [x] **★ 链路协议 L1 关闭：GPIO44 当输入**不与板载 CH343P 对打**（2026-09-23，微雪 2.8C 官方原理图）**
      **背景**：`ARCHITECTURE.md` §8 的 **L1** 是"双板链路协议 v1 能不能双向"的**唯一电气前提** ——
      43/44 是板载 USB-串口桥（CH343P）那一对，**桥的 TX 是推挽输出**，当初 VAN 的 RO 正是为这条挪去 GPIO15 的。
      L1 不关，v1 的 B→A 上行就只能走兜底（改 GPIO7 或放弃上行）。
      **核对时 HEAD = `e08b47e`**。**只改文档**（`ARCHITECTURE.md` + 本条目），代码/测试/配置/数据一行未动。

      **① 器件与拓扑（来源：官方原理图 PDF，逐个脚读出，不是推断）**
      · 出处：<https://files.waveshare.com/wiki/ESP32-S3-Touch-LCD-2.8C/ESP32-S3-Touch-LCD-2.8C_schematic_diagram.pdf>
        （**抓到了**：1 页 / **946,158 B** / `SHA256 = 01EAE811F2B777B3F919108ACB48C61E79734055F517A07D516185EF26299D76`；
        用 `pypdf` 抽文字 + 按坐标定位读数）。
        ⚠ 网页版 wiki（`waveshare.com/wiki/...`）对本机返回 **HTTP 403**，所以**没有**用网页正文当依据；
        但 `docs.waveshare.com/ESP32-S3-Touch-LCD-2.8C` 可读，用作**旁证**（器件描述 + 引脚表）。
      · **`U10` = `FSUSB42UMX`**，10 脚 **2:1 双向模拟开关**（**不是缓冲器** —— 它不额外驱动，只做通断）：
        公共端 `D+`(1)/`D-`(2) ↔ 两对选择端 `HSD1±`(5/4)、`HSD2±`(7/6)；`SEL`(10) 选边、`OE`(8) 使能、`VCC`(9)/`GND`(3)。
      · **连接关系（这就是 L1 的答案）**：
        - `HSD2±`(7/6) → 两根 **0 Ω**（`R38`/`R39`）→ 网络 `USB_RXD`/`USB_TXD` → **`U9` = CH343P 的 `RXD`(5)/`TXD`(4)**；
        - `HSD1±`(5/4) → 网络 `UART_RXD`/`UART_TXD` → **排针**（4PIN UART `J8` 与 12PIN `J9` 的 `TXD`/`RXD`），
          也就是 **ESP32 的 `IO43`/`IO44`**；
        - `SEL`(10) → 网络 `UART0SEL`，该网络**只有三个端子**：`IO0`（**GPIO0**）、10 K（`R45`）、10 K（`R44`）。
        ⇒ **`IO43`/`IO44` 一次只接到 CH343P 或排针的其中一边**，**不同时接两个驱动源**
        ⇒ **"桥的推挽 TX 与我们当输入的 44 并线对打"这个前提根本不成立** ⇒ **L1 判「不对打」**。
      · **旁证（wiki `Onboard Resources` #4/#5，原文）**：*"**FSUSB42UMX** UART selection chip: when the UART
        Type-C is connected, the 4Pin UART is disabled; when the UART Type-C is not connected, the 4Pin UART is enabled"*；
        #17 同口径：*"UART Header — This interface is not available when the USB TO UART Type-C interface is connected"*；
        引脚表：`UART Header` 的 `TXD` = **GPIO43**、`RXD` = **GPIO44**，12PIN 的 `TXD`/`RXD` 同为 **GPIO43**/**GPIO44**
        ⇒ **与原理图逐项吻合**。

      **② 另一个问题（桥的供电）也按图纸答了**
      · **`U9`(CH343P)**：`VDD`(3) = **3V3**（板载 `ME6217C33M5G` LDO）、`VBUS`(9) = **Type-C 的 `USB_5V`**、
        `UD+`(7)/`UD-`(8) ↔ `USB_RXD`/`USB_TXD`（即开关的那一对）；
      · ⇒ **桥是自供电（板载 3V3），不是"只靠 VBUS 供电"** ⇒ **它不受"插不插那根 Type-C"直接控制**；
        真正把 43/44 从桥那边摘下来的是**那颗 `FSUSB42UMX`**（这也是为什么"供电问题"与"对打问题"要分开答）。

      **③ 结论的边界（不写得比证据强）**
      · **什么时候仍可能出问题**：① **接上 USB-TO-UART Type-C 刷机/看日志时**，开关把 43/44 从排针切到 CH343P
        ⇒ **4PIN UART（含 12PIN 的 TXD/RXD）被禁用**、**v1 链路自然断**（从板按 §7 降级：>500 ms 冻结、>3 s 回退 Sim）
        —— 所以**别指望"边刷机边跑链路"**；② **上电/复位那一段**：`SEL` 的默认值由 **GPIO0（同时是 BOOT strap）**
        与 `R44`/`R45` 两个 10 K 决定，**固件在 `setup()` 之前说了不算**；③ 软件若把**两端都配成输出**，
        43↔43 就真的是对打 ⇒ v1 的**角色分工必须由编译期 `LINK_ROLE` 保证**，别指望硬件兜。
      · **还需要上板实测什么（最小残余风险，已写进 §8 的 L1 行）**：
        ⓐ `SEL` 的**默认电平**（复位期间哪一路在 43/44 上）+ 复位瞬间**用示波器看有没有抖**；
        ⓑ **串 1 kΩ 试**（能正常收发就是没人对打）；ⓒ **插上 UART Type-C 再重复一次**，确认链路是**按预期失效**
        而不是半死不活。★ 图纸文字抽取**看不出 `R44`/`R45` 哪个是上拉** ⇒ 极性老实标**待确认**，没有猜。
      · ⇒ **L1 的判定 = 关闭**（"不对打"这一条有原理图 + wiki 双重依据），
        但**"上电瞬间的默认选中态"与"1 kΩ 实测"两项仍列出**，不许写成"全都验完了"。

      **④ 落进 `ARCHITECTURE.md` 的位置**
      · §8 的 **L1 行**：原文划掉 → 换成「已关闭 + 器件/连接/供电三类依据 + 出处 + 残余待确认」；
      · §8 表头「仍未确认（L1~L7、L14）」→「**L2~L7、L14**」并注明 L1 已关闭（**编号保留**）；
      · §8 **L1 结论的用法**（新增一段）：**不启用 GPIO7 兜底**；三条"仍可能出问题"的场合；三条上板实测项；
      · §0 那条新增：去掉"必须实测或查原理图（待确认 L1）"，改成"已按原理图判定不对打"，兜底标"v1 不启用"；
      · **原 L12 的两处**（§5 角色冲突、§7 失败模式表第 6 条）：原来写"是否升级为只收不发**等 L1 的结论**"
        ⇒ 现在 L1 关了，明确**维持"丢帧 + 报警"、不升级**；
      · 「接线定案」那条"为什么挑 15 不挑 44"**保留原决定**，只追记一句：理由被修正（43/44 不是硬并线，是经开关二选一）。

      **未动**：`tools/theme-editor/theme.json`（未跟踪，未碰、未提交）；原理图 PDF 只落在 `%TEMP%`，**未入库**。

---

- [x] **★ 3 条陈旧断言修复（只改测试）+ 链路协议 v1 契约 6 处补全（2026-09-23）**
      **起点**：`0441838` 上 native 有 **3 条既有失败**（**不是本轮引入**，且**产品行为是对的**）。
      **根因**：`c596351` 把 `kSpeedScale` 从 `1.0` 改成 **`2.56f`** 时，改了 `test_van_source.cpp` /
      `test_van_phywire.cpp` / `test_van_real_capture.cpp`，**漏改 `test_data_service.cpp` 与 `test_van_replay.cpp`** ——
      这两处仍按「1 计数 = 1 km/h」断言（`data[2] = 0x64` ⇒ 100 计数 × 2.56 = **256** km/h，期望还写着 `100.0f`）。
      三条失败（PlatformIO 把文件名改写成 `test_main.cpp`，行号是**原文件里**的断言行）：
      `test_van_replay.cpp:88 test_replay_feeds_data_service`、`test_data_service.cpp:106 test_van_speed_and_fallback`、
      `test_data_service.cpp:180 test_speed_priority_van_obd_sim` —— 三条都是 `Expected 100 Was 256`。

      **① 修法：期望值由代码常量推导（耐久，不写死 km/h 字面量）**
      · 三条一律写成 `计数 × VanSource::kSpeedScale`；计数用文件级 `kVanSpeedCount = 0x64` 表达（**帧内容，与标度无关**），
        `test_data_service.cpp` 的两条共用它。
      · `test_van_replay.cpp` 那条另加一条断言把回放行解出的字节单独钉住（`p.data[kSpeedOffset] == 0x64`）⇒
        期望值不是从被验对象自己推出来的（**不循环论证**）。
      · 顺手改掉**造成事故的那条注释**（原文「100 km/h（★ 单字节，1 计数 = 1 km/h）」）—— 这种注释本身就是下一个地雷。
        `kRpmScale`(0.125) 那边只加了一行出处注，**值未动**。
      · ⇒ 标度以后再变，期望值自动跟着走，不会再出现"改了代码忘了改测试"的同类事故。
      · **断言语义原样保留**：三条验的仍是"回放喂进 data_service / 车速与兜底 / 来源优先级 Van > Obd > Sim"。
      · **产品代码一行未动**（`lib/dashcore/`、`src/` 只读未改；`theme.json` 未碰）。

      **② native 真跑（ASCII 临时副本 `C:\206dash-scratch\Neru`；未在真仓库里 `pio clean` / 删 `.pio`）**
      · 改前 **`189 test cases: 3 failed, 2 skipped, 183 succeeded`** → 改后 **`188 test cases: 2 skipped, 186 succeeded`**
        ⇒ **失败 3 → 0、通过 183 → 186（+3）**、跳过仍 2。
      · ★ 那个 `189` **不是用例数、更不是丢了用例** —— **`RUN_TEST` 注册数 = 188**（静态计数），
        两次运行的**用例结果行各 188 条、用例名集合逐一相同**：用 `git archive` 把 HEAD 上原始的两份测试文件
        按字节还原后重跑，再逐名 `Compare-Object`，**无差异**。
        `189` 是 **Unity 汇总在"有失败"时的多数 1**，同一现象上一轮已经出现过 —— `0441838` 的提交信息写着
        "RUN_TEST 注册数 155 → 188，Unity 汇总 156 → **189** 例（183 通过 / 3 既有失败 / 2 跳过）"，
        即 `156 = 155 + 1`、`189 = 188 + 1`，**两次都带着那同样的 3 条失败** ⇒ 修掉失败后两边对齐成 **188**。

      **③ 契约补全（`ARCHITECTURE.md` §「双板链路协议 v1 范围」，只改文档）：6 处补全 + 1 处标 `待确认`**
      都是上一轮实现方"照契约实现、**没擅自改契约**"时报出来的缺口：
      · **§2 `LEN` 合法范围 `5..16` → `4..16`（硬冲突）**：§3 的 `EVENT`(`0x40`) 载荷只有 **4 B** ⇒ 照原文实现
        会让 `EVENT` 被接收侧**全体当 `bad_len` 丢掉**，而 §3 又要求它可用；按"下界由最短载荷决定"改成 4..16
        （实现侧 `lib/link/link_frame.h` 早已取 `kLenMin = 4`）。
      · **§2 帧长清单补 `EVENT 11 B`**（= 7 + 4），清单改成按长度升序。
      · **§3 补字节序总则**：多字节字段**一律大端**（原文只在 `DATA.rpm_raw` 上标过一次 `u16 BE`）——
        `fw_ver` / `build_tag` / `tick_ms` / `uptime_ms` / 各计数器 / `EVENT.value` 同此。
      · **§3 补 `DATA.flags` 位号**：`rpm = bit7..6`、`speed = bit5..4`、`coolant = bit3..2`、`intake = bit1..0`。
      · **§3 补 `STATUS.flags` 位号**：`bit0 ver_mismatch` / `bit1 role_conflict` / `bit2` 无 `DATA` 超时 / `bit3` 温度弧无源。
      · **§2 补「次版本加尾巴不得越过 `LEN` 上限 16」**：`STATUS` 已顶格 16 B ⇒ 它要加字段只能**升主版本**或另开 `TYPE`。
      · ★ **`STATUS.last_gap_ms` 的语义仍标 `待确认`（不自己发明）**：§3 写清两种候选（"最近一次帧间隔" vs
        "观察到的最坏间隔"）与"**当前没有任何生产者填它**（需要接收侧自己的时钟）⇒ 一律发 0"；
        本条**未编 L 号**（§8 的 L 表是 owner 裁决用的编号表），只在 §8 表头留了一条指引。

      **未动**：`tools/theme-editor/theme.json`（未跟踪，未碰、未提交）；临时副本跑完即删。

---

- [x] **★ UART0 让给链路：显示构建不再往 43/44 写日志 + 契约两处收口（2026-09-23）**
      **起点**：`792035a`（工作区只有未跟踪的 `tools/theme-editor/theme.json`，未碰、未提交）。
      本轮只做三件：**A** 显示构建不再把 `dash_log` 写到 UART0；**B** `STATUS.last_gap_ms` 那条待确认按 v1 决定收口；
      **C** L2~L7/L14 里**只用静态依据**能定的那条（L6）定掉。**没做**的类别与理由见表末。

      **① A —— 改法与"为什么 VAN 采集那条路一字未变"**
      · **一个判据、一处开关**：`lib/dashcore/dash_log.h` 新增 **`DASH_LOG_UART0`**，
        默认值按"**这份固件里有没有链路 PHY**"判定（`LINK_PHY_UART` 是 `platformio.ini` 里只有链路固件才加的宏）：
        **没有** ⇒ `1`（`dash_log_begin()` 开 `Serial0`、`dash_logf()` 也写 `Serial0`，**双通道日志原样**）；
        **有** ⇒ `0`（这两处都不碰 `Serial0`，日志只走原生 USB-CDC = `Serial`/HWCDC）。
      · ⇒ **VAN 采集那条路（`[env:esp32s3]` 的实车用途）行为零变化**：它不带 `-DLINK_PHY_UART`
        （`main.cpp` 的链路那一段在 `LINK_ROLE`/`LINK_PHY_UART` 之外），所以 `DASH_LOG_UART0` 还是 1，
        车主插板载 CH340 口照样看得见全部日志、`van_replay_poll()` 也照旧从 `Serial0` 收回放行。
      · ⇒ **显示构建不再需要那条豁免**：`env:esp32s3` 与 `env:esp32s3-linkloop` 里的
        `-DLINK_PHY_UART_ALLOW_LOG_ON_UART0=1` **两处都删掉**（那条宏已退役、在
        `lib/link/link_phy_pins.h` 里保留为**恒 0**，只为让旧命令带着它也不会让闸门失效）。
      · ★ **那道编译期闸门保留、而且变成常开**：判据从"`LINK_UART_PORT==0 && !ALLOW_LOG_ON_UART0`"
        换成"`LINK_UART_PORT == 0 && DASH_LOG_UART0`" ⇒ 正常构建永远不触发；谁要显式
        `-DDASH_LOG_UART0=1` 把日志又放回 UART0，编译期当场报错（报错文本已改成两条出路）。
      · ★ **`USB_MODE` / `CDC_ON_BOOT` 一个字都没动**（2026-09-18 踩过的坑：`Serial` 必须是 HWCDC）。
        本轮也不去改"日志该走哪个口"这件事本身 —— 一个字节都没往别处搬。
      · 同步改掉"日志同时打到 USB-CDC 与 UART0"那 8 处**过时 include 注释**（`main.cpp` /
        `dash_ui.cpp` / `theme_load.cpp` / `dash_display_rgb.cpp` / `dash_display_spi.cpp` /
        `image_blob.cpp` / `van_phy_gpio.cpp` / `van_source.cpp`）。

      **② B —— `STATUS.last_gap_ms`：从"待确认"改成 v1 决定（不发明语义、不新写实现）**
      · 口径（owner，2026-09-23）：**字段保留、v1 一律发 0、将来要填需先有接收侧时钟**。
      · `ARCHITECTURE.md` §3 表下那一段的标题与正文都改掉了：`待确认` → `v1 决定`；写清三条 ——
        保留的理由（`STATUS` 已顶格 16 B，删字段要动载荷长度）、
        为什么 v1 只能是 0（**没有任何生产者**：要量的是"从板观测到的帧间隔"，而 v1 从板还没有那条时间基准）、
        将来要填的**前置条件**（先有接收侧时钟，再定语义）。
      · **没新开 L 号**、也没占号：§8 表头那条指引照旧（"已有字段的语义缺口不是新的电气/时序风险"）。

      **③ C —— L6 的静态一半 + 一条验证现状（其余照旧留在待确认表）**
      · **L6（主板 5V 支路能给从板多少电流）**：用**官方原理图 + 器件手册 + 仓库既有耗电口径**定了"静态那一半"——
        12PIN（`J9`）第 6 脚的网名是 **`USB_5V`**，而它就是 Type-C（`J3`）的 **VBUS**，
        且这条路上**没有**保险丝/二极管/PTC/开关/采样电阻（板上唯一的 `U1 = ETA6098` 是**电池充电**，走 `BAT`→`J1`）
        ⇒ **"能给多少电流"不由主板决定，只取决于上游那个 5V 源**（本项目选的是 OBD 取电降压线 **5V/2A**）；
        板上限那头有硬数字：3V3 全部由 **`U5 = ME6217C33M5G`**（`VIN` 直接吃 `USB_5V`）供给，
        **最大输出 800 mA**（微雪 wiki `Onboard Resources` #9 原文 *"current (Max) 800mA"*；同族手册
        *"最大输出电流：800 mA (VIN≥VOUT(T)+1.0V)"*）。
        ★ **L6 没有关闭**：第二个问题（主板刷固件 / USB 重枚举时 5V 会不会瞬跌、从板会不会跟着重启）
        **没有静态答案**（那时 5V 来源整个换成 PC 的 USB 口），照旧留在待确认表里等上板量。
      · **L2 / L3 / L5 / L7 / L14 一条都没动**：它们要真板（L2 中断密度、L3 临界区、L5 跨屏对齐）
        或要第二块板/最终板（L7 量 12PIN 的 VBUS、L14 声音告警的硬件）⇒ **如实留在待确认表**。
      · 文档里补了**一句**验证现状（`ARCHITECTURE.md` §8 表头附近 + `docs/LINK-LOOPBACK.md` 开头）：
        **单板回环已在车主那块裸 S3 上做过并通过（`linkloop: PASS`）**；
        **下一验证点 = 第二块 2.8C 到货后**（主板 `LINK_ROLE=1` / 从板 `0`、43 → 44）；
        **回环 env 不是上车固件**。

      **④ 三份固件目标 + 宿主机（都在 ASCII 副本 `C:\206dash-scratch\Neru` 里跑；真仓库没跑过 `pio clean`、没删 `.pio`）**
      · **`esp32dev`** → `[SUCCESS]`（RAM 32.9% / 107796 B，Flash 69.2% / 725393 B）；**`esp32s3`** → `[SUCCESS]`（RAM 37.5% / 122912 B，Flash 70.9% / 743437 B）；**`esp32s3-linkloop`** → `[SUCCESS]`（RAM 6.4% / 20996 B，Flash 28.2% / 295381 B）。
        ★ 三个 env 都是**去掉了那条豁免宏之后**编过的 —— 也就是说"显示构建不再需要 ack"这件事是**编过**的，不是推断的。
      · **宿主机 native：`208 test cases: 2 skipped, 206 succeeded`** —— 与基线（**208 / 206 通过 / 0 失败 / 2 跳过**）**逐项相同**。
        ★ **判据是用例计数，不是退出码**：本机 PlatformIO 在 `test`/`run` 成功时也可能以 **1** 退出（本轮三个 run + 一次 test 全是 exit 1，而 `[SUCCESS]`/`succeeded` 都打出来了）。
      · ★ **闸门的反向验证（一次性的，未入库）**：另建一个临时构建（同源 + 显式 `-DDASH_LOG_UART0=1`）编译
        `link_phy_uart.cpp`，**如期编译失败**，报错正是那道 `#error`（`link_phy_uart_probe.cpp:54: error: #error "link_phy_uart: 链路占了 UART0...`）
        ⇒ 证明"删掉豁免宏"没有把闸门一起删掉：**默认构通过、危险构被拦**。临时目录跑完即删。

      **⑤ 本轮明确没做的事（含红线里点到的那几条）**
      · **没**请车主再短接 17/18、**没**输出任何"再验一次回环"的清单（已在本机 PASS，见 `docs/LINK-LOOPBACK.md`）；
      · **没**把 `LinkPhyNull` 换成真 43/44（没有第二块板，43/44 正式链路验不了，GPIO0/`UART0SEL` 上电态仍待确认）；
      · **没**为车速饱和安排任何实车/超速动作；**没**在旧的 7 列 `drive-2026-09-22-van.csv` 上筛温度；
      · **没**买任何 OBD 适配器（车主已下单，等温度筛查结论）；
      · **没**动 CRC / SOF / `kSpeedScale=2.56` / VAN 脚极性；
      · **没**把"车上那件"（温度筛查：新列宽 + 全 IDEN + `0105`/`010F`）塞进本轮任何提交 —— 它与链路代码解耦，
        本轮只在文档里保留既有方案，代码/提交里一行都没有。

---

- [x] **★ 已解字段接进数据层 + 指示灯/告警框架 + 素材上传规格（2026-09-24；零硬件：两块 2.8C 未到、裸 S3 已拔）**

      起点 `bf41c79`。本轮**全部在 pcpreview / native / JS 测试上完成**，没碰车、没烧写、没插拔。

      **① 数据层：三类已解字段接进来（只有 VAN 一个来源）**
      · `0x4FC` / CMD `0xC` 的 `data[5]`（§4.3/§4.4 的**位域**：bit2 左转 `0x04`、bit3 右转 `0x08`、
        bit6 近光 `0x40`、bit7 仪表盘灯 `0x80`）与 `data[1]`（门信号，§4.6）；
        `0xE24` 的 17 字节明文 ASCII VIN（§4.7，**不需要跟 `0x5E4` 拼**）。
        全部解在 `lib/dashcore/van_source.*`（与车速/转速同一处、同一套 `offset < len` 判据）。
      · **★ 欠采样那条必须照文档设计**：`0x4FC` 只有 **4.7 帧/秒**，而闪烁全周期 0.80 s ⇒
        "亮"只持续 1~3 帧。所以灯位**不能**按帧直读，加了**保持窗口** `kIndicatorHoldMs = 600 ms`
        （`VanSource::lightsRecent(now)`）：窗口内算亮着，超窗 ⇒ 值清掉、来源回 `None`
        （屏上箭头**隐藏**，不是"停在最后那个状态"）。600 ms 的来历与指数分布估算写在头文件里。
      · **门只报"动过"，不报"哪扇门/开着还是关着"**：左右门可分辨性**未解**（§6 撤回①），
        而"`==1` 就是门开着"被 §4.6 否掉（脉冲段内还在 `00`↔`01` 跳变）⇒ API 就叫
        `doorActivity()`，**刻意不提供** `doorOpen()`（名字一旦叫"门开着"，上层就会当真值用）。
        基线取"第一帧见到的值"（实车静息 `0x00`）。
      · VIN 整帧校验：**17 字节全部可打印 ASCII 才收**（§4.7 的判据）；短帧/含控制字符的帧
        整帧丢，**不覆盖上一次的好值**。
      · **★ 没动既有优先级**（三条结构性证据）：① 这三类字段**在 OBD-II 上没有对应 PID** ⇒
        没有第二来源，也就没有"优先级"可言；② 合并写成 `data_service.cpp` 的**独立第 5 段**，
        不进 `applyLinkData` 的填空位逻辑（链路只搬那四个标量，一个字节没改）；
        ③ 判据用**各自的时间戳**（`lightsLastMs` / `vinLastMs`），与车速/转速共用的
        `lastUpdateMs` 分开 —— 这正是 `test_obd_intake_takeover_per_field` 钉过的那个坑。
        用例：`test_van_fields.cpp` 9 条（含"没有这些帧时字段保持默认且来源为 `None`"、
        0x00/0x04/0x08/0x0C/0x40/0x80/0xC0/**0x84**/0x8C/0xFF 的逐位边界、
        保持窗口的 500/700 ms 两侧、门"动过"、VIN 的三种坏帧、短帧不越界、
        "只有 0x824 帧时灯位五格一个都不许变 `Van`"）。

      **② 指示灯槽位（占位图形，480/240 自动缩放）**
      · 六格：左转箭头 / 右转箭头 / 双闪 / 近光 / 仪表盘灯 / 门。几何是 `lib/dashcore/lamp_view.h`
        的 `kLamp*`（480 基准，随 `theme_scale()` 缩 ⇒ 240 档自动一半）。
      · **占位图形**（明确不画正式素材，让逻辑与美术解耦）：左/右 = 双层 chevron（两根旋转条拼），
        双闪 = 两个三角，近光 = 圆 + 三条光线，仪表盘灯 = 实心圆，门 = 门扇 + 门把手圆点。
        全部只用矩形/圆/旋转（LVGL 没有三角形图元，而自绘路径在这个精简版 espLCD 上没验过）。
      · **几何是被用例钉住的**：六格四角必须落在 480 圆屏的**内切圆**内（实测最大半径 234.1 < 240）；
        灯条顶边（y=395）不许压副表小字（水温/进气墨迹下沿 y≈396 ⇒ 断言留 ≥2 px 余量）。
        这两条都在 `test_ui_lamps.cpp` 里（纯几何、不依赖 LVGL）。
      · **逐像素实测**（pcpreview 落帧 + `check-preview-frame.js` 新增的 `lamps=` 检查）：
        注入 `left=1 low_beam=1` 之后 —— 近光格**每帧都亮**（279 px）、左转格**按半周期闪**
        （亮帧 287 px / 灭帧 0 px，12 帧里 8 亮 4 灭）、右转/双闪/仪表盘灯/门**全为 0 px**；
        再注入 `speed=140` 之后出现告警帧：左右两格同时亮（各 555 px）+ **告警描边红 552 px**。

      **③ 告警层 + 蜂鸣器抽象**
      · `lib/dashcore/alerts.*`（**纯逻辑，不碰 LVGL/硬件**）：触发源**只用已解字段** ——
        超速（车速档位，阈值 `120`/退 `117` 迟滞）、转速红区（`5800`/退 `5600`）、
        门（"动过"，去抖 `200 ms`）、转向灯忘关（连续 `20000 ms`）。
        **不为水温/进气做告警**（那是 OBD，等验通），**不为未解字段建空壳告警**。
      · 三条规则都在用例里（`test_alerts.cpp` 11 条）：**去抖**（250 ms 的 100/300 ms 两侧、
        中途断掉要**重新计时**）、**最短重复间隔**（3000 ms；红区持续 15 秒 = 恰好 6 声，
        而不是"每一拍都响"）、**静音**（掐蜂鸣器、**不掐**屏上那条 `active()`；取消静音后立刻响；
        静音期间不刷新 `last_beep_ms`）。外加优先级（超速 > 红区 > 门 > 转向）、
        `reset()` 的语义（**不清静音与计数**）、以及"双闪**不算**忘关"。
      · **★ 双闪刻意排除**：双闪是人**主动**按的、而且本来就允许长期亮（临时停车）；
        踩双闪时 `indicator_left/right` 两位都是 1，所以显式排除。
      · **蜂鸣器放抽象后面**（`lib/dashcore/buzzer.h`：`begin/beep/off/name`）：
        `BuzzerNull`（默认不发声）、`BuzzerHost`（pcpreview：打印 `BEEP pattern=<name> ms=<n>`
        一行，可选 `-DBUZZER_HOST_SOUND=1` 出系统提示音，**格式被用例逐字节钉住**）。
      · **★ §8 L14 的口径**：按"**从板本地发声**"实现这个抽象，硬件写的是 2.8C 板上
        TCA9554 的 `EXIO8`（零额外引脚）—— 但文档里**明确标为"建议、待 owner 最终点头"**
        （`ARCHITECTURE.md` §8 的 L14 行 + 那句"仍未确认"的抬头），**没有**写成已裁决。
        本轮**没有写任何 I2C 时序**（没硬件、验不了）：真机那一档是**另一个 `Buzzer` 子类**，
        换它只动 `main.cpp` 里构造哪一个。

      **④ pcpreview 能注入输入（键盘 + 控制文件）**
      · 键盘：左/右方向键 = 左右转、`空格` 双闪、`L` 近光、`P` 仪表盘灯、`D` 门、
        `O` 超速（车速 130）、`R` 红区（转速 6000）、`M` 静音、`X`/`Esc` 全清。
      · 控制文件 `preview/inject.txt`（每帧重读 ⇒ 保存即生效），键名同一套；
        纯函数在 `lib/dashcore/preview_input.h`（native 可测：键位语义 + 控制文件语法
        含注释/空行/`on|true|yes`/认不出的键/`clear=1`）。
      · **踩到并修掉一个真坑**：第一版读控制文件的缓冲只有 512 字节且对"读满"一言不发 ——
        "把带注释的例子文件直接存成 inject.txt"就会**静默全失效**（注释占满缓冲、
        真正那几行一个字没读进来，日志上什么异常都没有；实测 `parsed=0`、屏上灯全灭）。
        现在缓冲 1024，**超了会打一行 `WARNING ... longer than 1024 bytes`**，例子文件也改短了。

      **⑤ 素材上传规格 + 简单转换（车主明确要求）**
      · **规格写死在** `tools/theme-editor/README.md` 的「★ 素材规格（上传端唯一权威）」一节，
        机器可读的那份在 `tools/theme-editor/asset-spec.js`；网页、命令行、文档引用**同一份**。
        内容包括：可接受格式（PNG 完整 alpha / WebP 有 alpha 但有损边缘会脏 / **JPEG 只能当背景** /
        其余一律拒）、产物格式（容器 + `RGB565` 2B/px、`RGB565A8` **3B/px 含 A8 平面**、
        最多 32 张、名字 ≤23 字符）、**必须带 alpha 的槽位**（10 张表情）、
        尺寸（480 档推荐 300/上限 320/背景必须 480；240 档 152/160/240）、
        **圆形可视区**（内切正方形边长 480 档 **336**、240 档 **168**）、
        体积上限（整包 ≤ image 分区：经典板 1MB / S3 8MB；单张无独立上限）、
        命名与角色映射（11 个在用编号 + 保留号 + 下一个可用号 23）。
      · **转换做了四件事**（`AssetSpec.prepareAsset()`）：① RGBA → `RGB565`/`RGB565A8`；
        ② **保持长宽比缩到槽位框**（contain、**只缩不放**、按 **alpha 加权**求平均以免脏边）；
        ③ 可选**裁掉透明边**（页面每张图一个勾选框，默认**关** —— 有人故意留白）；
        ④ **明确的拒绝/警告**（人能看懂、都指出路）：不认识的格式 / **JPEG 当表情** /
        整张全透明却要裁 / WebP 当表情 / 表情整张不透明 / 背景含半透明 / 四角超出可视圆 /
        超上限被自动缩小。整合进 `image-editor.html`（拖入即查+转换）与 `build-image-bin.js`
        （`specItems()` 顺手对账，**只警告不拦路**，`main()` 打出来）。
      · **用例**：新增 `test-asset-spec.js` **155 项断言**（含"规格里的每个数与打包器/分区表逐条对账"
        与上面每一条拒绝/警告路径）；`test-image-blob-build.js` 加一组"命令行也给出同样提醒"
        （**362 → 369**）。

      **⑥ 顺带修掉一个一直存在的构建缺口（不是本轮引入的行为改变）**
      · `src/main.cpp` 的 `g_data = attachObdSerial();` **没有**被 `#if defined(ARDUINO)` 圈起来，
        而函数**只在 `ARDUINO` 那一支里定义** ⇒ **pcpreview 编译到那一句就报
        `use of undeclared identifier 'attachObdSerial'`**：宿主机预览**根本编不出来**。
      · **怎么确认是起点上的问题**：拿 `git show HEAD:src/main.cpp` 用 pcpreview 的**逐字编译参数**
        预 processed 一遍 —— `attachObdSerial` 只剩调用点、没有定义；而且仓库 `.pio/build/` 里
        只有 `esp32dev` 一个目录、`pcpreview` 从没成功编过（按 `.pio/build/pcpreview` 目录时间戳核对）。
      · **修法**：给预览一份**显式的空实现**（`g_data` 保持 `nullptr` ⇒ `ObdSource` 不启用，
        与"没插 OBD"同一条路径；弧/表情继续由 `sim_source` 驱动），**并打一行日志**
        `obd: pcpreview 宿主机没有 UART1/ELM327 ⇒ 不启用 OBD` ——
        圈掉调用点是"静默地没有"，那正是最难查的一类。

      **⑦ 测试与构建（都在 ASCII 副本 `C:\206dash-scratch\Neru` 里跑；真仓库没跑过 `pio clean`、没删 `.pio`）**
      · native：**234 test cases: 2 skipped, 232 succeeded**（起点基线 **208 / 206 通过 / 2 跳过**
        ⇒ 新增 **26** 条：`test_van_fields` 9 + `test_alerts` 11 + `test_ui_lamps` 6）。
      · JS：`test-face-stages.js` **429**、`test-image-blob-build.js` **369**（362 + 7）、
        `test-theme-json.js` **259**、`test-gauge-geometry.js` **71**、
        `syntax-check-pages.js` **12**（11 + 1：新增的 `asset-spec.js` 脚本标签）、
        新增 `test-asset-spec.js` **155**。
      · 固件四个目标全 SUCCESS：`esp32dev`（RAM 33.0% / Flash 69.5%）、
        `esp32s3`（37.6% / 71.2%）、`esp32s3-linkloop`（6.4% / 28.2%）、`pcpreview`。

      **⑧ 本轮明确没做的事（含红线里点到的那几条）**
      · **没**碰 `tools/theme-editor/theme.json`（未跟踪，未碰、未提交）；指示灯颜色是
        `ui_theme.h` 里的**编译期常量**，**没进主题文件**（加字段要升版本号、会让已导出的主题重导）。
      · **没**动 CRC / SOF / `kSpeedScale=2.56` / VAN 脚极性；**没**新开 L 号；
        **没**为未解字段（油量/挡位/里程/刹车/雨刮/水温）建任何空壳 UI 或空壳告警。
      · **没**请车主动任何硬件、**没**烧写；**没**写任何 TCA9554 的 I2C 时序（L14 未裁决 + 无硬件）。
      · **没**把"车上那件"（温度筛查）塞进本轮任何提交。
      · **留待裁决**：L14 的两问（哪块板发声 / `EXIO8` 那颗蜂鸣器是有源还是无源）；
        门活动窗口 `kDoorActivityMs = 8000` 是**工程判断**（抓包那轮的门脉冲是 10 s 档），
        真车标定要 owner 拿屏试一次；超速告警阈值 `120`（表情那一档是 >130，两者**刻意不同**）；
        转向灯忘关 `20 s` 也是工程判断。
## 2.8C 圆屏：点亮、"整屏花屏"与"撕裂"的根因与修法（2026-09-24 实机）

**板**：微雪 **ESP32-S3-LCD-2.8C（非触控，最终板）**，CH343P → **COM6**；
屏 = 2.8" 圆 IPS 480×480 / **ST7701** / **RGB565 并口**。
引脚表、41 步初始化、时序、缓冲与同步策略的完整记录见 **`docs/RGB-PANEL-2.8C.md`**
（引脚顺序、7 个时序宏、41 步初始化都拿官方 `ESP32-S3-LCD-2.8C-Demo.zip` 的
`Display_ST7701.{h,cpp}` **逐条机器比对过**：全同）。

### 点屏时撞到的两个"静默失败"（都已修）

1. **少了 `esp_lcd_panel_init()`** ⇒ `rgb: frames=0`、屏全黑。
   旧 API 的 `esp_lcd_new_rgb_panel()` 只建对象，**真正开 DMA**的是 `esp_lcd_panel_init()`。
2. **LVGL 绘制缓冲没按 `LV_DRAW_BUF_ALIGN` 对齐** ⇒ `rgb: flush=0`、屏全黑。
   LVGL 9.3 的 `lv_display_set_buffers()` 校验 `buf1 == lv_draw_buf_align(buf1)`，
   不满足就**静默 return**；`static lv_color_t buf[...]`（uint16_t）只保证 2 字节对齐。

### ① 整屏花屏（每次更新整屏花，之后自恢复）

**根因**：这份 esp_lcd（arduino-esp32 2.0.17 / IDF 4.4 系）**没有 bounce buffer**
（头文件里没有 `bounce_buffer_size_px`），framebuffer 只有一块且在 PSRAM ⇒
LCD_CAM 的 DMA 在整帧有效像素期间**直接从 PSRAM 读**，而 LVGL flush 时 CPU 往同一块
PSRAM 里 memcpy（几十 KB + cache 回写）⇒ 抢带宽、DMA FIFO 欠载 ⇒ 那一帧整屏花。
**与 porch/极性配错的区分**：配错是"从头花到尾"，欠载是"只在写的那一下花"。

**★ 被证据否掉的一条假设**：不是"缺 cache 回写" —— 反汇编 `libesp_lcd.a` 的
`esp_lcd_rgb_panel.c.obj`，`rgb_panel_draw_bitmap` **确实调用** `Cache_WriteBack_Addr`
（`.literal.rgb_panel_draw_bitmap` + 1 个调用点）。

**修法 A**：PCLK 从 Arduino 例程的 **30MHz 降到 18MHz**。
18MHz 是官方 **ESP-IDF** 例程的值 —— 那份例程的 bounce buffer 是**可选项**；
30MHz 那档（Arduino 例程）是**依赖** bounce buffer 的，而我们要不了。

### ② 撕裂（同屏上下半新旧两帧，横缝）

**根因**：等"帧结束"的粒度不够 + 单次写的字节数超窗。
消隐期只有 **0.85ms**（18MHz、28 行）；而实测 `draw_bitmap` 写 19200B 要 **1309µs**
⇒ 一次写会溢进有效像素，横缝就出来了。

**修法 B**（两处，都在 `src/dash_display_rgb.cpp`）：

1. 等帧结束改成**微秒级自旋**（原来是 `delay(1)`，最坏晚 1ms 才醒 —— 早就扎进有效像素了）；
2. 每次写 fb **按字节定额分块**（`RGB_FLUSH_MAX_BYTES = 8192`，实测出来的余量），
   一块一块地等各自的消隐期。

### 实测（原始串口行）

```
rgb: RGB565 480x480 pclk=18000000Hz 数据位=16 已就绪(第二块屏待接)
rgb: RGB565 480x480 ... 板=微雪 ESP32-S3-LCD-2.8C(非触控) ST7701 RST=EXIO1 CS=EXIO3 BL=GPIO6/PWM20000 @50%
rgb: frames=499(+65/s) flush=136 sync=495(+65/s) timeout=0 chunk=495 copy_max=561us copy_avg=208us wait_max=15237us
```

* `frames` **+64~+67/s** = 18e6/(548×508) = 64.7Hz ✓
* `sync == chunk`、`timeout = 0` ⇒ **每一次**写 fb 都等到了帧结束，没有一次靠超时放行
* `copy_max = 561µs < 850µs`（消隐期）⇒ 每次写都落在消隐期之内
* heap 223KB（setup 后）/ 219KB（稳态）、PSRAM 8189KB、LVGL 堆 26KB/45.8KB

**★ 还没验的一条**：车主**肉眼**确认撕裂是否真的消失。数字只能说"每次写都落在消隐期内"，
**玻璃上什么样只有人眼能判**。
**已知边界**：整屏更新（开机动画）要 450KB÷8KB = 55 块 ≈ 0.85 秒 —— 慢，但不花不撕。
要"又快又干净"只剩**换到带 `num_fbs`/`on_vsync` 的 esp_lcd**（换构建级改动，
影响面见 `docs/RGB-PANEL-2.8C.md` 第 9 节）—— **本轮没做**。

### 顺带：PC 中继（没有 VAN 收发器也能把数据喂上屏）

新增 `tools/serial-capture/relay.ps1`（**零第三方依赖**：只用 .NET 自带 `System.IO.Ports`；
本机系统 python 里没有 pyserial）。实测（文件源 → 2.8C，`SRC` 行原文）：

```
SRC speed=van rpm=van coolant=sim intake=sim | v=99.8km/h 799rpm 93.0C 30.0C
SRC speed=van rpm=van coolant=sim intake=sim | v=105.0km/h 799rpm 92.9C 28.2C
SRC speed=van rpm=van coolant=sim intake=sim | v=102.4km/h 799rpm 92.4C 26.4C
```

★ 格式坑：设备/抓帧盒自己打的帧行**不能直接回放** ——
`VAN 824 18 F8 27 10 00 00 00   # cmd=1 ack=0` 里那段 `# …` 会让
`parseVanReplayLine()` 判错（回显 `VAN? …`，数据层一帧收不到）。
`-ReplayLinesOnly` 就是干这个的。**第一次中继 30 行全被判错**，就是少了这一步。

### 本轮没做 / 拿不准的

* **裸 S3 抓帧盒（COM7 / CH340）**：插着、能复位（`ESP-ROM:esp32s3-20210327` + `boot:0x8`），
  但**UART0 上一个字节都不打** —— 不是坏：`[env:esp32s3]` 带 `-DLINK_PHY_UART=1`
  ⇒ `DASH_LOG_UART0=0`，日志只走**原生 USB-CDC**（那一路没插）。
  ⇒ 明天做"真·两块板中继"时 PC 要接抓帧盒的**原生 USB 口**才有 `VAN …` 行。
  COM7→COM6 只做了通路自检（转发 0 行，因它没说话）。**没**为这个改任何固件。
* 桩驱动 `src/dash_display.cpp` 的 `buf_left/buf_right` 有**同类对齐隐患**，
  但它是 esp32dev / 抓帧盒在用的构建 ⇒ **本轮没动**。
* 外径/留白观感：**没对着 Ø89 表壳比过** ⇒ 仍未测（机械尺寸更正在 `PURCHASE.md`）。
* `theme.json` 未跟踪、**没碰**；CRC / SOF / `kSpeedScale=2.56` / VAN 脚极性**没动**；
  **没**新开 L 号；真仓库**没**跑 `pio clean`、**没**删 `.pio`；构建/烧写都在
  `C:\206dash-scratch\Neru`（ASCII 副本），且每次都显式 `--upload-port COM6`。
* native **234 test cases: 2 skipped, 232 succeeded**（基线不变）；
  JS 五套 71 / 429 / 259 / 155 / 369。
## 2.8C："表情不见了"的根因与修法 + 撕裂的结构性数字（2026-09-24 补）

### A) 表情不见了 —— 根因：开机动画的"显形"那一步被长 flush 跳过了

**症状**：弧、数字、灯都在，**只有中间的白色表情脸不见了**（车主肉眼）。

**排查（每一步都是实测/读码，不是猜）**：

1. **先证明"不是画到屏上没写进去"**：在 RGB 驱动里把**每次真正交给面板的字节**
   累成一张 12×12 亮度网格打出来 —— 结果显示脸所在的中间 6×6 格**全是黑的**，
   而同一帧里的数字/弧都在。⇒ 送进 fb 的**就是**黑背景，不是"写了没显示"。
2. **再证明"控件是在的"**：`uidbg` 打出活动屏上的表情对象 ——
   `show_face=1 hidden=0 x=140 y=140 w=200 h=200 ch=3`（200×200 居中、3 个子对象）
   ⇒ 树、几何、显隐都正常。
3. **排除"LVGL 堆不够"**：把这条 env 的 `LV_MEM_SIZE` 从 48KB 临时提到 128KB
   （`free=108008 biggest=107232`，`max_used` 一点没变 ⇒ 连分配都没尝试过）
   ⇒ **不是内存**，是**这一步根本没被调用**。
4. **读 `boot_apply()` 找到真相**：表情的显形**不是靠 HIDDEN 标志**，而是
   `g_boot.faceStage(t)` 在 `t≥BOOT_FACE_START_MS` 从 0 变 1 时，由 `boot_apply()`
   把 opa 从 `LV_OPA_TRANSP` 推到 `LV_OPA_COVER` —— **只在"档位变了"那一次设**
   （`static uint8_t last_st[2]`）。
   而 `boot_apply()` **只在 `g_boot.active(now)` 期间被调用**（`now < start+BOOT_TOTAL_MS`）。
   ⇒ **只要整个动画窗口里一次都没轮到，那次转移就永远不会发生，表情永久停在透明上。**

**为什么本次才发作（回归的来源）**：RGB 那条路上，开机第一次整屏刷新要**按块写、
每块等一个消隐期**（本轮的撕裂修法）。实测：一屏 = 8 条 60 行带 × 每带 8 块
≈ **64 帧 ≈ 1.0 秒**卡在一次 `lv_timer_handler()` 里 ⇒ 动画窗口（≈1.1s）在这期间
就过完了 ⇒ 显形那一步被跳过。
之前的 30MHz/整块写那版一次 flush 只等一帧、整屏 ≈ 几十毫秒，动画跑得完，
所以表情是在的 ✓（与车主"之前是在的"完全对得上）。
预览（pcpreview）flush 是即时的 ⇒ 这条 bug 在预览里**永远看不出来**。

**修法**（`src/dash_ui.cpp`，一处，与驱动无关）：
动画窗口结束后**补调一次 `boot_apply(now_ms)`** —— 此时 `faceStage(t≥end)=1`，
状态被落到最终值。这是"动画状态机不能被主循环卡顿跳过"该有的兜底。
串口上自证的证据（原始行）：

```
boot anim done (收尾补一次 boot_apply: faceStage=1 → 表情 opa=COVER)
```

★ **仍是"待肉眼确认"**：`faceStage=1` 只证明那次 opa→COVER 的转移**执行了**；
玻璃上那张脸回没回来，只有人眼能判。

### B) 撕裂：结构性数字（说明"调小分块"为什么治不了）

一次 flush 的**区域**实测（`rgbdiag area#1..8` 原文）：`(0,0)-(479,59)`、`(0,60)-(479,119)` …
⇒ 每条 **480×60 px = 57,600 字节**（RGB565）；按 8KB 定额 ⇒ **一条区域要 8 帧才写得完**，
开机整屏 = 8 条 × 8 = **64 帧 ≈ 1 秒**。
⇒ **同一块区域里的像素来自 8 个不同的帧** —— 这就是撕裂的结构性来源：
只有 1 个 framebuffer、没有 vsync 翻转，**改小定额只会让每块更小、区域照样跨帧**。
**根治的唯一一条路**：换到带 `num_fbs` / `on_vsync` 回调的 esp_lcd（双 fb + vsync 翻转）。
影响面（本轮**没做**，按红线先报方案）：pioarduino 新平台或直接用 ESP-IDF ⇒
`lib/link` 的 UART PHY、`dash_log.h` 的 `DASH_LOG_UART0`、本驱动那段 3 线 SPI
（`esp_lcd_panel_io_spi` 的写法在新平台里要跟着换）都要一起过一遍。

### 本轮的其它实测数字（未变）

`pclk=18000000Hz`、`frames +64~+96/s`（名义 64.7）、`sync == chunk`、`timeout = 0`、
`copy_max = 576µs < 消隐期 850µs`、heap 223KB→219KB、PSRAM 8189KB。
另记一条容易踩的：LVGL v9 的 `lv_color_t` 是 **24 位**类型（3 字节），
所以 `lv_color_t buf[480*40]` 实际是 **57,600 字节**，LVGL 据此算出 **60 行**一条带
（不是 40）—— 填缓冲尺寸时别按 `sizeof(uint16_t)` 想当然。

## 2.8C **撕裂根治**：换到 IDF 5.5（pioarduino）+ 双 framebuffer + vsync 换帧（2026-09-24 实机）

**一句话**：上一轮查明"撕裂是**结构性**的"（单 framebuffer、没有 vsync 翻转、一块 57,600B 的
flush 区域要跨 8 帧才写完）——本轮**已根治**：`[env:esp32s3-rgb]` 换到带 `num_fbs` / `on_vsync`
的 esp_lcd（**pioarduino 55.03.39 = arduino-esp32 3.3.9 + ESP-IDF 5.5.4**），LVGL **只往 back fb 画**，
一次刷新的**最后一块**在 **vsync 边界整块换帧**。开机整屏刷新从 **≈1 秒** 回到 **56.6ms**，
"8KB 定额 + 每块等消隐期"那套节流**整套删掉**。完整口径（含复核命令）记在
`docs/RGB-PANEL-2.8C.md` **第 9 节**。

### ① 选的路与理由（平台 / 版本）

* 选 **pioarduino 的 espressif32 分支 `55.03.39`**（= arduino-esp32 **3.3.9** + **ESP-IDF 5.5.4**，
  `framework-arduinoespressif32@3.3.9` / `framework-arduinoespressif32-libs@5.5.4+sha.735507283d` /
  `toolchain-xtensa-esp-elf@14.2.0`）。**保留 Arduino 框架** ⇒ 应用层（`main.cpp`、`dash_ui.cpp`、
  `lib/*`）**一行都没改**，只做 API 适配（见 ⑥）。
  **纯 ESP-IDF 方案被否**：要把整个 Arduino 应用层（LVGL 绑定、Serial/Timer/Wire 用法、
  编译开关体系）重写一遍，与本轮目标无关，风险也不成比例。
* **只动 `[env:esp32s3-rgb]` 一条 env**：
  `platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.39/platform-espressif32.zip`
  （写 **URL** 而不是 `espressif32@55.03.39`：pioarduino 不在官方 registry 里，用 URL 才能靠
  `spec.uri` **精确认出**它，而不是靠"版本最高的那个 espressif32"这条会误伤别的 env 的规则。）
* ★★ **`[env:esp32s3]`（抓帧盒）的平台与行为一个字没变**：它仍是官方 **`espressif32@7.1.3`**
  （arduino-esp32 2.0.17），只把版本**显式钉住**。为什么必须钉：PlatformIO 对**不带版本**的
  `espressif32` 取"已安装里**版本号最高**的那个"（`platformio/package/manager/base.py` 的
  `get_package`：`pkg.metadata.version > best.metadata.version`）⇒ 两套平台共存时**不钉就会被
  静默升到 arduino-esp32 3.x**，那正是红线。钉住之后行为不变的**证据**：`esp32s3` 与 `esp32dev`
  的 RAM/Flash 与上一轮**逐位相同**（见 ⑤）。
* `[env:esp32s3-spi]`（240 DualEye 那条 SPI 驱动，`esp_lcd_panel_io_spi`）extends `esp32s3`
  ⇒ 仍在旧平台；**这个驱动一个字节都没动** ✓。
* ★ **代价（实测，写清楚）**：两套平台的 `framework-arduinoespressif32` **共用同一个
  packages 目录名**（pioarduino 的 spec 是自定义名 `framework-arduinoespressif32=<url>`，
  `_install_tmp_pkg` 对自定义名一律 `overwrite` 到不带版本的那个目录），所以**交替构建**
  `esp32s3-rgb` 与 `esp32s3/esp32dev` 时，后者的框架包会被重装一次（从 PlatformIO 的本地
  下载缓存解包，**不走网络**）：实测 `esp32dev` 那次构建 **168s**、`esp32s3` **198s**
  （含工具链）。要两边都频繁构建，就让其中一边用独立的 `PLATFORMIO_CORE_DIR`。
  同一条也写在 `platformio.ini` 那段注释里。

### ② 撕裂的客观证据（COM6 原始串口行，18MHz 定案固件）

```
psram : 8187 KB 可用 / 8192 KB 总
heap  : 205 KB
rgb: RGB565 480x480 pclk=18000000Hz 数据位=16 已就绪(第二块屏待接)
rgb: 双framebuffer num_fbs=2 fb0=0x3c0d1b20 fb1=0x3c142340(各 450KB PSRAM) on_vsync=已注册 ⇒ vsync 边界换帧(无撕裂)
rgb: 双fb 之后 空闲 PSRAM=7285KB heap=200KB(内部)
rgb: 整屏刷新 56.6ms(块=8 拷贝20.0ms 数据450KB) 换帧在下一个vsync
boot anim done (收尾补一次 boot_apply: faceStage=1 → 表情 opa=COVER)
rgb: vsync=292(+65/s) swap=51(+5/s) flush=172 copy_max=3012us copy_avg=881us swap_wait_max=8875us timeout=0 fb=1/0 msync=1
rgb: vsync=357(+65/s) swap=56(+5/s) flush=194 copy_max=3012us copy_avg=897us swap_wait_max=8875us timeout=0 fb=0/1 msync=1
```

| 判据 | 实测 | 说明 |
|---|---|---|
| **`vsync` 回调计数** | **+64~+65/s** | 18e6/(548×508) = 64.7Hz ⇒ 面板在收帧，PCLK 也确实是 18MHz（30MHz 档实测是 +108/s） |
| **`swap` 换帧计数** | 稳态 **+5/s**（= UI 200ms 一次） | 每一次 LVGL 刷新都**换了一次 fb** |
| **`fb=`** | 在 `1/0` ↔ `0/1` **来回翻** | 两块 fb 真的在轮换（一直同一个值就说明换帧没生效） |
| **`timeout`** | **全程 0** | 等"换帧那个帧边界过去"**从来没有**靠超时放行 ⇒ 同步是真在起作用 |
| `msync` | **1**（不再涨） | 整块补拷只在开机后第一次换帧之后发生一次（设计如此） |
| `swap_wait_max` | 8875µs | < 一帧 15.5ms ⇒ 与"最坏等一帧"的设计一致 |
| **开机整屏刷新** | **56.6ms**（旧：≈0.85~1.0 秒 ⇒ 快 ~15 倍） | `块=8`、其中拷贝 20.0ms |
| 双 fb 的代价 | 空闲 PSRAM 8187KB → **7285KB**（正好 2×450KB） | heap（内部）205KB → 200KB，旧栈是 223/219KB |

### ③ 车主肉眼（**这一条只能人眼判，本轮结束时仍是"待确认"**）

上面的数字能证明"换帧发生在帧边界、且从不靠超时" —— 但**玻璃上有没有缝只有人眼能判**
（上一轮就是这么说的，这一轮同样不许用数字替代）。请车主看一眼：
**开机动画的扫表 + 表情显形那 1.1 秒**、以及**稳态下数值跳动的那一瞬**，是否还有横缝。

### ④ 功能回归四项

| 项 | 结果 | 证据（原始串口行 / 外观） |
|---|---|---|
| UI 正常 | ✓ | 每秒 `206 dash ok spd=… rpm=… coolant=… face=…`、`BEACON … step=6/7/8`、`SRC-VAN …`、`link: …` 全部照旧 |
| **表情在**（左右各 5 档） | ✓ | 开机 `boot anim done (收尾补一次 boot_apply: faceStage=1 → 表情 opa=COVER)`；每秒行里 **5 档都在转**：`face=idle/cruise/sport/high/redline`（那个兜底**照旧保留** ✓） |
| **中继仍通** | ✓ | `tools/serial-capture/relay.ps1 -From sample-log.txt -To COM6 -ReplayLinesOnly -Loop -LineDelayMs 1500 -Seconds 45` ⇒ 设备回 **`SRC speed=van rpm=van coolant=sim intake=sim \| v=105.0km/h 799rpm 80.4C 27.8C`**；中继汇总 `转发 30 行 / 762 字节 ← 11966 字节 / 144 行`；中继期间显示那行**没断**（`vsync=+65/s swap=+5/s timeout=0`） |
| 外设按现状 | ✓ | TCA9554（RST=EXIO1 / CS=EXIO3）—— 屏点亮本身就是它通的证据；背光 `BL=GPIO6/PWM20000 @50%`（`ledcAttach` 新版 API，频率/位数/占空比未变）；PSRAM **8187KB 可用 / 8192KB 总**；`frames/vsync` 稳定 +64~65/s |

### ⑤ 测试与构建（全部真跑，数字如下）

* **native**：`pio test -e native` ⇒ **234 test cases: 2 skipped, 232 succeeded**（基线不变，0 失败）✓
* **JS 五套**（`node tools/theme-editor/<file>`）：**71**（gauge-geometry）/ **429**（face-stages）/
  **259**（theme-json）/ **155**（asset-spec）/ **369**（image-blob-build）全绿 ✓
  （外加 `syntax-check-pages.js` **12** ✓）
* **五个固件 env 全部 SUCCESS**：

| env | 平台 | RAM | Flash |
|---|---|---|---|
| `esp32dev` | 旧（`espressif32@7.1.3`） | 33.0%（108,084 / 327,680） | 69.5%（728,689 / 1,048,576） |
| `esp32s3`（抓帧盒） | 旧（`espressif32@7.1.3`，**钉版本**） | 37.6%（123,200 / 327,680） | 71.2%（746,693 / 1,048,576） |
| `esp32s3-linkloop` | 旧（继承 esp32s3） | 6.4%（21,084 / 327,680） | 28.2%（295,333 / 1,048,576） |
| `pcpreview` | native | —（宿主机可执行） | — |
| **`esp32s3-rgb`（本 env）** | **新（pioarduino 55.03.39）** | **48.0%**（157,360 / 327,680） | **84.6%**（887,067 / 1,048,576） |

  ★ `esp32dev` 与 `esp32s3` 的 RAM/Flash 与上一轮记录**逐位相同** ⇒ "抓帧盒那条 env 行为没变"
  不只是嘴上说的。

### ⑥ 适配中改到的**每一个** `lib/` 或 `src/` 行为点（并说明为什么"逻辑没变、只适配 API"）

**改到的（共 2 个源文件）：**

1. **`src/dash_display_rgb.cpp`（重写，本轮的主角）** —— 换的是**缓冲与提交方式**，不是时序/初始化/引脚：
   * `cfg.on_frame_trans_done = …`（旧 API 的单回调）→ **删掉**；改用
     `esp_lcd_rgb_panel_register_event_callbacks(&cbs)` 注册 **`on_vsync`**（IDF 5.x 才有）。
   * 新增 `cfg.num_fbs = 2` + `cfg.flags.fb_in_psram = 1` + `cfg.bits_per_pixel = 16`；
     删掉 `cfg.psram_trans_align/sram_trans_align`（IDF 5.5 里这两个字段已 `deprecated`，
     同一个 union 的 `dma_burst_size` 不写即取驱动默认值）。
   * `rgb_flush_cb()`：旧版"等帧结束 → 按 8KB 分块 → memcpy 进唯一那块 fb"**整套删除**，
     改成"memcpy + `esp_cache_msync(C2M)` 进 **back fb**，最后一次 flush 调 `request_swap()`"。
     **cache 回写的语义一个字没变**（旧版是驱动内部做的，现在由我们显式做，因为写的是自己拿到的 fb）。
   * LEDC：`ledcSetup(通道,f,bits)` + `ledcAttachPin(脚,通道)` + `ledcWrite(通道,duty)`
     → **`ledcAttach(脚,f,bits)` + `ledcWrite(脚,duty)`**（arduino-esp32 3.x 的 API 变更，
     按**脚**寻址、不再自己挑通道）。**频率 20kHz / 10 位 / 占空比 512（50%）一个数都没改。**
   * `++g_vsync` → `g_vsync = g_vsync + 1u`（C++20 起对 volatile 的自增已弃用，GCC 会报
     `-Wvolatile`；语义完全相同，写者仍然只有那个 ISR）。
   * 新增**只读诊断**：初始化末尾多打一行"双 fb 之后的空闲 PSRAM/heap"，整屏刷新时多打一行
     耗时。**不参与任何逻辑**。
   * **没有变的东西**（逐项核对过）：41 步 ST7701 初始化表、引脚表、7 个时序宏、
     3 线 SPI 的 9 位帧写法（`command_bits=1` + `address_bits=8` + `spics_io_num=-1`）、
     TCA9554 的 RST/CS/蜂鸣器顺序与延时、LVGL 的 PARTIAL 绘制缓冲尺寸与对齐要求、
     `dash_display.h` 的三个接口、`g_left = g_right` 的单屏口径。
2. **`lib/themetool/image_blob.cpp`（一处**类型**适配，值与语义未变）** ——
   `esp_partition_mmap()` 在 IDF 5.x 把句柄与 memory 枚举都挪到了 `esp_partition_*` 名下：
   `spi_flash_mmap_handle_t` → `esp_partition_mmap_handle_t`（都是 `uint32_t`），
   `SPI_FLASH_MMAP_DATA` → `ESP_PARTITION_MMAP_DATA`（都是 **0 = data 映射区**；
   两个枚举在 C++ 里**不能隐式互转**，所以名字必须跟着换）。用
   `#if ESP_IDF_VERSION >= ESP_IDF_VERSION_VAL(5,0,0)` 分流 ⇒ **旧平台的 env 走原来的名字**，
   两边都编得过、映射的还是同一块只读 data 区。

**没改、而且**编译验证过**不需要改的**（上一轮列的"预期漂移"里有三条其实不成立，如实记）：

* `lib/link/link_phy_uart.cpp`（UART PHY）—— **一个字没改**：`[env:esp32s3-rgb]` 带
  `-ULINK_PHY_UART`（沿用上一轮的口径：这条构建里 43/44 是"日志+回放口"），所以这个 TU
  根本不进新平台的构建；而它进的那几条 env（`esp32s3` / `linkloop`）仍在旧平台。
* `lib/dashcore/dash_log.h` 的 `DASH_LOG_UART0` —— **一个字没改**：`Serial0` 在 arduino-esp32
  3.3.9 里**仍然存在**（`HardwareSerial.h` 照样声明），`Serial`/`Serial0`/CDC 的判断口径没变。
* `src/dash_display_spi.cpp` 那段 `esp_lcd_panel_io_spi` —— **一个字没改**（它只属于
  `[env:esp32s3-spi]`，那条 env 仍在旧平台）。
* `lib/dashcore/van_phy_gpio.cpp`（`attachInterrupt` / `digitalRead`）、`src/main.cpp`
  （`ESP.get*` / `esp_partition_find_first` / `esp_timer`）、`lib/themetool/*`、`src/dash_ui.cpp`
  —— 全部**零改动**，在新平台上直接编过。
  （`-DBOARD_HAS_PSRAM` 照旧保留；`memory_type = qio_opi` 照旧 → 自检 `psram: 8187KB` ✓）

### ⑦ 本轮没做 / 拿不准的

* **撕裂的"肉眼"确认**：见 ③ —— 数字齐了，**眼睛那一步留给车主**。
* **PCLK 定 18MHz**（30MHz 实测过：帧率 +108/s、`timeout=0`，但**同一块 fb 的拷贝耗时从 19.9ms
  涨到 27.2ms** ⇒ PSRAM 争用上升，而本栈没开 bounce buffer ⇒ 有欠载花屏的风险）⇒ **没有理由为了
  +43Hz 扫描率去冒这个险**。要试只改一个数（`RGB_PIXEL_CLOCK_HZ`），`clk_src` 不用动
  （`LCD_CLK_SRC_PLL160M` 带小数分频，160/30 也凑得出 30.0MHz；旧注释里"30MHz 必须 PLL240M"
  是**旧驱动**的口径，已作废）。
* **bounce buffer 没开**：它能让 DMA 从内部 SRAM 取像素，但与"双 fb + 局部刷新"互斥 ——
  开了它驱动每帧要 CPU 搬 450KB 进 bounce buffer，CPU 直接被吃光。取舍写在 docs 9.5。
* **两块屏共用一条总线**那件事**仍未做**（本文件还是单屏版本）—— 但换栈之后**硬阻塞没了**：
  `num_fbs` / `get_frame_buffer` / 帧切换回调三样都在了，将来做双屏可以直接复用这套
  back/换帧结构。
* `tools/theme-editor/theme.json` **没碰**；CRC / SOF / `kSpeedScale=2.56` / VAN 脚极性 **没动**；
  **没**新开 L 号；真仓库**没**跑 `pio clean`、**没**删 `.pio`；烧写**只认 COM6**（**没**烧 COM7）；
  构建与烧写都在 `C:\206dash-scratch\Neru`（ASCII 副本）。
* 本机 PlatformIO 的 Python 原本**校验不了 TLS 证书**（系统根证书在 certifi 里没有 ⇒ 任何
  `pio` 下载都 `CERTIFICATE_VERIFY_FAILED`）。本轮把 Windows 根证书导成 PEM 合进
  `C:\.platformio\penv\...\certifi\cacert.pem`（原文件备份为 `cacert.pem.orig`）才拉得下来。
  ★ 这是**本机工具链**的一处改动，不在仓库里，但下一个人在同一台机器上跑 `pio` 会受益。

---

## 2.8C **"残留 + 横纹/抖动"三轮收口**：bounce + 两块 fb 收敛 + 补拷小碎步（2026-09-24 实机，第三轮）

上一轮（本文件上一节）把撕裂的**结构性**根因查清了并换到 IDF 5.5 + 双 framebuffer。**实机结果是
"撕裂还在 + 新出现残留"** ⇒ 本轮从源头重查，一共收口了**三个不同的机制**（每一轮换的都是机制，
不是参数）。

### ① 车主的四次反馈 → 三个根因（每一次都由车主的观察定方向）

| # | 车主看到的现象（要点） | 真根因 | 证据（可复核） | 治法 | 结果 |
|---|---|---|---|---|---|
| 1 | 撕裂仍在 **+ 新出现"图像残留"** | **换帧根本没生效** | IDF 5.5.4 的 `esp_lcd_panel_rgb.c`：S3 上 `RGB_LCD_NEEDS_SEPARATE_RESTART_LINK=1`，restart link **固定挂在 `fbs[0]`** 且 `cur_fb_index` 变了也不重挂；而本栈 sdkconfig 里 `CONFIG_LCD_RGB_RESTART_IN_VSYNC=1` ⇒ **每个 VSYNC 都把 DMA 重置回 `fbs[0]`** ⇒ 屏上永远只有 `fbs[0]`（写进另一块的那些刷新**根本没上屏** = 残留；写进 `fbs[0]` 的那些轮 = 写在正在扫的显存上 = 撕裂） | **开 bounce buffer**（`-DRGB_BOUNCE_LINES=10`，**微雪官方 2.8C IDF/Arduino 两份例程的默认取值** `10 * H_RES`）：DMA 只读内部 SRAM，而"装哪块 fb"由驱动在**帧边界**锁进 `bb_fb_index` | 撕裂基本没了 ✓ |
| 2 | **残留**："表情已经切到下一段了，但每秒刷新时又回到上一段的那个表情；进度条也有残留" | **两块 fb 内容不一致** | 局部刷新只喂了"当次的后台"那一块 ⇒ 两块 fb 分叉，换帧就是在两块之间来回翻 | **每次刷新把上一次的脏区补拷进另一块 fb**（两块逐帧收敛） | **残留修好 ✓✓（车主确认）** |
| 3 | **抖动 + 横纹（随刷新移动）**；补一条决定性观察：**扫表阶段正常、稳态才出现** | **补拷的"大突发"把 bounce 填充挤掉** | 稳态每次刷新是一笔 ~90KB 的 PSRAM 拷贝；实测一笔 450KB 要 **33739µs（13MB/s）**；那一笔持续几毫秒，期间 bounce 来不及填 ⇒ 那一帧吐旧行 ⇒ 横带；扫表时画面持续变化把横带盖掉 ⇒ "扫表正常、稳态才暴露" | **补拷改成"空闲时间小碎步"（每步 8 行 ≈7.68KB）** + 关掉"全屏重绘"对照档（它自己就是 ~11 张整屏/秒的大突发） + PCLK 18 → 15MHz | **`copy_max` 33739µs → 3059µs（降 11 倍）；`refresh` 192~211ms → 199~201ms**；横纹/抖动**待车主再看** |

★ **第 3 条的两个"不是"**（都逐条查过，写下来免得下次走回头路）：
* **不是"拷贝没完成就换帧"**：`blit_area()` 的 `memcpy` + `esp_cache_msync(C2M)` 都是**同步**完成的，
  `request_swap()` 只在**本次刷新的最后一块** flush 里调用 ⇒ 没有那条路径 ✗；
* **不是"换帧等错了对象"**：`wait_swap_settled()` 等的是**"换帧已生效"**（驱动在帧边界锁存
  `bb_fb_index` = 我们的 `wrap` 计数），拷贝在它之前早就同步做完了 ✓ 两者没有混用 ✓。
* 也不是背光 PWM 那条（横纹**随刷新移动** ⇒ 不是固定干扰 ✓）、不是告警闪（实测 `fullrb=0/s`
  ⇒ 稳态下**没有**整屏失效 ✓）。

### ② 客观数字（COM6 原始串口行；四个版本对照）

| 指标 | 18MHz + 自动交替(10s) | 18MHz + 交替关 | 15MHz + **整笔**补拷 | **15MHz + 小碎步补拷（交付档）** |
|---|---|---|---|---|
| `pclk` / `vsync` | 18MHz / +64~66/s | 18MHz / +64~66/s | 15MHz / +53~54/s | **15MHz / +53~54/s**（15e6/548/508 = 53.9 ✓） |
| **`copy_max`**（单笔最大 PSRAM 搬运） | 36.7ms | 33.7ms ✗ | **33.7ms** ✗ | **3.06ms** ✓✓ |
| `refresh=avg/min/max`（刷新间隔） | — | — | 199/192/211ms | **200/199/201ms** ✓ |
| `fullrb`（整屏刷新频次） | **~11/s** ✗ | 0/s ✓ | 0/s ✓ | **0/s** ✓ |
| `catchup` | — | — | +5/s、~450KB/s | **+5/s、429~466KB/s**（总字节一样，只是摊开） |
| `bounce`（填充负载） | 28~31MB/s | 28~29MB/s | 23MB/s | **23MB/s** ✓ |
| `wrap` vs `vsync` | 1:1 | 1:1 | 1:1 | **1:1** ✓ |
| `timeout` / heap（内部）/ PSRAM | 0 / 183KB / 7285KB | 同 | 同 | **0 / 183KB / 7285KB** ✓ |
| 应用层 | `206 dash ok …` / `BEACON` / `SRC*` / `link:` 每秒照旧，`heap=183KB` 稳定 | 同 | 同 | **同** ✓（bounce 的 23MB/s 没把主循环/VAN 挤坏） |

### ③ 车主主观（**唯一收口判据**）

* **残留：已修好 ✓✓**（车主明确确认）；
* **撕裂：基本没了 ✓**；
* **抖动 + 横纹：本轮改动后待车主再看**（客观数字上：大突发降 11 倍、刷新间隔从 ±16ms 收到 ±1ms）。

### ④ 车主三次"看"的结论与本轮**仍然欠**的一项

* **官方 Demo 对照（同一块板/同一根线/同一电源）没烧** —— 这是排除供电与线材的**唯一干净做法**，
  **欠着**。没烧的两条理由：① 我这边只能读串口、**看不到屏**，烧完照样要占车主一次"看屏"，
  而那一次更该花在**修好的固件**上；② 它是 16MB 整片镜像（写 `0x0`，覆盖整片 flash）。
  ★ 已查：本机 `theme` / `image` 分区**本来就是空的**（开机日志 `theme: 分区为空,用默认主题` /
  `image: 镜像无效或未刷入`）⇒ 烧它**不会丢车上的数据** ✓。备份/烧写/回烧三条命令抄在
  `docs/RGB-PANEL-2.8C.md` 的 11.7。

### ⑤ 改动清单（**只动 2.8C 那条 env 与一个源文件**）

* `platformio.ini`：`[env:esp32s3-rgb]` 段**纯插入** 18 行（`git diff` 里 **0 删除**）——
  `-DRGB_BOUNCE_LINES=10`、`-DRGB_FULL_REFRESH_DEFAULT=0`、`-DRGB_FULL_REFRESH_ALTERNATE_MS=0`；
* `src/dash_display_rgb.cpp`：
  * **PCLK 18 → 15MHz**（只改 `RGB_PIXEL_CLOCK_HZ` 一个数，`clk_src` 不动）；
  * `cfg.bounce_buffer_size_px = 10 * THEME_DISPLAY_RES`（官方例程取值）；
  * 注册 `on_frame_buf_complete`（`wrap` 计数 = "换帧在帧边界锁存"的唯一判据）；
  * `wait_swap_settled()` 的屏障改成 `wrap`（等"换帧已生效"，不是"大概过了个 vsync"）；
  * 新增**待补拷清单 + 小碎步补拷**（`pend_step()` / `pend_finish()`，每步 8 行）；
  * 新增**只读诊断**：`wrap` / `catchup` / `bounce` / `refresh` / `fullrb` / `phase_max`；
  * 全屏重绘对照档（`lv_obj_invalidate(lv_screen_active())`）保留但**默认关**（`ALTERNATE=0`）。

### ⑥ 计数与红线复核

* **只动 2.8C 那条 env** ✓（`git diff platformio.ini` = 纯插入）；`[env:esp32s3]`（抓帧盒）与
  `[env:esp32dev]` 的**平台钉法（`espressif32@7.1.3`）一字未动** ✓；
* **没有新开 L 号** ✓；240(DualEye) SPI 驱动、`tools/theme-editor/theme.json`、
  CRC / SOF / `kSpeedScale=2.56` / VAN 脚极性 **一个都没碰** ✓；
* **只烧 COM6** ✓（COM7 一次没碰）；**没跑 `pio clean`、没删 `.pio`** ✓；构建与烧写全部在
  `C:\206dash-scratch\Neru`（ASCII 副本）✓；
* **native / JS 五套**：本轮**没有改 `lib/`、没有改 `test/`**，也没有改 native / pcpreview 的编译口径
  （`src/dash_display_rgb.cpp` 只在 `-DDASH_DISPLAY_RGB` 下参与编译）⇒ 与上一节 ⑤ 的记录**逐位相同**；
  **本轮已复跑，数字与基线一致** ✓：
  * **native**：`pio test -e native` ⇒ **234 test cases: 2 skipped, 232 succeeded**（0 失败）✓
    ★ 前置条件（踩过一次，写在这儿）:这台台式机**没有系统 gcc**，必须把 **`C:\206dash-scratch\zigbin`**
    （zig 转发桩：`gcc.cmd`/`cc.cmd`/`g++.cmd`/`c++.cmd` → `C:\ziglang\zig.exe cc %*`）**挂到 PATH 最前面**，
    否则会在编译阶段报 `'gcc' is not recognized as an internal or external command` ⇒ 那是环境问题、
    **不是回归** ✓；
  * **JS 五套**（`node tools/theme-editor/test-*.js`）：**71 / 429 / 259 / 155 / 369** 全绿 ✓
    （外加 `syntax-check-pages.js` **12** ✓）。
* **五个 env**：本轮重建 `esp32s3-rgb` 两次 —— 最后一次（第六轮，绘制缓冲 16 行）是
  **RAM 37.6% / 123,080B，Flash 84.8% / 889,247B**（RAM 比上一版少 34.5KB：绘制缓冲 40→16 行）；
  另四条 env（`esp32dev` / `esp32s3` / `esp32s3-linkloop` / `pcpreview`）本轮**没有重建** ——
  改动只在 2.8C 那条 env 的 build_flags 与一个只在 RGB 构建里编译的源文件里，
  对它们**没有可达路径** ✓。

### ⑦ 本轮没做 / 拿不准的（如实列）

* **横纹/抖动的最终结论**：客观数字已经大幅改善（大突发 33.7ms → 3.06ms、刷新间隔 ±16ms → ±1ms），
  但**"消没消"只能由车主看一眼** ⇒ 留给车主；
* **官方 Demo 对照**：欠着（见 ④）；
* **PCLK 15MHz 是不是"够"**：15MHz 把消耗速率从 36MB/s 压到 30MB/s（−17%），若横纹只是好转就把
  `RGB_PIXEL_CLOCK_HZ` 再改成 **12MHz**（24MB/s、43.2Hz；仍只改一个数）；再不够就
  `-DRGB_BOUNCE_LINES=40`（绝对余量 ×4，代价 76.8KB 内部 SRAM）；
* **catch-up 的两条备选路**（本轮选了"小碎步"）：① 用 `lv_obj_invalidate_area()` 让 LVGL 把脏区
  **重画一遍**顶掉拷贝（PSRAM 流量更小，但每次刷新都会再触发一次重画 ⇒ 会变成 30Hz 常亮刷新，
  代价反而大 ✗）；② 干脆单 fb + bounce + 写像素锁在帧边界之间（**微雪官方默认档**，但我们的
  UI 单次脏区 ~90KB，写它要 ~6ms > 一帧的消隐期 ⇒ 会撕 ✗）。两条都记在 docs 11.6。

### ⑧ 第六轮（同日稍后）：车主"**时有时无**"⇒ 把**仅剩的那一笔**也压下去

车主反馈"只是好转了一点：**有时消失、有时继续抖动+横纹**" ⇒ 这是**带宽争用**的指纹
（余量够就没事，一有别的 PSRAM 活动就被吃掉）。查下来还剩一笔大搬运：**"当次脏区"那一笔**
（= 一次 LVGL flush 的数据量 = 绘制缓冲高 × 480 × 2B；40 行 ⇒ 38.4KB ⇒ 实测 `blit_max ≈ 3ms` ✗）。
它**没法再摊平**（必须等 LVGL 回用这块缓冲才能 `flush_ready` ⇒ 不能跨圈拖着拷 ✗），
所以**只改了一个变量：绘制缓冲 40 行 → 16 行（15.36KB）** —— 依据是两块 bounce buffer 合计
19.2KB、只能吸收这个量级的赤字，**15.36KB < 19.2KB ⇒ 这一笔能被吸收** ✓（40 行的 38.4KB 不能 ✗）。

**实测（COM6 原始串口行）**：

```
rgb: 脏区单笔上限=16行(15360B) —— bounce 两块共18KB,能吸收 ~19KB 赤字
rgb: vsync=242(+54/s) wrap=242(+54/s) swap=48(+5/s) flush=211 blit_max=1325us step_max=950us copy_max=1325us copy_avg=695us swap_wait_max=10675us phase_max=17106us timeout=0 fb=0/1 catchup=48(+5/s 436KB/s forced0KB) refresh=200898/199362/205120us fullrb=0/s bounce=23MB/s 模式=局部刷新 psram=7285KB heap=217KB
```

| 指标 | 40 行（第五轮） | **16 行（本轮交付档）** |
|---|---|---|
| `blit_max`（当次脏区那一笔，**车主要的诊断**） | ~3011~33739µs ✗ | **1325~1332µs** ✓ |
| `step_max`（补拷一步 8 行） | — | **950~966µs** ✓ |
| `copy_max`（全系统最大单笔 PSRAM 搬运） | **33739µs** ✗ | **1332µs** ✓（**降 25 倍**） |
| `catchup=…forced?KB` | ~450KB/s | **+5/s、429~461KB/s、`forced0KB`** ✓（兜底一次没触发） |
| `refresh=avg/min/max` | 199/192/211ms | **199~201 / 186~199 / 205~214ms** ✓ |
| `wrap`vs`vsync` / `timeout` / `fullrb` / `bounce` / PSRAM | 1:1 / 0 / 0/s / 23MB/s / 7285KB | **1:1 / 0 / 0/s / 23MB/s / 7285KB** ✓ |
| **RAM / heap（内部）** | 48.1% / 183KB | **37.6% / 123,080B；heap 217KB** ✓（省 34.5KB） |
| Flash | 84.8% | 84.8%（889,247B） |

⇒ **全系统最大单笔 PSRAM 搬运：33.7ms → 1.33ms（两轮共降 25 倍）**，现在任何一笔都落在
bounce 能吸收的范围内。**主观结论仍然只能由车主那一眼收口**（本轮**没有**再叠加别的不确定改动：
PCLK 15MHz、bounce 10 行都保持不变，一次只动绘制缓冲行数 ✓）。

若还"时有时无"，按 docs 12.4 的顺序各来一次：① PCLK 15→12MHz；② `-DRGB_BOUNCE_LINES=40`；
③ 两个便宜的排除法（静音 1Hz 日志 30 秒 / 对齐时间戳看是否与 UI 刷新同拍）。

---

## 2.8C 板载蜂鸣器：**有源 / 无源**自检（2026-09-24，临时开关 `-DBUZZER_SELFTEST=1`）

**一句话**：车主提的那半问（L14 之前那一步：这颗 `Buzzer` 到底是**有源**还是**无源**）——
做了一条**临时自检路径**（默认关、不进 `alerts`/`buzzer` 抽象、由 `dash_display_poll()` 驱动、
`main.cpp` 一行没改），一次把四段波形打上串口标记，让车主把"听到什么"对上"哪一步"。
完整口径（四段判据、复现命令、原始串口行、两种结论各自的接线方案）在
`docs/RGB-PANEL-2.8C.md` **第 13 节**。

### ① 自检的四段与为什么这么排

| 段 | 波形 | 时长 | 判据 |
|---|---|---|---|
| ① 直流开关 | EXIO8 高 200ms / 低 200ms × 5 轮 | 2.0s | 有源 ⇒ "哔"5 声；无源 ⇒ 只有咔哒 |
| ② 方波 | 200 / 1k / 2k / 4k Hz 各 1 秒 | 4×1.3s | 音高随之变 ⇒ 像无源；只咔哒 ⇒ 像有源 |
| **③ 持续拉高（不翻转）** | 稳定直流 2 秒 | 2.0s | **不间断稳定音 ⇒ 有源**；咔哒+中间静音 ⇒ 无源 |
| ④ 持续拉低（对照） | 稳定 0 2 秒 | 2.0s | 应当完全静音（排除"响的是别的东西"） |

★ **③ 才是唯一定性判据**（本轮补上）：**有源**蜂鸣器被高频通断时"听起来也像音高在变"
（那是在**斩波它自己的输出**），**无源**在直流段只会咔哒 —— 所以 ①② 那两条对两种解释**都相容**。

### ② 实测（COM6 原始串口行）

```
buzzer: ② 方波 200Hz 段结束:翻转 332 次 / 实际 1002.477ms ⇒ **实测平均 ≈165.5Hz**(请求 200Hz)
        事务 334 次、平均 178us/次(= 音高上限 ≈2808Hz);两次翻转最大间隔 41801us;跳过格点 68 个
        自检占用 765.82ms(76.3% 的段时长);主循环间隔 平均3671us/最大42919us
buzzer: ② 方波 1000Hz 段结束:翻转 1760 次 / 实际 1000.136ms ⇒ **实测平均 ≈879.8Hz**(请求 1000Hz)
        事务 1762 次、平均 147us/次(= 音高上限 ≈3401Hz);跳过格点 240 个
buzzer: ② 方波 2000Hz 段结束:翻转 3070 次 / 实际 1011.746ms ⇒ **实测平均 ≈1517.1Hz**(请求 2000Hz)
buzzer: ② 方波 4000Hz 段结束:翻转 2910 次 / 实际 1000.009ms ⇒ **实测平均 ≈1454.9Hz**(请求 4000Hz)
        事务 2912 次、平均 141us/次;跳过格点 5090 个  <-- 请求频率超出 I2C 事务能力(预期之内)
buzzer: 回读 TCA9554 输出寄存器=0x05(本机影子=0x05)
buzzer: 期望 0x05 = LCD_RST(EXIO1)高 + LCD_CS(EXIO3)高 + 蜂鸣器(EXIO8)低 ⇒ 屏没被扰动、蜂鸣器已静音
```

* EXIO8 在 I2C 扩展器上 ⇒ **每次翻转一次事务**（400kHz 档实测 **141~178µs**）⇒ 音高上限 ~2.8~3.5kHz、
  **4kHz 那一段物理到不了**（只跑出 ≈1455Hz）；占空比与节奏都**很糙**（预期之内，只判有源/无源，不判音质）。
* 收尾回读 **0x05** ⇒ 自检**没写坏扩展器**、`RST`/`CS` 全程为高（屏没被扰动）、EXIO8 收在低（静音）。

### ③ UI / 内存（自检那 17 秒里，客观）

`206 dash ok` 每秒不断、`BEACON 1~40` 全在（step=6/7/8 轮转）、**heap 217KB 与 psram 7285KB 自检前后不变**、
`rgb: vsync +54/s wrap +54/s timeout=0`、`blit_max 1.28~1.34ms / catchup +5/s` 与自检前同量级 ⇒
**没有卡 UI、没有恶化上一轮收口的 PSRAM 搬运**。

### ④ 改动与体积

* **只动 2 个文件**：`src/dash_display_rgb.cpp`（`#if BUZZER_SELFTEST`，**默认 0**；`main.cpp`、`lib/dashcore/buzzer.h`
  一个字没动）+ `docs/RGB-PANEL-2.8C.md`、`ACCEPTANCE.md`（本节）。
  `platformio.ini` **不落盘**：开关只在**自检档**临时加一行，测完删掉（ASCII 副本里的那份与仓库逐字节一致）。
* **默认构建恢复原样（逐位相同）**：自检档 RAM 37.6%/123,136B、Flash 85.3%/894,027B ⇒
  删掉开关重建后 **RAM 37.6% / 123,080B、Flash 84.8% / 889,247B**，与改动前基线**逐位相同** ✓，
  并且**已回烧 COM6**（板上现在是默认档固件）。
* **回归**：native（挂 `C:\206dash-scratch\zigbin` zig 桩）⇒ **234 test cases: 2 skipped, 232 succeeded** ✓；
  JS 五套 ⇒ **71 / 429 / 259 / 155 / 369** ✓（外加 `syntax-check-pages.js` **12** ✓）—— 与基线一致，**没有降**。

### ⑤ 结论（**唯一定性判据在车主耳朵上**）

* 车主第一轮反馈：① **"哔"5 声 ✓**、② **出声且音高随频率变化 ✓** —— 但按 ①（本节第一张表）
  的论证，这两条**对"有源/无源"都相容**，**不能据此定性** ✗。
* 本轮固件补了 **③ 持续拉高 2 秒 / ④ 持续拉低 2 秒**：**听"一整段不间断的稳定音"⇒有源；
  听"咔哒 + 中间静音"⇒无源**（④ 应当完全静音）。**这一句要等车主回话**，回来即落在这里与 docs 13.6。
* 「接真机那一档怎么接」**只写方案不实现**（有源 ⇒ 一个 `Buzzer` 子类 + EXIO8 开/关，`alerts` 一行不改；
  无源 ⇒ 经 EXIO8 只能很粗糙地驱动、占比 71~84% 的主循环时间，要做音调得另找 LEDC 硬件 PWM 的出路）
  —— 方案见 docs 13.7，**等车主点头**。

### ⑥ 红线复核

只动 `[env:esp32s3-rgb]` 的**临时**构建与 `src/dash_display_rgb.cpp` ✓；**没碰**抓帧盒/esp32dev 的
`espressif32@7.1.3` 钉法 ✓；**没碰** 240(DualEye) 驱动 ✓；**没碰** CRC/SOF/`kSpeedScale=2.56`/VAN 极性 ✓；
**没新开 L 号** ✓；**没碰** `tools/theme-editor/theme.json`（仍未跟踪）✓；**只烧 COM6**（COM7 一次没碰）✓；
构建与烧写全在 `C:\206dash-scratch\Neru`（ASCII 副本）✓；**没跑** `pio clean`、**没删** `.pio` ✓。

### ⑦ 【结论】蜂鸣器 = **有源**（自带振荡，只会响/不响，无音调、无 PWM 通路、音量不可调）—— 2026-09-24 车主实测

**车主的三问回答**：① 直流 5 轮 ⇒ **"哔"5 声**；② 方波 200/1k/2k/4k ⇒
**换驱动频率音调不变**（判别性的一条）；③ 持续拉高 2 秒 ⇒ **持续长鸣**（不是两声咔哒）⇒ **有源**；
④ 持续拉低（对照）⇒ 静音；主观**较响、偏尖**。

**与电路事实吻合**：蜂鸣器挂在 **TCA9554 的 EXIO8**（I2C 扩展器的输出脚）⇒ 那条路上
**根本没有 LEDC/PWM 通路** ⇒ 原理上就**不可能**有不同音调，只能**开/关** —— "音调不变"是**设计使然**。

**设计影响**（只写方案，**本轮不改 `alerts` 实现**）：提示音只能靠**节奏与次数**区分
（1 短哔 / 2 短哔 / 长鸣）；**音量不可调** ⇒ **静音开关是必须功能**（`Alerts::setMuted()` 已有，
真机接线时要把它接到车主够得着的入口）。方案与接线形状见 `docs/RGB-PANEL-2.8C.md` **13.6 / 13.7**。

### ⑧ 当晚插曲：车主报"屏幕突然黑掉" ⇒ 自检**改版**（并把教训固化）

**先排除的一条**：**"主循环被自检阻塞"解释不了黑屏** —— 主循环被卡住时面板还在扫 framebuffer
⇒ 屏上是**定格**；**变黑 = 面板丢了初始化（或背光没了）**，而当时串口上**所有驱动计数都正常**
（`vsync=5900(+54/s)`、`swap=573`、`copy_max=1340us`、`timeout=0`、`206 dash ok`）。当时的原始行贴在
docs 13.8.1。**但那一版确实在主循环里自旋**（占段时长 71~84%），这不该做 ⇒ 照改。

**改版两条（已上板验证）**：
1. **自检整个搬进 core 0 的独立任务**（`xTaskCreatePinnedToCore`, 优先级 1, 8KB 栈）——
   `loopTask`/LVGL/flush/bounce 填充都在 core 1 ⇒ 自检**一个微秒都不占显示主循环**；
2. **运行期不再改 I2C 时钟**（删掉方波段的 `Wire.setClock(400kHz)`）—— 那颗 TCA9554 的输出寄存器里
   同时挂着 **LCD_RST/LCD_CS**，一个被打错的字节就是一次**面板复位**（而面板复位不会自己回来）。

**改版后的实测**（COM6 原始行）：自检期间 `vsync +54/s`、`wrap +54/s`、`swap +5/s`、`timeout=0`、
`blit_max 1.52ms`（与自检前同量级）、`206 dash ok` 每秒不断、`BEACON 1~40` 全在；
heap **自检中 208KB（任务 8KB 栈）→ 跑完回到 217KB**（任务确实自己 `vTaskDelete`）。

**仍未定论的一条**（如实记）：黑屏的两条候选机制（A：400kHz 打错字节复位面板 —— 已由改版去掉；
B：③ 持续通电 2 秒把 3.3V 轨拉低一下 —— 保留）**没有仪表能分辨**；能把它们分开的那条信息是
"**黑屏发生在哪一段**"，当时没记。下次复跑**先记这一条**。

### ⑨ 板上跑的是哪一版 + 回烧后的取证（**最重要**）

**当前 COM6 = 默认档固件**（`-DBUZZER_SELFTEST` 已删、重建、回烧；RAM 37.6%/123,080B、
Flash 84.8%/889,247B = 改动前基线逐位相同）。复位后 COM6 原始诊断行（自检固件**一行都没有**：

`buzzer:` 行数 = **0**）：

```
rgb: vsync=943(+54/s) wrap=943(+54/s) swap=113(+5/s) flush=577 blit_max=1332us step_max=968us copy_max=1332us copy_avg=685us swap_wait_max=10669us phase_max=18394us timeout=0 fb=1/0 catchup=113(+5/s 434KB/s forced0KB) refresh=200141/195371/204148us fullrb=0/s bounce=23MB/s 模式=局部刷新 psram=7285KB heap=217KB
206 dash ok  spd= 71% rpm= 15% coolant=91C face=idle/overspeed
BEACON 18  step=6(loop: 刚开始一轮)  uptime=18s heap=217KB psram=8192KB flash=16MB
rgb: vsync=997(+54/s) wrap=997(+54/s) swap=118(+5/s) flush=607 blit_max=1332us step_max=968us copy_max=1332us copy_avg=714us swap_wait_max=10669us phase_max=18394us timeout=0 fb=0/1 catchup=118(+5/s 434KB/s forced0KB) refresh=200320/199893/200721us fullrb=0/s bounce=23MB/s 模式=局部刷新 psram=7285KB heap=217KB
rgb: vsync=1050(+53/s) wrap=1050(+53/s) swap=123(+5/s) flush=643 blit_max=1332us step_max=968us copy_max=1332us copy_avg=739us swap_wait_max=10669us phase_max=18394us timeout=0 fb=1/0 catchup=123(+5/s 565KB/s forced0KB) refresh=199046/163989/226203us fullrb=0/s bounce=23MB/s 模式=局部刷新 psram=7285KB heap=217KB
```

⇒ `vsync` 有增量（+53~54/s）✓、`copy_max` 回到 **1332µs** ✓、`timeout=0` ✓、`fullrb=0/s` ✓。

---

## ⑨-bis 预览/主题编辑器跟上"2.8C 是最终板"（2026-09-24，**不动固件行为**）

范围：`env:pcpreview` + `tools/theme-editor/` + 文档。**没有**改 `src/dash_display*` 的
真驱动那一支、`lib/dashcore/*` 的任何判据、`platformio.ini` 的任何 env ——
固件行为逐位不变（本轮 `platformio.ini` **一个字节都没动**：2.8C 档是 `pcpreview`
的默认行为，不是新 env、也不是新编译开关）。

### ① `pcpreview` 的「2.8C（最终板）」档 = 落盘的帧上带"圆屏可视区"

* 板子口径：微雪 ESP32-S3-LCD-2.8C，480×480 圆屏，**可视圆 Ø70.13mm**
  （`PURCHASE.md` 第六节）、1 像素 = **0.1461 mm**、内切正方形 **336**。
* 几何**只有一份**：`lib/dashcore/panel_view.h`（纯头，不依赖 LVGL/Arduino ⇒ native 可测）。
  着色规则三段：圆内 256（**一个像素都不改**）、圆外 3px 圈带 128、更外四角 32/64 打点。
  **LVGL 帧缓冲不动** —— 压暗是在写 BMP 时对"落盘那一份"现算的（关掉遮罩不留痕）。
* 运行时开关：键 `V` / 控制文件 `mask=0`（默认开）。
* 落第一帧时打一行 banner（纯 ASCII）：
  `preview: panel 2.8C: 480x480 px = 70.13 mm active dia | inscribed square 336 px | corners outside the circle are NOT visible` ✓ 实测。
* 实测（本机 `C:\206dash-scratch`，注入时间线跑 17.5s，落 80 对帧）：
  圆外像素 `px(2,2)` = `0/4/0`（= 主题底色 20,20,20 的 1/8 + 斑点）✓；
  按 `mask=0` 之后同一像素回到 `16/20/16`（= 底色的 1/2，只有 1 px 边界被圈带压暗）✓。

### ② 六格指示灯条 + 告警在预览里**确实看得见**（逐帧读像素）

用 480 基准几何（`kLampSize=40 / kLampGap=6 / kLampCy=415 / kLampX0=105`）逐帧统计六格的
点亮像素数（亮格 340..690 px、误入格内的小字墨迹 ≈50 px ⇒ 阈值取 250）：

| 注入 | 帧上看到的 | 结论 |
|---|---|---|
| `low_beam=1` + `door=1` | `lamps=[Low,Door]` | 稳态灯不闪 ✓ |
| `+hazard=1` | 交替 `[L,R,Haz,Low,Door]` ↔ `[Low,Door]` | 转向/双闪**在闪** ✓ |
| `speed=130`（>120） | 左右两格借位闪 + 描边（`outline=[R,…]`）+ `alert: overspeed` + `BEEP pattern=urgent` | 超速告警 ✓ |
| `rpm=6000`（>5800） | 仪表盘灯格借位闪 + `alert: redline` + `BEEP pattern=triple` | 红区告警 ✓ |
| `door=1` 连续 200ms | `alert: door` + `BEEP pattern=long` + Door 格描边 | 门告警 ✓ |
| `left=1`（20 s 后 = 转向灯忘关） | 本演示只跑到"亮+闪"（忘关窗口 20s，未在预览里等够） | 判据在 native 用例里 ✓ |

★ **告警那一拍为什么会漏、以及怎么修的**（预览侧专有，判据/蜂鸣器一个字没改）：
`Alerts::beeping()` 只持续 `beep_ms` = 120ms，而预览 **200ms 才落一帧** ⇒ 真机 60fps 下
每次都拍得到、预览会漏。修法：脉冲开始时记住帧号，**保持到显示侧真的落了新的一帧**
（`dash_display_preview_frames()`），另有 1500ms 兜底。修前"按 O 偶尔不见闪"、修后逐帧可见 ✓。

### ③ 注入语义修掉一个真踩到的坑（**预览侧**）

控制文件是每帧重读的"绝对值"，但用户只写自己关心的那几位 ⇒ `hazard=1` 之后再来一次
`hazard=0`（或只写 `left=1`）时，上次写进 `indicator_left/right` 的 true 没人清，
屏上表现是"双闪关了、左右箭头还在闪"（实测踩到：演示脚本里写了 `left=1`，屏上却一直是双闪的样子）。
修法：`hazard` 在数据层的语义就是"两位同时置位"（`0x0C = 0x04|0x08`）⇒ 注入也照这个来，
**`hazard=0` 连左右箭头一起清**，并把顺序定成"双闪先、箭头后"（于是 `hazard=1 left=0 right=0`
这种组合仍然表达得出来）。纯逻辑搬进 `lib/dashcore/preview_input.h` 的
`preview_apply_snapshot()`（main.cpp 不参与 native 链接 ⇒ 不搬就测不到），用例钉住。

### ④ 主题编辑器：480 = 2.8C（最终板）/ 240 = 历史：DualEye（已退货）

* `image-blob-build.js`：`FACE_SIZE_TIERS[0].label` = `480×480 屏 · 2.8C（最终板）`；
  `[1].label` = `240×240 屏 · 历史：DualEye（已退货）`；`TARGETS.s3.label`
  = `ESP32-S3 N16R8(16MB flash) + 2.8C 圆屏（最终板）`（hint 里补了"四角不可见、矩形 ≤336"）；
  `TARGETS.s3_240.label` = `历史：ESP32-S3 微雪双屏 240×240(1.28", DualEye 已退货)`。
  **几何数字一个都没动**（320/300/163、160/152/81 逐位不变）。
* `asset-spec.js`：新增 `ROUND_PANEL`（480 档 `activeAreaMm10 = 7013` = Ø70.13mm；
  240 档 **留空** —— 那块板没量过有效区，不编数）+ `mmPerPixelX10000()`（480 档 = **1461**）。
  页面从这里取，不在 HTML 里再抄一份。
* `image-editor.html`：预览说明改成"按目标板的屏合成（默认 480×480 · 2.8C（最终板）…）"，
  并新增一行**圆形可视区**说明（Ø70.13mm / 0.1461 mm/px / 内切正方形 336 / 四角不可见）；
  刷写那栏原来写的"这块 DualEye 板走原生 USB 口，本机是 COM5"改成 2.8C 的实际口径
  （**板载 CH343P = 唯一的 Type-C 口**，见 `docs/RGB-PANEL-2.8C.md` 第 7 节），
  并把 COM5 那句明确标成"**针对 DualEye 的历史说明**"。
* `tools/theme-editor/README.md`：新增「★ 档位口径」表、「★ 2.8C（最终板）的素材规格」一节
  （含内切正方形 336 与"四角不可见"）与「2.8C 档」一节；240 那一节标题加上"历史档"。

### ⑤ 文档

新增 `docs/PREVIEW.md`：怎么跑（ASCII 副本 / zig 桩 / 两个环境变量）、每个键与每个注入项、
画布 px ↔ 面板 mm、**四角为什么不可见**、同一份代码在哪一层、
以及**第 4 节「预览看不到什么」**（撕裂/残留/横纹/抖动/帧率/面板时序/字体墨迹/物理边框
—— 逐条写清"为什么在这个预览里结构上不可能出现"）。
新增 `preview/preview-28c.html`（页面上画真机圆边虚线 + px↔mm 表 + 能验/验不了清单），
并把它加进 `syntax-check-pages.js` 的检查清单。

### ⑥ 回归数字（前 → 后）

| 项 | 基线 | 本轮 |
|---|---|---|
| `test-gauge-geometry` | 71 | **71** |
| `test-face-stages` | 429 | **429** |
| `test-theme-json` | 259 | **259** |
| `test-asset-spec` | 155 | **174**（+19，新增档位口径/物理口径/换算） |
| `test-image-blob-build` | 369 | **369** |
| `syntax-check-pages` | 12 | **14**（+2：多查一张预览页） |
| native（`-e native`） | 238 例（2 skipped / 236 succeeded） | **238 例（2 skipped / 236 succeeded）**，其中 `test_ui_lamps.cpp` 由 6 例 → **10 例**（+4：遮罩开关、注入→快照、圆屏几何、遮罩不误伤圆内） |
| `platformio run -e pcpreview` | SUCCESS | **SUCCESS**（含本轮改动） |

构建在纯 ASCII 副本里跑（`C:\206dash-scratch\Neru-pcp`），`PATH` 挂了
`C:\206dash-scratch\zigbin` 的 zig 转发桩；没跑 `pio clean`、没删 `.pio`。
`tools/theme-editor/theme.json`（未跟踪）全程未碰。

### ⑦ 红线复核

固件行为**未动**：`src/dash_display_rgb.cpp` / `dash_display_spi.cpp` / `platformio.ini`
本轮**零改动**；`lib/dashcore/*` 只动了 `preview_input.h`（新增预览注入的纯函数与两个键，
设备端不编这段调用点）；`src/main.cpp` 的改动全部落在 `#if defined(DASH_DISPLAY_PREVIEW)` 里。

---

## 系统状态：「数据不可信」提示 + 诊断页（2026-09-24，**本轮不烧板**）

★ **本轮刻意不烧板** ✗：另一个子代理正拿 **COM6** 测蜂鸣器（车主在听声音）⇒
**没有打开 COM6、没有烧写、没有上板**；全部验证走 `pcpreview` + native + JS。
**上板验证清单见本文第 ⑦ 条。**

### ① 做了哪两条（产品要求原文见 `ARCHITECTURE.md`「显示约定」）

- **「数据不可信」提示**：四个触发条件（来源回退 Sim / VAN 断流 >3 s /
  数据冻结 20 s / 双板链路 SimFallback），形态 = 表盘**右缘**一枚小角标
  （`SIM` / `VAN?` / `STALE` / `LINK`）+ **一声轻提示**（120 ms、可静音）；
  去抖 1500 ms 出现、800 ms 恢复；**数据恢复后自动消失**。
- **诊断页**：两页（数据这一路 / 链路与告警），**平时不显示**，
  `pcpreview` 用 `K` 唤出/翻页/关闭。内容**全部取本地现有信息**（零新数据源）。

判据 / 去抖 / 限速 / 字段映射全在 **`lib/dashcore/system_status.h` + `.cpp`**
（纯逻辑，不碰 LVGL/硬件）⇒ 预览与真机跑同一份；`dash_ui` 只负责把它画成像素。

### ② pcpreview 实测（可复制的演示步骤见 `tools/theme-editor/README.md`
###    「系统状态这两条」一节 + `docs/PREVIEW.md` 第 3 节）

一次跑完的实测（ASCII 副本 `C:\206dash-scratch\Neru-sys`，无板、无键盘：
用控制文件切状态，与按键走**同一条**判据）：

| 时刻 | 输入 | 串口（原始行，节选） | 落盘帧的像素结论 |
|---|---|---|---|
| 上电 ~1.5 s | （默认：预览无 VAN ⇒ 来源恒 Sim） | `trust: sim-fallback  <-- 屏上出现数据不可信提示 beep (episodes=1)` | `firstBadgeOn=9`（角标出现） |
| 写 `sim=1` 后 ~0.8 s | 数据层口径切成"实测" | `trust: none (episodes=1)` | `firstBadgeOffAfterOn=59`（角标消失） |
| 写 `sim=0` 后 ~1.5 s | 回到假数据 | `trust: sim-fallback … beep (episodes=2)` | `f=87 badge=ON` |
| 写 `diag=1` | 唤出诊断页 | `diag: open page=1/2` | `f=104 badge=off diag=OPEN`，`diag-open frames = 29 (104..132)`（整屏深色**且**有文字） |

逐像素核对工具（本轮新增）：`tools/theme-editor/check-system-status.js`
（`badge` / `diag` / `sweep` 三种用法）——
`diag` 的判据是"**底色深 + 有文字**"，因为**开机动画那几帧整屏也是黑的**，
只看亮度会把它们误判成"诊断页开着"（实测：`sweep` 因此跳掉前 8 帧）。

★ 一次真实踩坑（写下来免得下次再花半小时）：诊断页第一版**没给标签设尺寸**，
落到帧上**整屏全黑**（96% 采样点亮度 < 20），连表盘都没了 ——
多行文本的高度由字体度量算，而这个精简版驱动上那条路没验过；
标签自算出的尺寸把**整屏容器**撑出屏幕，而容器是 `LV_OPA_COVER` 的 ⇒
一次整屏重绘把自己（黑）盖满。修法：诊断页的文本是**定行数**的 ⇒ 尺寸写死。
（这条判据落在代码注释里，见 `dash_ui.cpp` 的 `build_diag`。）

### ③ 测试与构建（全部真跑，数字如下）

| 项 | 本轮起点 `f750b00` | 本轮 |
|---|---|---|
| native（`-e native`） | 234 例（2 skipped / 232 succeeded） | **253 例（2 skipped / 251 succeeded）**，全绿、0 失败 |
| `test-gauge-geometry` | 71 | **71** |
| `test-face-stages` | 429 | **429** |
| `test-theme-json` | 259 | **259** |
| `test-asset-spec` | 155 | **174**（+19，由并行那一轮的档位口径用例带入） |
| `test-image-blob-build` | 369 | **369** |
| `syntax-check-pages` | 12 | **14**（+2，同上） |
| `platformio run -e pcpreview` | SUCCESS | **SUCCESS** |
| `platformio run -e esp32s3-rgb` | SUCCESS | **SUCCESS**（RAM 38.3% / Flash 86.0%） |

native 的 **+19 例** = 本轮新增 `test/test_dashcore/test_system_status.cpp` **15 例**
（触发/恢复/去抖/限速/优先级/阈值/诊断页两页的字段映射/拿不到就是 `-`/页数边界/
名字对账/**预览那两个新键 K 与 T 的语义**）+ 并行那一轮的 `test_ui_lamps.cpp` **+4 例**。

★ 其中有一条**特意分两步断言**的用例（`test_sys_data_frozen`）：冻结窗口到点
（21000）时判据成立，但**提示还要过去抖窗口**（22500）才上屏 ——
第一版把这两件事当成一件事，用例当场红了，而**实现是对的**。
现在这条用例把"判据"与"什么时候上屏"分开钉住。

★ 构建在 ASCII 副本（`C:\206dash-scratch\Neru-sys`）里跑，`PATH` 挂了
`C:\206dash-scratch\zigbin` 的 zig 转发桩；**没跑 `pio clean`、没删 `.pio`**。
`tools/theme-editor/theme.json`（未跟踪）全程未碰。

★ 构建环境上**踩到一个与代码无关的坑**（如实记）：`esp32s3-rgb` 那一轮先是
`No module named 'esptool.__init__'` —— 全局 PlatformIO core 里的
`packages/tool-esptoolpy` 只剩两个 `.py` 残件（`esptool/` 目录下没有
`__init__.py`），是**工具链状态坏了**，不是仓库回归。修法 = 让 PlatformIO
重新装它（`tool-esptoolpy@5.3.0`），随后 **SUCCESS**。

### ④ 红线复核

- `src/dash_display_rgb.cpp`（蜂鸣器那个子代理在改）：**本轮一个字节都没碰** ✓
- `platformio.ini`：**一个字节都没碰** ✓
- 抓帧盒 / `esp32dev` 的 `espressif32@7.1.3` 钉法、240(DualEye) 驱动、
  CRC/SOF/`kSpeedScale=2.56`/VAN 脚极性：**全未动** ✓
- **没新开 L 号**，也没改任何既有 L 裁决（L11/L13 一个字没动）✓
- **没碰 `tools/theme-editor/theme.json`** ✓
- **没打开 COM6、没烧板** ✓

### ⑤ 与既有裁决的合规说明（**L11**）

`ARCHITECTURE.md` §8 的 **L11** 定的是「主板『从板离线』**角标** v1 不做，
从板在线状态只进 `STATUS` 日志、不上屏」。**诊断页不是常驻角标** ⇒ 不违反：
它平时**完全不显示**、只由长按/组合键主动唤出、退出即消失；L11 禁的是
"让从板在线状态**常驻**在表盘上"，而诊断页是**主动查询**式的
（与"插上 USB-C 看串口"同一类行为，只是换了个出口 —— 串口那一行照旧）。
诊断页第 2 页在**主板**上明文写着 `link - (master: log only)`：
它读的是**从板自己的 `LinkTime`**，不是主板对"从板在不在线"的判断（L13 也未动）。

### ⑥ 提示音（有源蜂鸣器）+ 静音

- 2.8C 那颗蜂鸣器挂在 **TCA9554 的 `EXIO8`**（I2C 扩展脚）⇒ **没有 PWM 通路**
  ⇒ 只有**开/关**两个自由度（**时长 + 次数**，不变调、音量也不可调）。
- 本轮那个"一声轻提示" = `BeepPattern::Short` + `kTrustBeepMs = 120 ms`，
  每次"变坏"只响一次、两次之间至少隔 5 s、**静音开关优先**；
  `static_assert(kTrustBeepMs <= 300u)` 钉住"单次哔 ≤ 300 ms"这条硬上限。
- **静音状态掉电保存**：`Preferences`（NVS，命名空间 `dash`、键 `mute`）
  在 `setup()` 读回、开关时写入；**默认有声**，静音是车主的选择。
  预览端**刻意不落盘**（免得"重新跑一次预览"继承上一次的静音）。
- `BeepPattern::Long`（长哔）在真机上**暂时不许用**（机制 B 未排除）⇒
  文档写明 **`long` ⇒ 真机降级为 3 短哔**，**待 B 排除后可恢复**；
  本轮**只写文档**，`alerts` 的实现与映射一个字没改（见
  `ARCHITECTURE.md`「提示音说明」第 3 条）。

### ⑦ 待上板验证清单（本轮**没烧板**）
1. **角标在真车上的触发/恢复**：拔 VAN 3 s 后角标是否出现；插回后 0.8 s 内是否消失；
   数据在阈值上下来回抖时**不许滴滴叫**（5 s 限速）。
2. **诊断页的真机入口**：长按/组合键**还没定** —— 这块板能当输入的只有 12PIN 的
   `GPIO0`（= BOOT strap，不建议）与排针上剩下的 `GPIO7`（I2C 的 SCL）。
   等最终板接线定案后写进 `ARCHITECTURE.md`（`main.cpp` 里已经是一个
   `dash_ui_diag_toggle()` 调用，接哪根线都不改这一层）。
3. **那一声轻提示的音量/时长在真机上是否正确**（有源蜂鸣器、不可变调）：
   听一声 120 ms 的短哔，并确认**没有长鸣**（机制 B 未排除）。
4. **`panel=` 那一格**：RGB 驱动还没有"面板帧率/flush/copy"的出口
   ⇒ 现在显示 `panel=- (driver n/a)`；等驱动那个 getter 落地后接上（**本轮不碰那个文件**）。
5. **静音掉电保存**：开机前设为静音 → 断电重上 → 是否仍是静音。

---

## 数据冻结的前提 + 诊断页真机入口（串口 `d`/`m`）+ **上板实测**（2026-09-24，COM6）

★ 本轮**上板了**（车主已批准；只碰 **COM6**，**COM7** 一次都没打开）：
起点 `59f6321` ⇒ 新固件烧进 COM6，下面是原始串口行。

### ① 数据冻结判据加了**前提**（消掉"停车熄火"那一类假警报）

- **改法**：冻结判据从"车速/转速 20 s 一个字节都没变"改成**同时要求**
  `rpm > kTrustEngineRunningRpm || speed_kmh > 0`（= **发动机在转 / 车在动**）。
- **常量**：`kTrustEngineRunningRpm = 500u`（`lib/dashcore/system_status.h`）——
  怠速约 700~900 ⇒ 500 以下一律当"不在转"；★ 判据是**严格大于**：`rpm` 恰好
  500 **不算**"在转"（边界由用例 `test_sys_freeze_rpm_boundary_500` 两侧钉住）。
- **为什么除了转速还要看车速**：万一 VAN 冻在"转速读 0 但车其实在跑"那一帧，
  只看 `rpm` 会把**真故障**漏掉 ⇒ 车在动时，静止的数据一律可疑。
- **为什么水温/进气不算进来**（写进头文件注释，免得以后"顺手补全"）：那两格
  20 秒不变是**正常**的（热惯量那么大）⇒ 算进来判据会一直成立、角标常挂。
- **代价**（`ARCHITECTURE.md`「显示约定」§2 同款）：**停车且熄火时的真实冻结
  不再报**；另有 ①来源回退 `Sim` ②VAN 断流 两条网仍在 —— 真坏了照样报。
- **用例**：`test/test_dashcore/test_system_status.cpp` 追加 **4 例**
  （十六节）：停车熄火 30 s ⇒ **不报** / `rpm=800` 冻结 20 s ⇒ **报** /
  `speed=60, rpm=0` 冻结 ⇒ **报** / 边界 `500` 不算、`501` 算。

### ② 诊断页的真机入口 = **串口单字符命令**（这块板上没有可用的按键）

板子上能当输入用的只剩 12PIN 的 `GPIO0`（BOOT strap）与排针上的 `GPIO7`
（I2C 的 SCL）⇒ 入口走**串口**（Type-C = 板载 CH343P = `Serial0`/UART0 = `COMx`，
本来就是看日志要接的那根线，**零额外引脚**）：

| 命令 | 作用 | 回执（原始串口行） |
|---|---|---|
| **`d`** | 诊断页：与预览的 `K` **同一个循环**（关着 ⇒ 打开第 1 页；还有下一页 ⇒ 翻页；最后一页 ⇒ 关闭） | `diag: open page=1/2` / `diag: open page=2/2` / `diag: closed page=2/2` |
| **`m`** | **静音开关取反 + 写 NVS**（掉电保存） | `mute: 1 (saved)` / `mute: 0 (saved)` |

- **编译门 = 既有的 `DASH_DISPLAY_RGB` 宏**（platformio.ini 里早就有、且**只有**
  `[env:esp32s3-rgb]` 定义）⇒ `[env:esp32s3]`/`[env:esp32dev]`（VAN 抓帧盒）与
  `-vaninv`/`-vansniff` 的编译单元里**这两段代码一行都不存在**。
  ★ **`platformio.ini` 一个字节都没动**（没有新增任何 `-D`）。
- 另加**一行**（同样只在 RGB 构建里）：`setup()` 里 `mute_load()` 之后打
  `mute: N (loaded from NVS)` —— "掉电保存成不成立"因此**可测**（复位后这一行就是证据）。
- ★ 命令**只认"行首"**（该口当前没有未完成的一行）：回放行里的十六进制**本来就可能
  含 `d`**（例 `VAN 824 18F8271D000000`）⇒ 不这么判会悄悄吃掉回放数据；**唯一例外**
  是"缓冲里那串字节不可能是回放行"（首字节不是 `V`/`v`）—— 那时也认命令并清掉它，
  免得一颗杂散字节把命令通道堵到下一个换行为止（上板用一颗 `x` 验过）。

### ③ 回归数字（前 → 后，全部真跑）

| 项 | 前 | 本 轮 |
|---|---|---|
| native（`-e native`，挂 zig 桩） | 253 例（2 skipped / 251 succeeded） | **257 例（2 skipped / 255 succeeded），0 失败** |
| `test-gauge-geometry` | 71 | **71** |
| `test-face-stages` | 429 | **429** |
| `test-theme-json` | 259 | **259** |
| `test-asset-spec` | 174 | **174** |
| `test-image-blob-build` | 369 | **369** |
| `syntax-check-pages` | 14 | **14** |
| `platformio run -e pcpreview` | SUCCESS | **SUCCESS**（19.0 s） |
| `platformio run -e esp32s3-rgb` | SUCCESS（RAM 38.3% / Flash 86.0% / **901,699 B**） | **SUCCESS**（RAM **38.3% / 125,416 B**；Flash **86.1% / 902,759 B**） |

- **Flash 涨了 1,060 B**（901,699 → 902,759；86.0% → 86.1%），**RAM 没动**（125,416 B）。
  涨的就是这两条命令 + 那一行 `mute: … (loaded from NVS)`。
- 构建在 ASCII 副本（`C:\206dash-scratch\Neru-frz`）里跑；**没跑 `pio clean`、没删 `.pio`**；
  `tools/theme-editor/theme.json`（未跟踪）全程未碰。
- ★ 两次**环境**报错（都不是回归，如实记）：`pcpreview` 第一次 8.4 s 就 FAILED、
  报 `'gcc' is not recognized` —— `PATH` 上没挂 `C:\206dash-scratch\zigbin` 的 zig 转发桩；
  挂上之后 **SUCCESS**（19.0 s）。

### ④ 上板实测（COM6；下面全是**原始串口行**）

**取证（先记下"板上原来是什么"，烧回用得到）**

- **开工前**（复位后抓的开机日志）：有 `206 dash boot`、有 `alerts: 已就绪(…)`、
  有第六轮那些 `rgb:` 行，**但一条 `trust:` 都没有** ⇒ **板上当时跑的不是
  `59f6321`（= 上一轮"健康显示"）那一版固件**，而是一个**早于 `a8ddea0`** 的
  esp32s3-rgb 构建。
- 两个镜像都留下来了（"一条命令烧回去"）：
  - **板上原样**（`esptool read_flash 0x10000 0x100000`，SHA256 `04BAAAB1…`）→
    `C:\206dash-scratch\asfound-app0.bin`；
  - **`59f6321` 的 esp32s3-rgb 构建**（本机重编，Flash 901,699 B，SHA256 `22E0735A…`）→
    `C:\206dash-scratch\rollback-59f6321-esp32s3-rgb.bin`。
- 烧回命令（esptool 5.3.0 在 `C:\206dash-scratch\pio-core-mix\penv`）：
  `python -m esptool --chip esp32s3 --port COM6 --baud 921600 --after hard_reset write_flash 0x10000 <那个 .bin>`

**烧写**：`python -m platformio run -e esp32s3-rgb -t upload --upload-port COM6`
⇒ `[SUCCESS] Took 44.96 seconds`、bootloader/partitions/boot_app0/app 四处
`Hash of data verified.`、`Hard resetting via RTS pin...`；**最终**烧进去的
`firmware.bin` SHA256 = `3C2E3682…`（下面 1~6 条都是在**这一份**上跑出来的；
中途只改过一次注释也重编重刷过 —— 镜像里带编译时间戳，所以以最后一次为准）。
★ 收尾时把板子**放回原来那个状态**：发一次 `m` ⇒ `mute: 0 (saved)`（开工前就是
"有声"；现在板上仍是这个状态，NVS 里 `mute=0`）。

1. **角标出现 + 一声轻提示**（复位后，`mute=0` 那一档）：

   ```
   mute: 0 (loaded from NVS)
   trust: none (episodes=0)
   trust: sim-fallback  <-- 屏上出现数据不可信提示 beep (episodes=1)
   ```

   （静音那一档同样验过：`trust: sim-fallback  <-- 屏上出现数据不可信提示 muted (episodes=1)`）
   ★ **"那一声是不是很短"没人替我听过** —— 机上没有能听声音的东西；客观证据是
   这一行里的 `beep` 标记 + `kTrustBeepMs = 120 ms`（`static_assert(<= 300 ms)` 钉着），
   **没有长鸣**这条限制由常量与断言保证。
   ★★ **补记（详见 ⑥-2，很重要）**：真机这一档的 `Buzzer` 是 **`BuzzerNull`（空实现）**
   ⇒ **那一声其实不会响**；这一行只说明"判据这一跳要求响、调用方确实调了它"。

2. **诊断页**（发 `d` 三次，原始行按顺序）：

   ```
   diag: open page=1/2
   diag: open page=2/2
   diag: closed page=2/2
   ```

   （回执格式与预览那一行**逐字同一条**；★ 一个既有口径：**关闭后再打开仍停在第 2 页**
   —— 页号不被关闭动作复位，预览的 `K` 本来就是这么走的，本轮没改。）

3. **诊断页里的数字是真的**：`heap=` / `psram=` 那一格的**同源**数字在真机日志里是
   真实值 —— `BEACON 17  step=6(loop: 刚开始一轮)  uptime=17s heap=214KB psram=8192KB flash=16MB`
   （预览里是 `heap=0 psram=-`）；`van frames=` 应为 **0**：台上没接 VAN ⇒
   整段日志**一行 `van: ` 都没有**、`SRC speed=sim …` + `link: sim tick_age=… seen=0`。
   ★ **如实说清边界**：屏幕上的那几格字**没有机器读回来**（板上没有回读通路、也没有相机）
   ⇒ 这一条的"像素级"证据**拿不到**；能证明的是"喂给它的数是真的"（同一份
   `ESP.getFreeHeap()`/`getFreePsram()`/帧计数）。`panel=` 那一格仍是 `- (driver n/a)`
   （RGB 驱动还没有那个出口，与上一轮同一条已知项）。

4. **静音持久化**（掉电保存 + 恢复有声）：

   ```
   mute: 1 (loaded from NVS)      ← 复位/重刷之后仍然记着"静音"
   diag: open page=1/2            ← 紧接着发 d
   diag: open page=2/2
   diag: closed page=2/2
   mute: 0 (saved)                ← 再发一次 m ⇒ 恢复有声
   ```

   旁证：静音那一段里 `SRC-VAN age … | alert=overspeed beeps=0 muted`（一声都没响），
   恢复之后同一行变成 `… alert=overspeed beeps=5`（又开始计哔数了）。

5. **显示没被搞坏**（四行原文，全部来自**烧完之后的最终固件**）：

   ```
   rgb: vsync=834(+54/s) wrap=834(+54/s) swap=103(+5/s) flush=519 blit_max=1324us step_max=948us copy_max=1324us copy_avg=713us swap_wait_max=3434us phase_max=18014us timeout=0 fb=1/0 catchup=103(+5/s 466KB/s forced0KB) refresh=201021/184836/216178us fullrb=0/s bounce=23MB/s 模式=局部刷新 psram=7285KB heap=214KB
   rgb: vsync=888(+54/s) wrap=888(+54/s) swap=108(+5/s) flush=549 blit_max=1324us step_max=948us copy_max=1324us copy_avg=695us swap_wait_max=3434us phase_max=18014us timeout=0 fb=0/1 catchup=108(+5/s 429KB/s forced0KB) refresh=199390/195242/201305us fullrb=0/s bounce=23MB/s 模式=局部刷新 psram=7285KB heap=214KB
   206 dash ok  spd= 83% rpm= 14% coolant=91C face=idle/overspeed
   206 dash ok  spd= 78% rpm= 13% coolant=91C face=idle/overspeed
   ```

   ⇒ `vsync +54/s`、`swap +5/s`、`copy_max≈1.32ms`、**`timeout=0`**、**`fullrb=0/s`**、
   `206 dash ok` 每秒不断 —— 与上一轮同一量级，**没有黑屏/花屏的驱动级征兆**
   （★ 屏幕本身是给人看的：机上没有相机，这条是**驱动级**证据）。

6. **台上"角标常挂"是预期**：没接 VAN ⇒ 四格来源全是 `Sim`、`link: sim` ⇒
   `trust: sim-fallback` 一直成立、角标挂着 —— **不是坏** ✗（这正是产品要求
   "不是实测数据就必须看得出来"在台面上的正常表现）。

### ⑤ 红线复核

- `platformio.ini`：**一个字节都没碰** ✓（两条命令的门用的是既有的 `DASH_DISPLAY_RGB`）
- `[env:esp32s3]`/`[env:esp32dev]`（抓帧盒）与其 `espressif32@7.1.3` 钉法：**未动** ✓
  （新增命令只编进 `[env:esp32s3-rgb]`；① 的判据改动在 `lib/dashcore/system_status.*`，
  那是 a8ddea0 起就有的共享显示层，**不碰 VAN 抓帧/协议那一路**）
- 240(DualEye) 驱动、CRC/SOF/`kSpeedScale=2.56`/VAN 脚极性：**全未动** ✓
- **没新开 L 号**、没改任何既有 L 裁决 ✓
- `tools/theme-editor/theme.json`（未跟踪）：**未碰** ✓
- **只开了 COM6**；**COM7 一次都没打开** ✓
- `src/dash_display_rgb.cpp` / `dash_display_spi.cpp`：**未动** ✓

### ⑥ 车主反馈 + 追加取证（同日补记）

1. ★ **角标位置经车主确认（真屏）**：车主看到了 `SIM` 角标，原话 **"SIM位置还可以"**
   ⇒「角标挂表盘**右缘**（480 基准 `x=340..456` / `y=205..241`）」这个设计选择**定稿、不再挪** ✓。
2. ★ **台上"角标常挂"是预期，不是故障**：台上没接 VAN ⇒ 四格来源全是 `Sim`、
   `link: sim` ⇒ `trust: sim-fallback` 一直成立 ⇒ 角标从开机就挂着。**别当 bug 查** ✗。
   ★★ **但"开机那一声短哔"在这份固件里其实不会响** —— 这一条要写清楚，免得两边误判：
   真机这一档的 `Buzzer` 还是 **`BuzzerNull`**（`src/main.cpp` 只给 pcpreview 挂
   `BuzzerHost`，`BuzzerNull::beep()` 是**空实现**）；TCA9554 `EXIO8` 那一档
   **只写方案、还没实现**（`ARCHITECTURE.md` §4 第 3 条 / `docs/RGB-PANEL-2.8C.md` §13.7）。
   ⇒ 日志里的 ` beep` 只代表"**判据这一跳要求响一声、调用方确实调了它**"，
   **不代表扬声器响了**：没听到**不是** bug；反过来，谁要真听到响了，那也不是这条路径
   （多半是 §13.3 那个自检档 `-DBUZZER_SELFTEST=1`，它默认关着）。
3. **`trust:` 那行的原文 + 出现时刻**（车主想确认"角标与那一声是不是同时"）：

   ```
   [  0.440s] mute: 0 (loaded from NVS)
   [  0.943s] trust: none (episodes=0)
   [  2.441s] trust: sim-fallback  <-- 屏上出现数据不可信提示 beep (episodes=1)
   ```

   （`[+X.XXXs]` = **宿主机**从"放开复位"那一刻起算的到达时刻，用
   `C:\206dash-scratch\com6-timed.ps1` 掐的；设备日志本身**没有**时间戳。）

   - **出现时刻 ≈ 开机后 2.44 s**；与设备自己的 1 Hz 行也对得上：它紧跟在
     `BEACON  2 … uptime=2s` 之后、`BEACON  3 … uptime=3s` 之前 ——
     **四次独立开机日志（含两次不同镜像）位置完全一致**。
   - **为什么正好是这个数**：第一次评估在 **0.943 s**（打出 `trust: none`），
     判据要**连续**成立 `kTrustSysDebounceMs = 1500 ms` ⇒ 0.943 + 1.500 = **2.443 s**，
     实测 **2.441 s** ⇒ 去抖窗口本身没跑偏。
   - **角标与"那一拍"是同一时刻**：这一行就打在 `g_buzzer->beep(...)` 与
     `dash_ui_render(...)` **之间**（同一次 200 ms 渲染拍）⇒ 屏上出现角标、
     与"要求响一声"**同一拍**，不存在先后差。
4. **车主当时正在看屏**：发 `d` 开 / 翻 / 关诊断页那三次他会瞄到；观感问题
   （跑出圆外 / 字看不清 / 整屏发黑）由他反馈 —— 本轮六项取证**不等他**，已全部做完。

---

## 诊断页可读性（"屏幕为什么黑了"）+ 弧永远在上（层序）+ 数据分区烧配置（2026-09-24 晚，COM6）

★ 本轮**上板了**（车主批准；只碰 **COM6**，**COM7 一次都没打开**）。
起点 `90853a8`（开工时 `git log -1` 实测）。★ 中途**别的子代理**提交了
`3b147e6` / `4be6846` —— 其中 `4be6846` 把 `tools/theme-editor/image-editor.html`
**整份进了索引**，于是本单的「预览侧层序改动」被它**带进了 HEAD**。
★ **图层顺序那部分先被 `4be6846` 带入，本轮补齐并验收**（不 amend、不回退、不重复应用）：

| 位置 | HEAD（`4be6846`）里已经有什么 | 本轮补了什么 |
|---|---|---|
| `tools/theme-editor/image-editor.html`（**预览侧**） | `drawScreen()` 已是 **背景 → 表情 → 弧 → 读数** ✓；`// 2. 表情图（在弧**下面**）` 那段说明 ✓ | 只补了**页面文案**那一句（`noteMain.textContent` 还写着"表情图盖在最上层" ✗）与两处注释 |
| `src/dash_ui.cpp`（**固件侧**） | ❌ **没有**：仍是 `build_arcs()` 在 `build_face()` **之前**（`887` vs `888`）、`g_face_img` 在**后面另一个循环**里建（`900`）⇒ **弧仍被表情压住** | ★ **整个固件侧层序**（本轮的主改动）+ 诊断页那几条 |
| `tools/theme-editor/README.md` 的「图层顺序」一节 | ❌ 没有 | 本轮新增 |
| `tools/theme-editor/asset-spec.js` | ❌ 没有 | 本轮新增注明（尺寸上限**一个数都没放宽**） |

⇒ **结论：被带进去的是"预览改了、固件没改"的半成品**；本轮把固件侧补齐，
并落帧实测两端一致（见下面 ②）。

### ① 诊断页"看起来全黑" —— 根因与修法（**像素实测**）

**车主原话**："屏幕为什么黑了"（发了 `d` 打开诊断页之后）。
**根因**：那一版底是 **`0x0A0A0A` 铺满整屏**（`LV_OPA_COVER`）。
圆屏上没有任何参照物 ⇒ 只能读成"黑了"。落帧实测**正是历史上判黑的那个数**：

| 指标（页区域 = 容器矩形 ∩ 可视圆，`n=160161` 像素） | 改前（`90853a8` 构建） | 改后 | 判据 |
|---|---|---|---|
| **页区域平均亮度** | **14.8** | **38.9** | ≥ 20 ✓ |
| **亮度 < 20 的采样点占比** | **95.3%** | **0.3%** | ≤ 60% ✓ |
| 标题带文字像素（`lum ≥ 150`） | 612 | **2296** | ≥ 100 ✓ |
| 正文框文字像素 | 2645 | **4794** | ≥ 300 ✓ |

★ 历史上这一页被判黑时的口径就是"**96% 的采样点亮度 < 20**"——
改前实测 **95.3%**，**与那次完全同一个现象**（所以车主的观感不是错觉）。

**改了四条**（判据与理由写在 `ARCHITECTURE.md`「显示约定」§3 的
「诊断页的可读性约定」）：

1. 底色**不铺满**：改成**内缩的圆角面板**（480 基准内缩 32、圆角 24），
   底色 `0x0A0A0A` → **`0x16212E`** ⇒ 四周露出表盘底色，一眼看出"这是一页界面"。
2. **可见描边**（琥珀 `0xFFB020`、2px）—— **失败也要看得见**（车主追加的硬要求）：
   哪怕一个字都没画出来，人也该看出"这里有东西"，而不是"板子死了"。
3. 标题/页码改用**大数字那一档**字体（480 档 **48 号**，改前是 18 号）；
   正文提到 `0xE8EDF2`（近白）。
4. ★ **文本一律 ASCII**：本构建**只使能 Montserrat**（`include/lv_conf.h`
   的 `LV_FONT_MONTSERRAT_*`）⇒ **没有 CJK 字形**。中文在这里不是"看不清"，
   是**一个字形都画不出来**。实测确认：**诊断页的两页正文本来就是纯 ASCII**
   （`diagBuild()` 由 `diagRenderText()` 拼装，`dataTrustReasonText()` 那条
   中文短文**从未被 UI 调用** ⇒ 车主看到的"连一个字都没有"**不是** CJK 造成的，
   而是**底色把 18 号的灰字压没了**）。这条取舍（要中文先引入 CJK 字体）写进文档。

★ **每个标签的尺寸显式写死**（这条在代码注释里，是上一版踩过的坑）：
第一版没给多行标签设尺寸 ⇒ LVGL 自算高度把整屏容器撑出屏幕 ⇒ 一次整屏重绘
把它自己盖满 ⇒ **整屏全黑**。

★ **几何自证日志**（打开时打**一行**，只打一次）——"页面上什么都没有"
从此能从串口一眼判出来：

```
diag: geom box=416x416 at 32,32 title=384x56 at 58,50 body=368x320 at 58,114 font title=m48/lh52 body=m18/lh21 page=1/2
```

★★ **这一行在真机上第一次打出来是 `box=0x0 at 0,0`** —— 如实记：
根因是 LVGL 的 `lv_obj_get_width()/lv_obj_get_coords()` 读的是**上一次布局的结果**，
而 `build_diag()` 建完就是 `HIDDEN`、**一帧都没画过** ⇒ `coords` 全是 0。
修法 = 读之前先 `lv_obj_remove_flag(HIDDEN)` + `lv_obj_update_layout()`。
**⇒ 这一行自己先踩了一次坑，而它在串口上把真相说出来了**（所以它必须留着）。

★ **每次打开都从第 1 页开始**（车主："关闭后重开停在第 2 页这个别扭一并改掉"）：
复位写在**打开的那一次**（`dash_ui_diag_toggle()`），
"关→开"与"开机后第一次开"走同一条路径、只有一处判据；
**预览的 `K` 与真机的 `d` 语义保持一致**。真机实测（原始串口行）：

```
>>> d (open)        diag: open page=1/2
>>> d (page 2)      diag: open page=2/2
>>> d (close)       diag: closed page=2/2
>>> d (reopen)      diag: open page=1/2      ← ★ 改前这里会是 page=2/2
```

**判据脚本**：`tools/theme-editor/check-diag-page.js`（新增，读落帧给上面那三个数）
+ `tools/theme-editor/check-system-status.js diag`（既有，必须仍然通过）：

```
diag-page l_0140.bmp: page mean=38.9 dark(<20)=0.3%  title ink=2296  body ink=4794 => READABLE
diag l_0140.bmp: bandMean=29.1 textPx=1835 => OPEN          ← 既有判据仍然成立 ✓
sweep: diag-open frames = 125 (23..147)                     ← 打开后一直开着 ✓
```

### ② 层序改成"弧永远在最上层"（车主定稿）—— **弧像素前后值**

**车主原话**："表情层应该处于背景层之上但是表盘弧应该**永远是最上层**才对吧"，
以及"读数在弧上好点吧…读数应该是最高层的"。
**目标层序（从下到上，五层）**：背景 → 表情 → **弧** → 灯 → **读数**。
**现状是反的**：`build_arcs()` 在 `build_face()` **之前**建、而 `g_face_img`
又在**更后面**建 ⇒ 表情（尤其**图片表情**）盖住了弧。

★ **本轮实测踩到的关键点**：只把 `build_face()` 挪到 `build_arcs()` 前面
**不够** —— `g_face_img` 是**另一个循环**里建的，它仍然落在弧之上。
**判据就是像素**：改完那一版与改前**一个像素都没变**（见下表"半成品"那一列）。
⇒ **两个表情对象都必须建在 `build_arcs()` 之前**。

**实测方法**（可复跑）：用 `IMAGE_BLOB` 注入一张**故意盖到弧带**的表情图
（320×320 全不透明，四角伸到半径 226，而主弧带在 181..205），
`rpm=7800`/`coolant=135` 把弧推到全亮，落帧后逐像素比对：

| 指标（主弧带 `r=184..202`，左屏，第 22 帧） | 改前 | 只挪 `build_face`（半成品） | **改后（两个表情都在前）** |
|---|---|---|---|
| 弧带里的**脸色**像素 | 5364 | 5364（**没变** ✗） | **924** |
| 弧带里的**弧色**像素 | 11273 | 11273（**没变** ✗） | **14860** |
| **"脸色 → 弧色"翻转的像素** | — | **0** ✗ | **3610** ✓ |
| 弧的可视角度（几何上该有 270°） | 216° | 216° ✗ | **251°** ✓ |

**单个像素的前后值**（原始，第 22 帧，`l_0022.bmp`）：

| 像素位置 | 改前 | 改后 |
|---|---|---|
| `(117,80)` | `#103452`（表情图的颜色） | **`#ff5d5a`（弧色）** |
| `(118,80)` … `(122,80)` | `#103452` | **`#ff5d5a`** |
| `(104,376)`（135°、弧起点方向） | `#103452` | **`#ff5d5a`** |
| `(104,104)`（225°） | `#103452` | **`#ff5d5a`** |
| `(376,104)`（315°） | `#103452` | **`#ff5d5a`** |

**读数不被弧盖住**（车主要求的另一半）：大数字墨迹顶在 y=55，**本来就蹭进
弧带内沿 4 像素** ⇒ 直接量"弧带里的白色像素"：

| 指标（主弧带 `r=181..205`，第 22 帧） | 改前 | 改后 |
|---|---|---|
| 读数（白）像素数 | 380 | **380**（一个都没被弧吃掉 ✓） |
| 例：`(196,55)` | `#ffffff` | **`#ffffff`** |

★ **回归改善**：`check-preview-frame.js` 在同一帧上，改前有 **6** 条 FAIL、
改后 **5** 条 —— 少掉的那条正是 **`水温弧在数字外侧绕行 弧带上命中 0 像素`**
（副弧被表情压得完全看不见 ✗）⇒ 改后 **705 像素** ✓。
剩下 5 条是**调色板不匹配**（那个检查器默认按 `spec-240.json` 那套样例素材配色，
而这两次跑的是**默认主题**的颜色），**改前改后逐条完全一样** ⇒ 与本次改动无关。

**判据脚本**：`check-layer-order.js`（单帧：弧带采样 + 读数最高 = `LAYERS-OK`）
与 `check-layer-ab.js`（改前/改后逐点对比）。
`ARCHITECTURE.md` 新增「显示约定 §1.5 表盘的图层顺序」（含"改层序前先读"的三条纪律），
`tools/theme-editor/README.md` 新增「图层顺序」一节，
`asset-spec.js` 只**注明**"弧在最上层 ⇒ 表情边缘会被弧裁掉（设计如此）"，
**尺寸上限（480 档 320 / 240 档 160）一个数都没放宽** ✓。

### ③ 回归数字（前 → 后，全部真跑）

| 项 | 前 | 本轮 |
|---|---|---|
| native（`-e native`，挂 zig 桩） | 257 例（2 skipped / 255 succeeded） | **257 例（2 skipped / 255 succeeded），0 失败** ✓ |
| `test-gauge-geometry` | 71 | **71** ✓ |
| `test-face-stages` | 429 | **429** ✓ |
| `test-theme-json` | 259 | **259** ✓ |
| `test-asset-spec` | 261 | **261** ✓ |
| `test-image-blob-build` | 372 | **372** ✓ |
| `syntax-check-pages` | 14 | **41**（+27：**不是本轮的改动** —— `4be6846` 新增了 `tools/web/index.html` 并给四个网页加了导航条，这个脚本会把**页数**一起算进去 ⇒ 页多了条目就多） |
| `platformio run -e pcpreview` | SUCCESS | **SUCCESS**（6.7 s） |
| `platformio run -e esp32s3-rgb` | SUCCESS（RAM 38.3% / Flash 86.1% / 902,759 B） | **SUCCESS**（RAM **38.3% / 125,416 B**；Flash **86.2% / 903,375 B**） |

- **Flash 涨 616 B**（902,759 → 903,375；86.1% → 86.2%），**RAM 一个字节没动**（125,416 B）。
  涨的是诊断页那几条（内缩面板 + 描边 + 48 号标题 + 几何自证日志 + 层序注释）。
- 构建在 ASCII 副本（`C:\206dash-scratch\Neru-diag`）里跑；**没跑 `pio clean`、没删 `.pio`**。
- ★ 两次**环境**报错（都不是回归，如实记）：
  ① 新副本 `.pio\libdeps` 里**没有 `pcpreview`** ⇒ 直接编会去联网装 lvgl 卡住；
  修法 = 从一个已编过的副本**播种** `.pio`（写进 `docs/RGB-PANEL-2.8C.md` §14.1 ②）。
  ② `python -m esptool` 在本机**不存在**（esptool 5.3.0 住在
  `C:\206dash-scratch\pio-core-mix\penv`）⇒ 要用**那个 penv 的 python**
  （写进 §16.2）。

### ④ 数据分区：把车主的两份配置烧进板上（**先备份**）

**板上开工时的状态**：`theme` 与 `image` 两个分区**全是 `0xFF`**（从没刷过）
⇒ 固件一直走"不用图片资源"的降级路径。**两份备份照做**（车主手写的东西没有别的副本）：

| 备份文件 | 偏移 / 长度 | sha256 |
|---|---|---|
| `C:\206dash-scratch\asfound-theme-0x210000.bin` | `0x210000` / 16384 B | `0FBBA07A833D4DCFC7024EAF313661A0BA8F80A05C6D29B8801C612E10E60DEE` |
| `C:\206dash-scratch\asfound-image-0x254000.bin` | `0x254000` / 8388608 B | `9F9B02F5EE6CBEF5E018C1EE424095FC21A842EA6968C0D36114B5930DAB2BA1` |

**偏移的来源 = `partitions-s3.csv`**（★ 不是 `partitions.csv` —— 4 MB 那张表的
`image` 只有 **1 MB**，装不下这份 1.8 MB 的镜像）：

```
theme,    data, 0x40,    0x210000, 0x4000,      ← 16 KB
image,    data, 0x41,    0x254000, 0x800000,    ← 8 MB
```

**写入的文件（只读使用，一个字节都没改）**：

| 文件 | 大小 | sha256 | 与分区比 |
|---|---|---|---|
| `…\.dsh-drop\session-…\3ac5f92ee198-theme.json` | **1,388 B** | `3AC5F92EE198CC6E…`（前缀与车主给的一致 ✓） | ≤ 16384 ✓ |
| `…\.dsh-drop\session-…\6fbe3b1a948c-image.bin` | **1,844,620 B** | `6FBE3B1A948CFFCC…`（前缀一致 ✓） | ≤ 8,388,608 ✓ |

**写入命令与回执**（`--after hard_reset`；esptool 5.3.0，penv 的 python）：

```
python -m esptool --chip esp32s3 --port COM6 --baud 921600 --after hard_reset write_flash `
    0x210000 <…>\3ac5f92ee198-theme.json 0x254000 <…>\6fbe3b1a948c-image.bin
→ Compressed 1388 bytes to 289...      Wrote 1388 bytes at 0x00210000      Hash of data verified.
→ Compressed 1844620 bytes to 390907... Wrote 1844620 bytes at 0x00254000  Hash of data verified.
→ Hard resetting via RTS pin...
```

**复位后抓串口（加载日志原文）**：

```
theme: 已加载 (1388 字节, bg=0x141414, face=200)
image: 已加载 9 张图 (1843200 字节数据)
image : 分区 8192 KB @ 0x254000(编译期口径 8192 KB)
image ok: 9 张,数据 1843200 字节,镜像 8388608 字节
206 dash ok  spd= 65% rpm= 80% coolant= 86C face=high/overspeed
206 dash ok  spd= 79% rpm= 96% coolant= 87C face=redline/overspeed
206 dash ok  spd= 94% rpm= 97% coolant= 88C face=redline/overspeed
```

★ `face=` 那一格开始按**新表情**走（`high` / `redline` / `sport` / `cruise` 全部
出现过 —— 这些是**图片表情**的状态名，说明 9 张图真的在驱动表情状态机，
不再走程序化占位表情）✓。★ `bg=0x141414` 与 `face=200` 说明**主题也生效了** ✓。
★ 加载这几行的**准确字样**直接从源码 grep 得到（`src/theme_load.cpp` 的
`theme: 已加载` / `lib/themetool/image_blob.cpp` 的 `image: 已加载` 与
`image ok:`），**不是猜的**。

**显示健康四行**（复位后最终固件，原始串口行）：

```
rgb: vsync=139(+54/s) wrap=139(+54/s) swap=35(+5/s) flush=153 blit_max=1323us step_max=800us copy_max=1323us copy_avg=706us swap_wait_max=8272us phase_max=18503us timeout=0 fb=1/0 catchup=35(+6/s 951KB/s forced400KB) refresh=175217/102596/217695us fullrb=0/s bounce=23MB/s 模式=局部刷新 psram=7285KB heap=214KB
rgb: vsync=462(+53/s) wrap=462(+53/s) swap=65(+5/s) flush=338 blit_max=1325us step_max=928us copy_max=1325us copy_avg=666us swap_wait_max=8272us phase_max=18503us timeout=0 fb=0/1 catchup=65(+5/s 388KB/s forced0KB) refresh=200334/160361/235836us fullrb=0/s bounce=23MB/s 模式=局部刷新 psram=7285KB heap=214KB
```

⇒ **`vsync +53~54/s`、`copy_max≈1.32ms`、`timeout=0`、`fullrb=0/s`** —— 与上一轮
同一量级，**没有黑屏/花屏的驱动级征兆** ✓。（★ 屏幕本身是给人看的：机上没有
相机，这条是**驱动级**证据。）

### ⑤ 收尾状态：板子在**表盘**上（**没留在诊断页** ✓）、`mute=0` ✓

```
>>> d (open)        diag: open page=1/2
>>> d (page 2)      diag: open page=2/2
>>> d (close)       diag: closed page=2/2     ← ★ 收尾这一行 = 板子回到表盘
>>> m (verify)      mute: 1 (saved)
>>> m (back to 0)   mute: 0 (saved)           ← ★ 有声逻辑开启
```

收尾后又跑了一轮"开→翻→关"并最终停在 `diag: closed page=2/2`；
最后一次 `m` 回执 **`mute: 0 (saved)`**（与开工前同一个状态）。

### ⑥ 红线复核

- `platformio.ini`：**一个字节都没碰** ✓
- `partitions.csv` / `partitions-s3.csv`：**未动** ✓（只**读**了偏移）
- `[env:esp32s3]`（**抓帧盒** COM7）/ `esp32dev` 的 `espressif32@7.1.3` 钉法：**未动** ✓
- `src/dash_display_rgb.cpp` / `dash_display_spi.cpp`：**未动** ✓
- 运行期**没有**改 I2C 时钟、**没有**长时间连续高电平 ✓
- **只开了 COM6**；**COM7 一次都没打开** ✓
- `tools/theme-editor/theme.json`（未跟踪）：**未碰** ✓（它仍未被跟踪）
- 车主那两份配置：**只读使用，一个字节都没改** ✓（sha256 与车主给的前缀一致）
- **没新开 L 号**、没改任何既有 L 裁决 ✓
- 未跟踪的新文件：`tools/theme-editor/{check-diag-page,check-layer-order,check-layer-ab}.js`（本轮新增）
  —— 与既有的 `theme.json` / `image.bin` 不同，**这三个是本轮要提交的**。

### ⑦ 还没做 / 拿不准的（如实列）

1. **真机上的"看得清"没有机器读数**：诊断页的三个数是**预览落帧**的像素实测；
   真机上那几行只有**几何自证日志**（`box=416x416 … font m48/lh52 · m18/lh21`）
   —— 机上没有相机，**屏上到底多亮只有车主能看**。★ 所以 **C 项的验收要请车主确认**。
2. **CJK 中文仍然画不出来**：本构建只使能 Montserrat。诊断页已保证**全 ASCII**
   （所以这一页不受影响），但 `dataTrustReasonText()` 那条中文短文若哪天要上屏，
   **必须先引入 CJK 字体**（`source_han_sans_sc_16_cjk`，占 Flash）—— **独立一单**。
3. **`panel=` 那一格仍是 `- (driver n/a)`**：RGB 驱动还没有"面板帧率/flush/copy"
   的出口（与上一轮同一条已知项，本轮**没碰那个文件**）。
4. **"表情图片比弧大"这件事在真机上没有直接证据**：本轮的弧/表情像素判据是
   **pcpreview 落帧**（注入一张 320×320 的测试 blob）。真机上目前只刷了车主的
   9 张图 —— 它们的四角是否伸进弧带**由车主那套素材决定**，本轮**没有**在真机上
   量（也不该为了量而临时刷测试素材上板）。
5. **`mute` 在 NVS 里被本轮反复切过**：最终值是 **0**（有声），与开工前一致；
   但 NVS 的写次数是**真实消耗**（本轮 4 次），如实记。

---

## 真机蜂鸣器：TCA9554 的 EXIO8 真正发声（2026-09-24，**上板 COM6**）

★ 起点 `438bef0`（开工 `git log -1` 实测 ✓）。本单**只碰** `src/`、`lib/dashcore/`、
`test/`，以及 `ARCHITECTURE.md` / `ACCEPTANCE.md` / `docs/RGB-PANEL-2.8C.md` 三份文档。
★ **`tools/theme-editor/*` 与 `tools/web/*` 一个字节都没动**（另有子代理在改那两处，
本单开工时它们已经是 dirty 的）；**`platformio.ini` 一个字节都没动**（门用的是既有的
`DASH_DISPLAY_RGB`）。
★ 只烧 **COM6**（微雪 2.8C）；**COM7（抓帧盒）一次都没打开**。

### ① 做了什么（这一单把 L14 落地）

出发点是那份结论（`docs/RGB-PANEL-2.8C.md` §13.6）：板载蜂鸣器是**有源**的、挂在
**TCA9554 的 EXIO8**；而在此之前 `src/main.cpp` 设备侧挂的是 **`BuzzerNull`**
（空实现）⇒ **真机根本不会响** ✗。

| 件 | 改动 |
|---|---|
| `lib/dashcore/buzzer_exio.h` / `.cpp` | **新增**：`BuzzerExio` —— 非阻塞多相序列、单次哔**夹到 ≤300ms**、`Long` ⇒ **3 短哔**；时钟与"写一位"都是**注入**的（主机可测） |
| `lib/dashcore/buzzer.h` | 加一个**默认空实现**的 `virtual void tick()`（唯一的接口改动） |
| `src/dash_display_rgb.cpp` | **新增 `dash_buzzer_set(bool, void*)`** —— 全仓**唯一**写那颗扩展器的地方，走**既有**的 `tca9554_set()` + 影子寄存器 `g_exio_out` |
| `src/dash_display.h` | 那句声明（`#if defined(DASH_DISPLAY_RGB)`） |
| `src/main.cpp` | 实例 + **绑定**（`setup()` 里 `#elif defined(DASH_DISPLAY_RGB)` ⇒ `g_buzzer = &g_buzzer_exio;`）+ `loop()` 每轮 `g_buzzer->tick()` + **串口命令 `b`** |
| `test/test_dashcore/test_buzzer_exio.cpp` | **新增 9 条**用例；`test_main.cpp` 注册（排在 `face_stages` 之前，顺序纪律照旧） |

**三条硬规则（源自"屏幕突然全黑"那次事故）逐条落在哪：**

1. **复用既有 I2C 总线对象** ✓ —— `dash_buzzer_set()` 只调 `tca9554_write()`；本单
   **没有**出现过 `Wire.begin()` / `Wire.setClock()`（全仓 grep 可核）。
2. **读-改-写只动喇叭那一位** ✓ —— 位号取自**既有常量** `BUZZER_EXIO_BIT`（就是
   `src/dash_display_rgb.cpp` 里 `#define BUZZER_EXIO_BIT 7 // EXIO8` 那一行）；
   影子寄存器仍只有 `g_exio_out` 一份。
3. **自证日志** ✓ —— 每次写都落 `buzz: exio 0xXX -> 0xXX (mask 0x80)`，且**开**的那一下
   追加一行回读（见下面 ⑤ 的原始行）。
4. **绝不做长时间连续高电平** ✓ —— `kMaxPulseMs = 300` 在 `pulsesFor()`/`clampPulse()`
   里**每一相**都夹；`Long` 在真机上落成 3 短哔（不是长鸣）；`beep_ms=0` 夹到 **1ms**
   （方向是"极短"，不是"长鸣"）。
5. **整屏近黑** ✓ —— 见 ⑤ 的显示健康四行 + 收尾 `diag: closed`。

### ② 掩码 / 位号来自哪几行既有代码

| 用的东西 | 出处（既有代码，**没有新写一套位号**） |
|---|---|
| `BUZZER_EXIO_BIT` = `7`（EXIO8） | `src/dash_display_rgb.cpp` 第 4 块：`#define BUZZER_EXIO_BIT 7 // EXIO8` |
| `TCA9554_ADDR 0x20` / `REG_OUTPUT 0x01` / `REG_CONFIG 0x03` | 同一块：`#define` 三行（例程口径） |
| 影子寄存器 + 读-改-写 | 同一块：`static uint8_t g_exio_out` + `tca9554_set(bit, high)` |
| `LCD_RST_EXIO_BIT 0` / `LCD_CS_EXIO_BIT 2` | 同一块（本单**没碰**，只用来解释"为什么不能写错位"） |
| `kTrustBeepMs = 120` + `static_assert(kTrustBeepMs <= 300u)` | `lib/dashcore/system_status.h`（既有） |

### ③ 回归数字（前 → 后，全部真跑）

| 套件 | 前（基线） | 后 | 判据 |
|---|---|---|---|
| native（`-e native`，挂 zig 桩） | **257 例（2 skipped / 255 succeeded）** | **266 例（2 skipped / 264 succeeded），0 失败** | 净增 **+9**（= 本单新增的那 9 条） |
| `tools/theme-editor/test-gauge-geometry.js` | 71 | **71** | 不动 |
| `tools/theme-editor/test-face-stages.js` | 429 | **429** | 不动 |
| `tools/theme-editor/test-theme-json.js` | 259 | **259** | 不动 |
| `tools/theme-editor/test-asset-spec.js` | 261 | **261** | 不动 |
| `tools/theme-editor/test-image-blob-build.js` | 372 | **372** | 不动 |
| `tools/theme-editor/syntax-check-pages.js` | 41 | **42** | ★ **不是本单带来的**：另有子代理在 `tools/web/index.html` 加了一个页面（本单**没碰** `tools/`）；数字差一是那个页 |
| `tools/theme-editor/test-asset-package.js` | —— | 105 | 同上，子代理那边正在改 `asset-package.js`（本单没碰） |

**新增的 9 条**（`test/test_dashcore/test_buzzer_exio.cpp`）：

1. `mask_only_target_bit` —— 每一次写回之后低 5 位（含 `LCD_RST`/`LCD_CS`）必须还是 `0b00101`；
2. `long_degrades_to_three_short` —— `Long` ⇒ **3 相**，电平序列是 开/关/开/关/开/关；
3. `pulse_counts` —— `Silent`/`Short`/`Triple`/`Urgent`/`Long` = 0/1/3/4/**3**；
4. `clamps_ms_to_300` —— 标称 5000ms 与**每一相**都夹到 300；`0ms` ⇒ ≥1ms；
5. `silent_writes_nothing` —— 静音期间（只 tick / 调到 `Silent` / 收到 `off()`）**一次总线都不动**；
6. `auto_off_at_deadline` —— **假时钟**逐格推进：到点前不关、到点自动关、之后不再响；
7. `repeat_beep_is_idempotent` —— 主循环每轮重调 `beep()` 仍是 3 声；走完后再来一次仍能响；
8. `off_cancels_immediately` —— `off()` 立刻掐断，且掐断后**同模式还能再响**；
9. `begin_is_idempotent` —— `begin()` 反复调用不动总线；正在响时 `begin()` 回到静音。

### ④ 编译（`SUCCESS` 行；本机成功也返回非 0，看 `SUCCESS`）

环境：ASCII 副本 `C:\206dash-scratch\Neru-bz`（robocopy 同步，**没跑 `pio clean`**）、
`PLATFORMIO_CORE_DIR=C:\206dash-scratch\pio-core-mix`、
`PYTHONPATH=C:\Users\张九思\206Dash\.pio-pylibs`、`PATH` 前挂 `C:\206dash-scratch\zigbin`。

| env | 结果 | RAM | Flash |
|---|---|---|---|
| `pcpreview` | **SUCCESS**（7.2 s） | n/a（native） | n/a（native） |
| `esp32s3-rgb` | **SUCCESS**（31.3 s） | **38.3% / 125,496 B** | **86.3% / 904,783 B** |

★ Flash 对比基线（`903,375 B` / 86.2%）：**+1,408 B（+0.1 个百分点）** ⇒ 86.3%，
**剩余 13.7%** 照旧够用。RAM 与基线**逐位相同**（125,496 B）——
新增的静态量（`BuzzerExio` 那几十个字节）落在 `.bss` 的既有对齐余量里。
★ 查 ⑥ 那个坑时另外量过一次 **904,683 B**（少 100 B）—— 那是**带一句临时诊断文字**的
中间版本，**最终烧回板子的就是上面这一版**（`904,783 B` / 与提交后的工作区逐字节一致）。

### ⑤ 上板原始日志（COM6，115200 8N2→8N1 按既有口径；`chcp 65001` + `PYTHONUTF8=1`）

**(a) 串口命令 `b`**（车主按需验听感的那个入口）—— 1 行回执 + **3 个完整开/关对**：

```
buzz: test beep cmd=b(Long>3short) ms=120 x3 gap=80ms muted=0
buzz: exio 0x05 -> 0x85 (mask 0x80)
buzz: 回读=0x85(影子=0x85)
buzz: exio 0x85 -> 0x05 (mask 0x80)
buzz: exio 0x05 -> 0x85 (mask 0x80)
buzz: 回读=0x85(影子=0x85)
buzz: exio 0x85 -> 0x05 (mask 0x80)
buzz: exio 0x05 -> 0x85 (mask 0x80)
buzz: 回读=0x85(影子=0x85)
buzz: exio 0x85 -> 0x05 (mask 0x80)
```

★ **只有 bit7 在动**：`0x05 -> 0x85` 与 `0x85 -> 0x05`，低 5 位（`LCD_RST`/`LCD_CS`）
**一次都没被碰** ✓。回读与影子**一致** ✓。3 个开/关对 = **3 声**（`Long` 的降级）✓。

**(b) 真实路径：`trust` 角标出现那一刻**（开机 ~2.44 s，没接 VAN）：

```
alert: overspeed
buzz: exio 0x05 -> 0x85 (mask 0x80)
buzz: exio 0x85 -> 0x05 (mask 0x80)
trust: sim-fallback  <-- 屏上出现数据不可信提示 beep (episodes=1)
buzz: exio 0x05 -> 0x85 (mask 0x80)
buzz: exio 0x85 -> 0x05 (mask 0x80)
```

★ `trust: … beep` 那一行**紧跟**着它自己的那一对 `buzz:` —— 判据层说"该响"、
驱动层真的把那一位打开了，**两件事在同一拍上对上了** ✓（就是本单要修的那条：
以前这一行有、声音没有）。
★ 上面第一对属于 `alert: overspeed`（Sim 假数据扫过 120），同样走通。

**(c) 开机自检（摘）**：`mute: 0 (loaded from NVS)`、
`alerts: 已就绪(超速 120 / 红区 5800 / 门 / 转向灯忘关 20s)` ✓（这两行既有的没变）。

**(d) 显示健康四行**（改完 + 响过之后，同一块板）：

```
rgb: vsync=2020(+54/s) wrap=2020(+54/s) swap=210(+5/s) flush=1172 blit_max=1327us
     step_max=947us copy_max=1327us copy_avg=715us swap_wait_max=10379us
     phase_max=18534us timeout=0 fb=0/1 catchup=210(+5/s 429KB/s forced0KB)
     refresh=188438/143066/200543us fullrb=0/s bounce=23MB/s 模式=局部刷新
     psram=7285KB heap=214K
206 dash ok  spd= 23% rpm= 15% coolant=93C face=idle/city
```

* `vsync +54/s` ✓（与改动前逐位同量级）
* `copy_max=1327us ≈ 1.3ms` ✓（`blit_max` 同一量级）
* `timeout=0` ✓
* `fullrb=0/s` ✓（局部刷新档，正式档）
* ⇒ **没有"整屏近黑"** ✓（另见 ⑤-补：`vsync` 在涨 = 面板真在收帧）

**(e) 收尾状态**（诊断页**开过、已关掉**；板子留在**表盘**上）：

```
diag: geom box=416x416 at 32,32 title=384x56 at 58,50 body=368x320 at 58,114
      font title=m48/lh52 body=m18/lh21 page=1/2
diag: open page=1/2
diag: open page=2/2
diag: closed page=2/2
```

★ `d` 的三步循环（开 → 翻页 → 关）**行为一字未变** ✓。

### ⑤-bis ★ **人耳判据：车主确认听到 3 短哔**（2026-09-24，本单唯一机器证明不了的一环）

> **车主原话：「我听见了哔哔哔三声」** ✓

★ 这一条为什么必须记在这里：机上**没有麦克风** ✗，日志能证明的只有
"**判据要求响 + 调用方真的把那一位写下去了**"；"**到底有没有真的响、响了几声**"
只有站在板子前的人能定。所以本单的验收是**两半合起来**才成立的：

| 那一半 | 证据 | 形态 |
|---|---|---|
| **听到什么**（人工） | 车主：**3 短哔**，**不是长鸣** | 与 `b` 的 `ms=120 x3 gap=80ms`（= `Long` 的降级）**对得上** ✓ |
| **写没写对**（机器） | `buzz: exio 0x05 -> 0x85 (mask 0x80)` / `buzz: exio 0x85 -> 0x05 (mask 0x80)` | `0x05` 的 **bit0 (`LCD_RST`/`EXIO1`) 与 bit2 (`LCD_CS`/`EXIO3`) 全程未变** ✓ |

★ **"3 声"这件事同时把两处口径一次钉住了**：① 驱动里 `Long ⇒ 3 短哔` 那条
**降级真的生效**（不是文档里写着、实现里没落）；② **"没有长鸣"** 这条硬约束
在这块板上**被听到了**（机制 B「持续高电平拉低 3.3V 轨」那条风险本轮**没有**被触发）。
★ 与**(下一条)同拍显示健康**合起来才是完整判据：**响了，而屏没坏**。

### ⑤-ter 同拍显示健康（**独立抓取**，与上面那条同一次会话）

```
rgb: vsync=8704(+54/s) ... copy_max=1336us ... timeout=0 ... fb=0/1 ... fullrb=0/s ...
```

* `vsync=8704(+54/s)` ✓ —— 面板**一直在收帧**（不是"驱动正常但玻璃黑"）；
* `copy_max=1336us ≈ 1.3ms` ✓ —— 与改动前同量级（没有新增 PSRAM 大突发）；
* `timeout=0` ✓ —— 换帧那道门一次都没靠超时放行；
* `fullrb=0/s` ✓ —— 跑在**局部刷新**（正式档）；
* `fb=0/1 ↔ 1/0` 在交替 ✓ —— 双 fb 换帧照旧生效（**没有残留/没有跳回旧内容**）。

★★ 这一组是 **"打错一位就是一次面板复位"那条风险的实证反例**：
`0x05 -> 0x85` 只动了 bit7，而 `vsync` 照涨、`timeout` 照 0
⇒ **面板既没有被复位、也没有丢初始化** ✓。值得留在记录里 ——
它是"读-改-写 + 影子寄存器只留一份"这条纪律**被真机验证过**的那一次。

### ⑤-quater ★ 使用提醒：台上跑模拟数据时会**间断出声**（不是故障）

台架上 `rpm` 由 `sim_source` 周期扫量程 ⇒ **会周期扫进红区（>5800）**，
于是 `alert=redline` 会**反复出现**，蜂鸣器也就**间断地叫**
（车主的抓取里那行是 `alert=redline beeps=38` ✓）。

* ★ **这是正常的**（判据就是要它响），**不是**"驱动作怪"或"响个不停"：
  两次响之间至少隔 `beep_min_interval_ms = 3000ms`，且每声只有 120ms
  ⇒ **绝不会**变成长鸣（两道界都在 `lib/dashcore/alerts.cpp` 里，native 用例钉着）。
* ★ **静音入口 = 串口命令 `m`**（取反 + 写 NVS **掉电保存**；回执 `mute: 1 (saved)`）——
  台上想安静就发一个 `m`，**重启之后仍然静音**。这一条既有的路**本单一个字没改** ✓。
* ★ 将来做**实体按键**时，静音入口放**诊断页**那条口径不变
  （`ARCHITECTURE.md`「显示约定」§4 第 5 条）；串口 `m` 是它现在的真机替代入口。

### ⑥ 一次中途踩到的坑（**写下来，因为它差点蒙混过关**）

第一版把 `BuzzerExio` 的实例声明出来了，但 **`setup()` 里漏了 `g_buzzer = &g_buzzer_exio;`
那一行绑定** ⇒ 真机仍然指着 `BuzzerNull`。
症状极具误导性：**串口上 `buzz: test beep cmd=b…` 照打、编译/回归全绿、`beep()` 也真的被调了**
—— 但一声不响、而且**一行 `buzz: exio` 都没有**（`BuzzerNull::beep()` 是空的，所以没有写）。
★ 判据：**"`beep()` 被调了"不等于"那一位被写了"** —— 前者只有调用点能证明，
后者只有 `buzz: exio …` 那一行能证明。所以本条日志是**必需**的，不是好看。
★ 这条与本仓库既有的那条教训同源（"日志里一切正常、硬件没动"）。

### ⑦ ★ 红线复核

* **没动** 240 / DualEye SPI 驱动（`src/dash_display_spi.cpp` 一字未改）✓
* **没动** CRC / SOF / `kSpeedScale=2.56` / VAN 脚极性 ✓（`lib/link/*`、`van_*` 一字未改）
* **没动** `[env:esp32s3]` / `[env:esp32dev]` 的抓帧行为与 `espressif32@7.1.3` 钉法 ✓
  —— `platformio.ini` **整份未改**（`git status` 里没有它）
* **没有新开 L 号** ✓ —— 本单是 **L14 的收口**（§8 那一行改成结论，编号保留）
* **抓帧盒那三个 env 里"这段代码一行都不存在"** ✓ —— 判据是编译期那道门：
  `DASH_DISPLAY_RGB` 只有 `[env:esp32s3-rgb]` 定义（`platformio.ini` 既有），
  本单新增的 `buzzer_exio.*` 里的类型只被该 env 的编译单元引用；
  `src/main.cpp` 里的实例/绑定/`tick()`/命令 `b` **全在那道门里**。
  ⇒ `esp32s3` / `esp32dev` / `-vaninv` / `-vansniff` / `-spi` / `-linkloop` 的编译单元
  **逐字节不变**。
* **只烧 COM6** ✓；**COM7 一次都没打开** ✓；`tools/theme-editor/theme.json`（车主资产）**没碰** ✓

### ⑧ 还没做 / 拿不准的（如实列）

1. ★ **"听感"这一条已经由车主给过了**（2026-09-24：「我听见了哔哔哔三声」✓，
   见 ⑤-bis）—— 所以本条**不再是缺口**。但它**不能再被机器复核**：
   以后每次改蜂鸣器（换模式映射、改 `beep_ms`、动 `kMaxPulseMs`）都**必须重新请车主听一次**
   （机上没有麦克风这条事实不会变）。
2. **另一块板（左/从板）有没有同一颗蜂鸣器：未实测** —— 第二块 2.8C 还没到货。
   两块同型号 ⇒ 理论上同构，但那是**推测**；到货后按 §13 那套自检
   （`-DBUZZER_SELFTEST=1`）重跑一遍才算数。这一条**不占新 L 号**（等硬件，不等裁决）。
3. **`2 短哔` 这一档还没有调用方**：词表先留着（`ARCHITECTURE.md` §4.1）。
   要加时**只改 `alerts` 的模式映射**，驱动不用动。
4. **`Urgent`（4 声）与 `Triple`/`Long`（3 声）在这块板上听起来一样** ——
   有源蜂鸣器的自由度决定，**已知代价**，不是 bug。
5. **`m`（静音）不拦 `b`**：`b` 是车主**主动**按的测试动作，而 `m` 关的是**自动告警**。
   这一条是**有意的**，写在命令表里；静音对 `alerts` 与那一声轻提示的拦截**没动**
   （`mute: 1` 那一路上板行为照旧）。
6. **回读自证只在"开"的那一下做**（每次响一小段最多 1 次 I2C 读）——
   如果哪天要更严，可以把"关"那一下也加上；本轮按"开才是要自证的那一刻"取舍。
7. **`esp32s3` / `esp32dev`（抓帧盒）那两个 env 本单没有重新构建过**——
   原因是**环境**：`espressif32@7.1.3` 的 `framework-arduinoespressif32` 已被
   pioarduino 那份换掉（`platformio.ini` 里早就写明的平台共存边界），
   再编就要**重新联网下载** ~200MB 框架包，而本机对 github 的网络是坏的
   ⇒ 构建卡在 `Tool Manager: Installing platformio/framework-arduinoespressif32 @ ~4.20017.0`。
   ★ **这不是"本单改了抓帧盒"**：改用**编译器那边**的证据（下面第 8 条），
   它比一次构建更直接。
8. ★ **"抓帧盒三个 env 里这段代码一行都不存在"的证据（编译器级，可复现）**：
   把**同一份** `src/main.cpp` 用**同一组**包含路径预编译两遍，只差 `-DDASH_DISPLAY_RGB`：

   | 假设的标志集 | `BuzzerExio`（实例/类型引用） | `beep_cmd_window` | `dash_buzzer_set` | `beep_test_from_serial` |
   |---|---|---|---|---|
   | **不带** `-DDASH_DISPLAY_RGB`（= `esp32s3`/`esp32dev`/`-vaninv`/`-vansniff`/`-linkloop`/`-spi`） | 0 | **0** | **0** | **0** |
   | 带 `-DDASH_DISPLAY_RGB=1`（= `[env:esp32s3-rgb]`） | 有 | 2 | 2 | 2 |

   ⇒ **实例、绑定、`tick()` 调用、串口命令 `b` 全部在那道门里消失**。
   ★ 另外两条同向的证据：① `platformio.ini` **整份未改**（`git status` 里没有它），
   而 `-DDASH_DISPLAY_RGB=1` 是它**早就有**的、且**只有** `[env:esp32s3-rgb]` 定义
   （第 183 行）；② 库源码的编译由 LDF 决定，`lib/dashcore/buzzer_exio.cpp`
   **只被 `src/main.cpp` 的 `#include` 拉到**（全仓 grep：只有 `main.cpp` 与它自己
   引这个头）⇒ 那三个 env 的编译单元里**连这个 .cpp 都不会被编**。
   ★ 剩下的一处**已知且无副作用**的差异：`#include "buzzer_exio.h"` 那一行本身
   在门之前（它是纯声明头，不含 Arduino/I2C），所以类型**声明**在被预处理掉的
   那份里仍在 —— 但**没有任何一个字节的代码**，也没被实例化。如实记在这里。